---
title: 海螺姑娘 · 决策记录
tags: [海螺姑娘, decision]
created: 2026-05-14
updated: 2026-05-14
status: active
type: decision
project: 海螺姑娘
---

# 海螺姑娘 · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

格式：

```
## YYYY-MM-DD · 决策标题

**背景**：为什么要做这个决策

**选项**：
- A：...
- B：...

**选择**：B

**理由**：

**影响**：
```

---

<!-- 在下方追加新决策 -->

## 2026-06-24 · 海螺姑娘升格为「全局资产管理平台」

**背景**：要做一个 AI 驱动的本地全资产盘点看板(Resource/Database/Project + Infrastructure)。与其独立 artifact，不如内化进海螺姑娘——conch 本就 dry-run 默认 + audit + rollback，天然是合规的"审查并修复"引擎。

**选择**：海螺从「单项目维护 /conch skill」**升格为全局资产管理平台**。

**理由**：conch dry-run+rollback 天然合规 propose-then-confirm；给"审查并修复"配真后端；盘点 + 看板 + 专项修复一体。

**影响**：① `conch_engine.py` 加 `survey` 全局盘点命令(遍历项目→健康信号→回写 manifest)；② 新建看板 artifact `global-asset-inventory`(读 manifest·按行分组卡片·**颜色=类别/外发光=健康度**·审修按钮)；③ `data/asset_manifest.json` 为单一真源(nodes/edges 手工策展·status/freshness survey 回写)。

**两条设计裁定**：① **conch obsolete_count 纯信息·绝不自动改策展 status**(避免知识库版本链误判待修·渊图 22 旧文件案)；② **database 节点带 `health_file` → survey 读自检产物驱动 status**(被动告警闭环)。

**来源** → logs/2026-06-24-海螺全局资产平台与金融线四修复.md

## 2026-06-24 · 数灵纳入 GAI 但「不作数据资产」

**背景**：退役 Brain Vault Dashboard，要把它的「数灵协作」板迁进 Global Asset Inventory。数灵(白泽/烛阴/句芒/CC)是 brain 里的 agent/操作者。

**选择**：数灵在 GAI 里**单列一行(Infrastructure 之下)、用同款资产卡形但不接任何关系连线**，黛青 #4c6b6b 类别色、签名色作星云；点开走独立 agent 面板。

**理由**：数灵是**操作者不是数据资产**——没有 supplies/feed/consume 这类数据流；若给它接上下游边会污染 GAI 关系星河的语义(让人误以为 agent 是管线节点)。视觉统一(同卡形)但语义隔离(无边、无健康外发光、独立面板)。

**影响**：① manifest 加 `agents[]`(非 nodes)，渲染时单独成行、不进 `cardEl`/`edges`/highlight；② 数灵无健康轴故无外发光；③ 项目二级卡加 brain 治理(TODO/GOTCHAS=错题本)+ 错题本积压≥3 叠青碧呼吸告警晕 + 内置「审查并修复」(复盘错题本/清理 dry-run)。

**关联原则**：往既有看板加一类新实体前，先判它是不是与现有类别**同质**(可同样连线/同样发光)——不同质就视觉统一、语义隔离，别硬塞进关系图。

**来源** → logs/2026-06-24-GAI并入BVD数灵与错题本告警.md

## 2026-06-24 · BVD 退役走可逆替代 + L3 钻取走通用 detail_view 钩子

**背景**：① 退役 Brain Vault Dashboard，把最后一个高密度件「DVA 常更作者表」迁进 GAI；② CC 无「删 artifact / 删定时任务」工具。

**选择**：
1. **L3 钻取通用钩子**：DVA 作者表作 L3 全屏 modal，入口不为 DVA 特例化，而是节点带 `detail_view:"dva_authors"` 才出「📖 …」按钮——将来 recap.db/market_data.db 想加表级钻取，节点加个 `detail_view` 字段即复用同一机制。
2. **退役用可逆替代**（工具受限下）：CC 不能删 artifact/任务 → ① 归档原件到 `_archive/`（叠加 artifact 自带 versions/ 双保险）；② `refresh-brain-vault-dashboard` 设 `enabled:false` 暂停（非删）；③ artifact 由 Doctor 在界面删（原件已备份·可逆）。

**理由**：钩子化避免每加一个钻取就特例化一处渲染；退役的可逆替代守「不删/可逆」铁律，即便工具能力缺失也不破例硬删。

**影响**：① `detail_view` 成为二级卡→三级详表的标准扩展点；② 退役动作全部可回退（归档 + enabled:true 即复活）。

**来源** → logs/2026-06-24-DVA表迁入与BVD退役·GAI管线治本.md

## 2026-09-12 · 4AI「临时文件」「散落归档文件」两纪律目录（Doctor 立）

**背景**：Documents 根目录反复出现 staging 散落件；TTS mp3 因 tts/audio 在沙箱挂载面外长期积压，「播完即删」落不了地。Doctor 令在 4AI 下建两文件夹分流。

**选择**：
1. `4AI/临时文件/`——可删临时件统一归宿（含 ElevenLabs 语音 mp3）。铁律：**落时即确认可删**（非「先放着回头评估」）；判断不了能否删的不落这里。清理=随 conch 周期性**清空**（不需逐件审批）。
2. `4AI/散落归档文件/`——需归档散落件暂存。铁律：只进不出、只归档不删；随 conch 归档批次送 fuxi；落件留来源注记。
3. TTS 通道：ElevenLabs MCP BASE 由 `~/Library/Application Support/Claude-3p/tts/audio` 改指 `~/Documents/4AI/临时文件`（config 命令贴 Doctor 终端·待重启验证）——mp3 从此落纪律目录，滞留由 conch 清空兜底。
4. 纪律正文＝两文件夹内 README.md（含铁律与清理机制）；另已落 auto-memory feedback_4ai_temp_folders。

**影响**：conch 下一场需把「清空 4AI/临时文件」「归档 4AI/散落归档文件→fuxi」纳入例行；若要挂进 refresh-asset-dashboard 班自动执行，属调度变更（班 prompt 改动）走 Doctor 裁。

**来源** → 2026-09-12 深夜会话（Doctor 令）

## 2026-09-19 · 两纪律目录迁至 `Claude/` 下（Doctor 立 · 三问答裁定）

**背景**：Claude 根下出现两处空壳——`Claude/4AI/临时文件/`（空）与 `Claude/tts/`（只剩空 `audio/`），加上隐藏的 `Claude/.tts-scratch/`（brain-resume Step 0.5 写的 TTS 落点，亦空），与在役的 `4AI/临时文件` 共成四处并存。Doctor 令「整合成一个『临时文件』放在根目录下」，指定落点 `Claude/临时文件`。

**选择**：① **收拢为该唯一临时落点**（TTS BASE、conch 清空、定时班三处改指它；旧 `Documents/4AI/临时文件` 退役）；② **归档夹同迁**——`散落归档文件` 一并迁 `Claude/`，两夹保持配对；③ 移除空壳（`Claude/4AI/`、`Claude/tts/` 整体移除，真实文件 0，仅 `.DS_Store`）。

**实施与证据**：两 README 随迁并加「迁移留痕」段；`4AI/` 下两旧夹 `rmdir`；复验两旧路径已不存在、新目录各含 README 一件。影响面实测：全 Documents grep「Claude/4AI」「Claude/tts」**零活引用**（仅历史日志叙述），故移除零风险。

**两处改指已执行（2026-09-19 凌晨 · Doctor 终端）**：① **TTS BASE** env `ELEVENLABS_MCP_BASE_PATH` 落点＝`~/Library/Application Support/Claude-3p/claude_desktop_config.json`，已单点替换为 `~/Documents/Claude/临时文件`（备份 `.bak_*_preTtsDirMove`、回读通过、JSON 可解析）——**待 ⌘Q 重启后功能验证**（改前实跑硬证据：新落点 mp3 播放被拒 `outside of allowed directory …/4AI/临时文件`）；② **conch 班 Step 4** 改为两叶子挂载——store 与镜像 SHA 同为 `c9316f12…`、镜像与 staging 逐字节一致（沙箱侧实算复核；「4AI」字样残留 0）。注：该班命令块被执行不止一遍，`.bak_preDirMove` 内容＝新版、不构成回退点；真回退源为 brain 仓 git HEAD。

**来源** → 2026-09-19 凌晨场（Doctor 令 + AskUserQuestion 三问答）
