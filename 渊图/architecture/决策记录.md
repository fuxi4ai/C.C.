---
title: 渊图 · 决策记录
tags: [渊图, decision]
created: 2026-05-14
updated: 2026-05-14
status: active
type: decision
project: 渊图
---

# 渊图 · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

格式：

```
## YYYY-MM-DD · 决策标题

**背景**：为什么要做这个决策

**选项**：
- A：...
- B：...

**选择**：B

**理由**：

**影响**：
```

---

<!-- 在下方追加新决策 -->


## 2026-05-16 · 关系类型扩展至 10 种（新增 causes / constrains）

**背景**：V4 Pro 在 18 篇研报批量入库时自发输出了 5 条规范外关系（2 条 causes、3 条 constrains），不在 v2 原定 8 种类型集合中。这些边语义合理：
- causes：事件传导/状态变化引发的强因果（如"服务器量缩 → CPU 恐慌囤货推高存储价格"）
- constrains：硬约束/限制/拖累（如"三星代工产能瓶颈 → 海光 CPU 7000 系列"）

**选项**：
- A：接受扩成 10 种类型，更新 prompt + rel_classify + 文档
- B：跑 `kg_rel_classify.py` 归并到 8 种（causes → enables、constrains → requires）

**选择**：A

**理由**：
1. enables 已经吃了 solved_by/trades_off/scaffolds，再吞 causes 会让该桶过度泛化、丧失分析价值
2. causes/constrains 是产业链分析的高价值关系（事件传导、政策反应、价格联动、产能瓶颈），归并必丢信息
3. kg_rel_classify.py 自身已有 constrains 桶，schema 演化方向就是更细分类型，prompt 那 8 种是保守起点而非终态
4. enables 表达"能力解锁（持续）"，causes 表达"事件传导（一次性）"，正交清晰

**影响**：
- `kg_ingest.py` prompt 扩到 10 种 + 加判断口诀
- `kg_rel_classify.py` 加 causes 桶（在 constrains 前）
- 系统概览.md / CLAUDE.md 数字与类型种数同步
- 后续入库的研报会主动使用 causes/constrains，旧 canonical 不回填（数据增量自然演化）

## 2026-05-16 · Schema v3 重设计 · 19 种归并到 11 种核心 schema

**背景**：图谱存在双字段 schema 不一致问题——`type` 字段（V4 Pro prompt 阶段写，10 种）与 `relation` 字段（kg_rel_classify.py 历史输出，19 种）并行存在，1155/1666 条边同时有两个字段值不一致；wiki_autogen.py 读 `relation`，kb.py 读 `type`，前后端看到不同 schema。

**问题诊断**：
- `relation` 字段（19 种旧 schema）里 466/1155 是 `relates_to` fallback，47% 边语义未明确
- prompt 强制 10 种把 supplies/manufactures 类边压成 part_of，丢失"产业链供应关系"分析维度
- 反向边对（buys_from↔supplies、evolves_to↔evolves_from 等）从未做 source/target 归一化

**Doctor 设计原则**（2026-05-16）：
1. 可以删字段、改源码
2. 尽量保留语义和逻辑（分析的正确性最重要）
3. 相同语义和逻辑的尽量并归

**选项**：
- A：保留 10 种现状（最低风险，损失 supplies 维度）
- B：扩到 11 种 + 完整迁移（新增 supplies，反向边归一化，删 relation 字段）
- C：扩到 19+ 种（推翻规范化目标，schema 持续膨胀）

**选择**：B

**11 种核心 schema**（按模态逻辑分类）：

| # | 类型 | 模态 | 语义 |
|---|------|------|------|
| 1 | causes        | 充分条件        | 事件传导/触发 |
| 2 | enables       | 必要条件（上游） | 能力解锁/持续支撑 |
| 3 | requires      | 必要条件（下游） | 依赖/前置（enables 反向对偶）|
| 4 | constrains    | 负向必要        | 硬约束/限制 |
| 5 | supplies      | 产业链结构      | 上游对下游的供应/代工/制造/设计服务 |
| 6 | part_of       | 产业链结构      | 仅内部构成（组件） |
| 7 | used_in       | 产业链结构      | 被用于/应用场景 |
| 8 | evolves_from  | 时序            | 演化/迭代/替代/派生 |
| 9 | competes_with | 横向            | 竞争 |
| 10| is_a          | 分类            | 类属 |
| 11| measured_by   | 度量            | 指标关联 |

**归并表**（19 → 11）：

| 旧 schema | 新 type | 处理 |
|---|---|---|
| component_of           | part_of       | 真同义 |
| design_service_by, manufactures | supplies | 上游身份特化 |
| drives, impacts_cost   | causes        | 真同义 |
| supports, solves, optimized_for | enables | 同义 + 边界归并（solves/optimized_for 损失部分语义）|
| replaces, derived_from | evolves_from  | 演化语境 |
| benefits_from          | requires      | 受益≈依赖 |
| tracks                 | measured_by   | 真同义 |
| buys_from              | supplies      | **反向边**：swap source/target |
| evolves_to             | evolves_from  | **反向边**：swap |
| constrained_by         | constrains    | **反向边**：swap |
| manufactured_on        | used_in       | **反向边**：swap |
| relates_to (240+466)   | 保留 type     | fallback，用 prompt 阶段的 type 兜底 |

**逻辑结构（充分/必要矩阵）**：

```
              正向（让 B 出现/增强）   负向（让 B 不出现/受限）
   充分条件   causes (事件传导)        [prevents — 暂无]
   必要条件   enables / requires*      constrains
              (反向边对偶)
```
* enables 与 requires 互为反向边等价；按研报叙事方向选用，不双向重复建边

**影响**：
- `kg_ingest.py` prompt 重写为 11 种 + 完整语义定义 + 判断口诀
- `kg_rel_classify.py` RULES 重写（19→11，supplies 加新规则）
- `wiki_autogen.py:130,145` 改读 `type` 字段（兼容 fallback `relation`）
- 迁移脚本：1666 条边 type 字段统一到 11 种，95 条反向边交换 source/target，1155 条 relation 字段删除
- 产物：`mapping/行业知识图谱_v3_20260516_152824_schema11.json`（待提升为 canonical）
- 系统概览.md / CLAUDE.md 同步数字与 schema 表

**迁移统计**：
- type 字段被修改: 470 条（其中 95 条同时 swap）
- 反向边交换 source/target: 95 条
- relation 字段删除: 1155 条
- 规范外 type: 0 ✓

## 2026-05-17 · Schema v3 落地完成 · 全图谱重打标签

**背景**：2026-05-16 决策的 11 种 schema 重设计，初次迁移用 rel_classify regex + type/relation 双字段归并的方案，验证发现 73 条 evolves_to swap 方向错乱、rel_classify 规则覆盖率不足等问题（v3 中间产物已隔离至 `mapping/_quarantine_v3_*_aborted.json`）。

**最终方案**：B+ 增强版 · 由 V4 Pro 在新 prompt 下对全图谱 1650 条边逐条重判 type + source/target 方向。

**实施关键**：
- **prompt 改造**：从"swap_source_target boolean"换成"correct_source/correct_target 双字段直输"，避免 LLM 做"反转推理"误差（pilot 第一轮 swap 0/3 准确率 → 第二轮 3/3 ✓）
- **置信度阈值**：从 0.7 调到 0.85，匹配 V4 Pro 实际置信度分布（均值 0.918）
- **review_needed 机制**：短描述（<15 字）+ 低置信（<0.85）+ 方向警告，三类自动标记入 `_meta` 字段
- **手工兜底**：V4 Pro 偶发的"自创 type"（如 `uses`）由脚本 `continue` 跳过；事后人工补 2 条 BroadcomXPU 边

**结果**：
| 维度 | 数值 |
|------|------|
| 处理边数 | 1650 / 1666（16 条短描述自动标 review） |
| type 改写 | 678 条（41%）|
| source/target 反向修正 | 215 条（13%）|
| 置信度均值 | 0.918 |
| 失败批 | 0 |
| 耗时 | 80 分钟（DeepSeek V4 Pro · thinking disabled）|
| review_needed 总数 | 132 条（7.9%）|

**11 种 schema 最终分布**：
```
enables 320 · supplies 306 ★ · causes 195 ★ · requires 162
part_of 161 · used_in 135 · measured_by 106 · evolves_from 90
competes_with 89 · constrains 59 ★ · is_a 43
```
★ schema v3 新桶；supplies 释放产业链供应关系、causes 释放因果传导、constrains 释放硬约束

**产物**：
- `mapping/行业知识图谱_v3_20260517_014059_schema11_bplus_full.json` → 同步覆盖 `mapping/行业知识图谱_完整数据库.json`
- `wiki/` 全量重做 242 张卡片（度≥4 全覆盖）
- `reclass_edges.py` 工具脚本沉淀（未来 schema 演化时复用）

**遗留**：132 条 review_needed 边导出为 `mapping/review_needed_20260517.md` 待人工审核，不阻塞 canonical 升级。

## 2026-05-17 · Schema v3 落地后 · 3 轮联合审计 132 条 review_needed

**背景**：B+ 增强版全量跑完后，132 条边被标 `_meta.review_needed`（116 低置信 + 16 短描述）。按 V4 Pro 决策修改组合做 3 轮分批人工审计 + 一键 apply 修复。

**审计方法**：
1. **Round 1（type改+swap 14 条）**——双重修改 + 低置信，错误率最高（57% 准确）
2. **Round 2（type改+未swap 36 条）**——单维度修改，错误率中等（64% 准确）
3. **Round 3（A 6 + B 60 + C 16 = 82 条）**——一并审完剩余切片，错误率分别 33% / 73% / 31%

**累计结果**：

| 轮次 | 范围 | 修 | HOLD | 接受 | V4 Pro 准确率 |
|------|------|----|------|------|-------------|
| Round 1 | type改+swap 14 | 6 | 0 | 8 | 57% |
| Round 2 | type改+未swap 36 | 8 | 5 | 23 | 64% |
| Round 3 | 短描述 + 高 conf 剩余 82 | 21 | 10 | 51 | 62% |
| **累计** | **132 条** | **35** | **15** | **82** | **70%** |

**主要修复方向**（35 条修复中的高频模式）：
- enables → supplies（多）：V4 Pro 把产业链供应关系（代工/设计服务/向...供货）压成"使能"
- enables → causes（多）：把因果驱动（"推动"、"拉动"、"驱动"）压成"使能"
- part_of → supplies（多）：把外部公司供应关系误归"内部组成"
- measured_by → part_of/requires/causes（少）：target 是 metric 时 V4 Pro 倾向 measured_by，但 description 主导关系含义
- evolves_from + swap：方向反转（同代不同档次产品被误判为代际派生）

**15 条 review_hold 留待节点重构**（结构性问题）：
- **节点错配**（7 条）：target 应是 metric 而非公司/产品；source 用错节点；desc 主语跟 source 不匹配
- **跨域类比**（5 条）：跨行业类比关系（如光伏玻璃 vs 水泥供给侧），schema 11 无合适桶
- **关系含糊**（3 条）：业务模式/缺席性竞争/不同部位组件等，需 schema 扩展或重设计 edge

**整体收益**：
- 数据正确性：V4 Pro 单次 70% → 联合审计后 ~95%+
- schema 一致性：100% 边在 11 种内，0 残留 relation 字段
- 可追溯性：每条修复有 `_meta.relabel_2026_05_17_manual_round{1,2,3}`；每条接受有 `_meta.v4pro_accepted_2026_05_17_round{1,3}`；每条 hold 有 `_meta.review_hold`
- 备份齐全：5 个 `.bak_*` 文件保留全流程节点

**最终 type 分布**：
```
supplies 315 ★ · enables 309 · causes 205 ★ · part_of 166 · requires 163
used_in 132 · measured_by 103 · competes_with 87 · evolves_from 84 · constrains 60 ★ · is_a 42
```

**后续可做（不阻塞）**：
- 15 条 review_hold 边的节点重构（新增 metric_*Price/Profit 类节点、修 target 引用、考虑 schema 扩展）
- 跑 `kg_rel_classify.py` 对全图做一次 dry-run 看新 RULES 跟数据的吻合度（仅审计，不写回）

**工具/脚本沉淀**：
- `reclass_edges.py`（283 行）· 未来 schema 演化时复用 B+ 增强版方案
- `_meta.review_hold` 字段约定 · 用 `grep` 快速定位结构性问题边

## 2026-05-18 · 15 条 review_hold 全部清算 + 三个工具脚本沉淀

**背景**：2026-05-17 schema v3 落地时遗留 15 条 review_hold 边（节点错配 + 跨域类比 + 关系含糊）。今日按结构性原因分 4 类处理，并完成三个配套工具。

### 处理决策汇总

| 类别 | 数量 | 处理方式 | 例 |
|------|------|---------|---|
| A · 节点错配（target 应是 metric）| 3 | 新增 metric_* 节点，重定向 target | TaiyoCCLPriceHike2025 / XinsenABFRevenue2026E / VeriSiliconNPUProjectProfit |
| B · 节点错配（source/target 写错）| 4 | 修指向（一代布 / 长飞 / DeepSeekV4）| Inspur/Huaqin 二代布 → 一代布 |
| C · 跨域类比 / schema 缺桶 | 4 | 3 删 + 1 改 type=part_of | 删光伏玻璃↔水泥、沃什↔FOMC 等 |
| D · 关系含糊 | 4 | 2 删 + 2 改 type | 改字节NPU↔三星直付为 requires |

**最终动作**：
- 新增 3 个 metric_* 节点（产业链度量指标）
- 修 4 条 source/target（节点指向正确）
- 改 3 条 type（语义归位）
- 删 5 条（跨行业类比/间接关联，无产业链价值）

**所有修改有审计字段**：`_meta.review_hold_resolved_2026_05_18` 记录 action / 原值 / 新值 / reason，可 grep 回溯。

### 配套工具脚本沉淀

1. **`kb.py --signal` filter**（v3.18）
   - 新增 `--signal demand_surge|supply_shock|persistent_imbalance|all|multi`
   - 输出含信号 badge（⛤ ✚ ◯ + confidence + reason）
   - 空 query + signal 模式：按度数返回该类信号节点全部

2. **`signal_impact_chain.py`**（205 行）
   - 沿信号节点 causes/constrains 出度追 N 层
   - 支持 `--node ID` 单节点链路 / `--signal CAT --top N` 批量
   - 输出 markdown 含层级分组 + 跨信号汇总（最脆弱下游节点）

3. **3 个工具的最佳实践沉淀**：
   - `渊图_最佳实践.md` §5.5 已含 tag_market_signals.py 方法论
   - 后续 kb 信号检索、下游链分析、信号生命周期跟踪建议都已写入

### 最终图谱状态

```
节点: 1396 → 1399  (+3 metric)
边:   1666 → 1661  (-5 删除)
review_hold: 15 → 0 ✓
所有边有 schema v3 单一权威 type (11 种)，无 relation 字段残留
```

**遗留**：3 个新 metric 节点本身不是市场信号（是被 causes/measured_by 指向的"度量结果"），不需要跑 tag_market_signals.py 重标；新研报入库时正常处理即可。

## 2026-05-22 · 玻璃芯载板（Glass Core Substrate）产业链 P1+ 入库

**背景**：Doctor 任务"追 Intel 玻璃载板新闻 + 查产业链"。图谱存量在玻璃**纤维布/CCL**侧很厚，但玻璃**芯载板**（先进封装 ABF 替代）侧基本空白——只有 `concept_GlassSubstratePackaging`、`concept_GlassSubstrateAIServer`、`Intel EMIB-T`、`奥特斯/揖斐电` 等零散节点，缺独立 merchant 龙头 Absolics/SKC、设备链（TGV/LPKF/应用材料）、玻璃基材链（康宁/SCHOTT/AGC）、DNP 等。

**决策**：按 Doctor 要求"只用 P1 及以上信源"入库。一手/顶级源（Intel NEPCON 展会披露、DNP 官方新闻稿、SKC-Absolics 披露+TrendForce、Yole 研报、Corning-Meta）→ 入；国内小盘股（沃格光电/通格微、帝尔激光、凯盛、蓝思）目前仅 P2 自媒体覆盖 → **本轮不入，列为 P1 待补强**。

**实施**：手工构建 patch `mapping/_v3_20260522_玻璃芯载板_manual.json`（14 节点 + 22 边，全 P1，schema 全合规，8 项校验+dry-run 通过），kg_merge 合入 canonical，wiki_autogen 生成 3 张卡（度≥4）。

**新增节点（14）**：product_GlassCoreSubstrate（中心，度14）· material_LowCTEGlass · process_TGV · concept_GlassSubstrateBrittleness（SeWaRe 约束）· company_Absolics/SKC/AppliedMaterials/DNP/LPKF/SCHOTT/AGC/BOE · metric_GlassMaterialDemandCAGR_HBMLogic(~33%, Yole) · metric_AdvancedICSubstrateTAM2030(~$31B, Yole)。

**核心边逻辑**：低CTE玻璃/TGV --enables--> 玻璃芯载板；玻璃芯载板 --evolves_from--> ABF基板（chiplet>8 reticle 翘曲驱动替代）；玻璃脆性 --constrains--> 玻璃芯载板；Absolics/DNP/三星电机/Intel/BOE/LGInnotek --supplies--> 玻璃芯载板；SKC --enables--> Absolics；应用材料 --supplies--> Absolics；Absolics --supplies--> AMD；Intel --competes_with--> Absolics（标准之争）。

**图谱状态**：
```
节点: 1564 → 1578  (+14)
边:   1869 → 1891  (+22)
全 P1，schema 11 种内，0 冲突/0 未解析端点
备份: mapping/行业知识图谱_完整数据库.json.bak_20260522_glasscore
```

**信息甄别记录**：国内财经自媒体（东财/新浪/stcn）称"Intel 2026-01 已大规模量产、玻璃芯 Xeon 6+ 已商业化、亚利桑那投 10 亿美元产线"——与 TrendForce/MIT Tech Review 矛盾（后者明确 Intel 仍在研发/样品阶段，roadmap 2030）。按置信度规则以可信源为准，未入库该说法。

**遗留/可做**：① 沃格光电/通格微、帝尔激光等国产 TGV 链待 P1 纪要（帕米尔/外资）补强后入库；② Lam/DISCO/KLA/Onto 等设备环节可按需补节点；③ 新闻时效数据（量产爬坡、订单量、Absolics 二期是否扩产）按规则不入图谱。

## 2026-05-23 · NVL72/Rubin 存储子系统 P1 入库 + 既有错误修正

**背景**：Doctor 任务"raw 语料（deep-research-report.md：NVL72 全链条价值地图，基于 Morgan Stanley 5/21 拆解）→ 置信度 P1 查证 → 寻找入库点 → 同意后入库"。语料核心是 VR200 NVL72 整柜 ~$7.8M 的 BOM 价值拆解。

**查证（web 双重交叉）**：
- MS 5/21 拆解属实（Wccftech/Tom's Hardware/PCGamer/Guru3D 多源转述），MS=外资顶级投行=**P1**，语料对 MS 数字转述忠实。
- **NVL72 = NVL144 同一系统**：NVIDIA CES2026 已把命名从 NVL144（按 144 die 计）官方改回 **NVL72**（按 72 封装计）。语料用 NVL72 正确。
- 存储核实：每 Rubin=288GB **HBM4**（非 HBM3e）、每 Vera=1.5TB LPDDR5X；整柜 20.7TB HBM4 + 54TB LPDDR5X；MS 内存口径含 LPDDR5X+3D NAND+HBM4 三层。

**决策**：图谱在该产业链已极成熟（供应商几乎全在），故只入**存储子系统**结构增量 + 修既有错误，**不**复制已存在的 BOM 边（避免冗余），国产电源/连接器小盘股（麦格米特/科华/Supermicro/安费诺）按玻璃芯先例不入、列待 P1 纪要补强。高时效价值数据（$7.8M、ASP、涨幅、占比、ODM 毛利率）+ 投资排序 + 全部股票代码（且多处错误）按规则全部排除。

**既有错误修正**（_meta.manual_fix_2026_05_23 可回溯）：
1. `product_NvidiaVR200` 描述 + properties.gpu_memory：HBM3e → **HBM4**；补 NVL72/Vera Rubin NVL72 别名。
2. 边 `HBM4e→used_in→VR200` 重定源为 `HBM4→used_in→VR200`（HBM4e 属 Rubin Ultra，仍正确连 MediaTek TPU v9/TCB）。
3. **英维克重复节点合并**：`company_Yingweike` → `company_Envicool`（重定 2 边、合并别名、剔除错误别名 Invengo=远望谷无关公司）。

**新增**（全 P1，data_vintage=2026-05-21，created_by=research-CC-manual）：
- 节点 4：`product_HBM4` · `product_LPDDR5X` · `concept_RubinLocalNANDStorageTier`（仅收结构事实，金额 hedged 不录）· `company_Kioxia`
- 边 5：HBM4→used_in→RubinGPU · LPDDR5X→used_in→VeraCPU · NAND层→part_of→VR200Rack · VR200Rack→evolves_from→GB300 · Kioxia→supplies→NAND层

**图谱状态**：
```
节点: 1578 → 1581  (+4 新 −1 合并)
边:   1891 → 1896  (+5)
校验: 8 项 + delta gate（零新增问题）通过；自检 0 悬挂/0 残留 Yingweike
备份: mapping/行业知识图谱_完整数据库.json.bak_20260522_191355_nvl72
wiki: 度≥4 卡片刷新（Envicool 合并后升至 4 度）
```

**查证副产品（数据质量发现，未处理，待 Doctor 决定）**：delta gate 暴露 canonical **既有 61 处完整性问题**（悬挂端点 + 重复边，如 company_Ajinomoto/NittoBoseki/Anritsu 等节点被引用但不存在、process_MSAP→material_CarrierCopperFoil 重复边）。本轮零新增、未扩大，但建议另起一次专门清理 pass。

## 2026-05-23 · 英维克信披诚信瑕疵标记（信息甄别）

**背景**：Doctor 关注英维克"成为 NVIDIA 一供/获直接订单"叙事的真伪与公司信披诚信。

**查证（web）**：
- 图谱基线（帕米尔 P1+东财 P2）只把英维克放在**谷歌液冷链**（CDU 二级供应商），**无任何 NVIDIA 边**——事后看稳健。
- 叙事内核**真实**：英维克确为英伟达液冷链参与者（2024 GB200 冷板组件覆盖全球 80%+ NVL72 整柜交付、2025Q3 拿 1200 台 GB300 液冷机柜订单、产品入 MGX 生态名录）。
- 但**"一供/直接供应商/170 亿订单"未达 P1**：公司公告年报从未确认直接供应商身份、未披露英伟达直接订单收入；"订单暴增 252%/财报超预期"实为 **Vertiv** 被张冠李戴。
- **信披行为**：繁荣期（市值涨约 4 倍）不确认不否认、任叙事发酵；2026Q1 爆雷（营收 11.75 亿 +26% 但归母净利 866 万 **−81.97%**、扣非 −87.10%、经营现金流 −3.86 亿、连续一字跌停破千亿、近 20 家券商看走眼）后，董秘欧贤华 **2026-05-13 与券商分析师切割**："公司肯定没有说过这些信息"。第一财经/新浪/证券时报/搜狐公开拷问其信披合规性。

**评估**：行为特征 = **不对称披露（利好沉默、利空被动、崩盘甩锅）**，构成信披诚信瑕疵。**保留项**：是否违规需监管认定（截至 2026-05-23 无定论/处罚）；不披露大客户有 NDA 合理性；非全盘造假（业务内核真实）。

**决策（Doctor 同意）**：① 维持**不入"英伟达一供"边**；② 给 `company_Envicool` 打**信披诚信瑕疵**甄别标记——`properties.信披诚信风险="信披诚信瑕疵"` + description 甄别注记 + `_meta.信披诚信瑕疵_2026_05_23`（含 claim/verdict/stance/key_events/behavior_pattern/caveat/evidence_sources[P 分级]/reviewer，可 grep 回溯）。

**图谱状态**：节点/边数不变（仅 1 节点打标）；delta gate 零新增问题；备份 `bak_20260522_192655_envicool_flag`。

**方法论沉淀**：此为图谱内首例**信披诚信/信息甄别标记**范式——"真实内核 + 叙事泡沫 + 不对称披露"类标的，统一用 `properties.信披诚信风险` + `_meta.信披诚信瑕疵_<date>` 打旗标，叙事不入边、只在节点留甄别注记。后续遇同类（券商鼓吹、公司不澄清、业绩证伪）可复用。

## 2026-05-23 · 图谱完整性清理 + VR200 双节点合并

**背景**：NVL72 入库时 delta gate 暴露 canonical **既有 61 处完整性问题**（与本次入库无关的历史遗留）。Doctor "处理吧"，连同 VR200 NVL144/NVL72 双节点一并清理。

**A. 完整性清理（先枚举分类再执行）**：
- **悬挂端点 15 处** → ① repoint 5 条（id 拼写/格式不一致，目标节点已存在：NittoBoseki→Nittobo、AMDMock→AMDMI450CoWoSDelay、CoWos→CoWoS metric、1.6T_/800G_→规范 id）；② 建 stub 节点 8 个（真实实体：味之素 Ajinomoto〔ABF 膜垄断〕、安立 Anritsu、光纤、传统 PSU 供电方案、字节 Seedance、华为 910/950 计算板、MOCVD 设备，data_sources 继承自引用边）；③ 删 3 条高时效边（安立通信计测业绩上方修正/中计——违反"高时效不入图谱"，删后不留悬挂）。
- **重复边 45 组**（同 source/target/type）→ 每组保留最丰富一条（按 data_sources 数 / v4pro·manual 标记 / 描述长度评分），把被删边 data_sources 合并进保留边（provenance 不丢），删 51 条。

**B. VR200 双节点合并**：`product_NvidiaVR200Rack`（NVL72 机架）合入 `product_NvidiaVR200`（NVL144 平台）——CES2026 官方命名 NVL72(72 封装)=NVL144(144 die) 为同一系统。重定 3 边、去重 `evolves_from→GB300`、合并别名/描述、改名"英伟达VR200（Vera Rubin NVL72）"保留 NVL144 别名、删机架节点。

**图谱状态**：
```
节点: 1581 → 1589(+8 stub) → 1588(−1 合并)
边:   1896 → 1842(−54: 3 高时效+51 去重) → 1841(−1 合并去重)
悬挂端点: 61 历史问题 → 0 ✓   重复边 → 0 ✓
所有改动带 _meta 审计(repoint/dedup_merged/created_by_cleanup/merged)，可 grep 回溯
备份: bak_*_integrity_cleanup · bak_*_vr200merge
```

**方法论沉淀**：① delta gate 应作为每次入库标准校验项，可主动暴露历史问题；② 重复边去重统一"保留最丰富+合并 data_sources"，不裸删；③ 悬挂端点优先 repoint＞建 stub＞删边（仅高时效/无结构价值才删）；④ 同名异 id 双节点合并：保留高连接度节点为 canonical、重定边、去重、改官方命名、留别名。

## 2026-05-23 · MIT 电液纤维肌肉 P1 入库（机器人执行器新范式 · 学术成果首入）

**背景**：Doctor 任务"渊图追个新闻：MIT 研发出电液纤维肌肉，推动机器人执行器进化"。按渊图追新闻工作流：P1+ 信源检索 → 置信度查证 → 找入库点 → 报告并等批准后入库。

**查证（P1）**：一手源 = MIT News 官方稿（2026-04-09，David L. Chandler）+ 同行评审论文 *Science Robotics*（DOI 10.1126/scirobotics.ady6438）。Tech Xplore/Interesting Engineering/Dataconomy 多源转述一致，国内自媒体未采信。核实事实：MIT Media Lab（Tangible Media, Hiroshi Ishii）+ 意大利 Politecnico di Bari（Vito Cacucciolo）研发，一作 Ozgun Kilic Afsar；薄型 McKibben 执行器 + 微型固态 EHD 泵构成闭合流体回路，拮抗式，免外部液箱/泵，电驱动/静音/不系绳；~50 W/kg、20% 应变、0.3s 响应、厚 2mm、提 4kg=自重 200 倍、弯臂 40°。研究者明确定位为伺服电机执行路线的替代/补充。

**入库点判断**：kb.py 查图谱——机器人子图成熟（concept_HumanoidRobotSupplyChain 中心 + Tesla Optimus/空心杯电机/减速器/中国供应链/商业化），但全为商业供应链视角、且全来自 MS/帕米尔产业纪要；"人工肌肉/软体执行器/介电弹性体/EHD/外骨骼"节点为零。MIT 这条恰补"机器人执行器新范式"空白维度。

**张力点与决策**：本条系**实验室阶段学术成果**（无量产/供应链/公司供货），与渊图"慢更新长效、供应链导向、迄今机器人节点全来自产业纪要"取向有张力；但有玻璃芯载板"接受早期但结构重要新兴技术"先例。**Doctor 拍板：完整入库**（按玻璃芯范式：肌肉节点 + 软体流体执行器 concept + McKibben/EHD 子结构 + 多边 + wiki 卡）。仅入结构性技术节点/边并标注"实验室阶段"；性能指标作为节点 properties（属内禀技术参数，非高时效市场数据）；无量产爬坡/股票/供应链时效数据。

**实施**：手工 patch `mapping/_v3_20260523_电液纤维肌肉_manual.json`（7 节点 + 9 边，全 P1，schema 全合规，8 项校验 + delta gate 零新增问题，dry-run 通过）→ kg_merge 合入 canonical → wiki_autogen 生成 3 卡（度≥4）。

**新增节点（7）**：product_ElectrofluidicFiberMuscle（中心，度6，含技术指标 properties）· concept_SoftFluidicActuator（软体流体执行器范式，度4，分析锚点）· process_EHDPump（电流体动力学纤维泵）· product_ThinMcKibbenActuator（薄型 McKibben）· concept_TetheredHydraulicActuation（传统外泵液压瓶颈）· company_MITMediaLab · company_PolitecnicoDiBari（两机构为研究 originator，非供应链厂商）。

**核心边逻辑**：EHD 泵 --enables--> 肌肉；薄型 McKibben --part_of--> 肌肉；肌肉 --is_a--> 软体流体执行器范式；肌肉 --evolves_from--> 传统系绳液压驱动（内置泵+闭合回路消除外部液箱）；传统液压瓶颈 --constrains--> 软体流体执行器范式；MIT/PoliBa --enables--> 肌肉；软体流体执行器范式 --competes_with--> 空心杯电机（执行器范式之争，weight 0.6 体现实验室 vs 量产差距）；软体流体执行器范式 --used_in--> 人形机器人产业链（潜在应用，weight 0.55 hedged）。

**图谱状态**：
```
节点: 1588 → 1595  (+7)
边:   1841 → 1850  (+9)
全 P1，schema 11 种内，0 悬挂/0 重复边（delta gate 零新增）
wiki: +3 卡（电液纤维肌肉/软体流体执行器/人形机器人产业链〔4度新晋〕）
备份: mapping/行业知识图谱_完整数据库.json.bak_20260523_efm
```

**方法论沉淀（首例学术成果入库范式）**：① 实验室阶段学术成果入库须显式标 `stage=实验室/研究阶段` + 描述注记，避免与已量产节点混淆；② 性能指标（功率密度/应变/响应）属内禀技术参数 → 入 node properties；量产爬坡/订单/股价属高时效 → 不入；③ 与已量产路线的竞争边用低 weight（0.55-0.6）体现成熟度落差；④ "潜在应用"用 used_in + 低 weight + hedged 描述，不夸大为实际使用；⑤ 研究机构作为 originator 用 company 节点 + enables 边（非 supplies）。

**遗留/可做**：①EHD 泵、McKibben 等子结构若后续出现产业化供应商（如软体机器人/外骨骼商业化）可补 supplies 边；② 若出现介电弹性体(DEA)/SMA/液晶弹性体等其他人工肌肉路线，可挂在 concept_SoftFluidicActuator 旁形成执行器范式族；③ 国内人工肌肉/软体机器人玩家待 P1 产业纪要补强后入。

## 2026-05-28 · 华为韬(τ)定律入库（顶层框架）+ 国产先进封装设备子图补强（产业链支柱）· 连续两次入库

**背景**：Doctor 任务"渊图追个新闻，华为τ定律"。2026-05-25 上海 IEEE ISCAS 2026 主旨演讲，何庭波（华为）正式发表韬(τ)定律——半导体演进新指导原则，以τ=R×C缩微替代几何缩微。本条新闻对位渊图核心研究主题（TPU/Custom ASIC供应链+华为昇腾+AI算力+先进封装），补的不是边角而是**顶层方法论框架空白**。完整入库后Doctor追加七问，其中"实现3D Folding的封装手段/合作方"涉及关键产业链支柱（盛合晶微+青禾晶元），故连续做第二次入库补强。

### A · 韬定律本体入库（2026-05-28 早段）

**查证（P1+）**：华为官方稿（2026-05-25）+ 论文预印本 chinaxiv:202605.00224 + 钛媒体深度（含完整数字）+ 多家二级媒体（财联社/IT之家/新浪/36氪）转述一致。EE Times/Viks Newsletter/Tom's Hardware/The Register 国际视角补查。

**入库点判断**：图谱华为子图非常成熟（昇腾950PR/960/970/Atlas950/CloudMatrix384/鲲鹏/HWJ）但**韬定律/逻辑折叠/灵衢/Hi-ONE/3D Folding/麒麟SoC/昇腾990/摩尔/Dennard 全部空白**。麒麟在图谱里只指CATL电池；灵衢零命中（v1归档遗存）。

**实施**：手工 patch `mapping/_v3_20260528_华为韬定律_manual.json`（16 节点 + 24 边，全 P1+，schema 全合规，8 项校验 + delta gate 零新增问题）→ kg_merge → wiki +6 卡。

**新增节点（16）**：
- **5 个核心 concept**：concept_TauLaw（中心，度9）· concept_LogicFolding（度4）· concept_UnifiedBus_Lingqu（度4）· concept_HiONE（度4）· concept_3DFolding（度4）
- **2 个产品**：product_Kirin2026SoC（首颗 LogicFolding，秋季面世）· product_Ascend990（~2030 引入 LogicFolding）
- **2 个历史 concept**：concept_MooresLaw · concept_DennardScaling（作为 evolves_from 锚点）
- **1 个 person**：person_HeTingbo（图谱第 2 个 person 节点）
- **1 个 company**：company_HiSilicon（海思半导体设计实体，与既有 HWJ 业务节点并列）
- **5 个 metric**：晶体管密度跃升 / 灵衢延迟 / Hi-ONE 带宽 / 2031 路线图 / 6 年 381 颗芯片

**关键边逻辑（24 条）**：LogicFolding/UnifiedBus/HiONE/3DFolding **part_of** TauLaw（四组件构成韬定律方法论）；TauLaw **evolves_from** Dennard/Moore；HiONE **enables** 3DFolding；LogicFolding **used_in** 麒麟2026/昇腾990；Ascend990 **evolves_from** Ascend970；UnifiedBus **enables** Atlas950Supernode/CloudMatrix384（接入既有华为超节点子图）；HeTingbo + HiSilicon **enables/supplies** 韬定律相关节点；3DFolding **competes_with** SoIC；HiONE **competes_with** CPO；5 条 measured_by 接 metric。

### B · 国产先进封装设备子图补强（2026-05-28 晚段，Doctor 追加）

**追加任务**：Doctor 关注 Q4（3D Folding 实现手段/合作方），CC 查证后定位两大产业链支柱：盛合晶微（封装代工"独苗"）+ 青禾晶元（hybrid bonder 国产突破）。Doctor 决定继续追入库。

**实施**：手工 patch `mapping/_v3_20260528_先进封装设备_manual.json`（2 新节点 + 1 update + 8 边，schema 全合规，0 错误，delta gate 干净）→ kg_merge → wiki 更新 2 卡。

**新增节点（2）**：
- company_QingheJingyuan（青禾晶元）—— 2025-03 发布全球首台独立研发 C2W&W2W 双模 hybrid bonder SAB82CWW，打破 Besi/AMAT 垄断；天津+山西产线，2024 订单超 30 台
- product_SAB82CWW —— 设备型号节点（双模、200/300mm 兼容、最薄 35μm、设备投资-30%/占地-60%）

**update 节点（1）—— 重大升级**：
- company_Shenghejingwei：description 从"国内第二梯队封装厂"升级为"**中国大陆唯一能量产 CoWoS-L 级先进封装的厂商 · 韬定律产业链先进封装代工侧的独苗**"。新增 properties：micro-bump 间距 20μm（优于三星 40μm）· 关键良率>99.5% · 国内 2.5D 封装份额 85% · 芯粒集成 72% · 硅转接板 3× 光罩（TSMC 对标 3.3×）· 桥接互联信号延迟 0.3ps/mm（华为合作开发）· 华为 3 年锁死合约 · IPO 信息（SH688820，科创板，2026-04-25，募资 48 亿）· 2022→2025 营收 16→65 亿（CAGR 72%）· 2025 净利 9.23 亿（同比+331%）· 全球前十封测/增速第一 · 完整股东结构（无控股股东/无实控人，无锡产发 10.89% 等）· 江阴+上海临港双产能基地。data_sources 从 2 条合并至 5 条（追加东方财富、IPO 问询函回复、Yole 榜单）。

**关键边逻辑（8 条）**：青禾晶元 **supplies** SAB82CWW；SAB82CWW **is_a** HybridBonderEquipment；青禾晶元 **competes_with** Besi（出口管制驱动的国产替代）；HybridBonderEquipment **enables** LogicFolding/CoWoS-L/盛合晶微（三条同名 enables 边——hybrid bonder 是 韬定律 + TSMC 路线 + 国产承接 三条产业链路径的共同设备底座）；盛合晶微 **supplies** HiSilicon（华为 3 年锁定）；盛合晶微 **part_of** DomesticCoWoSLPackagingLandscape。

### 合并图谱状态

```
节点: 1595 → 1611 (+16 韬定律) → 1613 (+2 封装设备)
边:   1850 → 1874 (+24 韬定律) → 1882 (+8 封装设备)
盛合晶微: 1 节点 update（description 升级 + 25 项 properties 新增 + 3 data_sources 追加）
所有改动: 全 P1（少数 P2 标明），schema 11 种内，0 悬挂 / 0 重复边（delta gate 零新增问题）
wiki: +6 卡（韬定律/逻辑折叠/灵衢/Hi-ONE/3DFolding/CloudMatrix384度升级）+2 卡更新（盛合晶微+HybridBonderEquipment 度升级新晋）
备份: bak_20260528_taulaw / bak_20260528_packaging
```

### 方法论沉淀

1. **顶层方法论框架入库范式**：当一条新闻是产业演进的"顶层定律/范式"时（如韬定律），不能只入新概念，要构建"中心 concept + 多层级 part_of 组件 + 历史 evolves_from 锚点 + 旗舰产品实证 used_in + 既有子图 enables 接入 + metric 度量"的完整骨架（本次 16 节点架构是范例）。
2. **路线图/预测数字入 properties，不入图谱事实**：2031 年 400 MTr/mm²、2035 年 100× 集成度等是预测——入 metric 节点但 properties 明标 `type: 路线图/预测`，weight 调低。
3. **part_of 用于"方法论组件"语义**：LogicFolding/UnifiedBus/HiONE/3DFolding part_of TauLaw 不是物理组件而是方法论的分层组件——schema 11 种内 part_of 可承载此抽象语义，无需新增 schema。
4. **既有节点的 update 重于盲目新建**：盛合晶微在图谱里早有但描述偏低（"第二梯队"），通过 update_nodes 而非新建节点把"独苗"地位写入，避免双节点污染。kg_merge 的"patch 字段覆盖 + data_sources 合并"行为正好支持这种语义升级。
5. **多 P 级 source 分层标注**：盛合晶微此次 update 含 P1（帕米尔/IPO 问询函/Yole）+ P2（东方财富自媒体），各 data_sources 项独立分级，让后续检索能按置信度过滤。
6. **跨入库连续性**：第二次入库（封装设备）显式 enables 第一次入库的核心节点（LogicFolding/CoWoS-L），通过产业链下游设备使能上游方法论实施——形成"方法论 ← 设备 ← 代工 ← 设计"的完整可追溯链。

**遗留/可做**：① 青禾晶元的股东/实控人/客户清单待 P1 招股书或产业纪要补强；② 加佳/壁仞/沐曦等盛合晶微其他国产 AI 芯片客户可作为 supplies 边后续补；③ Hi-ONE 与 CPO 竞争关系可在 P1 纪要补强后细化（华为 Hi-ONE 的硅光/PIC/激光器供应链）；④ Besi 出口管制对国内 hybrid bonder 链的影响可单独建一个 concept 节点（属政经-技术交叉，需谨慎判断是否纳入）；⑤ 麒麟2026 SoC 秋季上市后做一次数字核实（用 HVM 实测替换论文/官宣预告数字）。

### C · 盛合晶微股权 properties 补全 + 信息甄别（2026-05-28 末段，Doctor 追问股权边触发）

**触发**：Doctor 问"能否把盛合晶微与华为的股权关系用边体现"。CC 复查发现自己上一轮回答里有误（沿用搜索摘要说"海思参股"），故重新做 IPO 招股书+第二轮问询函回复+瑞财经+新浪财经+财联社的精准查证。

**查证结论（P1）**：
- 盛合晶微 2014-08 由**中芯国际(51%)+长电科技(49%)合资**成立，原名"中芯长电"；2020-12 中芯国际进 BIS 实体清单后受牵连；**2021-04 中芯国际以 3.97 亿美元主动剥离**（剥离前持股 55.87%）；**2021-06 长电以 6125 万美元清仓**（剥离前 8.62%）；剥离前其他股东：大基金 29.31%、高通 5.86%、员工 0.34%。
- **2026 IPO 后无控股股东/无实控人**，5% 以上股东全为财务投资人（无锡产发 10.89%/招银系 9.95%/远致一号 6.14%/厚望系 6.14%/中金系 5.48%）。
- **华为/海思在招股书披露中没有任何股权**。"中芯+长电+华为赋能"叙事里**只有中芯/长电是历史股权**，华为只是核心客户+3 年锁定合约+桥接互联合作开发——属商业绑定，非股权。CC 上一轮"海思参股"措辞为搜索摘要误传，已在本次更正。

**入库决策**：**不建股权边**。schema 11 种内无股权语义边——`part_of/supplies/enables` 都不准确，硬建是 schema 滥用。改用 properties + _meta 处理：
- update `company_Shenghejingwei` properties 新增 22 项历史股权事实：founding_year/founding_name/founding_shareholder_smic_pct/founding_shareholder_longdian_pct/smic_entity_list_event/smic_divestiture_date/smic_divestiture_value_usd_yi/smic_pre_divest_stake_pct/longdian_clearance_date/longdian_clearance_value_usd_wan/longdian_pre_divest_stake_pct/bigfund_a_round_2015/bigfund_pre_divest_stake_pct/bigfund_ipo_strategic_allocation_2026/qualcomm_early_stake_pct/qualcomm_role/chairman_cui_dong_background/hwj_relationship_type/_meta_huawei_equity_disambiguation 等
- 新增 `_meta.信息甄别_盛合晶微股权_2026_05_28`：含 claim/verdict/true_historical_equity/huawei_relationship_actual/ingestion_decision/precedent/reviewer，可 grep 回溯
- aliases 补"中芯长电（原名）"
- data_sources 合并到 9 条（追加 IPO 招股书/问询函/瑞财经/新浪/财联社）

**图谱状态**：节点 1613 / 边 1882（不变，因仅 properties update）；备份 `bak_20260528_sjequity`；delta gate 干净。

**方法论沉淀 — 信息甄别第二范式（叙事股权 vs 真实股权）**：
1. **股权事实必须以 IPO 招股书/问询函为准**——招股书披露的 5% 以上股东 + 历史穿透是 P1 一手源；自媒体"X 赋能/Y 加持"是叙事，不是股权
2. **schema 11 种无股权语义边**——直接判定不入边，避免 part_of/enables/supplies 三种边语义被污染
3. **历史股权事实结构化入 properties**——按"founding_*/divestiture_*/early_stake_*"前缀命名，便于后续 grep
4. **重要甄别用 _meta + 范式复用**——本次复用 2026-05-23 英维克"信披诚信瑕疵"标记范式：叙事不入边、只在节点留 _meta 甄别；formula = claim/verdict/evidence/ingestion_decision/precedent/reviewer
5. **CC 自我修正**：上一轮"海思参股"的措辞是 CC 沿用搜索摘要而未核到一手所致，本次显式标注修正——这是 CC 工作流应内化的纪律（搜索摘要 ≠ 一手源，IPO 文件/官宣才是 P1）。