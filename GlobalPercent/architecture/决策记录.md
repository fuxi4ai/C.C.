---
title: GlobalPercent · 决策记录
tags: [GlobalPercent, decision, Financial]
created: 2026-06-29
updated: 2026-06-29
status: active
type: decision
project: GlobalPercent
---

# GlobalPercent · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

---

## 2026-06-06 · 上游隔离审查 + 小改 vs 重构

**背景**：Doctor 在 GitHub 发现 `simonlin1212/globalpercent`。原项目是「参考代码 skill」，依赖宿主、不能独立跑，且名叫 GlobalPercent 却从未真正算出任何百分比——只做分类+按成交量列表。

**选项**：A 小改沿用 / B 重构成可独立跑应用。

**选择**：先隔离克隆做静态审查（确认干净、无后门、无 hook），再 **B 彻底重构**。

**影响**：落成 clone-and-run 应用，宿主依赖（`get_data_dir`/`build_llm`）全部替成 env 驱动的本地等价物。

---

## 2026-06-06 · 指数口径 = 注意力/分歧温度（非方向性）

**背景**：要给「GlobalPercent」这个名字配上一个真能算的数。

**选项**：A 方向性（市场看涨/看跌）/ B 注意力·分歧温度（熵 + 动量 + 成交量）。

**选择**：**B**。

**理由**：方向性=隐含买卖信号，违背「不是交易信号」的定位；注意力/分歧温度客观、可复现、money-weighted。方向信息单独以 `net_drift_24h` 给出，明确标注「仅方向上下文，非建议」。

**影响**：headline 是 CORE 模块成交量加权温度；攒满 30 点后转表达为历史百分位。

---

## 2026-06-06 · 前端单文件 ECharts、概率归一化、状态存储

| 子决策 | 选择 | 理由 |
|---|---|---|
| 前端 | 单文件 ECharts（非 React） | 零构建、clone 即跑 |
| overround 校正 | **默认开** | 多腿市场价格和 >1，除以总和才是真分布；原码读 `prices[0]` 高估概率 |
| favorite-longshot 去偏 | **默认关**（`GP_DEBIAS_ALPHA=1.0`，可设 0.7–0.9 开） | 校正方向有共识但幅度有争议，留 env 开关不写死 |
| 状态存储 | SQLite + `asyncio.Lock` | 替原模块级可变全局 `_CACHE`/`_rebuilding`（非原子布尔当锁，多 worker 不安全） |
| 分类 | 单一 `taxonomy.classify()` | 原分类散在两文件、模块名分歧（「政治选举」vs「美国大选」），统一为一套有序模块+一份关键词 |

---

## 2026-06-29 · 捞回 brain + 再审计（金融线启用）

**背景**：Doctor「我们是不是做过一个国际市场温度的项目？」→ 检索定位到本项目（早期 CLI 所建，未进项目总览、无会话日志，故首轮 /resume 搜不到）。Doctor 指令「全量捞回来，再审计」。

**做了什么**：
- 全量静态再审计（约 1450 行自有代码，四框：数据真实性/正确性/安全/工程卫生）。
- 沙箱实跑测试 **28/28 绿**（21 纯算 + 7 IO/mock；INDEX 写的 26 已过时，新增 test_aggregator.py）。
- 捞回 brain：本项目骨架（系统概览/决策记录/GOTCHAS）+ 项目总览加行 + 会话日志。

**审计结论**：无 🔴 阻断、无安全问题、数据真实性铁律通过。6-07 旧审计两条问题（.venv 38M 入树、无 git 仓）均已解决。

**遗留（见 GOTCHAS）**：
- 🟡 Polymarket `oneDayPriceChange` 量纲未核（动量可能静默饱和到 100）——沙箱白名单不含该域名，只能本机核。
- 🟡 一批改动改了未提交（aggregator/publish/GOTCHAS/requirements/test_publish 改 + test_aggregator 新增，悬在 commit beffe24 之后）。
- 🟡 百分位校准用全量历史、无滚动窗口，长期 regime 漂移。
- 🟢 文档测试数 21/26/28 三处不一致。

**影响**：项目从「记忆库外游离」转为 brain 正式登记的金融线项目；后续 /resume 与锚点可寻。
