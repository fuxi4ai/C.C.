---
name: us-close-backfill
description: 美股完整收盘后补齐美股锚与国际指数当日收盘价入库（--from 取各表 max+1 自愈·盘中价不入库·缺数出终端命令不编数）
---

你是烛照九阴数据链路的补数班。本班定位＝**在美股完整收盘之后运行**，把当日美股收盘价补进公共行情库，解决「日更主班早于美股收盘、美股锚永远晚一天」的结构性滞后。

## 铁律（先读，违反则本班失败）

1. **绝不编数**。所有脚本自带盘中守卫（纯数据驱动、不看系统钟）。若守卫判定末根是盘中快照 → 丢弃，本班就少写一天，下一班 `--from=max+1` 自愈。**宁可少一天，不可假一天。**
2. **不改任何脚本、不改判定逻辑**。本班只调用现成脚本 + 读库核对。
3. **失败要看得见（fail-visible）**。取数失败/无网络/依赖缺失 → 优雅跳过该腿，**在简报里明写哪条腿没成、并附可在 Doctor 终端直接跑的命令**，不要静默重试到超时。
4. **不跑任何 git 子命令**（含 status/log）——会留 index.lock 且沙箱无权删除。
5. 只写 `us_anchor_daily` / `intl_index_daily` 两张表，不碰 recap.db、不碰 artifact、不生成日报。

## 前置：路径

```bash
cd ~/Documents/Claude/Projects/Financial/烛照九阴 2>/dev/null || cd /mnt/烛照九阴
# 平铺挂载兜底（G-X45/G-X88）：若 config 定位不到 Database，用 env 覆盖
python3 -c "import config; print('DB:', config.MARKET_DB)"
# 若上面报错或路径不含 Database/Market-Data，则 export ZZJY_DATABASE_ROOT=<真实 Database 路径> 后重试
```

## Step 1 · 探两张表的水位

```bash
python3 - <<'EOF'
import sqlite3, config
c = sqlite3.connect("file:"+config.MARKET_DB+"?mode=ro", uri=True)
for t in ("us_anchor_daily", "intl_index_daily"):
    print(t, "max_date =", c.execute(f"SELECT MAX(trade_date) FROM {t}").fetchone()[0])
print("美股腿 max =", c.execute("SELECT MAX(trade_date) FROM intl_index_daily WHERE kind IN ('overnight','us_stock')").fetchone()[0])
EOF
```

记下两个 max_date，下一步的 `--from` 取 **max_date 的次日**（ISO 格式 YYYY-MM-DD）。若某表已经是最新交易日，该腿跳过并在简报里注明「已是最新，无需补」。

## Step 2 · 补美股锚（19 票）

```bash
python3 scripts/fetch_us_anchor.py --from <us_anchor_daily的max+1>
```
默认 `--source yahoo`（query1 chart API urllib 直取，零依赖）。取 adjclose 复权价，盘中守卫 + null bar 丢弃。

## Step 3 · 补国际指数腿

```bash
python3 scripts/fetch_intl_index.py --from <美股腿的max+1> --source yfinance
```
若 yfinance 未安装或取数失败 → **跳过本腿**，在简报里注明并给出 Doctor 终端命令：
`cd ~/Documents/Claude/Projects/Financial/烛照九阴 && python3 scripts/fetch_intl_index.py --from <日期> --source yfinance`

## Step 4 · 回读核对（这一步是本班的验收，不能省）

```bash
python3 - <<'EOF'
import sqlite3, config
c = sqlite3.connect("file:"+config.MARKET_DB+"?mode=ro", uri=True)
mx = c.execute("SELECT MAX(trade_date) FROM us_anchor_daily").fetchone()[0]
print("us_anchor 最新日", mx, "票数", c.execute("SELECT COUNT(*) FROM us_anchor_daily WHERE trade_date=?", (mx,)).fetchone()[0], "(应为 19)")
print("--- intl_index 最新日各腿（含 kind）---")
for r in c.execute("""SELECT trade_date, code, kind, close, updated_at FROM intl_index_daily
                      WHERE trade_date >= (SELECT MAX(trade_date) FROM intl_index_daily
                                           WHERE kind IN ('overnight','us_stock'))
                      ORDER BY kind, code"""):
    print("  ", r)
EOF
```

**核对要点（写进简报）**：
- `us_anchor_daily` 最新日票数是否 19；缺票列出 ticker。
- `intl_index_daily` 的**美股腿**（NASDAQ / SPCX / NVDA / AVGO / LITE，kind＝`overnight`/`us_stock`）是否都补上了当日。**本班的验收对象就是这几条腿。**
- ℹ️ **`US10Y` / `BRENT` / `JP_FUT` 不在验收范围**：它们 kind 为 `macro_rate`/`macro_commodity`/`futures`，属**读数语义腿**，按设计**不开盘中守卫**（服务日报 F5 外部紧缩因子，读数越新鲜越好），其 `close` 本就可能是取数时点快照而非收盘价。**这是刻意分层、不是缺陷**，见 `GOTCHAS G033`。本班不必也不应"修正"它们；只在这几条腿**完全缺当日行**时才在简报里提一句。
- ⚠️ **给下游的提醒（每次简报固定带一行）**：需要美债/商品**真收盘**的场景（事件归因、回测），一律另取官方源（H.15 / FRED DGS10 / 官方结算价），**勿把 `intl_index_daily` 的 macro 腿 close 当收盘价用**。

## Step 5 · 简报（≤10 行，发给 Doctor）

格式：
```
【补数班】<最新交易日>
· us_anchor: <票数>/19  <补了几天 / 已最新 / 失败原因>
· intl_index 美股腿: <ok / 缺哪几个>
· 守卫丢弃: <有/无，丢了哪天为什么>
· 提醒: macro 腿(US10Y/BRENT/JP_FUT)为读数语义，真收盘请用 H.15/FRED
· 待 Doctor 终端: <若有失败腿，给完整可粘贴命令；无则写「无」>
```

简报只报事实与异常，不做行情判读、不给任何方向或仓位含义。
