---
title: 风险日报 · 系统概览
tags: [风险日报, architecture]
created: 2026-08-13
updated: 2026-09-11
status: active
type: architecture
project: 风险日报
---

# 风险日报 · 系统概览

> 项目全貌浓缩（brain-save Step 4 更新「最后活跃」字段的依据）。

## 定位

双轴风险看板（杀伤影响 × 触发概率），构建器 `Projects/风险日报/build_risk_daily.py`，产出走「暖色·警示页」范式。

## 入口

- 决策记录：`architecture/决策记录.md` · GOTCHAS：`GOTCHAS.md`

## 当前状态

活跃。每日 09:08 PT `refresh-risk-daily` 班刷新 artifact；R 融资保证比日更已接入（三道齐上）。

## 关键决策（最近）

- R 融资保证比日更接入（2026-07-28）：重试（3 次指数退避）+ 日链 + 陈旧护栏（>3 交易日标红、>10 降级仅供参考）；陈旧判定用交易日不用日历日。
- 成交额定源甲案修订（2026-08-13 问答板裁）：2020+ 窗口分位/回测切 `volume_trillion`，待挂载 `Market-Data`。

## 已知坑

见 `GOTCHAS.md`（如 TACO regime 文案易被误读成触发判定、卡面右下角文案超宽、AI 面板呈现层源头等）。

## 手工解读层维护（2026-09-11 立 · 2026-09-10 停更 45 天事故沉淀）

卡片分两层，勿混判：**机器数值层**（pain/走势/温度·每日 09:08 PT 班自动刷新·数据在 `data/taco_components.db` 与 market_data.db）与**手工解读层**（`taco_reading.json` 的 regime/note/as_of、`geo_reading.json` 的 phase/note/as_of·手动改文件即生效、无需改码）。

- **诊断**：「某卡很久没更新」先分两层核——机器层查 `taco_components.db` 最新日期与 envelope sources 的 machine 标志；手工层查 reading json 的 mtime/as_of 内嵌键。机器层日更会掩盖手工层停更（2026-09-10 TACO 案：机器值 09-08 vs 手工 07-27 同卡共存 45 天）。
- **更新流程**：改两 json（regime/phase/note 属判断性内容 → Doctor 批草案）→ `python3 build_risk_daily.py` 重跑（沙箱可跑·exit 0）→ dashboard 回读验证 as_of/相位/触发态 → 全报 `data_cutoff.value` 随最旧手工源前移（本次 07-27→08-13）。
- **词表**：geo 五档 escalating / standoff / easing_start / easing_confirmed / resolved → 触发态 fired/near/none（仅 IR-P4 命中时生效）；taco regime 三档 normal / complacent / anti_taco。
- **手工源清单**：taco_reading.json · geo_reading.json · fomc_market_exp.json（CME FedWatch·每会前手更·无免费 API）。任一停更都会拖 data_cutoff。
- PEC register 是读数依据（引用不修改·自有 9-30 中评节奏）。

## 最后活跃

2026-09-10 TACO/地缘手工解读层更新（Doctor 批「起草待确认」→ 确认落盘）：geo `phase→escalating`（触发态 fired）· taco `regime=normal` 维持（「折服」推迟至 11 月中选后）· 两 reading json as_of→09-10 · build 重跑验证 · 全报 `data_cutoff` 07-27→08-13。详见 logs/2026-09-11-TACO与地缘手工层更新.md。

2026-09-09 AI-Tech-Alarm 迭代三阶段（VV 请求 · Doctor 批）：消费端接通 alarm_store v2（渲染器 render_alarm_tab.py + build 改造 + 旧快照归档）· 语义修正（研究关注优先级/定价证据/真实窗口/证据状态列）· 七主题迁移（新 revision r-a52c920d）· EAL 固定版本绑定 · 独立审查 12 PASS · artifact 已进（顶部卡片挂载路径修复待明早班重推复验）。详见 logs/2026-09-09-Alarm迭代与渊图三批及CPO测试设备.md。

2026-08-14 BT-19 四闸结论登记（观星维持信息层三重锁 · Δp_hike 入预注册观察）+ PRD 分轨验收机制落地（模板阶段 2.5 · G-X4 第 7 条 · brain-prd v1.3）；此前 08-13：成交额定源甲案修订裁定（待挂载执行）。
