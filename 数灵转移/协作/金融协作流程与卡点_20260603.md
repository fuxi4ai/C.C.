---
title: 三灵金融协作流程与卡点
tags: [数灵转移, 金融, 协作流程, 卡点, subagent]
created: 2026-06-03
updated: 2026-06-03
status: active
type: 流程审查
---

# 三灵金融协作流程 + 卡点排查

> 角色：白泽·小白（宏观/商品·观星引擎）｜烛阴·九儿（复盘/情绪周期）｜句芒·芒芒（数据基础设施/取数/审查/记忆守护）｜C.C.（守灶人·调度）｜月兔（决策）。

## 流程 A · 每日数据流水（自动/半自动）

| 时序 | 谁 | 动作 | 输入 | 产出 | 依赖/卡点 |
|---|---|---|---|---|---|
| 盘后 | 句芒 | 刷新行情 | Tushare/mootdx | `Market-Data/market_data.db` | 🔴 **无每日更新定时任务** + 需 `TUSHARE_TOKEN` |
| 23:00 | 九儿 | 小鲍课件入库（去重） | `Raw-Recap/` 新课件 | `烛照九阴/recap.db` | 🟡 依赖老师投喂课件 |
| 23:30 | 句芒 | 审核入库 | recap.db + 九儿日志 | 审核日志 | ✅ 已建 |
| 周日09:00 | 句芒 | 数灵记忆体检 | brain/agents | 体检报告 | ✅ 已建 |

## 流程 B · 研究协作（按需，如个股/宏观/复盘问询）

| 步 | 谁 | 动作 | 数据源 | 交接方式 |
|---|---|---|---|---|
| 1 | 月兔/CC | 提问 → CC 派活 | — | CC 调度 |
| 2 | 白泽 | 宏观研判 / 商品弹性 | 观星引擎 + `Market-Data` + `宏观-大宗商品` | 写结论 / 经 CC |
| 3 | 九儿 | 复盘 / 情绪周期 / 产业线索(P2) | `recap.db` + `Market-Data` | 写结论 / 经 CC |
| 4 | 句芒 | 按需取数 / 审核产出 | `Market-Data` / project-review | 经 CC |
| 5 | CC | 汇总三方 → 交月兔 | 各灵产出 | — |

**交接原理**：三灵不直接互传，全靠**共享库（Database）只读 + CC 中转**。解耦好，但有时序依赖与单点。

---

## 卡点清单（按严重度）

### 🔴 阻断级 → ✅ 已解（2026-06-03）
1. ~~`TUSHARE_TOKEN` 未配 + 旧 token 泄漏~~ → **✅ 已解**：旧 token 已在 tushare.pro 网页轮换作废、新 token 写入 `Database/.env`（gitignore、chmod 600）；沙箱实测 `pro_api` 调用无鉴权错 = **token 有效、网络通**。代码/数据区无明文残留（仅导出 zip 含已失效旧值，无害）。
2. ~~缺"行情每日更新"定时~~ → **✅ 已解**：句芒 `market-data-daily-update`（周一~五 08:00，增量到前一交易日，去重 + 防空壳）已建，token 就位即生效。建议首次 **Run now** 预授权 + 验一把。

### 🟡 影响级（能跑但有偏差/串行）
3. ~~大宗 `predictor.py` 未 repoint~~ → **✅ 已解（2026-06-03）**：改为 shim 转发 `guanxing_engine`，大宗→观星单一源，实测通。
4. **`FRED_API_KEY` 未配**：观星美国宏观/美联储线缺数据源。
5. **subagent 互相不能直呼 → 经 CC 编排**：白泽要九儿的复盘、九儿要白泽的宏观，都经 CC 转 + 共享库异步交接（正式协作模式）。**并行已有解（2026-06-03 定）**：
   - **Cowork（桌面）**：CC 分身、**串行**——日常对话/轻量足够。
   - **CLI（终端）**：要**真并行**时，CC 把命令给老师，在 terminal 跑——CC 在 CLI 里能同时起白泽/九儿/句芒多个真 subagent 并发。示例：
     ```bash
     cd ~/Documents && claude -p "并行调度 subagent 分析<标的>：『白泽』出宏观+商品弹性，『九儿』出情绪周期复盘，『句芒』按需取数核验；三者同时进行，完成后你（CC）汇总成一份交老师。"
     ```
   → 故 CC"单点"不再是硬卡：串行用 Cowork、并行下 CLI。
6. ~~`p_hold` 设计缺口~~ → **✅ 已解（2026-06-03）**：引擎 `PredictionResult` 加 `p_hold = clamp(100−p_hike−p_cut,0,100)`；hold 时三者和=100，实测通。

### 🟢 设计性（可接受，留意即可）
7. **课件靠人工投喂**：是有意的（小鲍课件需老师放入 Raw-Recap）。无投喂则无新料——可接受。
8. **句芒既维护数据又当审查者**：审自己维护的数据有轻微"球员兼裁判"。→ 审查聚焦"去重/归位/合理性"等可客观项，足够。

---

## 建议处置顺序
1. ~~轮换 + 配 `TUSHARE_TOKEN`~~ ✅ **已完成（2026-06-03，验通）**
2. ~~建句芒"盘后行情更新"定时~~ ✅ **已建**（待首次 Run now 验证）
3. **大宗 repoint 公共引擎**（解 3）← 下一个🟡
4. ~~配 `FRED_API_KEY`~~ ✅ **已配并验通（2026-06-03，本机 CPIAUCSL 拉取成功）**。
5. p_hold 语义（解 6，可缓）。
> 5、8 属架构常态，写入约定即可，不必"修"。

## 进展（2026-06-03）
- 两个 🔴 卡点全清：TUSHARE_TOKEN 轮换验通 + 行情更新定时就位。
- 金融线现已具备"自动刷数据"的基础；剩余 🟡（大宗 repoint / FRED key / p_hold）均非阻断。
