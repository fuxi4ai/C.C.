---
title: 龙鱼五力 · 决策记录
tags: [龙鱼五力, decision]
created: 2026-05-14
updated: 2026-08-06
status: active
type: decision
project: 龙鱼五力
---

# 龙鱼五力 · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

## 2026-08-06 · 「不编分」占位记录标准形态：空 analyses + _meta 待研究标记，下游以容错审计为前提


**触发**：holdings.json 11 只持仓里泡泡玛特 09992.HK 缺 records 记录，总览只显示 10 只。Doctor 明令「没有可靠六维分析时不要编分，可明确标记待研究」。

**裁定**：占位记录标准形态 = `analyses: []` + `latest: ""` + `_meta.补建_YYYYMMDD{note, reviewer}`，**不填任何分数**；INDEX/看板降级显示「待打分 / —」。

**为何安全（关键机制核实）**：对 9 个下游读取器逐一审计空数组容忍度——build_index.py / board_data.py / build_dark_board.py / record_writer.py / backfill.py / score_subitems.py / run_dual_scorer_batch.py / build_dual_compare.py / 白泽 lib_public_read.py 共 8 个容忍；唯 build_html.py `latest(r)` 对空数组返回 undefined、sort 访问 `.total` 抛 TypeError 整板白屏 → 打 `||{}` 补丁 + 弹窗「未打分」兜底（当日已修）。**日后新增占位记录无需重审；新增读取器须过同一张空值清单**。

**全文**：`brain/logs/2026-08-06-龙鱼白泽双库修复与空analyses审计.md`

---

## 2026-07-12 · 两周更 race 错开：longyu 挪周六 22:00、白泽保持周日 01:00

**触发**：`longyu-weekly-dualscorer`（周日 00:01）与 `baize-weekly-report`（白泽大宗周报·周日 01:00）间隔仅 ~1h，longyu 跑 19 只串行常超时、撞进白泽窗口；白泽 Stage 0.5[3][4] 又会并发写龙鱼 records，实测 07-12 抢重打 5 只 tech 的 deepseek 腿、覆盖了 claude 腿（详双scorer log 的 race 事故）。

**裁定**：longyu cron `0 0 * * 0` → **`0 22 * * 6`（周六 22:00）**；白泽大宗保持周日 01:00 不动（Doctor 拍板，AskUserQuestion 三方案选「白泽推后」的对偶——最终选「longyu 前挪」）。description 未擅改（回退只改 cron）。

**为何安全（关键机制核实）**：白泽周报六维经 `lib_public_read.lookup_company` 按**最新 mtime** 读龙鱼库 records，**不认「周日那一份」**，故 longyu 周六产出、周日白泽照读到最新；且白泽重跑龙鱼分的阈值是「**过龄≥1周才重核**」，longyu 周六刚跑完、周日才 <1 天龄 → 白泽**跳过、不重打** → 当初的「抢重写 deepseek 腿」从源头掐掉。间隔约 3h + 过龄阈值 = 双保险。

**代价 / 残留**：longyu 产物（对比/趋势/看板）文件名日期与标的分析日期落**周六**（cosmetic，白泽不读这些文件、按 mtime 取库）。根因「两周更共享龙鱼 records 并发写」未根治，错开只缓解；若未来再撞可上文件锁/仅 append。

**全文**：`brain/logs/2026-07-12-龙鱼白泽周更错开与收尾.md`

---

## 2026-07-07 · 分类边界裁定：航天机电归航天 / 中兴→其他｜通信设备 / 昊华→半导体｜特气材料

**触发**：承 07-06「板块｜环节」两级分类里悬置的三处边界（航天机电、中兴、昊华）。Doctor 定航天机电归航天，中兴/昊华授权 CC 裁。

**裁定**：① **航天机电 600151.SH** = `商业航天｜宇航电源`（本就在此，确认保留；确有卫星太阳能电池阵/空间电源业务）。② **中兴通讯 000063.SZ**：`商业航天｜地面终端` → **`其他｜通信设备`**——千亿级综合通信+服务器巨头、卫星敞口相对边际，按「主业在别处按主业归」单拎出，避免虚胖商业航天组（其余地面终端标的海能达/广和通/信维/通宇卫星终端是核心增量，保留）。③ **昊华科技 600378.SH**：`油气化工｜化工材料` → **`半导体｜特气材料`**——氟化工/精细化工不沾油气、放油气化工本就别扭，进 universe 系冲电子特气→半导体，归特气材料组（中船特气/金宏/凯美特气旁）更说明其为何在此。两条均落 Doctor 07-06 floated 的备选。

**影响**：改后板块分布 半导体 11→12 / 商业航天 31→30 / 油气化工 5→4 / 其他 2→3，95 标的不变；随 commit `1abb7fe` 一并上云。改类＝改 `细分环节.json` 重跑生成器（承 07-06 决策，版式不动）。

**全文**：`brain/logs/2026-07-07-龙鱼库上云与gh认证及分类修订.md`

---

## 2026-07-06 · 个股库看板行业分类＝「板块｜环节」两级，真源 `细分环节.json`

**触发**：Doctor 要看板行业分类参渊图口径细化。先试「细分到光模块/CW光源/激光直写设备」被撤回，改定**两级 `大板块｜环节`**（如 AI硬件｜光通信、半导体｜设备）。

**裁定**：① 真源＝`Database/龙鱼-标的分析库/_index/细分环节.json`（ts→{板块,环节,label,name}+`_meta.板块序/板块色`），全 95 只、**8 板块**（AI硬件/半导体/商业航天/机器人/资源/油气化工/新能源/其他）；records 不改，生成器读表出 `sector`(板块·卡片色/分组/图例) `seg`(环节·卡片小标签) `seglabel`(板块｜环节·侧栏)。② **归类口径**：先进封装（盛合/通富/长电/华天）+ 算力芯片（寒武纪/海光）**归 AI硬件**；排雷/主业在别处的航天 universe 标的**按主业归**（卓胜微/唯捷→半导体射频、三安/乾照→化合物、蓝思→消费电子、腾讯→其他互联网平台）；乾照/钧达→商业航天｜宇航电源、天银→商业航天｜星上配套（Doctor 追加）。③ 改分类＝改 json 重跑生成器，版式不动。

**边界待定**：航天机电（暂宇航电源）、中兴（暂商业航天｜地面终端，可另立其他｜通信设备）、昊华（暂油气化工，可挪半导体｜特气材料）；新能源板块现 0 成员。

**踩坑**：看板 JS 加载时 `DATA.forEach(d=>{ d.sector=sectorOf(d.industry) })` 无条件重算把 payload 分类冲掉 → AI硬件 落「其他」；改条件化。详见 [[通用教训]] G-X55 / O MY HTML GOTCHAS ERR-20260706-002。

**全文**：`brain/logs/2026-07-06-龙鱼看板行业分类与渲染真凶-2324.md` · 审阅表 `_index/细分环节_审阅.md`

---

## 2026-06-29 · 受益公司库领域分库（龙鱼=公司真源 / 白泽=涨价真源）

**触发**：Doctor「白泽大宗与龙鱼个股库割裂，想把受益公司合并一处」。多轮对齐后否掉「物理合并成一张表」，定**领域分库·各司其职**。

**裁定**：① **龙鱼库＝公司级公共真源**（六维评分/评级/thesis/engine_facts），是大家只读调用的公司库；② **白泽库 business_breakdown.db＝大宗涨价真源**（品种行情 + 受益关系 `benefit_relations`，原 `fundamentals` 表改名）；③ 字段按 node/edge 切——六维（公司节点）归龙鱼、纯度/弹性（品种×公司边）留白泽；④ 白泽弃自存评分镜像（`stock_scores` 冻结、`longyu_scores.json` 归档），周报经 `lib_public_read` 实时读龙鱼库；⑤ 白泽新受益公司经**沙箱自动化跑分**（batch_score→score_with_llm→record_writer）落龙鱼库。

**实施**：白泽 `lib_public_read.py`(新) / `build_warehouse.py`(去 stock_scores·建 benefit_relations) / `build_weekly_report.py`(读龙鱼库) / `sync_longyu_universe.py`(弃 A ingest)；烛照九阴 `fundamentals_lookup.py`(读龙鱼库六维 + 表名)；db 迁移脚本 `migrate_rename_benefit_relations_20260629.py`(Doctor 终端跑·改前备份·stock_scores 不 drop)。

**全文**：`brain/logs/checkpoints/2026-06-29_统一受益公司库_合并口径设计稿.md`（v2）+ `..._逐文件实施清单.md`。

---

格式：

```
## YYYY-MM-DD · 决策标题

**背景**：为什么要做这个决策

**选项**：
- A：...
- B：...

**选择**：B

**理由**：

**影响**：
```

---

<!-- 在下方追加新决策 -->

---

## 2026-05-29 · 渊图↔龙鱼五力 耦合优化 coupling-v2（L1+L2+L4）

**触发**：Doctor「对耦合再做一次优化设计，同意后执行」→ 设计四层，Doctor 定 L1+L2+L4（L3 反向反馈下一轮）+ 软链修法 + 质性维度存文件活算 τk/浮现。

**根因**：2026-05-23 优化评估发现的"哑接"仍在——`KG_PATTERN` 停在 `行业知识图谱_v2_*.json`，但渊图 canonical 已是固定名 `行业知识图谱_完整数据库.json`，glob 不到→`available=False` 静默→技术变革35+竞争格局25+新赛道15 共 75 分退化为 LLM 裸推不报错。

**落地**：
- **L1**：`KG_PATTERN` → `mapping/latest.json`（渊图建软链→canonical，根治改名漂移）；docstring 4AI→Database 修正
- **L2**：渊图建 `yuantu_scoring.py`（`get_company_score(ts_code)`：质性维度读 `scoring/韬定律_scores.json`，τ-key 图距+浮现度 log2 查询时从 latest.json 活算）；龙鱼 `KGClient.company_score()` 接入 run() step 11b，塞进 `kg_context.company_score` 供脚手架消费 `wuli_consumable_fields`（竞争格局/技术供需/政策/估值四维映射）
- **L4**：`KGClient.healthcheck()` + `warn_if_kg_down()` 启动自检（链路断→🔴 显眼警告，杜绝静默退化）；`coupling_contract.md` 版本化契约（任一侧改名须同步）

**测试**：healthcheck OK（1857节点/14家评分库/coupling-v2）；SMIC 688981.SH override τk=10（图距4·建边缺口）总分 83.6；沃尔德品格典范 +5=74.6；华大九天 80.3；未上市公司降级返回 None。

**未做（下一轮）**：L3 反向反馈（龙鱼财务/估值→`scoring/wuli_feedback/`，交叉验证高tier是否高估值）；v3.2 自动打分 vs v3.3 引擎只产数据的战略分叉（需 Doctor 拍板后再删 FinancialScorer/_build_scaffold）。

---

## 2026-06-09 · 龙鱼标的库.html 输出目录迁移

**背景**：`龙鱼标的库.html` 原存于 `Database/龙鱼-标的分析库/`，Doctor 要求归入龙鱼五力产出区。

**选择**：迁移到 `~/Documents/AI4ME/龙鱼五力-outputs/`。

**影响**：以后对 `龙鱼标的库.html` 的所有更新一律生成/保存到 `AI4ME/龙鱼五力-outputs/`，不再写回 `Database/龙鱼-标的分析库/`（旧路径已无该文件）。

---

## 2026-06-10 · 龙鱼五力输出规约三条 + 引擎写库接口

**背景**：Doctor 定龙鱼报告输出逻辑，统一产出路由。

**决策**（成文于 `consumers/龙鱼五力/输出规约.md`，含决策树）：
- **规则①·结构化库（引擎可写·带时间戳）**：`龙鱼-标的分析库` 为唯一 source of truth；新建写库接口 `record_writer.py` 的 `upsert_analysis()`——`analysis_date` 时间戳缺省今天，**同日覆盖、异日追加**时间序列（不覆盖历史），total/rating 自动算。引擎接入点：`five_forces_engine_v3.py` 打分结束处调用（当前仍 CC 手工 upsert，待接入）。结构化库**不算 outputs 产物**。
- **规则②·默认 inline**：没点名 "html/报告/可视化报告" → 六维打分+论点+甄别**直接对话展示**，不在 outputs 建文件（数据照常落库）。
- **规则③·点名 html 或"报告" → O MY HTML → AI4ME**：走设计 skill 套生成 html 可视化报告，存 `AI4ME/龙鱼五力-outputs/[龙鱼五力-持仓研究-outputs/]{code}_{name}_{date}.html`，落库记录 `report_path` 回指。

**配套落地**：①兴森 002436 六维（手工 69 → 引擎校准 65.5/观察，引擎暴露主营 68%PCB/载板仅 23%）入库；②回填存量 md：生益电子 688183(80.5)+南亚新材 688519(58.5)，库 13→15；③`record_writer` 自测通过（写入+同日覆盖+分级）。

**影响**：后续所有龙鱼分析按此路由；区分两类 HTML——`龙鱼标的库.html`=全库总览看板，规则③=单标的可视化报告。

**未做**：record_writer 接入引擎打分结束处（自动"实跑即落库"）；标的库 repo 未配 git remote。

---

## 2026-06-11 · 规约① 落地：score_with_llm_apiyi 打分结束自动落库

**背景**：6-10 规约① 的"未做"项。Doctor 批准卡点修复批次（全部同意），CC 接入。

**落点选择**：接入点放 `score_with_llm_apiyi.py`（非引擎本体）——六维完整分在 LLM 评分结束才产生，引擎只产数据+脚手架（v3.3 战略方向待裁不受影响）。新函数 `upsert_to_library()`：评分成功即调 `record_writer.upsert_analysis()`，`--no-db` 可跳过；**任何落库失败只警告不抛出**，绝不阻塞评分主流程。

**口径**：六维键与库内既有格式逐键对齐（'财务健康与风险(5)' 等六键，实测与 000988 华工科技存量记录一致）；total 用 LLM 结果，rating 由 record_writer 按库内阈值重算；thesis=summary 截 500 字；engine_facts 带 pe_ttm/pb/rd_rate 等非空项；report_path 回指 `--save` 的 llm_score json。

**影响**：⚠️ 周日 baize-weekly-report Stage0.5[4] 跑六维即自动写标的库（同日覆盖幂等）——6/14 首验时标的库会新增当周 20 票记录，属预期行为非异常。离线自测通过（临时目录，未污染真库）。

**未做**：标的库 repo 配 git remote（命令已交 Doctor 终端）；build_index.py 自动刷新（现仍手动）。

---

## 2026-06-25 · LLM 评分通道弃用 APIYI/sonnet → DeepSeek 官方端点

**背景**：发现「默认漂移」——生产周报工作流早已跑 DeepSeek（`api.deepseek.com/anthropic` + `deepseek-v4-pro`，key 走 `.env` 的 `KG_API_KEY`），但独立脚本 `score_with_llm_apiyi.py` 的硬编码默认仍是 `api.apiyi.com` + `claude-sonnet-4-5`，靠周报注入 env 才指向 DeepSeek；Doctor 直接手跑会落回 APIYI/sonnet。本质是把脚本出厂默认对齐到既有生产通道，非高风险切换。

**选项**：
- 默认模型：A `deepseek-v4-pro`（与生产一致）/ B `deepseek-v4-flash`（官方主推·更快更省）
- APIYI 处置：A 软弃用保留可回退 / B 硬清除改名
- DeepSeek 端点：用官方 Anthropic-兼容端点（保留现 /v1/messages 协议）vs 改写 OpenAI 协议

**选择**：默认 `deepseek-v4-pro`；硬清除改名 `score_with_llm.py`（但旧名留薄 shim 不物理删）；端点用 `https://api.deepseek.com/anthropic`，协议不变。

**理由**：
- v4-pro 与生产周报一致、投研质量优先；thinking:disabled 后 ~22s 稳在沙箱 45s bash 限内（GOTCHA-023）。flash 仍可 `--model` 临时覆盖。
- 官方 Anthropic-兼容端点 = 最小改动，现 `/v1/messages` + x-api-key 代码直收，已有 deepseek thinking-disable 分支复用。
- 硬清除是 Doctor 选；但「可逆优先·不删文件」硬约束仍生效，故旧 `score_with_llm_apiyi.py` 改薄 shim（透传 + DeprecationWarning）防周报 SKILL 断档，非物理删。
- DeepSeek 旧模型名 `deepseek-chat`/`deepseek-reasoner` 官方 2026-07-24 弃用，故用 `v4-pro`/`v4-flash` 新名。

**落地**：新建 `score_with_llm.py`（默认 deepseek-v4-pro + 官方端点；key 候选首位 `KG_API_KEY`；`call_apiyi→call_deepseek`；scorer 标签 `DeepSeek/{model}`；APIYI 痕迹清零）；旧名转薄 shim；`白泽大宗/周日工作流-接手指南.md` 引用名更新。

**验证**：沙箱离线自检全绿（语法/导入/默认值/shim 透传 + DeprecationWarning/无 APIYI 残留）。德明利 001309.SZ 端到端实测通过：引擎 leg 9s、DeepSeek leg 22s、key 自动命中 `KG_API_KEY`、scorer = `DeepSeek/deepseek-v4-pro`、六维齐全（总分 43.5/回避）。

**未做**：周报 Cowork SKILL（`Scheduled/baize-weekly-report/SKILL.md` 第 25 行）调用名改 `score_with_llm.py`（非紧急，shim 兜底）；改完撤 shim。两 git 仓提交（渊图 monorepo + brain）。

---

## 2026-06-26 · 腾讯 engine_facts 待办从「待跑引擎校准」改写为 B「待接港股数据源」

**背景**：/resume 清理遗留待办时，发现腾讯 00700.HK 记录挂着「engine_facts 待 Doctor 终端跑引擎校准（前提:引擎是否支持港股）」。此项是死项——龙鱼引擎基于 Tushare A 股，结构上不覆盖港股 00700.HK，不存在「跑引擎校准」路径（与沙箱连通性无关，06-25 更正已写明）。

**选项**：A 关掉（承认手工标的、引擎覆盖不到）/ B 改写成「待接港股数据源作可校验底座」。

**选择**：B（Doctor 裁定）。

**理由**：腾讯仍想要可校验的财务底座，只是来源不能是 A 股引擎；改 B 把待办指向真问题（接港股数据源，如 Tushare 港股接口/其他），而非挂个永远做不到的「跑引擎校准」。

**影响**：`标的库/records/00700.HK_腾讯控股.json` 的 `_meta.数据局限` 改写为「引擎不覆盖港股、待接港股数据源、当前手工口径」；06-09 log 加 06-26 裁定脚注留痕（原待办文字不改）。engine_facts 维持手工财报口径。

## 2026-07-05 · 双scorer交叉打分制度落地（全库92 A股）

**背景**：Doctor「工程升级：两个库/交叉打分/对比校正制度」。把龙鱼从单一评分升级为双 scorer 独立盲打交叉验证。

**设计（多轮对齐）**：同库加 `scorer` 维度（不建新目录）。claude=自上而下维度级(research-CC)、deepseek=自下而上子项级(score_subitems加锚点·带subscores·reviewer=DeepSeek-v4-pro)，并存不覆盖。财务维两者取引擎 breakdown 真值(客观锚)。独立盲打(deepseek prompt 不含 CC 分)→ |总分diff|≥15 进待校正队列 → 人工定夺，不自动合并。所有 DeepSeek 分(含白泽 score_with_llm 维度级)统一标 scorer=deepseek。

**落地**：Doctor 终端 `run_dual_scorer_batch.py` 全库跑：92 A股 deepseek 落库(2港股跳过)，成功91/跳1/失败2。沙箱全量断言 92/92：subscores/打分日期/reviewer 齐、总分=子项累加误差0、越界0、财务同源。对比产物 `对比校正/全库_对比_20260705.md`：覆盖92·待校正12·平均Δ−4.0(DeepSeek更严·主集中技术供需)。独立子agent复审7类全过。

**周更**：常更标的双scorer周更归独立 scheduled `longyu-weekly-dualscorer`(周日08:01·deepseek子项+claude top-down)；白泽撤回常更接线、回归大宗受益。

**新增脚本/文档**：`score_subitems.py`(盲打子项评分器) `write_claude_score.py`(claude写库器) `build_dual_compare.py`(对比) `run_dual_scorer_batch.py`(批量) `双scorer交叉打分制度.md`；SCHEMA.md +scorer段；score_with_llm.py 补标 scorer=deepseek。

**待办**：待校正12只 Doctor 定夺(8倾向deepseek/北方稀土·紫光疑过严/联讯疑过松)；PRD `2026-07-05_龙鱼双库交叉打分制度_PRD.md` 待 Doctor 验收打✓。

## 2026-07-05 · 双scorer 分歧解决方法论（铁律）

**Doctor 定**：对每条 |Δ|≥15 分歧，**禁止**①简单"按哪方来" ②偷懒"折衷取中"。正确流程：诊断分歧是「错误」还是「合理不同看法」→ **错误**则修正错误方的判分方法（改 prompt/锚点/RULES）重跑、使错误方**回归理想标准**（根治在方法层，非改个案分）；**不同看法**（两方无硬伤）则**接受差异共存**、不强制收敛。错误可能在任一方（deepseek 或 claude/CC）。先排错误、修完剩余 Δ 才归共存。

**首个案例**：源杰议价——deepseek 拿旭创80%集中度反推议价弱＝方法错；修锚点「议价先看毛利率(77.8%升)+供需结构(CW供不应求)」→ 重跑 deepseek 议价2→4/总分46.5→53.5 回归。非"采信claude"。

**落地**：写进 `consumers/龙鱼五力/双scorer交叉打分制度.md`「分歧解决方法论」节 + score_subitems.py 锚点（议价铁律+维度正交铁律）。周更/重跑均按新锚点。前文"采信X"处置表作废。

**待执行**：整条待校正队列按此方法论逐条重走（诊断→修方法重跑 或 标共存）；全库可 `run_dual_scorer_batch.py --force` 用新锚点重跑令错误分回归。

## 2026-07-29 · claude 腿全库判分（59 只）+ 判分尺度定档 + 两条新纪律

双 scorer 制度首次由 CC 完整跑完 claude 腿：非常更陈旧批 60 只（deepseek 腿 59 成功，`1045.HK` 港股引擎不支持）+ 盐湖股份新增，claude 腿实打 **59 只**（中兴通讯因盲打污染弃打）。

### 判分尺度（本场定档，后续周更沿用）

- **估值维压得狠**：PE/PB 处 90%+ 历史分位近乎直接判死"相对估值"5 分。半导体设备四家（中微 67.3 / 华海清科 63.3 / 盛美 61.7 / 芯源微 53.3）估值维**全部落在 1.8–4.2/15**，板块性透支；拉开它们的是兑现质量（中微营收 +36.6% 且净利同步 +30.7%，华海清科营收 +36.5% 而净利仅 +5.9%，芯源微净利 −64.6%）。
- **供需端严格要求公司自身兑现**，年度口径优先（本批 `growth_is_interim` 普遍为 true，一律用 `*_yoy_annual`，Q1 窄口径仅作加速/减速信号标注）。
- **不搞平均主义**：59 只跨度 24.8（ST亚光）→ 77.5（紫光国微）。
- **资源股口径**（盐湖股份首次）：估值维看**商品价格周期位置 + 成本曲线位置**，而非单纯 PE 分位；**高毛利若来自资源禀赋成本优势而非定价权，议价给 4/5 不给满档**（锂为标准化商品、公司是价格接受者）。
- **EDA/工业软件口径**（华大九天、概伦电子）：计入 agentic EDA 对工具链壁垒的稀释——壁垒与客户粘性各下调一档。机制判定见渊图 `concept_EDAMoatRecompute`。

### 两条新纪律（已写入 `consumers/龙鱼五力/RULES.md` 避坑清单）

- **第 9 条 · 周期股「出清预期」归位**（Doctor 裁定）：产能出清/周期见底属**主业供需（维度2）**，不得计入维度4新赛道；出清未兑现前不给分；**禁止以「估值已跌到历史低位」反推任何维度加分**（那是估值维已计过的信息，二次计入即同情分）。触发场景：光伏、面板、锂电、封测、存储。
- **第 10 条 · 兑现真伪辨别**（江波龙教训）：涨价周期中的高毛利+高净利增速须先辨来源——经营性兑现 vs **库存价差红利**。后者不计供需、不支撑议价；且此时 PE 低分位是**分母虚高的假便宜**、PEG 分母不可持续。（完整三分法含"资源禀赋成本优势"第三类，见 [[经验库]] EXP-20260729-001-P。）

### 两次订正（`--correction` 留痕）

| 标的 | 原→现 | 根因 |
|---|---|---|
| 江波龙 301308 | 72.2→**57.5** | 把存储涨价的**库存价差红利**误读为需求兑现；原 thesis「毛利55.5%证明量价齐升」写反。订正后 Δ(ds−cl) 从 −26.2 收窄到 −11.5，落回阈值内——印证「修方法而非折衷」的收敛机制 |
| 华大九天 301269 | 60.1→**56.1** | 竞争维原 22/25 为全库最高、未计入 agentic EDA 对工具链壁垒的稀释 |

### claude 腿基础设施（本场新建/修复）

- 新建 `consumers/龙鱼五力/extract_packs_for_claude.py`：从 records 抽 claude 腿盲打包。**白名单+硬断言结构性保盲打**（物理上不含 six_dim/subscores/total/rating/evidence，不靠自律）。
- **补 kg_context**（Doctor 指出漏读渊图）：RULES §二 明写「步骤二：渊图知识图谱」、制度写 claude 腿读「kg/估值/财务」，而抽取器初版只有 engine_facts。加 `build_kg_context()` 按代码/名称匹配 + 一跳邻居。**渊图是客观产业知识，读它不破坏盲打。**
- **已知缺陷待治本**：`run_dual_scorer_batch.py` 未透传 `--save-pack`，故批量周更后 claude 腿仍需靠本抽取器绕道。

### 渊图侧产出

手工 patch `_v3_20260729_AgenticEDA_manual.json`：+2 节点（`concept_AgenticEDA`、`concept_EDAMoatRecompute`）/ +6 边，3549/4049 → 3551/4055，已 promote。`concept_EDAMoatRecompute` 带**反向记账**（3DIC 仍是 agent 未攻克处、国产未落后）与**证伪路标**（若出现绕过传统工具链的 AI 原生设计流程被主流流片采用则推论失效）。

**待续**：中兴通讯 claude 腿（盲打污染）；港股引擎缺口（`1045.HK`、`00700.HK`）；本次 59 只**尚未刷看板**（需跑 `build_dark_board.py`）。
**来源** → logs/2026-07-29-龙鱼五力claude腿全库判分与渊图补EDA.md


## 2026-08-10 · 持仓分态双轴量化规则落地（市值30万 × 利润率30%）

Doctor 三轮迭代收敛出的正式判定口径，把原先的手工 status 标签变成可复算规则：
1. 持仓市值 < 30万 → **最小观察**（象征仓，不看利润率）
2. 市值 ≥ 30万 且 利润率(=盈亏/成本) < 30% → **成本较高**
3. 市值 ≥ 30万 且 利润率 ≥ 30% → **利润奔跑**
- 市值折¥（港股×汇率）；**负成本（已止盈）=纯利，不参与 30% 判定**，市值够→利润奔跑、不够→最小观察。
- 临界值贴边必须实测（源杰 28.3万<30万→最小观察），勿凭印象。
- 当日 16 只按此重判并代码核验 0 不一致：利润奔跑3/最小观察7/成本较高6。
- 配套纪律「**评分不开后门**」（Doctor「不用开后门」）：持仓不松口抬分。南亚新材基本面更硬但股价弹回高位、PE分位33.9%→85.3%，仍如实 66观察→63.5谨慎下调。

**来源** → logs/2026-08-10-龙鱼持仓板并入个股库与持仓迭代.md

## 2026-08-10 · ETF 手录价机制 px_manual（不接公共行情库）

半导体设备ETF国泰(159516)无本地行情源（theme_etf_daily 无此码、持仓板只读 stock_daily）。裁定**沿用港股 px_hkd 模式一般化成 `px_manual`**，不接公共库：
- **为什么不接公共行情库**：①公共库是句芒管的单一可信源，沙箱直写 211MB 主库有 disk I/O 风险；②一次性填入不会自动日更（要日更得改烛照九阴 theme_etf 清单，跨项目）；③收价只为一只 ETF 不值当。
- **实现**：board_data 节点加 `px_manual`；前端 computeDerived 非港股 `px_a` 空时回退 `px_manual`；编辑器对无自动价标的显示「当日价/净值」输入；buildHoldingsObj 写回 holdings.json。预填 0.762@0810。
- 代价：价靠手维护、不自动日更。任何「无本地行情」的非港股标的（含未来新股）通用此道。

**来源** → logs/2026-08-10-龙鱼持仓板并入个股库与持仓迭代.md

## 2026-08-11 · 看板背景定稿：星空照片嵌入 @25% 不透明度（手绘星轨层废弃）

Doctor 给参考图 `4AI/标的库背景.png`（长曝星轨：深空蓝黑 + 蓝青星轨 + 地平线钠灯暖橙），三迭代收敛：
1. **v1 手画星轨**：色板收束进照片色谱（`--cta` 紫→星轨青蓝 `#7CC4EE`、`--amber`→地平线金橙 `#E8A24C`、品红退役、`--bg` #000→深夜蓝黑）+ 30 条同心断弧 SVG 层（固定种子、600s 缓转）——Doctor 报「没有效果」。**根因**：层叠透明度是乘法，层 .55 × 弧线 .03~.10 = 有效 0.017~0.055，约等于隐形。「宁可淡」起调过了头。
2. **v2 加重**（有效 0.08~0.29）后 Doctor 改裁：**「简单做一下，把图片嵌入页面」**——真照片 JPEG q84 base64 内联（1.95MB PNG→268KB），`center top/cover` 不拉伸、超出裁切、留黑 `#04070C`；手绘星轨层与星云层全撤零残留。
3. **v4 定稿**：CSS 背景图无不透明度属性 → 照片移 `body::before` 层 `opacity:.25`，body 回纯色 `#04070C`（黑底即压暗层）。Doctor「现在挺好」。

- **调节旋钮**：`body::before` 的 `opacity` 单变量；想多看地平线改 `center top`→`center`。
- **口径备忘**：Doctor 口语「25%的透明度」= 不透明度 25%（淡）；若指「淡掉 25%」应改 .75——下次先确认口径再动手。
- **不动的**：评级功能色（绿/白/红）、K线红绿、track 标签紫 `#B39DFF`、暖色持仓页（iframe 全覆盖，照片只在个股库页可见）。
- 版式真源 `board_template.html`（原图前备份 `.bak_20260811`）+ artifact 双落，weekly dualscorer 班继承。

**来源** → logs/2026-08-11-龙鱼看板星空背景定稿.md

## 2026-08-11 · 看板行业分组细分定稿：8 板块 → 14 行业组（foundry 并入半导体设计与制备）

**触发**：Doctor「AI硬件分成光通信/PCB/半导体设备/半导体材料等」→ 两轮拍板：① AskUserQuestion 批准 14 组全表、商业航天 30 只本轮不动；② 中芯国际是 foundry 不是设计，但**不用单列**——所有半导体设计、算力芯片合并为「半导体设计与制备」。

**裁定**：
- 真源=`_index/细分环节.json` 新增 `_meta.环节分组`（16 条 环节→行业组）+ `_meta.分组色`；records 不动，`build_dark_board.py` 读 GRP 出 `sector=环节分组.get(环节, 板块)`。
- 14 组：光通信/PCB/先进封装/半导体设备/半导体材料/存储/半导体设计与制备/温控散热/商业航天/机器人/资源/油气化工/新能源/其他。
- foundry（中芯国际 seg=制造）并入「半导体设计与制备」不单列——组名覆盖设计+制造+算力芯片，避免一组一只。
- 同步纪律：json._meta（数据）↔ 模板 SECTORS/SEC_EN/sub 文案（呈现，配色与 _meta.分组色 一致）↔ 重建 + update_artifact（上线），只改一处必漂移。

**影响**：看板卡片色/分组/图例按 14 组渲染，分布实核 115 只（商业航天30/资源15/其他12/半导体材料11/光通信9/半导体设计与制备8/PCB5/存储5/半导体设备5/机器人4/先进封装4/油气化工4/温控散热3，新能源 0 键保留）；旧组名零残留；weekly dualscorer 班重建自动继承。备份：细分环节.json.bak_20260811、board_template.html.bak_20260811_pregrp。

**来源** → logs/2026-08-11-龙鱼看板行业分组细分定稿.md

## 2026-08-19 · 校正队列销账裁定：奥比/海光双销账（Doctor 裁）

**触发**：08-15 畸形收口场 /resume 补挂的两条旧账（源日志口径「奥比 Δ+17/海光 Δ−16」）；08-15 晚周更场已重跑双腿，本场为专项校正场。

**现核**（records 08-15 实读）：奥比 ds 74.0/claude 69.5 = Δ+4.5（ds 84→80→74 三周收敛，「放水赛道β」疑云自行消散）；海光 claude 60.0/ds 51.5 = Δ−8.5（三周持续，全库同期最大负分歧；旧表述「claude 升/ds 降」有误，实为 claude 72.5→61→60 下修收敛、ds 56.5→51.5 稳定）。

**审计**：奥比 ds 证据逐条对 canonical company_Orbbec properties 全部命中，无未证实数字；海光 ds 供需端引「中芯产能排满至 2027」canonical 零命中且属华为链错配（海光代工=三星），claude 新赛道 11 有 DCU 百度阿里认可/HiSwitch 全栈 canonical 支撑、ds 的 7 依「不如寒武纪 690」无图谱锚。

**裁定**（Doctor 双销账）：奥比=双分共存销账（收敛+无硬伤）；海光=真分歧共存销账（真实视角差、错锚方向为抬分非扭曲）；主分 69.5/60.0 均不变。

**影响**：TODO 两条勾销；ds 引用未证实数字族第二例登记 GOTCHAS NOTE-20260819-001（应升格通用教训·检测机制 TODO 沿用）；对比文件 08-15 追记。

**来源** → 本会话 2026-08-19 专项校正场（TODO 待校正队列条勾销 · 对比校正/全库_对比_20260815.md 追记）
