---
title: 科技资讯看板 · 系统概览
tags: [科技资讯看板, architecture]
created: 2026-08-21
updated: 2026-09-26
status: active
type: architecture
project: 科技资讯看板
---

# 科技资讯看板 · 系统概览

> 项目全貌浓缩（brain-save Step 4 更新「最后活跃」字段的依据 · 2026-08-21 Doctor「补上」注册+建本页）。

## 定位

科技、金融资讯聚合看板（X 看板）：七位高信号 X 博主 + 两位抖音作者（老毛聊交易 / 投知君君买方视角）· 48 小时热窗口 · 消息纵排、博主横排、点击卡片看详情。产出 Gateway artifact `x-board`（暖色日报视觉范式）。

## 入口

- 生产代码与唯一运维文档：`Codex/科技资讯看板/`（本地-only git 仓 · `docs/X-BOARD-OPERATIONS.md`）
- artifact 生成器：`Claude/Projects/Financial/X-Board/gen_xboard_artifact.py`（无 git · 输出 xboard-artifact.html → update_artifact）
- GOTCHAS：`Codex/科技资讯看板/GOTCHAS.md`
- 数据源（**2026-09-26 起**）：统一 SQLite 发布合同——`Database/科技资讯看板/tech-intel-history.sqlite3` 的 `board_current_snapshot_v1` 三视图（X 资讯 + 逐条判读 + 同快照综合分析）＋ `Database/Douyin/`（抖音两列 · dva-mac-mirror 回流直供）。**公开 JSON `app/generated/public-snapshot.json` 已降为数据库的单向兼容投影，不再是 X-Board 入口**（旧说「数据源＝tech-intel 公开快照」已作废）；X Board 无 JSON 兜底，库不可用即明确告警并隐藏 X 资讯。
- 合同与文档：`Codex/科技资讯看板/docs/BOARD-DATABASE-CONTRACT.md`（统一读取合同 v1）· `docs/PRD-unified-board-database.md`（生产侧改造 PRD + 独立验收凭证）

## 当前状态

🟢 活跃。生成链四角色（2026-08-21 核）：Codex automation（05:30/17:30 PT 调度 · 状态 ACTIVE）→ `npm run refresh:scheduled`（唯一生产入口）→ 本机 twitter CLI + Techmeme 采集（Codex CLI 做可选 AI 标题/影响加工）→ Fuxi 只做不可变冷存档、不生成不调度。artifact 侧是静态快照：重推 = 生成器 + update_artifact（**不实时轮询**；页面可用不代表资讯新鲜，须看显示时间）。自动重推班 **`xboard-daily-repush` 已于 2026-08-22 落地**（每日 18:00 PT · 只跑不改生成器 · 异常不推）——旧记「自动重推班待裁」已作废。

## 关键决策（最近）

- 发布日回填约定（2026-08-21 Doctor 批）：抖音两列时间＝视频发布时间 create_time +8h 北京日；「转写」文案全删（2026-08-21 深夜 Doctor 纠错）——时间口径/排序键/fallback 三链全部锚定发布时间，缺数显示「—」沉底出列。
- §9 先 build 再测（2026-08-21 Doctor 批 · NOTE-20260821-006）：渲染/API 测试针对 dist 构建产物，受控刷新后须先重建。
- 一次性重推由未参与镜像实施方执行（2026-08-21 Doctor 批）：DVA 镜像回流后一推+二推，payload 级回读收口（PAYLOAD-MATCH: True）。

## 已知坑

见 `Codex/科技资讯看板/GOTCHAS.md`（NOTE-20260821-006 渲染测试须先 build）；Gateway 包装块回读见 [[通用教训]] G-X120；批量回流刷新 mtime 错排见 G-X157；生成器只认业务时间排序（不认 mtime）。

## 最后活跃

2026-09-26 **数据入口切换到统一 SQLite 发布合同 + artifact 推送闭环（CC 本场实读核验）**：`board_reader.py` 以 `?mode=ro` + `PRAGMA query_only=ON` + 单次 `SELECT * FROM board_current_snapshot_v1` 读取，核 `payload_sha256` 与 snapshotId/计数身份（失败码 `BOARD_HASH_MISMATCH`）；`gen_xboard_artifact.py` **已不再 import 公开 JSON**（改 `from board_reader import load_board`），新增 `analysis_ui.py` 渲染顶层综合研判。发布身份：`20260926133526947-e558a07e-…` / `board-df55fd24…`，底稿 **107** 条 / 当日分析 **44** 条 / 37 张卡（X21 · 抖音16）；**19 项测试实跑 OK**。artifact `updatedAt` `03:09Z` → **`16:09:42Z`** 推送生效，**payload 级回读 `PAYLOAD-MATCH: True`**（两侧同为 722,059 B · sha256 `8b500df4…`；平台注入块 374 B）。**未核**：跨机／Cowork 载入下 SQLite WAL 一致性 · 后续调度运行 · 公网发布；「两项研究先验仍缺失」的具体所指亦未核。**当晚 18:00 PT 重推班为新入口下首跑。**

2026-08-30 X-Board 老毛停更诊断与外发授权——全链分层实核（重推班/回流/镜像全活）定位 fuxi 采集侧 08-22 起停摆；双通道口径厘清（下载通道 aweme 表止 08-20、转写通道 videos 元数据至 08-22，生成器回填优先级 manifest→videos json→aweme 表）；VV 修复部署核验（镜像 6,579 文件/278,760,038 字节逐位一致 · 两条新视频入库 · 老毛 videos/ 540）；Doctor 批「允许提取并发布」→ **同日深夜 CC 执行推板闭环**（dry-run 候选恰 2 → extract ok 2/fail 0 → gen exit 0 · 37 items → update_artifact → 回读 updatedAt 05:08:40Z · 老毛列顶部两条新料带要点 · 停摆账销）。

2026-08-22 两份 X-Board PRD Doctor 总签（delivered · 客观轨 12 条 ✓）+ 存量 103 篇裁不回补 + DVA refill 班干净跑（16/16 ASR · canonical 538 · finance-repair 已发布哈希回读）。

2026-08-22 VV 交接单验收收口——盘面实核老毛 522 件止 08-20 / 投知 196 件止 08-18 → C.C. 重建重推（生成器 exit 0 · SHA `a380892d…` · Gateway 真源回读命中）→ 交接单六项 [?]→[✓] → Doctor 目验「过」· 2h 回流 heartbeat 已启用。

2026-08-21 DVA 镜像回流首跑（6,450 文件 PUBLISHED）→ X-Board 两连重推（「转写」文案删 · 时间口径=视频发布时间北京日 · 排序键修复 · aweme 表双源 fallback · 缺数沉底）→ Gateway 回读 payload 级收口 → Doctor「目验 ok」视觉验收销账 → brain 补注册 + 本概览补建。
