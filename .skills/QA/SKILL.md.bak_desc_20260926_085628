---
name: "wenda-ban"
description: "把一堆待 Doctor 拍板的裁定项收成一张单文件暖色 HTML「问答板」，替代多轮 AskUserQuestion 马拉松。三条触发：① /todo 流程内裁定项 >4 题自动开（brain-todo Step 3.5）；② 其他任务里 CC 判断裁定项偏多/值得对齐时自主开；③ Doctor 直接呼叫「问答板」「开问答板」「出个问答板」或敲 /qa 即开。每题一卡（问题+原条/背景+证据一行+CC 建议钉推荐/不推荐+勾选控件），顶部吸顶「复制答案串」按钮，勾选完一键复制紧凑答案串贴回会话即完成答题。只是「问法」升级，不扩权：勾项归 Doctor、propose-then-confirm 与永不自动清单全不变。"
---

# 问答板 — 单文件暖色对齐页（多裁定项一次问清）

> 机器名 `wenda-ban`（ASCII kebab，供各环境装载器注册）；人与命令的名字是「问答板」/「/qa」，见触发。

把「要 Doctor 拍好几板」的场景，从一轮轮 AskUserQuestion 马拉松，换成**一张可勾选的单文件 HTML**：每题一张卡，Doctor 在页面上勾完，点吸顶按钮复制答案串贴回会话，CC 照串执行。命名与命令化由 Doctor 2026-08-12 立。

## 触发（三条路径）

1. **自动（/todo 内）**：brain-todo 流程 Step 3 分流后裁定项 **> 4 题**（AskUserQuestion 一轮上限）→ 自动出问答板（见 brain-todo Step 3.5）。
2. **CC 自主（其他任务）**：任何任务里，CC 判断「要 Doctor 拍板的点偏多、逐条问太碎」时，可自主出问答板一次问清。参考阈值 >4 题，但不死卡——值得对齐就开。
3. **Doctor 呼叫**：Doctor 说「问答板」「开问答板」「出个问答板」或敲 `/qa` → 直接开。（`/qa` 是 Doctor 2026-08-12 定的启动快捷命令。）

> 反过来，**裁定项 ≤4 且是同构小题**时，用 AskUserQuestion 更轻，不必开板。问答板是给「题多 / 题杂 / 要带背景与证据」的场景用的。

## 什么时候用 / 不用

- **用**：一轮要 Doctor 裁的题 >4；题目之间不同构、各自带背景/证据/原条；希望 Doctor 能通览全局再一次性勾答；想留一张可回看的对齐快照。
- **不用**：≤4 个同构选择题（AskUserQuestion 更顺手）；纯通知无需拍板；需要实时刷新数据的状态页（那是常驻 artifact 的活儿，问答板是一次性快照）。

## 页面规约（暖色·日报范式）

- **单文件、零外部依赖**：inline CSS/JS、不引 CDN、双击即开。暖米底（#faf6f0 族）、卡片分节、衬线大标题——观感照烛照九阴暖色日报。
- **每裁定项一张卡**：**问题 + 原条/背景摘要 + 证据一行 + CC 建议（选项钉推荐/不推荐）+ 勾选控件**（单选 radio、多选 checkbox，与题型一致；**推荐项预选中**）。
- **顶部吸顶「复制答案串」按钮**：勾完一键复制形如 `1A · 2全选 · 3否 · 4AC` 的紧凑串，Doctor 贴回会话即完成答题。
- **页尾**：「未挤进本板的留挂项」清单 + 生成时间戳 + 条数。
- **落点**：outputs → `present_files` 卡片直开；文件名 `问答板_{YYYY-MM-DD-HHmm}.html`（**一次性快照、不覆盖旧板、留档可回退**）。**不做成常驻 artifact**（对齐是一次性动作，无刷新价值）。

## 答题回路

Doctor 回贴答案串 → CC **逐条解析照单执行**（执行面纪律按所在任务的既有授权走，问答板本身不扩权）→ 末报**逐条对账「答案串 → 动作」**。**解析不出或缺题 → 缺项重问，不猜、不默认。**

## 边界（不扩权）

问答板只是把「怎么问」升级成一张板，**执行面与授权边界一字不改**：

- 勾项 / 打 ✓ 归 Doctor（G-X4；brain-todo 目标模式的 A 类代勾限定放宽只在其自身流程内有效，不随问答板外溢）。
- 改既有资产仍 propose-then-confirm（批准做某事 ≠ 批准具体怎么改）。
- 永不自动清单照原：删文件（弃用走 `archived/_DEPRECATED_`）· 改调度 · 数据库写 · 活班 prompt/watchlist 等活配置 · 任何 git 子命令 · fuxi 远端删除 · D 类外部动作本身。
- 选项建议一律钉（推荐/不推荐），并给一句理由。

## 相关

- 姊妹/出处 skill：brain-todo（Step 3.5 = 问答板的 /todo 特化应用，自动触发阈值 >4 题）。
- 暖色范式参照：烛照九阴暖色日报。

## v 历史

- **v1.0**（2026-08-12 · Doctor 立）：「开 html 对齐」正名**问答板**并独立成通用协作命令。原型是 brain-todo v2.1（2026-08-11）的 Step 3.5 大型对齐页（当日四轮 15 题实战触发）；本场 Doctor 定为三触发路径（/todo>4 自动 · CC 自主 · Doctor 呼叫），格式规范独立成此 skill，brain-todo Step 3.5 改为 /todo 特化引用。
- **v1.1**（2026-08-12 · Doctor 立）：启动快捷命令定为 `/qa`，并入触发路径③（呼叫「问答板」或敲 `/qa` 等效）。
- **v1.2**（2026-08-12）：frontmatter 机器名由中文「问答板」改为 ASCII `wenda-ban`（与 Cowork 存储键对齐，修装载器把非 ASCII 名 slug 化成 `---` 的注册表乱码）；中文「问答板」/「/qa」仍为人与命令的名字，写在描述与触发里，不受影响。
- **v1.3**（2026-08-12 · Doctor 立）：为 `/qa` 配 UI 层兜底 slash 文件 `~/.claude/commands/qa.md`（Claude Code 原生 slash 路径；**Cowork 是否认此路径待实测**——不认则由 skill 触发词「问答板」/「/qa」兜底）。沙箱到不了 `~/.claude/`，该文件由 Doctor 终端建。
