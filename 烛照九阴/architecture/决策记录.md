---
title: 烛照九阴 · 决策记录
tags: [烛照九阴, decision, 兑现引擎]
created: 2026-06-24
updated: 2026-08-11
status: active
type: decision
project: 烛照九阴
---

# 烛照九阴 · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

---

## 2026-06-24 · 兑现跟踪走「案2」：价格管亮暗、信号管去留

**决策**：兑现跟踪机制采纳案2——close 判据不变（仍 5pp 绝对回撤），但 closed **不剔除**、转 **dormant（暗态）** 仍在库内跟；价格重走强（连续超额为正≥Y′日 且 自暗态低点回升≥Z pp）→ **点亮** 回 closing（二段），可多轮循环。**去留只由产业逻辑（渊图信号 thesis）决定**：买入逻辑在就一直跟（亮/暗切换），逻辑改变→提示风险→才真正剔。

**理由**：与「信号驱动去留」哲学自洽（价格不再判生死、只管亮暗）；能捕捉「close→沉寂→二段主升」。案1（改 close 为 35%相对回撤）虽简单，但仍让价格当刽子手，与该哲学拧着。

**分期**：价格层先行（Phase1，本次立 PRD），信号层（渊图 thesis-change→剔）二期。回测作 Phase1 交付前置。

**关联**：`logs/checkpoints/2026-06-24_兑现跟踪暗态点亮Phase1_PRD.md`

---

## 2026-06-24 · closure 5pp 绝对回撤对大峰值线过敏（待修口径）

**问题**：closing→closed 用「峰值≥5% 且 绝对回撤(peak−cum)≥5pp」。5pp 是**绝对值、不随峰值缩放**——对峰值 5% 的线＝全吐光才收（合理），对峰值 25%/65% 的线＝只回撤 1/5 甚至 1/10 就被判完毕（过敏）。

**实测（2026-06-24，782 条 closed）**：绝对回撤中位 5.9pp（紧贴下限＝主 binding）；**46%（356 条）是「峰值≥15% 却回撤<40%峰值」被提前收口**（极端：峰值 +65%→+59%，仅回撤 6.3pp＝峰值 10%）；全样本相对回撤中位 42%。

**候选修法（未落，待回测）**：`peak−cum ≥ max(5pp, R%·peak)`（≡「回撤≥5pp 且 回撤≥R%·峰值」），R≈35%（临界峰值 14.3%）。R=35% 下 42% closed 退回 closing，R=40% 则 48%。

**现状**：Doctor 2026-06-24 议定**先不动 5pp**；案2 Phase1 也保留 5pp（靠暗态/点亮兜二段，而非改 close）。此条留作待修口径，推进需前瞻回测验证。

---

## 2026-06-24 · closed 终态盲区：不复活 + 再进场只认渊图新信号

**事实**：现行 `closure_engine.run_machine` 命中 closed 即 `break` 冻结，`excess_cum/peak` 停在 closed 当日，**不看其后行情**；每日 --apply 重跑也回到同一历史 closed 点，状态恒 closed——**价格再加速不复活**。而「再进场」入库口径 `INSERT ... ON CONFLICT(signal_node) DO UPDATE`：唯一能把线重新拉回雷达的是**渊图再喊一条信号**（新 signal_node 或同 node 带新日期），**价格自证能力为零**。

**后果**：强线 closed 后走二段主升、但渊图没补喊 → 系统完全不追；叠加上条「5pp 过敏」导致的假 closed → 既易提前判死、又判死不回头，双重漏接。

**对策**：案2 Phase1（价格层 dormant/点亮）解「价格不复活」；Phase2（渊图驱动 thesis 变化才剔）解「去留」。**Phase2 前置依赖**：渊图 KG 需能产出 thesis 证伪/降级/移除信号——待核实渊图端能力。

---

## 2026-06-29 · DB 写护栏：硬拒绝 + 单点集中（connect_write）

**背景**：recap.db 当日反复损坏（G019）。根因＝沙箱里有 writer 绕过 /tmp 对挂载盘库 live commit。审计发现「拒写挂载盘」护栏只在 `_ingest_25/26` 复制粘贴存在，连同胞 `_ingest_28` 都漏——护栏没集中是反复坏的结构性原因。

**决策**：
- **硬拒绝** 而非自动重定向——路径落挂载盘真盘且未设 `ZZJY_ALLOW_MOUNT_WRITE` 即 raise（fail-loud）。不选「自动改指 /tmp」因其藏 bug、且 cp-back 仍需人做；宁报错也不默默写坏。
- **单点集中** 到 `config.connect_write(path)`，所有写库者改走它，废弃复制粘贴护栏。Doctor 2026-06-29 拍板硬拒绝。

**落地（Phase-1）**：config 加 connect_write；4 个 Tier-1（硬编码真盘）迁移之（`_ingest_28`/`dedup_kejian`/`xiaobao_position_write`/`bt_xiaobao_pos_3d`，其中 bt_ 只护 `--commit` 写连接、读连接不动）。py_compile + 护栏单测过。

**留尾（Phase-2）**：Tier-2 共 15 文件写连接收口；删 `_ingest_25/26` 旧护栏统一到中央；cp-back 卫生（无 wal/journal 校验 + cp 新名+mv 原子替换 + 强制 integrity_check + 自动回滚）写进日更 SKILL。

---

## 2026-06-29 · closure 去掉 leg_peak≥5% 幅度门槛（低幅困死池）

**问题**：`closing→dormant` 需 `leg_peak≥X_PEAK(5%)` 且回撤≥5pp。低幅信号（峰值从没够 5%）永远触发不了变暗，回吐到负还卡在 closing 流血（89 条回吐里 66% 属此）；二段点亮后 `leg_peak` 重置、新腿没再够 5% 又卡。根＝绝对 5% 阈值不随幅度缩放。

**决策**：去掉 `leg_peak≥X_PEAK` 门槛，`closing→dormant` 仅凭绝对回撤≥5pp。X_PEAK 标弃用、不删（可逆）。

**回测背书**：去门槛后 116 条 closing→dormant；低幅信号点亮质量（假点亮 49%）≈高幅（48%）→ 无需给低幅单列 stopped 终态或剥夺点亮（撤回 CC 误提的「失败信号」方案——5% 是状态机触发阈值、非成功/失败判决，低幅是小赢非失败）。详 `docs/兑现回测_去门槛_20260629.md`。

**顺带（另案）**：点亮机制整体约方向硬币（点亮后 48–49% 回赔），`点亮`(Y′=4) 值不值待单独评估。

---

## 2026-06-30 · 光芯片缺货锚＝源杰+长光华芯个股篮子（不用光模块宽基）

**决策**：`concept_LaserShortage2026` 的锚从「光模块/CPO/光通信/光纤」ETF 宽基，改为「光芯片」＝源杰科技(688498)+长光华芯(688048) 等权个股篮子；仕佳光子排除（无源/连接基本盘主导，掺水）。`concept_CWLaserShortage` **不动**、留光模块锚。

**理由**：① 光芯片无纯 ETF，光模块宽基量的是中际旭创/新易盛等**模组厂**，而芯片缺货对模组厂是成本利空、对芯片厂是收入利好，逻辑可**反号**；② 两信号受益方不同——LaserShortage2026 渊图受益方就是源杰等芯片厂（干净匹配），CWLaserShortage 渊图写的是「CW 缺货→头部模组厂市占率维持」（模组厂受益），锚芯片篮子会错配。

**实现**：closure_engine 加 STOCK_BASKET+SIGNAL_THEME_OVERRIDE（见经验库 EXP-20260630-005）。dry-run + Doctor 终端 apply 落库，全表仅 1 条换锚、`recap.db.bak_20260630_closure` 兜底。换锚后露出源杰超额峰值 +62.8%（曾被宽基盖掉）。

**caveat**：长光华芯 stock_daily 仅 20260603 起（口径断裂），篮子 0603 前实为源杰单只、前向才真双名；且信号仍 dormant，日报正文要等二段点亮才显形（案2 价格门控）。

---

## 2026-06-30 · CWLaserShortage「头部受益」机制——先不补边，等新语料

**背景**：CWLaserShortage 节点（高盛 5-03 源）只写了结论「CW 供应紧张→头部光模块厂市占率/毛利维持」，未写机制。Doctor 追问是否暗含「头部厂锁 CW 产能/优先保供」。查证：该机制确在渊图、但散在**别的节点**（`concept_EML_CapacityHoardingEffect`「光模块厂也积极锁定上游」帕米尔 6-15；`concept_NvidiaLockHighEndChipCapacity` 主语是 NVDA），CWLaserShortage 本节点没把这条边显式连过来。

**决策**：**先不补边**。① 机制连机构都没拍定——高盛这条其实偏「竞争纪律/保毛利」而非「配给/锁产能」，两者投资含义不同（前者保毛利率、后者抢份额）；补结构化边＝伪精确。② 验证需 Q2 报设观察窗口。等新语料。

**重访触发器（判别尺）**：新语料里**看到旭创/新易盛对 CW 有明确长协/预付/配额** → 「配给·抢份额」，连边；**只看到毛利稳、无锁货证据** → 「竞争纪律·保毛利」。届时再连边 + Q2 观察窗口验证。

---

## 2026-06-30 · 日报离岸人民币汇率栏：源＝Tushare fx_daily，dim1 种子

**决策**：日报「情绪快照」与「外盘」之间加「美元兑（离岸）人民币汇率」栏（当前值 + 近7交易日曲线）。**数据源＝Tushare `fx_daily`（ts_code=USDCNH.FXCM）**；首次用 `dim1_external_pricing.usd_cny` 已录真实离岸点做一次性种子（`fetch_fx_cnh.py --backfill-dim1`）。口径：**USD/CNH 下行＝人民币走强**（曲线区间走强=绿）。

**为什么不用别的源**：① `dim1.usd_cny` 虽是离岸但稀疏（课件驱动、近10天仅3点），画不出真日线；② stockanalysis **无外汇盘**（导航无 Forex）；③ yfinance 在生产沙箱 **yahoo 403**。只有 Tushare fx_daily 与 stock_daily **同 provider/proxy/token**、有历史、与全库同源一致——外部真实汇率源会和复盘世界 6.79 打架，Tushare 不会。

**落地**：`scripts/fetch_fx_cnh.py`（新）写 recap.`fx_cnh_daily`；`gen_daily_report.py` 加 `fx_section()`（缺数走「待回填」）；zhuzhao SKILL 5c 加日更步。沙箱 /tmp 副本验渲染通过、真库未动。

**留尾**：Tushare 实取（USDCNH.FXCM 码/token/复盘世界一致性）需 Doctor 环境实跑验；dim1 种子稀疏期曲线实跨数周，标签「近7交易日」待 Tushare 灌成日线才名副其实。

---

## 2026-06-30 · 外部定价「韩国」改直追三星/SK海力士两只票（弃 EWY 代理）

**决策**：日报外部定价栏的「韩国」从 EWY（MSCI 韩国 ETF 代理）改为**直追三星电子(005930.KS)+SK海力士(000660.KS)两只票**，区头「韩国·存储双雄」。

**为什么不用 EWY/其它**：EWY 滞后（CDN 陈旧坑，停 0626）；查遍源——stockanalysis 有 KRX 个股但**延迟 ~3 周**（三星页 At close Jun-11）、yfinance 包沙箱未装、Tushare **无 KRX 个股**（只有 index_global 指数级 KS11）。最终 **Yahoo chart API（urllib 直取）通且新鲜**（三星/SK海力士实测到 0630）。两家占 KOSPI ~30%、存储行情里就是 KOSPI 本体，直追比 ETF/指数更贴。

**落地**：`scripts/fetch_kr_stocks.py`（Yahoo urllib）写 intl_index_daily(code=KR_SAMSUNG/KR_HYNIX)；报告 `INTL_KR` 渲两行；EWY/KR_PROXY 旧行不删、不再更新。

**关联**：同日把整个外盘取数（纳指 QQQ/NVDA/AVGO/LITE/SPCX/日经 NKD=F）也从 stockanalysis 切 Yahoo（修 LITE 滞后），见经验库 EXP-20260630-010。

## 2026-07-06 · 回测驱动的信号体系四裁定（点亮短窗/过热信息层/加权显性化/统一注记层）

**背景**：Doctor 授权自主回测（只读，829 标的级+182 主线级事件）+ 两次补测（MACD 扳机替代 / 天量顶部卖出），七发现落《自主回测报告-26.7.6》。

**裁定**：
1. **点亮终裁**：扳机维持 Y′=4日/回升5pp 不变 + 叠加 **3 日短窗**（窗内未创信号前高自动回暗）。依据：点亮后 10/20d 中位 −1.3%/−6.2% 输给正基线；MACD 底部承接替代方案补测判负（价格版超额 0 天）——弱势 regime 里更灵敏=更接刀，改善方向是更迟钝更确认。
2. **天量顶部 = "⚡过热"信息层**：K=3 口径只列明不指导卖出——全卖提高中位（0.96→2.2）但砍掉肥尾均值（5.25→4.85），own 池收益靠肥尾扛。
3. **信息差加权显性化**：L1<L2<L3 单调实证（10d +0.1/+3.5/+4.8%）→ GAP_LEVEL_WEIGHT 显式常量（初值 w1=0.7/w3=1.2 待审）；persistent_imbalance 只列明（逆风徽标）不降权。
4. **F7 统一回测注记层**：日报六处模组挂历史胜率小字，单一真源 `config/backtest_stats.json`，统一"样本期25.10-26.7·过往不代表未来"尾缀；另加"今日兑现启动"栏（3d 84.5%）与季节仓位参考栏（与小鲍口径三月并行对照）。

**影响**：closure_engine（短窗转移）/ gen_daily_report（新栏+徽标+注记）/ gap_rater（显性乘数）；执行归 `2026-07-06_回测发现落地批_PRD.md`（25 条，待批）。方法论沉淀 [[信号三仪表-进退节奏]]。

## 2026-07-09 · 同链多 concept 信号按【整链机制集】判归属（根治同名跨组）

**背景**：G657A2弯曲不敏感光纤同时出现在「需求爆发」卡和「纯持续失衡」组，看似矛盾。查库发现它是**两条独立记录、两个 concept 节点**：`concept_OpticalFiberSupplyShortage2026`(持续失衡) + `concept_FiberSeasonalDemandPeak`(需求爆发)——同一条受益产业链被渊图从供给/需求两个角度各标一个信号。不是数据错误，是 chip 以 `industry_chain` 去重、掩盖了「一条链对应多个 concept 信号」。

**裁定**：机制归属判据从「单条记录 `signal_type`」升级为「**整条产业链的机制集** `chain_mechs`」——
- 「纯持续失衡」组 = 该链机制集**只有** persistent_imbalance（整链无正 edge 机制）；
- 「信息差收敛/无信息差」标 = 按整链机制集含不含 persistent 判。
- G657A2 因整链含需求爆发（正 edge）→ 从纯持续失衡组**剔除**、在需求爆发卡带「信息差收敛」标，与真正的组合信号（单记录多机制标签，如英伟达Rubin）待遇统一。

**影响**：gather 加 `chain_mechs` 统计 + `_pure_persist` 集；mech_signals/mech_inflight_n/contra 判据全改按整链。**通则**：凡「一实体对应多条子信号（不同角度/来源）」的归属判断，都该按实体聚合的机制/属性集判，别用单条记录判——单条判会造成同名跨组像自相矛盾。经验：[[经验库]] EXP-20260708-002-P（组合标事实）同族。

## 2026-07-09 · PRD F7 落地：回测注记单一真源（config + fallback 内嵌）

**决策**：把日报散在 `gen_daily_report.py` 六处的硬编码回测数字（`MECH_WINDOW`/`RISK_MECH`/两条 glow bt-note/`BT_LOGIC`/`fixed_win=3`）抽进新建 `config/backtest_stats.json` 单一真源；脚本加 `_load_bt_stats()` + `_BT_FALLBACK`（**内嵌全部现值**），config 缺失/损坏即 `logger.warning` 回落硬编码。

**理由**：月度复算只改 JSON、不再脚本里散改漂移（对齐 2026-07-06 F7 裁定）；fallback 内嵌保 07:00 定时链不因 config 缺失而断——新依赖是软的、可逆。自检：/tmp 断言两条 glow 注记 + BT_LOGIC 输出与原硬编码逐字一致（数值零漂移）、`fallback ≡ config` 全项一致。

**留尾**：`SIGNAL_OVERRIDE` 补注表仍硬编码（F7-b 后续可抽 config）。备份 `gen_daily_report.py.bak_20260709-013921`。**关联**：`logs/checkpoints/2026-07-06_回测发现落地批_PRD.md` F7。

## 2026-07-17 · 日报加「五因风险温度」预警带（Phase-1·档A分期）

**背景**：2026-07-17 A股年度冰点 case study 提炼出『催化剂链·五因共振』（①外盘冲击 ②拥挤度释放 ③杠杆被动出清 ④IPO虹吸 ⑤地缘/油价·美债）。Doctor 要求把五因落成每日在复盘日报综合分析、做风险提示。

**决策（propose-then-confirm 两轮批准）**：档 A 分期 + 顶部带+可展开详情。日报顶部（四联条与 P0 之间）加 `risk_radar_section`：触发因子数 → 风险温度 🟢平静/🟡观察/🟠警戒/🔴冰点风险 + 五灯 + `<details>` 逐因子风险提示。

**Phase-1 在线可算（复用现成数据·零新取数）**：
- **F1 外盘传导** = `D["intl"]`（费半系 NASDAQ/AVGO/NVDA/LITE + 亚太 JP_FUT/KR 存储双雄）隔夜跌幅 ≤ −2%。
- **F2 拥挤度释放** = `D["capacity"].state ∈ {虹吸,满载}` 或 情绪分位≥80%且下行。
- **F5 外部紧缩** = `D["fx"]`（离岸人民币日贬≥0.05 或 7日≥0.10）；**油价数值列（dim1.brent_oil）为空、美债无源 → 该两块占位待接**。

**Phase-1 占位（待接源·渲灰不计温度）**：F3 两融（无表·待 tushare margin）、F4 IPO（待 tushare new_share）、F5 油价/美债。

**范式对齐**：仿 F7 单一真源——新建 `config/risk_factors.json` + 脚本内嵌 `_RISK_FALLBACK`/`_load_risk_cfg()`，config 缺失即 `logger.warning` 回落，不断 07:00 链。阈值皆**初值·待 emotion_cycle 历史冰点日回测校准**，带「过往不代表未来·非投资建议」注记；与既有「五·风险提示」（信号级锚风险）互补不重复。

**自检**：`py_compile` 过；`--output /tmp --no-archive` 试渲，冰点日正确报 🟠警戒(2/3触发)——F1触发(费半−6.1%/亚太−11.5%)、F2触发(满载·下行)、F5平静(CNH近7日反走强)、F3/F4灰；四联条/P0 无回归；顺序 四联条→预警带→详情→P0 正确。备份 `gen_daily_report.py.bak_20260717-195826`。

**留尾（Phase-2 子PRD）**：`fetch_margin.py`(F3)/`fetch_ipo.py`(F4)/美债源(F5)入 recap + SKILL 日更步；到位后上 `emotion_cycle` 历史冰点日回测校准五因阈值、写回 `risk_factors.json`（`calibrated:true`）。**关联**：`logs/2026-07-17-五因风险温度预警带Phase1.md` · case study `AI4ME/... 或 Documents/A股年度冰点-20260717-case-study.html`。

## 2026-07-17 · F3 两融接入（Phase-2 第一源·tushare margin）

**决策**：`scripts/fetch_margin.py`（新）拉 tushare `pro.margin` 市场级 → 公共 `market_data.db.margin_daily`（附加表·沿用 fetch_market_amount 范式，不动句芒既有表）；主指标**融资余额 rzye**（媒体『两融减1700亿』口径），单位统一亿元。回填 20240102→20260717 共 614 日（Doctor 终端跑，网络禁沙箱）。F3 由『待接源』转**在线可算**：`_eval_risk_factors` 读 `D["margin"]`，触发＝融资余额 **5日净减≤−400亿 或 单日≤−300亿**（初值据 20240102-20260716 分布 5日 p05≈−413/单日 p01≈−332，待历史冰点日回测校准）。`risk_factors.json` phase1_online 增 f3、写入阈值。

**GOTCHA · 两融 T+1 半日陷阱**：两融明细 T+1——当日尾部常仅 SSE 先发、SZSE/BSE 次日到。首版脚本按交易所 loop 累加，把「SSE-only 的 0717」与「三所齐全的 0716」相减→**假摔 −14448 亿**。修：`fetch_margin.py` 只写 SSE+SZSE 齐全的完整日、半日跳过（次日 INSERT OR REPLACE 补全）；`--check` 与日报 F3 一律 `WHERE szse_rzrqye IS NOT NULL` 取完整日。诊断确认数值为**真·分所口径**（2024-01 SSE 8765/SZSE 7776；非整市重复），本牛市世界两融升至约 2.85 万亿内洽。已登记 `Market-Data/MANIFEST.md`「两融缺口」必读段。

**自检**：`py_compile` 过；`--check` 修后最新完整日 0716 融资余额 28377亿/日变 −286/5日 −965（正常）；`/tmp` 试渲 F3 触发（5日 −965≤−400）→ 温度由 🟠警戒 升 🔴冰点风险(3/4触发)、摘要动态转「4/5 在线·仅 F4 待接源」。备份 `gen_daily_report.py.bak_20260717-195826`。

**留尾**：F4 IPO(`pro.new_share`)、F5 美债(^TNX) 照此依次接；`margin_daily` 日更步待加入句芒/烛阴日更链（当前仅回填，未接 07:00 自动增量）。

## 2026-07-17 · F3 日更步接入定时链 + F4 IPO 接源（Doctor：接上/同意/都做）

**决策（三合一指令）**：① F3 margin 日更步接入；② F4 IPO 接源；③ 五因阈值回测校准——同意，按原 framing 排在 F5 美债也接齐后统一做（本轮不执行）。

**① F3 日更步**：`zhuzhao-market-fetch-daily-report/SKILL.md` step3 增 `fetch_margin.py --from` 一步（与 fetch_market_amount 并列），frontmatter/目标/防空壳同步改「四表→五表」。因 `config.MARKET_DB` 认 `ZZJY_DATABASE_ROOT`，margin 自动跟走 /tmp 副本往返，与既有机制一致。**两融 T+1 不纳入「必到前一交易日」硬门**（完整日 max 允许落后 1 日）。SKILL 为 Scheduled 任务本体、磁盘直读，改毕次日 07:00 链生效，无需重装。

**② F4 IPO**：`scripts/fetch_ipo.py`（新）拉 tushare `new_share` 按申购日(上网发行日·2026-07-22更正,原误记「上市日」,详见本日 F4窗口条)聚合募资 → `market_data.ipo_daily`（funds_yi 亿元）。`_eval_risk_factors` F4 读 `D["ipo"]` 近20日历日募资合计，触发＝≥`funds_win_th`（**占位初值 500亿·待 Doctor 回填后据 `--check` 分布校准**，与 F3 同法）。graceful：表未回填→F4 渲『待接源』不计温度；回填后自动激活。config phase1_online 增 f4。

**自检**：`py_compile` + JSON OK；`/tmp` 试渲 F4 正确显『待接源｜ipo_daily 待回填』（未回填），F1/F2/F3 触发、温度 🔴冰点风险(3/4)、摘要动态『4/5 在线·仅 F4 待接源』——F4 回填后将转在线。

**留尾**：F4 回填后据 `--check` 定 funds_win_th 真值；F5 美债(^TNX)接入；**五因阈值统一回测校准**（同意·待 F5 齐）；`fetch_ipo.py --from` 日更步待随 F4 验收后加入 SKILL step3。

## 2026-07-17 · F5 补齐为「汇率+油价+美债」三件套（复用 fetch_intl_index·零新脚本）

**决策**：F5 不另起取数脚本——把 `^TNX`(美债10Y·code=US10Y·kind=macro_rate) 与 `BZ=F`(布伦特·code=BRENT·kind=macro_commodity) **直接加进 `fetch_intl_index.py` 的 INDICES 清单**（该脚本已走 Yahoo chart 通道、且已是日更链 step②b）。**故 F5 数据源日更自动跟随、无需再改 SKILL**——这是选它而非新写 fetch_bond.py 的主因（复用为主 + 少一个日更步）。

**F5 判据**（三件套任一触发即亮，缺者标『待回填』不阻断）：CNH 日贬≥0.05 或 7日≥0.10；布伦特单日≥+3.0%；美债10Y 日升≥+8bp（bp 由 close 与 pct 反推前值算得，因 `^TNX` 的 close 即收益率%）。阈值皆初值待校准。

**不侵入外盘展示区**：日报 `external_pricing_section` 按 INTL_US_INDEX/STOCKS/ASIA/KR 显式清单渲染，新增两码不在其中→**只供 F5 计算、不改外盘栏版面**，零回归。

**自检**：`--dry-run` 列出 8 标的含 US10Y/BRENT；`py_compile`+JSON OK；`/tmp` 试渲 F5 正确显「CNH 6.7738(+0.0005) · 油价待回填 · 美债待回填」＝汇率照算、两项优雅待回填。

**留尾**：Doctor 终端跑 `fetch_intl_index.py --from` 激活油价/美债（回测校准需较长历史，建议 `--from 2024-01-01`，副作用是同时回填既有 6 码历史·纯增量无害、日报只读 latest-per-code）。

## 2026-07-17 · 五源接齐：F4 阈值校准 + F1 隔夜护栏（裁定 A）

**回填结果**（Doctor 终端）：`ipo_daily` 236 个申购日(2026-07-22更正,原误记上市日)/304 只/募资 3642 亿 [20240102→20260716]；`intl_index_daily` +US10Y/BRENT（8 码全取到，缺 0）。

**F4 阈值校准（占位→实证）**：滚动 20 日历日募资分布 p50=44 / p90=139 / p95=201 / p99=357 / max=712（＝当下）。原占位 500 亿仅 0.1% 窗口触发**过严**，改 **300 亿**（≈p95~p99 间、触发率 1.2%）。史上最大单笔＝**长鑫科技 666.1 亿（20260716）**，次大仅 246 亿——即案例所指「超级 IPO 虹吸」的实体。

**F1 隔夜护栏（Doctor 裁定 A 档）**：`D["intl_on"]`＝每码取 **trade_date < A股数据日** 的最新场次，F1 专用；展示栏仍读 `D["intl"]`、版面零改。**根因**：外盘取「最新一行」，手工/盘中补数会取到**美股当日未收盘场次**——对 A 股 D 日构成①前视（美股 D 场在 A 股收盘后）②未收盘快照。实证：重跑后 F1 由 −6.1% 变 −2.2%（错），加护栏后回 −6.1%＝0716 真隔夜（对）。定时链 16:00 北京跑时护栏为 no-op（美股当日未开盘）。**顺带修**：场次日期标签原取字典首码（显示成 06-26 的无关旧码），改为只取 F1 实际用到的 semi+asia 码之 max。

**当日读数**：5/5 全在线全触发 → 🔴 冰点风险。**⚠️ 此为样本内**——五因本就自 0717 复盘提炼，全中属构造性必然，**不构成预测力证据**；真检验在其它日期（尤其假阳性率），待统一回测。

**留尾/待裁**：F5 的 BRENT/US10Y 同样读 `D["intl"]`、**有与 F1 相同的前视暴露**，A 档字面只覆盖 F1——是否一并套用护栏待 Doctor 定（CC 倾向套用，一行改动）。另：`fetch_ipo.py` 日更步待加入 SKILL step3。

## 2026-07-17 · 五因首轮回测校准：F3 降信息层 + F4 阈值 300→200（实证推翻直觉）

**工具**：新建 `tools/calibrate_risk_factors.py`（**只读 DB、只写 docs/ 报告，绝不改 config**）。逐历史交易日 **point-in-time 重建** F1~F5（外盘严格 <D＝真隔夜、两融完整日 ≤D、IPO 滚动20日历日 ≤D、情绪/汇率 ≤D），标签「冰点」＝未来 3 交易日创业板累计 ≤ −5%（基准率 **3.9%**，62/1581）。报告 `docs/五因回测校准_20260718.md`。

**核心发现（推翻先验）**：
- **F3 两融无预测力、甚至反向**：n=606 可评/33 触发，**lift 0.87**（<1）；触发日 fwd3 **+1.24%** 反高于未触发 +0.38%；阈值扫描非单调（−200:1.75 / −300:0.51 / −400:0.77 / −700:1.70 / −900:0.00）＝噪声形状。**机理**：两融余额 T+1 公布，是**滞后/同期量**——它下降是因为市场已跌，不是要跌的前兆。0717 那个「5日 −965亿」是**追认**不是预警。此前 CC 对 F3 信心最足，被数据推翻。
- **F4 IPO**：现行 300 亿仅 n=8（lift 6.38 好看但不可信）；**200 亿** n=33/lift 1.55/触发日 fwd3 −0.22%＝样本与区分力的平衡点。
- **F1(n=22 可评)/F5(n=179)/F2 容量腿** 样本不足，无法校准。

**裁定（Doctor 三选）**：① **F3 降『信息层』**——`status="info"`，只列融资余额水位与增减、**不计入风险温度**，沿用项目「⚡过热信息层只列明不指导」先例（保留追认价值、不假装预警）；② **F4 阈值 300→200 亿**；③ F1/F5 待外盘历史回填后重跑校准。

**连带修（阻断校准的 bug）**：`fetch_intl_index.fetch_yf` 的 URL **硬编码 `range=1mo`**，`--from/--to` 只用于裁剪、从未传给 Yahoo → **任何长区间回填都静默只回 1 个月**（F1/F5 只有 ~21 行历史即因此）。改用 `period1/period2` 真区间（日更短窗行为不变），并修 `utcfromtimestamp` DeprecationWarning。

**呈现改动**：`gen_daily_report` 新增 `info` 态（灰蓝 #7b8aa0·"信息层"）；温度口径由「在线因子」改为「**计温因子**」——info 与 pending 均不计温。当日读数：**4/4 计温触发 🔴**（F1/F2/F4/F5），F3 列为信息层。

**`calibrated` 置 `"partial"` 不置 true**——F1/F2/F5 未校准，数据不支持。温度分层 2/3 触发的 lift 4.25/8.50 但 n=6/3，**暂不可依赖**。

**留尾**：Doctor 重跑 `fetch_intl_index.py --from 2024-01-01`（bug 修后才真回填）→ CC 重跑校准补 F1/F5；F2 容量腿需按时点重建 theme 容量才可校准（另议）。

## 2026-07-17 · 第二轮校准（外盘补齐 n=610）：五因只剩两因计温，温度改二值

**背景**：外盘回填到位（各码 637–640 行 [20240102→20260717]），F1/F5 首次可真校准。

**结果（现行阈值·样本 n=610）**：
| 因子 | 可评 | 触发率 | lift | 判定 |
|---|---|---|---|---|
| F1 隔夜外盘 | 610 | 35.6%(217) | **1.06** | 无区分力 |
| F2 拥挤度(情绪腿) | 354 | 1.7%(6) | 0.00 | 样本不足 |
| F3 两融 | 606 | 5.4%(33) | 0.87 | 无区分力（已降 info） |
| F4 IPO | 1581 | 2.1%(33) | **1.55** | **有区分力** |
| F5 外部 | 610 | 12.5%(76) | **1.46** | **弱区分力** |

**F1 阈值扫描＝决定性否定**：−1.5%:1.02 / −2%:1.06 / −3%:0.51 / −4%:0.47 / −5%:0.35 / −6%:0.45——**无任何操作点有效，收紧反更差**。
**F5 油价腿扫描单调向好**：3%:1.34 / 4%:1.42 / 5%:1.74 / 6%:1.87 → 取 **5%**（n=44 仍可信）。
**复合验证**：仅 F4+F5 时「≥1 触发」lift **1.85**(n=75)；把 F1/F3 放回去，同档 lift 塌到 **0.56**——噪声因子稀释信号。「触发2」n=1~2，**0-5 级温度计无数据支撑**。

**裁定（Doctor 三选 + 一项机械调整）**：① **F1 降信息层**（同 F3）；② **温度改二值** 🟢平静(0触发)/🟠警戒(≥1)；③ **F2 暂降信息层**（未经校验不计温，待容量腿可按时点重建后再评升回）；④ F5 油价腿 3%→5%（机械调整）。**计温因子只剩 F4/F5**，F1/F2/F3 为信息层（只列数、不计温、灰蓝 #7b8aa0）。

**通则（本轮最大收获·已写入 config `calibration_note`）**：F1 与 F3 失效**同源**——隔夜外盘在 A 股**开盘跳空那一刻已被定价**，收盘后读它信息已耗尽；两融 **T+1 公布属追认**。二者都是「已被吸收 / 滞后」的信息。存活的 F4（发行日程**提前公告**）与 F5（油价地缘**外生且持续**）携带的是**尚未进入 A 股收盘价**的信息。**事后能解释 ≠ 事前能预测**——0717 复盘正确找出了「为什么跌」，但把五个原因平移成五个预警因子这一步是错的：原因里只有一部分具备前瞻性。

**当日读数变化**：由「🔴 5/5 触发」→ **🟠 警戒 1/2 计温触发**（仅 F4；F5 油价 +4.59% 差一点未达收紧后的 5%）。更清醒，也才是校准后系统的真话。

**留尾**：F2 容量腿按时点重建后重评；`calibrated` 维持 `"partial"`（F2 仍未校准）；样本仅 2024-2026 牛市段，遇熊市需重校。

## 2026-07-17 · 第三轮修正：F1 由「信息层」改「条件放大器」（Doctor 领域判断推翻 CC 校准结论）

**Doctor 指出**：「外盘传导对开盘点位有效，对当天走势无效；但当多因素都指向风险偏好降低时，尤其外盘走势背后有逻辑性看空因素时，会有传导效果。」

**CC 的方法论错误（两处，均被证实）**：
1. **标签错配**：前一轮用 D+1~D+3 **收盘对收盘**做标签，**从未测过「开盘点位」**。若隔夜冲击在跳空一刻兑现完毕，此标签必然测出 lift≈1——这只能证明「不影响跳空之后的路径」，不能证明「外盘无效」。二者被 CC 混为一谈。
2. **主效应 vs 调节效应**：把 F1 当**平权投票**加进复合（结果稀释信号 lift 0.56），而 Doctor 说的是**调节变量**。用测主效应的方法去测调节效应，方法本身就错。

**补测结果（n=610, 20240103-20260714）**：
- **测试A（开盘 vs 走势）**：F1 触发日 当日开盘跳空 **−0.17%** vs 未触发 +0.04%（差 −0.21pp）；当日创业板收盘 −0.09% vs +0.27%；未来3日仅差 −0.40pp。→ **「对开盘有效、对后续走势弱」成立**。
- **测试B（条件效应）**：在 F4/F5 已触发的日子里——F1 也触发 n=31：冰点率 **9.7%**/lift **2.68**/fwd3 **−1.05%**；F1 未触发 n=45：4.4%/1.23/**+0.79%**。
- **对照组（决定性）**：在 F4/F5 均未触发的平静日，F1 触发与否 **3.2% vs 3.2%**、fwd3 +0.36% vs +0.53%——**零差别**。→ 干净的**交互效应而非主效应**。
- **F3 同法验证不合格**：条件冰点率 10.0%(lift 2.77) 看似同样好，但 fwd3 **+0.43% vs +0.08%** 方向相反、且 n=10 → 两指标打架，维持信息层。F2 在该子集 0 次触发无法评。

**裁定**：F1 layer 由 `info` 改 **`amplifier`**——**不参与「够不够格报警」，只决定「报多重」**。温度回三态：🟢平静（F4/F5均未触发, 3.2%）/ 🟠警戒（F4或F5触发、F1未触发, 4.4%·lift1.23）/ 🔴**警戒·外盘共振**（叠加F1触发, 9.7%·lift2.68·fwd3 −1.05%）。**平静日 F1 不升温**（实证该情形下它零信息）。当日读数：🟠警戒 → **🔴警戒·外盘共振**。

**未能机械化的一环（诚实标注）**：Doctor 第三句「外盘背后有**逻辑性看空因素**时」——价格数据分不出「有实质利空的下跌」与「噪声性下跌」，故 lift 2.68 是**二者混合均值**（真有实质利空时应更强、纯噪声时更弱）。候选源 `dim1_external_pricing.key_signals`/`pricing_direction`（课件定性文本·151行·稀疏），需文本→标签提取，另设计。已写入 config `f1.caveat`。

**教训（与前一轮的「事后解释≠事前预测」并列）**：**「无主效应」不等于「无用」**。因子可以只在特定状态下携带信息；用平权投票测调节变量会同时错杀因子与稀释信号。回测设计必须先想清楚「这个因子被主张的作用形式是什么」（主效应/调节/择时/条件），再选标签与检验方式——否则测的是另一个命题。领域判断（Doctor）在此优先于机械回测（CC）。

## 2026-07-17 · 叙事状态层 narrative_regime（承 F1「逻辑性看空因素」未竟项）

**Doctor 的关键反框**：CC 原设想是「给每天的下跌贴原因标签」，被指出应改为「**只提取有长期指导意义的逻辑（如 AI Capex 叙事改变等"大事"），当天的下跌原因因为已过时而不再重要**」。此一改换掉了分析单元，**同时化解 CC 提出的两条反对**：① 不需日度配对 → 9 个月只有个位数事件也够用，无「31天切成5天」问题；② 前视污染消失 → 叙事标记在其后数周仍有效，不存在用答案倒推题目。当日原因确实过期极快（与 F3「追认当预警」同病）。

**扫描实测**：`dim1_external_pricing.pricing_direction` 93 条**绝大多数是日度战术判读**（"中性偏多(等待6/18美联储)"），9 个月仅 2~3 条够结构级 → **产出率低到不值得写解析器**。反而 **`industry_signals` 是结构级叙事富矿**（"康波起点AI主线"、"AI高景气未变·字节token两月+50%"＝Doctor 所指 AI Capex 叙事、"科技牛非科技熊·机构认错增配"、"OpenAI延期IPO·行业天花板未到"）。

**裁定**：① 建小表 `narrative_regime`（date_start/tag/direction/summary/source/status/date_end），**采「扫描→候选→人确认→落表」不做自动解析**（2.5 个月即扫出 54 条强候选，显然需人判；误判成本＝给放大器塞假信号）；② **先只作上下文注释、不门控温度**——叙事事件一年才几次，无法验证就不让它动定级（同「未经校验不计温」纪律）。

**落地**：`tools/scan_narrative_regime.py`（只读扫描器，双源 dim1+industry_signals，区分「强候选/疑似战术」；`--ddl` 打印建表+种子 SQL）。`gen_daily_report` 加 `D["narrative"]` 载入 active 叙事，F1 共振时在证据后挂「▸ 叙事背景：…（自 X）」；**表未建则静默跳过**。首批种子两条（tech_valuation_bubble_burst / asia_deleveraging_spillover，均起 2026-07-13，据 dim1 07-13/07-16 实证文本）。

**约束**：CC 不在沙箱写 recap.db（G019/FUSE）→ 建表与种子由 Doctor 终端 `--ddl | sqlite3` 执行。

**留尾**：叙事事件积累够（估计需 1~2 年）后再评是否升为放大器门控；`industry_signals` 既是富矿，后续可考虑把叙事态直接挂到渊图信号的机制层而非另立标签体系。


## 2026-07-18 · 情绪周期「春」判据审查后维持现状(Doctor 裁定)

- **背景**:0715 宣春 × 0717 大跌(春内 score 22.7/跌停192)。只读审查(`AI4ME/CC-情绪周期春判据审查-20260718.md`)发现:冬→春=仅 MA5 上穿(全机最弱门槛,置信低为设计);**春撤回线=本轮冬谷 8.2(0713 极寒铸成,历史中位≈24),机制性不可达且史上零触发**;历史 12 次冬→春 fwd10 正率 7/11。
- **裁定**:可以接受,判据一字不动;改进选项 O1(撤回线剔极寒)/O2(确认期)/O3(置信渲染)/O4(级别联动注记)全部搁置。
- **预注册**:①0729 收盘核本轮春 fwd10(正→弱先行成功,负→归入 6/11 反例池);②若 score<8.2 触发史上首次春→冬撤回,单独记录该保险丝首验。
- **口径分工备忘**:情绪周期=~26td 短周期风险偏好摆(春=短线修复尝试);回调级别判别器=中期回撤 nowcast——同屏非矛盾。

## 2026-07-19 · 计温 function 改造定 S2 两层重构（触发层 × 环境层）

**背景**：事件级重估后五因计温层只剩 F4（3 事件·机理强但薄）；Doctor 裁定「仅 F4 有意义，但意义不大，需要进一步挖掘因子，修改 function」。同日两条研究线补齐了选型依据：① 7.17 第一性原理筛查产出 A6 量能脆弱态（53 事件·双尾放大器·禁看空计温）与 B6 浮盈集中度（24 事件·lift 2.20），均为**状态型**；② F5 油价腿长样本三裁定——5% 档平反（45 事件 lift 2.03）、跨区制首例（四段全 >2）、单向看空，为**事件型**。

**裁定（Doctor 2026-07-19 批 S2）**：function 由「触发因子数→三态」改为**两层结构：触发层（F4 IPO、F5 油价腿）× 环境层（F1 外盘、A6 量能、B6 集中度）**，输出仍三态但语义变为「事件×状态共振」。S1 最小改（单因子计温几乎常绿）与 S3 整体重构/并入判别器（forecast/nowcast 任务不同 + 12 事件必过拟合）均否。

**依据链**：平权投票 lift 0.56（把状态当票）早被否；F1 平静日 3.2% vs 3.2% 零信息、A6 双尾、B6 状态粘滞（膨胀 7–17×）——状态型三者全部只能调节不能定级；F4/F5 均为外生事件型（日程/地缘）。**有效信号的两分类与两层结构一一对应**。

**前置（开工顺序）**：① F5 油价腿回补回测 ✅（同日完成，纪要 v1.6）；② A6/B6 对 F4/F5 共线检验（待做，半天）——回答「新候选是否只是 F4 的影子」（2026-06 下旬 A6/B6 与长鑫 666 亿共现）；③ 共线检验后立 PRD（起草≠开工）。

**约束**：新 function 一律过《回测设计七问》；三态阈值需按两层结构重新校准（现 3.2/4.4/9.7% 为三腿旧口径已作废）；`auto_trade_allowed: false` 不变。

**来源** → logs/2026-07-19-遗漏因子筛查与F5长样本三裁定.md · 纪要 v1.5/v1.6 · 两报告（AI4ME）

## 2026-07-22 · F5 维持计温、不降信息层（Doctor 拍板，闭合「未触发降层裁定点」）

- **背景**：五因收尾 PRD 挂着一个「可能触发的下一个裁定点」——F5 债腿降层后两腿重校只剩 **9 独立事件 < 20 门槛**（lift 2.22），按项目自定铁律「独立事件<20 视为不可信」，F5 是否该从触发层降为信息层待裁。
- **CC 端到端 regen 验收后提请**：给出一个横跨 Doctor 二元命题的观察——**F4 比 F5 样本还薄**（F4=4 事件 vs F5=9 事件），两根触发层因子**都不达 20 门槛**。故「降 F5 留 F4」逻辑不自洽（拿更薄的当准、更厚的当噪声）；真按事件门槛砍该先砍 F4。
- **裁定（Doctor 2026-07-22）**：**F5 维持计温、不降层**。判据不在事件数、在**形状**：F5 油价腿阈值扫描**单调**（2%:0.95→3%:1.11→4%:1.16→5%:2.22→6%:3.33）+ **外生持续机理**（油价/地缘未被 A 股收盘吸收）+ **2010+ 跨区制长样本**支撑，与债腿（非单调噪声，已正确降层）**本质不同**；F4 亦是靠机理挂账留的。三个自洽选项中，「维持 F5 计温」与保留 F4 一致，且优于「只降 F5 留 F4」的逻辑倒置。
- **状态**：此裁定 = config 现状（F5 本就在触发层），**无需改文件**；两根因子一律 k/n 呈现、标「样本薄·预注册中」，不作确证。PRD「未触发降层裁定点」**认可闭合**。
- **预注册**：F5/F4 事件继续累积，任一腿事件数过 20 门槛后重评；若后续证据显示油价腿单调性破裂或长样本区制支撑不成立，再启降层。
- **来源** → logs/2026-07-22-五因regen验收与resume开声固化.md · [[2026-07-21_五因收尾-F5重校与温度带呈现_PRD]] · docs/五因回测校准_20260721.md

## 2026-07-22 · F4 窗口 20→10（申购锚）+ ipo_date 误标更正 + 除 win_days 硬编码

**背景**：Doctor 第一性原理质疑「2.5万亿/日市场，666亿虹吸没有 20 天影响」。CC 只读扫描（`outputs/f4_window_phase_sweep.py`，复用 calibrate 口径：标签+PIT+独立事件切分 GAP=3）。

**关键更正（推翻前提）**：tushare `new_share` 的 `ipo_date`=**上网发行日=申购日**、`issue_date`=上市日（Doctor 终端实测 ipo_date 一律早于 issue_date、长鑫 issue_date=NaN 未上市为证）。`fetch_ipo.py` 原通篇误标「上市日」。故 **F4 本就锚申购日**（第一性原理要的锚），CC 一度误判「相位错·锚上市日」**已收回**。参 [[通用教训]] G-X81。

**决策**：
- **① win_days 20→10**——两标签（3日累计 / 5日最大回撤）一致在 **10 日**见顶（lift 4.92–5.03 / P暴跌50%），14→20 单调稀释；短窗曾显 lift 0 系「3 日累计」被 V 型反弹（长鑫 07-17 −7% / 07-21 +7%）抹平之**假象**。
- **② 阈值 200 亿保留、不降阈凑样本**——10 日口径下 200 亿≈单只巨型 IPO 探测；**Doctor 认可「基本只认单只巨型 IPO、小型 IPO 无影响＝可接受」**。降到 100 亿(5–9事件) lift 塌回 ~1 噪声＝样本硬墙（2024–2026 仅 2–4 轮 ≥200亿虹吸）。
- **③ 窗口改读 config、除 `gen_daily_report.py` `timedelta(days=20)` 硬编码潜 bug**（此前 config.win_days 存了没人读）。

**成色**：`calibrated:partial`（仅 2–4 事件·不足实证，方向支持）。**落地**：`fetch_ipo.py`（修 7 处误标+docstring锚点，逻辑零改）/ `gen_daily_report.py`（读 config）/ `risk_factors.json`（win_days+注记，`_meta` 历史叙事不动·不改写历史）。py_compile×2 过、Doctor 试渲 `/tmp/烛照九阴日报_20260721.html` 无回归、`diff --stat` +17/−15。**下游**：长鑫（申购日 20260716，666亿）10 日窗最后计入 07-25、**F4 清除 08-05→07-26**。**依据** → `outputs/F4窗口相位扫描_20260722.md` · logs/2026-07-22-F4虹吸窗口20到10与ipo_date误标修正.md


## 2026-07-22 · 日报成交额取数由成指代理改 daily_market 全口径（A 档）+ kcap 全口径心智裁定

**背景**：Doctor 第一性原理核「周二两市成交额确定只有 2.38 万亿么」。查实日报三处成交额（快照条、主线表、K_cap 容量模型）全取 `market_amount_daily.total_trillion`＝沪+深成指指数加总代理（漏北交所+深市非成分股），系统性低估约 0.5–0.6 万亿 / **~20%**，却标「两市/全市场成交额」。全口径 `daily_market.volume_trillion`（含北交所全A）07-21=**2.97 万亿**（原显 2.38）。

**决策（A 档 · propose-then-confirm 两道门）**：
- 取数统一换 `daily_market.volume_trillion` + `WHERE volume_trillion>0`（挡 ERR-20260719-003 历史 0-fill）；显示+K_cap 同源；标签「全市场成交额」名实相符。
- **kcap() 不重标**——**Doctor 裁定**经验表 `1/2/3万亿→1.5/3.5/5.5` 本是**全市场读数心智**（整数即人读全口径），非按代理调。故换源＝纯纠偏：既修显示、又把 K_cap 从长期偏紧（少喂 ~20%）拨回本意，容量判定回归、主线放行略增（本次 18 条）。
- 历史长序列量能研究仍按 ERR-20260719-003 用 `index_research.db`（本改只服务当日日报显示+当日 K_cap，用最近 3 日、全 post-20260603，不踩 0-fill）。

**落地**：`gen_daily_report.py` line337/603 换源、line2338 停更提示改名、快照标签改；kcap 零改。py_compile 过、Doctor 正式重渲无 disk-I/O、实图核 2.97 显示与条幅两行。code 仓 `0045bd5`。**依据** → logs/2026-07-22-成交额口径纠偏与条幅两行artifact推送.md · [[烛照九阴/GOTCHAS]] ERR-20260722-001


## 2026-07-22 · 写库任务挪回 Mac 原生 launchd 根治「日报隔天退回」（迁移 Phase 1）

**背景**：日报 artifact 落后一天（07-23 显 07-21）。根因坐实 **A 类·写没落住**——沙箱经 FUSE 整库 mv 回写，大表 stock_daily 未 durable 落 Mac 真实盘（[[烛照九阴/GOTCHAS]] ERR-20260722-003）。Doctor 拍板走「挪回 Mac 原生」根治。

**决策**：
- **Phase 1**：market_data.db 写入（stock_daily 落库 + 句芒派生 + 烛照五表 fetch）搬到 Mac 原生 launchd（`烛照九阴/ops/` 三文件：ingest_stock_daily.py / mac_daily_marketdata.py 编排器 / com.zhuzhao.marketdata.plist，周一~五 02:30 本地）。原生写即 durable，脱离 FUSE。
- **launchd 入口用 python3.13 不用 bash**——TCC 授权主体＝顶层程序，bash 未授 FDA（[[通用教训]] G-X82）。
- **本阶段不动 Cowork 两任务 + artifact 推送**（留兜底先验证）：`update_artifact` 是 Cowork 专属、Mac 侧调不到，正好各司其职；Mac 排 02:30 在 Cowork 句芒 01:30 之后＝最后的 durable 写入方。
- **代价**：Mac 须开盖+插电醒着（launchd 睡眠不触发）。

**验证**：装载 + kickstart，全链 durable 落库、各表到 07-22、status=OK、零权限错。**待终检**：今晚 02:30 真实 overnight 跑后次日日报是否免手动到最新交易日。**Phase 2（验稳后）**：停 Cowork 句芒、九儿改 push-only、recap 派生去留。**依据** → brain/logs/2026-07-22-日报隔天退回根治与Mac原生Phase1.md

## 2026-07-30 · 情绪周期立「人工标注层」：记反转日、不记季节，引擎零改动

**裁定**：Doctor「情绪周期，先不改判定标准，加一行手动修正/人工标注」→ 两度收敛后定形为 **`docs/情绪周期_人工标注.tsv`（纯记录文件，引擎完全不读）** ＋ 追加器 `tools/mark_reversal.py`。`emotion_engine_v2.py` **本场净零改动**（中途加过叠加层，Doctor「回退掉，不改引擎」后已恢复 `.bak_20260730` 并逐字校验一致）。

- **记「反转日」不记「季节」**：反转日是事件点、季节是状态区间。初版把 07-30 标成「春第一天」，Doctor 纠回——标成春等于宣布本轮跳过秋冬开新周期（引擎当日读数为**秋·第一天**，cycle 15：07-14 春→07-21 夏→07-30 秋），而单向状态机禁止该路径。现在标注只声明「这天发生了反转」，**季节归引擎判**。详 [[通用教训]] G-X108。
- **定位＝给引擎攒事后真值标签**，不是覆盖引擎读数。Doctor 定次序：**先攒标注 → 写评测脚本算提前量 → 有基线了再谈改判定**（「引擎肯定要改，但改之前得先攒够数据/标注」）。在此之前不动判定标准。
- **每条带 `engine_*` 快照**（season/cycle_no/score/level_pct/trend/**hint**）自动抓取、禁手抄——基线被改过则提前量永远说不清（[[经验库]] `EXP-20260730-002-P`）。`rationale` 列录 Doctor 原话且必填，是评测时判「人工凭什么看到、引擎凭什么没看到」的证据。

**首条样本 `20260730 · reversal · up`（见底反转向上）**，rationale＝「科技股跟随美股出清，知名基金爆仓（出清比较彻底）；7.29 美股夜盘反转；7.30 A股跟随见底反转」。

**⚠ 两点观察（n=1，不作结论，记此备日后改引擎时回溯）**：
1. **顶部会喊、底部哑火**——07-23 火热点日 `hint`＝「🔥火热点·秋风险积累中」（夏·分位 98.0·上行）；07-30 底部反转日 `hint`＝**空**（秋·分位 26.0·下行）。与引擎自身校准一致：火热点（顶）命中 42%、中位先行 6.5 日；极寒（底）命中 75% 但**中位距离 0 日＝同步、零提前量**。引擎头注「顶侧置信高/底侧置信低」写的就是这个，只是过去没拿真实底部验过。
2. **首条 rationale 三个依据全在引擎输入之外**——美股出清 / 基金爆仓 / 美股夜盘反转；而八成分只吃 A股内部量价（晋级率·次日溢价·涨跌停·涨跌比·主线宽度·成交额·连板高度），**既无外盘腿、也无强制出清·杠杆爆仓腿**。故它不是「算错」而是「**看不见**」。若样本反复指向同一缺口，改造方向可能是**补成分**而非调阈值/状态机。

**依据** → brain/logs/2026-07-30-情绪周期反转日标注层.md · brain/TODO.md「情绪周期四季『提前发现』能力改造 → 等标注积累」

## 2026-08-09 · 成交额定源乙案（Doctor 批）：F4/快照分母换 volume_trillion + 阈值分位等效重锚

**背景**：07-28 甲案锁 `market_amount_daily` 的两个前提同日被破——「volume_trillion 仅 2026-06 起」被 A2 回补破除（ERR-20260719-003 收口，2020+ 全段有效）；「少计 20-25% 属口径特征非误差」被实测证伪（两源比值日摆 1.18–1.79、年均 1.35–1.53；252 日分位差中位 3.2pp / p90 11.6pp / max 29.4pp；近期旧源分位系统性偏热 5–15pp）。

**决策（propose-then-confirm 两道门）**：2020+ 分位/比值/回测统一锁 `daily_market.volume_trillion`；`market_amount_daily` 保留日更、降为 2010 前长史专用+双源对撞监控、2020+ 用途禁用。阈值分位等效重锚：ratio_th 0.045→**0.030**（生产口径全史 p95=0.0301）·near 0.036→**0.025**（p92.4）·calibrate 扫描网格平移 [0.020, 0.025, 0.030, 0.038, 0.047]。

**无损验证**：F4 全史回放触发 78/78 全保留、仅多 20210415 一日；0603 拼接缝实测平滑（A2 段缺北交所·系统低 ~0.7%·两源比值 1.31→1.35 无跳变）。emotion_engine 成交额 5 日变化率成分同步换源，新旧源 5 日窗差实测 +1.22/+3.66/+2.58pp（08-04~08-08 三日样本·原预估 <2pp 略偏低）——vol_chg 为 8 成分滞后症状层之一、量纲为 ±15% 摆动，2-4pp 噪声不改成分符号方向，判成分级无质影响。

**落地**：gen_daily_report（F4 分母 SQL+快照注释+两处 ratio_th default）·risk_function（docstring 乙案记录+default 0.030）·calibrate_risk_factors（D["amt"] 查询+fallback+网格）·emotion_engine_v2（数据源注释+amt 查询）·risk_factors.json（f4.ratio_th/calibration/desc/source 四键+a6.source 注记刷新）·风险日报 build_risk_daily（8 处·liq/ipo/化合物层剂量档 0.014/0.030/0.063）·MANIFEST（裁定节+schema+黑名单摘帽）·GOTCHAS（ERR-20260728-005/007 状态行）。备份 `.bak_20260809_yuan`。零写库。

**留尾**：① index_research.db 的 2020+ 量能研究角色被 volume_trillion 取代（A6 走 399006.SZ 专口径·不动）；② emotion 成分换源后首轮引擎读数留意 vol_chg 分量。**依据** → brain/logs/2026-08-09-目标模式首验与量能回补收口.md · 风险日报决策记录 2026-08-09 同案条 · `Database/Market-Data/MANIFEST.md` 定源裁定节

## 2026-08-11 · 涨跌停 limit_list 取数链路「维持现状·零改动」＋日报「—」时点判法

**背景**：当日复盘日报「涨跌停家数比」显「—」，Doctor 指令「涨跌停每天从 tushare 拿，固化进行情库取数班与日报取数班」。CC 核全链路（两份 SKILL.md 全文＋`list_scheduled_tasks`＋DB 水位＋Tushare 源实测 76 行 U58/D1/Z17）。

**事实链**：
- Tushare `limit_list_d` **北京晚间才发布**当日数据 → 任何早于该时点的取数腿恒 0 行。
- **日报班 `zhuzhao-market-fetch-daily-report`（10:03 PT＝北京 01:03）＝当日唯一有效取数点**：步骤 3 明钉 `fetch_limit_list.py --from {该表max次日}`（自愈式补齐漏跑日），直写行情库 `limit_list_daily`（昨日 118 行 @10:06 PT 实证）。
- **句芒班 `market-data-daily-update`（北京 16:30）明文不碰**：SKILL 第 9 步「limit_list/theme_etf 由烛照九阴那班补写（非句芒表）」——结构性早于发布，加腿恒 0。
- **launchd `mac_daily_marketdata`（02:30 PT＝北京 17:30）** 有 limit_list 腿但同样早于发布、当日恒 0 行，仅作 7 天回看补网。

**裁定（Doctor 2026-08-11）**：**维持现状分工、零改动**——不加北京晚间（21:30）专拉班（Tushare 该时点必发未验证＋多一条 launchd 维护面），句芒班不加腿。**「—」判法：先问「班跑了没有／是不是手动提前重报」**——当日「—」根因即 08:27 PT 手动重报早于 10:03 PT 班，非链路缺陷。漏跑兜底非静默：`_health.json` overdue 亮红＋次日 launchd 回看补网。

**同日小修（/save 分拣 Doctor 勾 B）**：`gen_daily_report.py` meta description 与两处注释写死「07:00」系 cron 改 10:03 后的漂移（SKILL 5f「写死钟点不跟 cron 变」同类），三处「07:00」→「10:03」，备份 `.bak_20260811_meta`。

**依据** → logs/2026-08-11-烛照日报UI与涨跌停链路裁定.md · CC auto-memory `project_zhuzhao_limit_list`

## 2026-08-13 · 兑现闸门换代：open→closing 由「连续超额为正 Y=3」改为「累计超额 ≥2pp」（BT-21 回测背书）

**背景**：Doctor 质疑 Y=3「连续超额为正 3 日」闸门太严、建议考虑按超额绝对值计算。CC 扫参回测（61 条可回测信号 · 20 日窗 · 日频超额=主线 ETF 均值−510300 · 真启动基准=峰值超额≥5%）。

**结果矩阵（节选）**：Y=1/2/3 触发率 97–100%、精度 67–68%——**不是太严而是几乎不筛**，且误启动率 32%；Y=4 反向过严（召回 37%）；**累计超额 ≥2pp：精度 84%、召回 100%、误启动 16%、中位触发 2 天（原 6 天）**；≥3pp 精度 85%、触发率略低。

**裁定（Doctor 2026-08-13 批）**：换 `CUM_GATE = 0.02`。`closure_engine.py` 已改（Y_STREAK 标弃用留档）；dry-run 对比 61/102 条变动——11 条 dormant→open（旧闸门误启动消除：3 日连正但累计从未到 2pp 即崩）、3 条当前 open→closing（AMD Venice/AI推理CPU/x86 CPU，头几日已过 2pp）、date_realized 普遍提前；Doctor 终端 `--apply` 落库（备份 `recap.db.bak_20260813_closure`），沙箱只读核验通过。

**边界（BT-21 §四）**：单区制样本（2026-03~08 科技牛，41/61 达标 5% 偏高），**弱市表现未验**——遇弱市建议重跑本扫参；「峰值≥3%」基准下累计判据精度 100% 系同义反复（peak≥cum），不作证据。

**依据** → brain/logs/2026-08-13-手机卡终卡打磨与兑现闸门回测落地.md · 剑酒青丘回测报告 BT-21《2026-08-13_兑现_open-closing闸门扫参.md》

## 2026-08-13 · 手机卡视觉定稿：字号体系 / 字体栈 / GAP 三列式 / fit 缩放范式

**背景**：9:16 手机卡（`AI4ME/烛照九阴-outputs/手机卡_20260811.html`）经一天迭代定稿。以下范式供「每日一键出卡固化」时照此实现。

**字号体系**：全卡字号 ×1.15（「夏」season-glyph 118/92px 原大保留）；h2 19.5px/900（与 `<b>` bolder 实际解析值对齐）；灰字 .vintage 17px/700；正文 base 15px。**字体栈**：`--zh` 纯思源宋体三名（零 fallback）、body 700；石刻体（夏）不动。**GAP 条三列式**：左列 126px「行业/市场信息差」标签正下方逐行「消息触发」+5 档竖条（小鲍 info_gap_level 交叉印证，无触发留空）；中列信号正文+类别 pill（实色浅底）；受益标的列（A 股 ×2，藏蓝 #1B365D）；状态列统一「状态 · 剩N天」（兑现中橙色 #c25e14）。**fit 缩放**：按宽缩放（`s2=(clientWidth/1080)×zoom`，zoom=dpr 比值放行浏览器缩放）、外框随内容长高（不回缩）、body flex 居中；宽屏填满可下滑。

**红线（坑，详见 GOTCHAS ERR-20260813-001/002）**：壳内可见布局**禁 grid 全用 flex**（Safari 不吃）；容器改字重时核对 `<b>` bolder 漂移；改 2MB 大文件必验首尾字节 + 新旧片段 div 配平 + `test -s && cat` 落盘（空串切片与 0 字节截断两案已入 auto-memory）。

**依据** → brain/logs/2026-08-13-手机卡终卡打磨与兑现闸门回测落地.md
