---
title: 会话日志 2026-07-21 — 龙鱼看板 artifact 更新与 mcp_tools 白名单漏传坑
tags: [log, artifact, cowork, 龙鱼五力, update_artifact, mcp_tools]
created: 2026-07-21
updated: 2026-07-21
status: active
type: log
project: 龙鱼五力（持仓看板 artifact）
---

# 会话日志 — 2026-07-21

**项目**：龙鱼五力 — 持仓看板 artifact（longyu-holdings-board）
**主题**：用 pending HTML 更新看板 artifact；排查「更新不了/点了没反应」根因＝update_artifact 漏传 mcp_tools 白名单

---

## 完成的工作

- 挂载 `Database/龙鱼-标的分析库`，用 `_artifact_pending_longyu-holdings-board.html`（46KB·704 行完整版）整体替换 `longyu-holdings-board` artifact 的 HTML。
- 首次 `update_artifact` 只传了 `html_path`，未传 `mcp_tools` → 沿用旧白名单（仅前两个工具）。清单 `updatedAt` 从 02:07 推进到 02:35，确认文件层写入成功。
- Doctor 反馈「还是更新不了」→ 定位为「看到端」问题。经 AskUserQuestion 收敛为「点了没反应/报错」，再由 Doctor 直接给出报错原文：`Tool "mcp__longyu-holdings__get_cockpit_payload" is not in this artifact's mcp_tools allowlist`。
- 排查过程：先 `node --check` 抽出整段 JS（296–702 行）验语法 → ✅ 无语法错，排除全页失效于语法；再 `grep` 确认正文第 581 行调用 `get_cockpit_payload`（第二页驾驶舱），而旧白名单不含它。
- 二次 `update_artifact` 显式带全三工具白名单（`get_board_payload` / `save_holdings` / `get_cockpit_payload`），修复第二页取数被 allowlist 挡下的报错。Doctor 确认「好了」。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| `update_artifact` 换 HTML 时**必须显式传 `mcp_tools`** | 省略即静默沿用旧白名单；HTML 新增/变更工具调用会被 allowlist 挡下、且报错只在运行时点击才现 | 往后凡新 HTML 的工具集与旧版不同，一律带全 `mcp_tools` |
| 落盘日期用**设备本地 07-21**（非 session-env UTC 07-22） | 沿用同日既定口径；artifact 时间戳 UTC 02:35＝PDT 19:35·07-21 | 本日志系 07-21 第 10 篇 |

## 遗留问题 / 待办

- [ ] （Doctor 提示）`get_cockpit_payload` 若是本次新加进 connector 的工具，MCP 骨架不热更 → 需**重启一次 Claude Desktop** 才真正可用；重启后复验第二页驾驶舱。

## 相关笔记

- [[通用教训]]（拟落新 G-X：update_artifact 漏传 mcp_tools → 静默沿用旧白名单 → 新页工具被 allowlist 挡下）
- [[全局偏好-Settings镜像]]（本场起手已载）
