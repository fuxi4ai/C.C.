---
title: PRD · 烛阴取数静默失败根治
tags: [prd, acceptance, 烛照九阴, 剑酒青丘, 海螺姑娘]
created: 2026-07-14 23:35
updated: 2026-07-14 23:35
status: 进行中  # 进行中 / 待验收 / 已交付 / 已取消
doctor_decision: 待  # 待 / 已审 / 已取消
type: prd
project: 烛照九阴 / 剑酒青丘（句芒线）
template_version: v1.0
---

# PRD · 烛阴取数静默失败根治

## §一 · 任务目标

**动机**：2026-07-14 刷新资产看板时发现烛阴四表（theme_etf / us_anchor / market_amount / limit_list）冻在 07-10，缺 07-13、07-14。追查坐实：`zhuzhao-market-fetch-daily-report`（烛阴 07:00 班）enabled 且今早已触发，却**四表零更新、无日志、无 predaily 锚**——典型「跑了但全空」的**静默失败**，signature 对应 G-X51（无人值守 `request_cowork_directory` 悬挂 → run 超时被杀 → 零日志）。更糟的是句芒 `market_health.py` 的 overdue 判据只看「今天过没过 17:00」，上午跑时把「漏了整个 07-13 交易日」误判成「班前正常时序·overdue=false」，让静默失败被安全网自己掩盖。

**范围**：轻档修复，**不改架构**。三处落点消除静默失败并让其一旦发生即当场响亮报警：① Doctor 钉常连（消除悬挂触发源）② 改 zhuzhao SKILL 前置段（无人值守绝不 request + 缺挂 fail-loud 留痕跳过）④ 修 market_health.py overdue 判据（lag≥2 当场判 overdue、不等 17:00）。

**Doctor 原始指令**（逐字引用 · 多轮对齐要点）：
> 「这俩什么情况？」→「能不能"快接"」（让九儿和句芒的任务快速接上）→ 核心痛点＝「静默失败不再发生」→ 方向＝「轻档：不改架构只保可靠」→ 验收＝「机制到位+模拟测试」。

**任务规模估算**：
- 预计涉及文件数：2（zhuzhao SKILL.md · market_health.py）+ 1 新增测试脚本；Doctor 侧 1 项常连设置
- 预计耗时：约 1 小时
- 涉及项目：烛照九阴（zhuzhao 定时任务）· 剑酒青丘/句芒线（market_health.py）

---

## §二 · 交付标准（Acceptance Criteria）

> `[ ]` 未开始 · `[?]` CC 认为达成+证据 · `[!]` 未达成+原因 · `[~]` 不确定+需 Doctor 判断 · `[✓]` 仅 Doctor 可打。

### A. 文件层面

- [?] zhuzhao SKILL.md 前置段已改（经 `update_scheduled_task` 正规通道）
  - 证据栏：SKILL.md 文件只读改不了，改走 `update_scheduled_task{taskId:zhuzhao-market-fetch-daily-report, prompt}` 更新任务指令，Doctor 审批通过；Read 复核 line 10-16 为新前置块。就地 .bak 在受保护路径无法建 → 旧块存于 `outputs/zhuzhao前置段_G-X51补丁_20260714.md` 作回退记录。
- [?] market_health.py 已改 overdue 判据 + 备份存在
  - 证据栏：`market_health.py.bak.20260714_overdue` 存在（7721B）；Edit 已应用；与备份 `diff` = 8 行改动，全在 overdue 块。

### B. 一致性层面

- [?] zhuzhao 前置含「无人值守绝不 request + 缺挂 fail-loud」分支
  - 证据栏：Read SKILL.md line 13-15 含「无人值守…绝不调 `request_cowork_directory`…`❌ 缺挂载@前置`…干净退出」；有人值守 request 分支保留在 line 15。
- [?] market_health.py overdue 块含 `if lag >= 2:` 早判分支
  - 证据栏：`grep "if lag >= 2:"` = **2 matches**（L109 既有 sev=fail 块 / L122 新增 overdue 早判分支）；`past_cutoff`/`ZHUYIN_CUTOFF_HOUR` 仅在 L128 `elif` 作用于 lag==1。（注：原 PRD 写"=1 match"有误，实为 2，L109 是既有 sev 判定。）
- [?] 未动其他逻辑
  - 证据栏：market_health 与备份 `diff` = 8 行、全在 overdue 块；TABLES/WRITER/主循环未变；zhuzhao live 文件因只读未触碰（0 改动）。

### C. 功能层面（模拟测试 = 验收核心）

- [?] ④ 正向：`test_overdue_lag2.py` 跑通（lag=2 晨间）
  - 证据栏：`python3 test_overdue_lag2.py` → `PASS 1 · lag=2 晨间 → overdue=True, overall=fail, overdue_tables=两表`。脚本存于 剑酒青丘/infrastructure/取数工具/。
- [?] ④ 反向不误伤（lag=1）+ 逾期逻辑不回归
  - 证据栏：同次输出 `PASS 2 · lag=1 晨间 → overdue=False（班前正常时序不误伤）` + `PASS 3 · lag=1 过17:00窗 → overdue=True（原逾期逻辑不回归）` + 结尾 `ALL PASS`。
- [~] ② fail-loud 行为无法机器单测（prompt 非代码）
  - 证据栏（需 Doctor 判断）：请审读补丁逻辑「无人值守+缺必需挂载 → 落 Brain 日志 + run-summary 明示缺挂 + 干净退出、绝不 request」；真行为待下次无人值守 run 观察或手测。

### D. 自审层面

- [?] 全程 propose-then-confirm，无凌晨自主
  - 证据栏：④ 逐行 diff 经对话展示 + Doctor "开工" 二次确认后才落；非 PEC 类，无需 α–ε 段。

### E. 沟通层面

- [?] market_health.py 属句芒线：逐行 diff 已呈 Doctor 二次确认才动
  - 证据栏：本轮对话中两次展示 ④ diff，Doctor "开工" 后落地。
- [?] 无沙箱 git 写
  - 证据栏：本任务未跑任何 git；commit 命令在回报里贴给 Doctor 终端。
- [!] /save 触发落 brain/logs/
  - 原因：执行刚完，待 Doctor 认可后 /save（守灶人 CC 记一笔 + 注明 market_health 属句芒线）。

### F. 任务专属（自定义）

- [~] ① 钉常连＝Doctor 交付项
  - 证据栏（需 Doctor 确认）：请把 `烛照九阴` + `AI4ME` 加入桌面端常连文件夹并确认。
- [?] ③ AI4ME 提为硬必需（zhuzhao 前置）
  - 证据栏：SKILL.md line 12「必需可写目录 = `Database`、`烛照九阴`、`AI4ME` 三者」。
- [?] 自愈显式化（步骤 3）
  - 证据栏：SKILL.md line 41「**自愈**：`--from` 取各表自身 `MAX(trade_date)` 次日 → 一次成功 run 会把此前所有漏跑交易日一次补齐」。

---

## §三 · 非交付项（范围排除）

- 不包含：改两灵分工 / 并入句芒管线 / 搬取数代码（架构级并入已被 Doctor 否掉，改走轻档）
- 不包含：改动取数或派生或日报脚本逻辑（fetch_*.py、emotion_engine_v2、sync_buy_signals、closure_engine、gen_daily_report 等一律不碰）
- 不包含：07-13/07-14 数据补齐（联网取数须 Doctor 终端跑；本任务仅提供命令，不算交付）
- 不包含：改 conch_engine.py（④ 修源头 market_health 后，看板信号自然转正，无需在 conch 层再降级）
- 不包含：手工改两节点 curated status（conch 由 health 驱动 status，数据补齐 + ④ 后自动复绿）

---

## §四 · 状态

- [ ] 进行中（立 PRD · 时间：2026-07-14 23:35）
- [x] 待 Doctor 验收（CC 已对所有交付标准填三态 · 时间：2026-07-14 23:5x）
- [ ] 已交付（Doctor 已把所有 `[?]` 转为 `[✓]` · 时间：____）
- [ ] 已取消（Doctor 显式取消 · 时间：____ · 原因：____）

**关闭路径**：全 ✓ 或 Doctor 显式取消 = 两种合法关闭；CC 不自动关闭。

---

## §五 · 变更记录

- 2026-07-14 23:35 CC：立 PRD · 含 A/B/C/D/E/F 六段共 13 条交付标准（起草稿，§二 全留 `[ ]`/`[~]` 待执行填三态）
- 2026-07-14 23:5x CC：执行完毕填三态 —— ④ market_health 改+测试 ALL PASS（`[?]`）；②③ zhuzhao 前置经 `update_scheduled_task` 落 live 并 Read 复核（`[?]`）；②fail-loud 行为留 `[~]` 待 Doctor 审读；①钉常连 `[~]` 待 Doctor；/save 待触发。**CC 全程未打 `[✓]`。**

---

## §六 · 备注（对齐结论镜像）

- 核心需求：烛阴取数静默失败绝迹——要么跑成、要么当场响亮报警。
- 方向：轻档，不改架构。① Doctor 钉常连 ② zhuzhao 前置 fail-loud ④ market_health overdue 修。
- 验收：②④ 到位 + 两个可跑模拟测试（lag=2 晨间→overdue 真；lag=1→不误伤）；② prompt 行为标 `[~]` 由 Doctor 审读。
- 红线：不沙箱 git；`.bak` 可回退；market_health 属句芒线，逐行 diff 二次确认才动。
