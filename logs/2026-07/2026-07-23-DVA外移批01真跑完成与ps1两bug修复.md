---
title: 会话日志 2026-07-23 — DVA 抖音外移批01真跑完成 + ps1 两个终审 bug 修复
tags: [log, DVA, 抖音, 异地存储, fuxi-station, 跨AI协作, VV, 编码坑, 幂等]
created: 2026-07-23
updated: 2026-07-23
status: active
type: log
project: DVA
---

# 会话日志 — 2026-07-23（DVA 外移批01真跑完成）

**项目**：DVA（抖音视频链路）→ 外移到 fuxi-station（`F:\DVA_Vault\Douyin`）
**主题**：VV 六轮硬化后，批01（438 文件 / 20GB）**第一件真实外移**跑通；过程中两阶段 fail-closed 四次兜住、揪出并修好两个终审漏网 bug；批01 闭环（receipt 发布、源未删）。CC↔VV 由 Doctor 转述。
**承接**：[[2026-07-23-DVA外移-VV六轮安全硬化]]（VV 终审）· 本会话另半程见 [[2026-07-23-宝丰布伦特科创50地缘曲线与PEC市场传导]]

---

## 完成的工作

- **部署 + 跨机 SHA-256 比对**：`dva_remote_commit.ps1` scp 到 `F:\DVA_Vault\_tools\`，远端 `Get-FileHash` 逐字符核对（首版 `ee25d6f2…`）。
- **批01 dry-run PREFLIGHT 复核**（CC 任 VV 复核角色）：DRY_RUN/local_delete=false、dup/collision/missing 三 0、438 文件、20.00GB、F: 余量水位 OK、两阶段+永不删源结构完整。
- **批01 `--execute` 真跑**：两阶段提交（远端逐条 Commit → FinalizeSession+StatusSession 全量核验 → 才写本地 pointer/DB）。**四次 fail-closed 兜住**，最终 438/438 远端 SHA-256 核过、闭环。
- **修复①（首次外移 bootstrap）**：远端 REFUSED「storage root missing」——`F:\DVA_Vault\Douyin` 从没建过（ps1 刻意不自建根·VV6 收紧）。带外手动 `New-Item` 建根后 Prepare 过。
- **修复②（ps1 第111行·rel_path `..` 误杀）**：省略号标题（`答应....`）被 `(\.\.)` 子串规则误判穿越。全量 1556 审计：.py `rel_path_issue` 段级判 `..` 接受全部、ps1 多了冗余子串。**删 ps1 第111行 `|(\.\.)`**（段级判#118已覆盖），两端对齐 → 8 个误杀清零、批01+批02 一并解。SHA→`056896e0…`。
- **修复③（Phase 3 DB 缺行 88）**：麦橘MERJIC 86 + AI个体指南 2 是带外导入、从没 seed 进 `dy_downloader.db`，`mark_db_offsite` UPDATE 缺行 → final_scrub 报不符、receipt 不发布（远端已定案·源未删）。写 `tools/seed_offsite_missing_rows.py`（按 `<video>_data.json` sidecar 补富行 87 + 最小行 1，offsite 留 0，先备份整库、INSERT OR IGNORE）。
- **修复④（ps1 第78行·`Set-OrVerifyText` 编码坑）**：`--resume` InstallPlan 报「内容不同」，但本地/远端 `_plan.json` **diff 逐字节相同**。根因：写入侧 `$Utf8NoBom`（UTF-8 无 BOM），校验侧 `Get-Content -Raw` **漏 `-Encoding UTF8`** → PS5.1 按 GBK 误解满屏中文 → 永远判「不同」。第一次全走「创建」分支掩盖了它，resume 走「校验」分支才炸。**第78行补 `-Encoding UTF8`**。SHA→`23e5ce71…`。
- **批01 闭环**：`--resume` 补齐 Phase 3 → 最终 receipt 发布：`file_count=438 · total_bytes=21472975389 · manifest_sha256=0FE6E607… · pointers_verified=true · db_verified=true · source_deleted=false`。DB 批01 **438/438 offsite=1**。
- ps1 SHA-256 演进链：`ee25d6f2`(--list-batches 部署) → `056896e0`(修②) → `23e5ce71`(修④)，每步重部署+跨机核对。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 三独立授权层（部署 / 真实视频 smoke / 批 execute）逐层放行 | 66GB 不可逆搬迁；批准某层≠扩展到下层 | 直跑批01（跳过单文件 smoke·Doctor 拍）；删源仍未授权 |
| ps1 两 bug 走 VV/Doctor 批准→改→commit→scp 重部署→重算 SHA→重跑 | 动终审 hash 锁定件 | 与「`..`」同流程；两 fix 已 push+部署 |
| DB 缺行选「seed 补行」（选项1）非改工具/接受 | 补齐 DB 让 receipt 可发布；不臆造字段 | 富行取自 sidecar `_data.json`；offsite 留 0 交工具标 uri（保证与 committed plan 一致） |
| `..` 修法用「删冗余子串」非「收紧正则」 | .py 已段级判 `..`、ps1 冗余；删了两端逐字对齐 | 比加复杂度更干净；全量审计证 ps1==py |

## 遗留问题 / 待办

- [ ] **批02–04** 未跑（另起 session·同流程·两 fix 已生效）。批02 含 SansanYe×5/浪浪 等，可能也有 DB 缺行→用 seed 工具补。
- [ ] **删源**：`source_deleted:false` 是该有的样子——按 PRD 须先抽样恢复演练（小/大/中文/深层各1）+ 逐批 Doctor 批准，才谈删。
- [ ] **两 bug 告知 VV**：`..` 过宽 + `Set-OrVerifyText` UTF-8——VV 六轮 review 照不见，因「带中文的 resume/幂等复验」在第一次真跑前从没被走到。
- [ ] 孤儿清理：远端 `.incoming/{034500Z,035500Z}/_plan.json.uploading.*` 临时、旧 session 目录、`dy_downloader.db.bak-*`。

## 相关笔记

- [[2026-07-23-DVA外移-VV六轮安全硬化]]（VV 终审前置）
- [[2026-07-23_抖音视频外移fuxi-station_PRD]]（checkpoints）
- [[DVA/GOTCHAS]]（INFRA-20260723-001/002 编码族 · 本次两 bug 同族）
- [[通用教训]] G-X83（git add -A 先探后加·本会话立）
