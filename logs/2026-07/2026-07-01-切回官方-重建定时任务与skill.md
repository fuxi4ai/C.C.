---
title: 会话日志 2026-07-01 — 切回官方工作区·重建定时任务与 skill 包
tags: [log, 跨项目基建, 定时任务, skill, 切环境, 方案B, Cowork]
created: 2026-07-01
updated: 2026-07-01
status: active
type: log
project: 跨项目基建
---

# 会话日志 — 2026-07-01

**项目**：跨项目基建（切环境善后）
**主题**：Anthropic 官方订阅恢复，切回官方桌面工作区——搁置方案 B、重建 10 定时任务、重打并重装 6 个 brain skill

---

## 完成的工作

- **方案 B（投知君君移交 Codex）搁置**：订阅恢复后迁移动因消失。更新 `permanent/跨AI协作握手层.md`（顶部加 🅿️ 搁置横幅 + 迁移清单状态标注）、`permanent/定时任务清单.md`（touzhijunjun 行补搁置备注）。地基/schema/consumer skill 全保留封存，随时可复起；投知君君继续 Claude 侧运行、不关停。
- **重建 10 个定时任务**：当前官方环境 `list_scheduled_tasks`=0（印证「切环境注册表不带过来」），照 Doctor 终端 `cat` 出的各 `SKILL.md` 全文，逐个 `create_scheduled_task` 重建 9 定时 + repair-finance-chain（手动/按钮）。cron 逐条 `list` 核对无误。`handshake-consumer-daily`（方案 B 搁置）与 `_archived/dva-update-all-reminder`（废弃）**不重建**。
- **重打 + 重装 6 个 brain skill**：`brain/.skills/` 里旧 `.skill` 包已过时（无引号 frontmatter + 缺新内容），直接装会触发 `frontmatter missing name or description`。从现源用 python zipfile 重打 6 包（排除探针垃圾文件、frontmatter 引号合规），Doctor 经 Settings 手动装到位（brain-anchors/resume/save/note/prd/consolidate 已上线）。并用新包**覆盖刷新 `brain/.skills/` 镜像**。
- **补记两份清单**：`定时任务清单.md`、`已装skill清单.md` 均补记 2026-07-01 切回官方的重建/装机始末 + 复用提醒。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 方案 B 移交 Codex 搁置而非废弃 | 订阅恢复、迁移动因消失，但地基验证已通过、成本低 | 保留封存，复起条件=再遇额度/成本压力 |
| 搁置期不重建 handshake-consumer-daily | 全新环境不重建即等于暂停，通道无数据也不空转 | 达成「暂停」诉求，零额外操作 |
| repair-finance-chain 以 ad-hoc（无 cron）注册 | 它本是看板按钮触发，非定时 | `create_scheduled_task` 省略 cron/fireAt → Manual only |
| brain skill 从现源重打包、不发旧镜像 | 旧 `.skill` frontmatter 无引号（违 rule 4）+ 内容过时 | 保证可装 + 最新；镜像同步刷新防再漂移 |

## 遗留问题 / 待办

- [x] **侧栏 Artifacts 重建（4 个）** — 已完成，见下「追记」。（更正：第 4 个是 `yuantu-starry-skies` 渊图星河，非星图；星图 06-24 已退役并入 GAI。）
- [ ] **网络白名单**：官方沙箱默认拒绝+逐域名加白（非 gateway 开放出网），取数任务（Tushare/SMM/DeepSeek/stockanalysis/Yahoo/抖音）首跑前确认放行（G-X44）。
- [ ] **Run now 预热**：对一个取数任务点 Run now，预批 bash/web_fetch/request_cowork_directory/update_artifact 工具权限，免定时跑卡权限弹窗。
- [ ] **git 提交**：本次改动（2 permanent 笔记 + 2 清单 + 6 刷新的 .skill 镜像 + 1 日志）命令见下，待 Doctor 终端跑。

## 追记（同日 · Artifacts 复原）

- **4 个 artifact 全复原**（`list_artifacts` 核对就位）：`global-asset-inventory`、`zhuzhao-jiuyin-daily`、`baize-weekly-dashboard`、`yuantu-starry-skies`。**注册的是抢救出的原本体**（非重跑数据），保真度最高，含 2.8M 三维星河。
- **认知更正**：先前把第 4 个误当作 Brain Vault Dashboard 星图。查海螺 `BVD退役盘点单` 确认星图 **2026-06-24 已退役、并入 GAI**，不在这 4 个里、不重建。真正第 4 个是 `yuantu-starry-skies`（渊图知识星河，Three.js 暗色，无任务刷新）。
- **新坑（已固化进 [[切环境复原清单]] §3）**：切环境后 `list_artifacts`=0 但 `create_artifact` 报「folder already exists」——Documents 下 `Claude/Artifacts/{id}/` **孤儿文件夹**残留（Library manifest 清了、Documents 本体没清），且该目录**受保护、挂不了**。解法：Doctor 终端把孤儿里的 `index.html` 抢救到已挂载处 → CC 读到原本体（连带解决星河没有其他源的问题）→ Doctor `rm -rf` 清孤儿 → CC 逐个 `create_artifact` 重注册。`update_artifact` 在 manifest 空时 not found、不可用。
- 复原路线由 Doctor 拍板走「路 A·重新生成」，实操中发现抢救原本体比重跑脚本更优，遂采之。

## 追记（同日 · Vault skill 归置 + 两清单合并）

- **taste-skills 来路查清**：`Vault/archived/taste-skills/` 全部来自 GitHub `github.com/Leonxlnx/taste-skill`（第三方"品味"合集，2026-05-10 整包导入）；旁边 `emil/`←emilkowalski/skill、`system-skills/`←Anthropic。Doctor 眼生的几个是同包"备用档"，一直没用。查证据在 `Vault/SKILLS_MANIFEST.md` 末「来源」。
- **Codex/图生线分出**：10 个 taste-skills 里 `image-to-code`（明写 for Codex + CODEX-SPECIFIC 段）+ `imagegen-frontend-web/mobile` + `brandkit` 依赖 agent 原生图像生成（GPT/Codex 能力），Claude Cowork 无此工具、装了空转 → 移到 `Vault/Codex/`。另把 `Vault/taste-skills/` 里的 `gpt-taste`（GPT/Codex 严格变体）也归入 `Vault/Codex/`。共 5 个。`taste-skills/` 只留 Claude 可用的纯设计指导。
- **两份 skill 清单合并（以 brain 为主）**：brain `permanent/已装skill清单.md` 重写为**唯一真源**（真源镜像+重装手册合一，吸收 Vault 的备份路径/来源仓/Tier 分层/待装/加载约定/重装 SOP）；`Vault/SKILLS_MANIFEST.md` 降为指针；`SESSION_STARTER.md`、`Vault/README.md` 交叉引用改指 brain。
- **文件删除靠 `allow_cowork_file_delete`**：沙箱 rm 挂载盘报 `Operation not permitted`，调该工具对目标文件夹授权后即可 rm（taste-skills、Vault 各授权一次）。
- **Doctor 更正（当下便签）**：frontmatter 无引号报错**只在 gateway**，官方桌面版能正常装——我之前把 rule 4 当通用规则说过头了，已在 `已装skill清单` G/I 栏更正。

## 追记（同日 · Run now 实测 · ingest 任务修复 + 白名单实证）

- **网络白名单实测（curl 交互沙箱）**：官方沙箱 `github.com`→200、取数域名（tushare/waditu/smm/deepseek/yahoo/stockanalysis/eastmoney）+ `example.org` **全 000**。证实官方＝默认拒绝+白名单、取数域名当前未放行。补白名单法＝对取数任务 Run now 逐个批域名。落 G-X44 进展。
- **`recap-kejian-daily-ingest` 修复（方案 A）+ Run now 验证通过**：该任务原缺「前置：挂载」块（其他任务都有、唯它漏）→ 沙箱只挂 Brain、够不到 recap.db，首跑九儿只能提炼+staged+交回（铁律守住、没假装）。补①前置 `request_cowork_directory` 挂 Database+烛照九阴 ②zhuzhao 的 /tmp recap.db 往返写 ③库不可达则 staged 兜底。二次 Run now：挂载成功、入库 36 条/7 表、放回 integrity ok、只增不减核过。
- **顺带实证**：scheduled/Run-now 运行里 `request_cowork_directory` **能自动挂载成功**——推论所有带挂载块的任务在官方环境"挂载"这关天然过（取数任务另受白名单限）。落 G-X45 进展。

## 追记（同日末段 · 排期调整 / 时区 / 中立化 / 挂载策略 / 数据目录调研）

- **定时任务全线移美西本地午后**：Doctor 在 UI 把日更簇由晨间改到午后（market14:45/ingest15:00/review15:30/zhuzhao16:00/refresh17:30，baize 周日01:00，touzhijunjun 周三六17:00 我改）。**关键：cron 走机器本地时区（美西 PDT），数据逻辑走 UTC+8 交易日历**——美西午后正落在目标时区清晨窗口。依赖链核过仍成立。清单同步。
- **星期漂移待核**：weekday 任务 `* * 1-5`（美西周一~五）→ 落目标时区周二~周六（目标周一清晨无专班）。标待核。
- **prompt 中立语境化**：去掉"盘前/06:xx/静默时段/早安"等钟点措辞（Doctor 在 UI 改 6 处 find→replace）。**且不在自动化文案里显式标注地域**（Doctor 明示：账号安全，避免被判地域触发再封号）。
- **"不能整挂 Documents"结论**：`~/Documents` 含应用级受保护目录 `Claude/Scheduled`+`Claude/Artifacts`，挂载系统拒绝盖住受保护位置的父目录，故结构性封死。官方文档查证：**数据目录不可配置、无搬家开关**（Scheduled 存 `~/Documents/Claude/Scheduled/`、artifacts 同级，均应用写死）。手动搬/软链均不推荐（破坏应用 + sandbox 不跟随跨界软链）。迁移到 C.C. 的工程量实测 300+ 文件、1500+ 处硬编码路径，投入产出严重倒挂，不做。
- **挂载策略定案**：默认只挂 Brain（它是"地图"，含清单/anchors），其余 CC **开工前预判 + 批量请求**；替代方案＝一次挂 5 个宽目录（Brain/Projects/Database/AI4ME/Vault，`Claude/Projects` 一条覆盖全部项目）。

## 相关笔记

- [[切环境复原清单]]（本次新立 + 补 Artifacts 实战复原法）
- [[已装skill清单]]（合并为唯一真源 · 含 taste pack 来源 + Codex 分离）
- [[定时任务清单]]（全线移午后 + 时区/中立语境说明 + 星期漂移待核）
- [[通用教训]] G-X44（官方取数白名单实测）· G-X45（scheduled 挂载可行 + ingest /tmp 写库）
- [[跨AI协作握手层]]（方案 B 主笔记，已标搁置）
- [[定时任务清单]]（10 任务真源镜像，已补切回官方重建）
- [[已装skill清单]]（skill 真源镜像，已补官方装机进度）
- [[通用教训]] G-X44（gateway 开放出网 vs 官方白名单）· G-X48（挂载盘 zip/删除限制·python zipfile 直写）
- [[Doctor协作偏好]]（propose-then-confirm、CC 不沙箱跑 git 写）
