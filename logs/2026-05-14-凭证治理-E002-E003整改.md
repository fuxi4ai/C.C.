---
title: 凭证治理 · E-002 E-003 整改落地
tags: [credentials, security, dva, refactor]
created: 2026-05-14
session_type: maintenance
project: DVA
updated: 2026-05-14
status: active
type: log
---

# 凭证治理 · E-002 / E-003 整改落地

## 做了什么

### E-002（DashScope 硬编码）· 方案 C 执行完毕
- `DVA/dyd/dva_asr.py:54` 删除 `os.getenv("DASHSCOPE_KEY", "<完整key>")` 的 default 参数
- `DVA/dyd/config.yml:34` `api_key` 清空，`api_key_env` 填为 `"DASHSCOPE_KEY"`
- 凭证清单 #4 存放位置从旧的问题描述更新为 `DVA/dyd/.env.dva`

### E-003（TOS AK/SK 硬编码）· 审计新发现，同步处理
- `DVA/dyd/dva_asr.py:47-48` 删除 `TOS_AK` / `TOS_SK` 的 hardcoded fallback（含 base64 编码的 SK）
- 凭证清单新增第 5 条 · 火山引擎 TOS
- E-003 事件登记完成

### 配套设施
- 新建 `DVA/dyd/.env.dva.example`：7 个变量的占位模版，使用者 cp + source 即可
- 凭证清单 E-003 事件段落已追加

## 决策记录
| 决策 | 方案 | 理由 |
|------|------|------|
| E-002 DashScope 轮换？ | ❌ 不轮换（C方案） | DVA 非 git 仓库，key 未到 remote；transcript disabled；仅对话暴露（ephemeral） |
| E-003 TOS AK/SK 轮换？ | ❌ 不轮换（同 C方案） | 同上；dragonpalace 仅存临时音频；损失上限低 |

## 遗留

- Doctor 需手动创建 `DVA/dyd/.env.dva`，把真实 AK/SK 和 DASHSCOPE_KEY 填进去：
  ```bash
  cp DVA/dyd/.env.dva.example DVA/dyd/.env.dva
  # 用编辑器填入真实值
  source DVA/dyd/.env.dva   # 每次用 dva_asr.py 前 source 一次
  ```
- 下次 brain commit 需包含本次修改（dva_asr.py / config.yml / .env.dva.example 不在 brain git 里，只需 commit brain 侧的清单和 log）
