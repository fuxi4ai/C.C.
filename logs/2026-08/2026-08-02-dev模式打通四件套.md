---
title: 会话日志 2026-08-02 — dev模式打通四件套（brain/skills/三灵/定时任务镜像 → ~/.claude）
tags: [log, 数灵转移, portable, claude-code]
created: 2026-08-02
updated: 2026-08-02
status: active
type: log
project: 数灵转移
---

# 会话日志 — 2026-08-02（第二场 · 沙箱 08:4x–09:5x PDT）

**项目**：数灵转移（续 2026-06-02/03 那批决策）
**主题**：Doctor 最高优先级命题「打通官方订阅模式与第三方模式的壁垒」→ 盘四层载体 → 定位 dev 模式目录 → A–D 四件套落地（commit `782b404`）→ E 验收挂 TODO

> 承上：同日 `logs/2026-08-02-定时任务巡检机制.md`（昨夜场）。本场从 `/resume` 起手，交接摘要里两条判断当场被本场实测推翻（见「我栽的」）。

---

## 一 · 命题与收窄

Doctor 原话要点：以后用第三方（开发者）模式接 Kimi/DeepSeek 时，**CC 依然存在**——brain、logs、系统、项目、远程 git，含记忆/协作/定时任务/skills/操作说明全套。

两轮 AskUserQuestion 收窄：
- 载体＝**Claude 客户端的第三方开发者模式**（CC Switch 只是切模型的辅助，不等同）；
- 原则＝**能打通打通，打不通做双备份**；
- 双轨＝**官方 Cowork 为主、第三方为备**；
- skills 安装＝**symlink**；
- 第三方侧旧文件**全部可清可归档可覆盖**（Doctor 明示）。

## 二 · 盘出来的现状（全部实测）

**CC 的「存在」分四层**：L1 数据（brain 81M/991 md/12 git 仓）已通；L2 指令（CLAUDE.md+偏好+6 skill）可通；L3 执行（20 班+MCP）部分通；**L4 判断（两问纪律、自我推翻）搬不了，只能实测**——E 验收因此存在。

**dev 模式解剖**（Doctor 终端探针 ×2）：
- 载体＝Claude Code CLI 2.1.181，配置根 `~/.claude/`，宿主壳「Clawd on Desk.app」（hooks 指向它，PermissionRequest 走 `127.0.0.1:23333`）。
- `~/.claude/CLAUDE.md` 仅 226 字节（一行 graphify 指针）——**dev 模式的模型开局不知道 brain、不知道敬语、不知道任何 G-X**。这就是「壁垒」的实体。
- `~/.claude/agents/` 已有三灵薄壳（6-02 版，数灵转移 D1 的产物）；`~/.claude/skills/` 已有 4 个（dream 已是 symlink→cc-switch，**symlink 有先例**）。
- **三灵壳的过期路径只在 `~/.claude` 旧拷贝里**：brain 侧真身 `agents/{灵}/{灵}.agent.md` 早已是修正版（引擎 `Projects/Financial/白泽观星/engine/`、`Market-Data` 大小写全对）⇒ C 项从「重写三份」收窄为「symlink 指真身」，零编辑。

**workspace 双树（重要副产，F 处置未批、只定性）**：`/Users/lunarabbit/Claude's workspace/`（Cowork live 根）下 Brain/BRAIN_VAULT/Env/Infrastructure/Projects **mtime 全部定格 2026-07-05 10:46 同一分钟**＝一次性拷贝事件，非持续写入。live 方向**按资产劈开、恰好相反**：Brain/Projects live 在 `~/Documents/Claude`（git 活跃），Scheduled live 在 workspace（今晨 01:20 还在写）。07-31 的「Scheduled 双树」是这个几何的一半。风险定性：中——旧副本不再被写，但可能被未来会话静默误读。

**大小写地雷**：`Brain`/`brain` 同 inode 34（macOS 大小写不敏感救着）；磁盘真名大写、全套文档写小写。**将来任何 Linux/Docker 环境此雷即炸**，本轮未处理、记档。

## 三 · 落地（A–D，commit `782b404` 已推 `fuxi4ai/C.C..git`）

| 件 | 内容 | 验证 |
|---|---|---|
| A bootstrap | `brain/portable/claude-code/CLAUDE.md` → cp 到 `~/.claude/CLAUDE.md`（**copy 不 symlink**：Claude Code 可能整文件重写，断 symlink 是静默的；copy 漂移风险低——内容只是指针） | head -3 确认 |
| B skills | 6 个 brain-* 从 Cowork 缓存导出 `brain/portable/skills/`，**diff 6/6 逐字一致**（共 73,496 B），`ln -sfn` ×6 | ls -la 全 symlink |
| C 三灵 | `ln -sfn` ×3 指 brain 真身 `{灵}.agent.md`；旧壳挪 `~/.claude/_archived_20260802/`（只挪不删） | ls -la 全 symlink |
| D 镜像 | rsync live Scheduled → `brain/references/scheduled-live-mirror/live/`（21 目录含 .bak 全收），git 跟踪 ⇒ 班的 prompt 变更一条 diff 可见 | 39 files/2594 行入库 |

配套：`portable/README.md`（单向流向+更新纪律：Cowork 账号改 skill **必须重导出**，否则 dev 侧静默用旧版，G-X118 同族）、`scheduled-live-mirror/README.md`（⚠ rsync 方向不可反）。

## 四 · 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 双轨：官方主、第三方备 | 可逆、不打断 20 个班 | 单向同步纪律写进 bootstrap 与 README |
| bootstrap 用 copy、skills/agents 用 symlink | 断 symlink 静默 vs 单源防漂，按文件被重写概率分开选 | 两种安装方式并存，README 记明理由 |
| C 不重写、直接 symlink brain 真身 | 实测 brain 侧 `{灵}.agent.md` 已是新版——**过期认知在我，不在文件** | 清单 12 项砍 3 项 |
| dev 模式 git 边界与沙箱不同 | 沙箱禁 git 是 FUSE/index.lock 问题，本地无此病；但 commit/push 仍交 Doctor（ERR-20260718-002 并发教训） | 写进 bootstrap「别搬错场景」 |
| F（workspace 旧副本标死）不动 | Doctor 未批该方向 | 仅镜像 README 一句「不获豁免」+ 本日志定性 |

## 五 · 我栽的 / 被推翻的（同族：凭推断给收敛结论）

1. **「周班从未跑过、今晚 20:00 前必须预批」——错**。`list_scheduled_tasks` 的 `lastRunAt`＝今晨 01:24（昨夜手动 Run now 跑通）。我把「快照是手动会话写的」推成「周班没跑过」，一条字段就推翻。G-X119 活例。
2. **交接摘要说 Step 0.6 绿灯是假的——对了一半**：快照确实是手动跑的产物，但「周班停摆」的引申不成立。**新鲜度判据区分不了「谁写的」**，这是巡检机制的真实盲区（候选注记，待 Doctor 裁）。
3. **控件连挂**：AskUserQuestion 两次 `stream closed` 报错，Doctor 说 re-ask 才成。教训：控件挂时正文要先把选项讲全，让 Doctor 可直接打字批（本场第三次问前已照做）。
4. **自己造的垃圾**：`Brain/.__case_probe` 探针文件沙箱删不掉；`.tts-scratch` 今晨 mp3 同样删不掉（现存 7 个）——「播完即删」的执行侧权限问题依旧在（EXP-20260731-004-P 未愈）。均需 Doctor 终端清。

## 六 · 副产发现

- **`handshake-consumer-daily` 幽灵**：live 磁盘树有目录，`list_scheduled_tasks` 19 班里没有——盘上有/清单无，与「第三个 launchd job」同族反向。已挂 TODO。
- Settings 镜像 L20「请重贴 Settings」提醒已过期（G-X119 条已在注入里），可撤。
- `brain/portable/` 自此成为 brain 的第五类顶层资产（导出层），与 permanent/references 并列。

## 遗留问题 / 待办

> 主待办已进 `brain/TODO.md`（E 验收 ★ + handshake 幽灵），此处只记未进 TODO 的。

- [ ] Doctor 终端清两处沙箱删不掉的垃圾：`rm -f ~/Documents/Claude/Brain/.__case_probe` · `.tts-scratch` 下 7 个 mp3
- [ ] workspace 7-05 旧副本（Brain/Projects 等）处置——F 方向未批，先留定性
- [ ] `Brain`/`brain` 大小写归一——Linux 化前必做，本轮记档不动
- [ ] 镜像常态化刷新机制（并进周巡检班 vs 手动随 /save）——涉及巡检器只读章程，需 Doctor 裁
- [ ] Settings 镜像 L20 过期提醒撤除（1 行改动，待批）

## 相关笔记

- [[数灵转移/architecture/决策记录]] D1–D10（本场直接续其 D1）
- `brain/portable/README.md` · `brain/portable/claude-code/CLAUDE.md` · `brain/references/scheduled-live-mirror/README.md`
- [[通用教训]] G-X119（两问）· G-X118（改动落在不被消费处）· ERR-20260718-002（并发覆盖）
- 上游：logs/2026-08-02-定时任务巡检机制.md
- 提交：C.C. `782b404`（四件套）；TODO 两条另有一笔待 Doctor 跑
