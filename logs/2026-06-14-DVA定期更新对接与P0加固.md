---
title: 会话日志 2026-06-14 — DVA定期更新对接与P0加固
tags: [log, DVA]
created: 2026-06-14
updated: 2026-06-14
status: active
type: log
project: DVA
---

# 会话日志 — 2026-06-14

**项目**：DVA
**主题**：DVA 定期更新对接 + 源码 P0 加固 + 运维落点迁 DVA-ops

---

## 完成的工作

- finalize 了一条卡住的 ASR 任务（aweme 7647130810704809254，老毛聊交易《声明！》），14 段/1297 字写入 `Transcripts/老毛聊交易/`。真相：6/13 note 说的「OSS 北京区白名单」blocker 这次已不存在，实际卡点是 state 文件 `out_txt` 硬编码到 stale `/tmp/dva_asr_out_5`（属主 nobody 不可写）；改向规范 Transcripts 目录后 `--poll-once` 一次跑通。
- 诊断「多作者更新（`node dva.js update-all`）通不通」：本机设计可行，但沙箱跑不通——harvest 内 ASR 是同步全程模式撞 45s 上限，且 `~/.dva_secrets` 不在沙箱。结论：batch 归 Mac，沙箱只能跑分段 ASR。
- 起草《PRD-定期更新-DVA.md》（源项目根）供 Codex 实现，覆盖工具链/凭证/4 项可靠性/验收。
- 对接 Codex（V.V.）新边界：源项目只读、数据层可写不可删、运维归已放开路径。
- 发现并验证一个真问题：`node dva.js update-all` **失败路径会写源 `GOTCHAS.md`**（`dva.js:80` 写死 `__dirname/GOTCHAS.md`，无 env 覆盖），成功路径干净。
- 核查后更正自己的提案：GOTCHAS 里 ffmpeg 卡死（BUG-20260610-001）、DYD 文件名查重（BUG-20260610-002）两条都标着「✅ 已修复 2026-06-10」，我误当待办——只看标题没看状态行。
- 实施并验证 **P0**：`dva.js:80` 改 `process.env.DVA_ERROR_LOG_PATH || __dirname/GOTCHAS.md`。默认不变，自动任务设 env 改向数据层。实测错误正确写入 `DVA-ops/failures/dva-errors.md`，源 GOTCHAS.md 字节前后一致；`node --check` 通过。
- 建 `Database/Douyin/DVA-ops/{logs/cron,summaries,failures,asr_pending,cache,tmp}` + 带插入标记的 `dva-errors.md`。
- PRD → v1.2、改源提案 → v2.0（实施记录），全部 ops 路径迁 DVA-ops。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 运维产物统一落 `Database/Douyin/DVA-ops/`，弃用 V.V. 的 `Project Mirror/DVA/ops/` | Doctor 原则：被自动任务写的文件放已放开路径；与数据同盘、复盘方便 | V.V. 需同步改其 README/INDEX/GOTCHAS 的 ops 路径 |
| 源码修改由 CC 做，Codex 自动任务绝不写源 | Doctor 拍板的分工 | 改源走提案/授权；CC 本轮做了 P0 |
| P0 用 env 网关而非改默认 | 人工/交互式调用保持原行为，最小惊讶 | 自动任务必须显式设 `DVA_ERROR_LOG_PATH`，否则仍写源 |
| 调度用 Codex automation 替代 crontab | 免 `/usr/sbin/cron` 完全磁盘访问；产物天然落数据层 | 不用 `run-update-all.sh`（它写源 logs/cron） |

## 遗留问题 / 待办

- [ ] Codex/V.V. 按 PRD v1.2 R0–R4 搭定时任务（R1/R3/R4 都是 ops 层包装，不碰源）
- [ ] 自动任务务必 export `DVA_ERROR_LOG_PATH=.../DVA-ops/failures/dva-errors.md`
- [ ] V.V. 同步镜像 README/INDEX/GOTCHAS：`ops/` → `Database/Douyin/DVA-ops/`
- [ ] 错误处理器控制台仍打印「已记录到 GOTCHAS.md」纯文案（实际写 DVA-ops），如要改是另一处源改动
- [ ] P2 可选项（harvest ASR 分段、import 扫描收窄）未做，按需再授权
- [ ] Doctor 在 Mac commit：源 DVA 仓的 `dva.js`(P0) + 新 PRD + 已删的 `.asr_pending` state

## 相关笔记

- [[DVA]] · [[系统概览]] · [[GOTCHAS]]
- 源：`Projects/DVA/PRD-定期更新-DVA.md`（v1.2）
- 镜像：`Codex/Project Mirror/DVA/改源提案-DVA加固.md`（v2.0）
