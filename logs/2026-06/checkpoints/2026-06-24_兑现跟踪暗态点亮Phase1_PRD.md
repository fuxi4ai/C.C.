---
title: PRD · 兑现跟踪暗态点亮 Phase1（价格层）
tags: [prd, acceptance, 烛照九阴]
created: 2026-06-24 10:30
updated: 2026-06-24 10:30
status: 进行中  # 进行中 / 待验收 / 已交付 / 已取消
doctor_decision: 已审  # 待 / 已审 / 已取消（Doctor 2026-06-24 批准·开工 Phase1·回测先行）
type: prd
project: 烛照九阴
template_version: v1.0
---

> 起草稿 · 待 Doctor 审/改/批 · CC 不开工、不打 ✓、不自动关闭。§二全留 `[ ]`。

# PRD · 兑现跟踪暗态点亮 Phase1（价格层）

## §一 · 任务目标

**动机**：现行 `closure_engine` 把 closed 当「价格判死 + 终态 + 剔除」——一条主线一旦命中 close（峰值≥5% 且绝对回撤≥5pp）即 `break` 冻结、退出在途台账，且不会因后续行情复活。实测今日 782 条 closed 中 46%（356 条）是「高峰值小回撤」被提前判完毕（极端例：峰值 +65% 仅回撤 6.3pp＝峰值 10% 即收口）；叠加「再进场只认渊图新信号、价格自证为零」，强线的二段主升被系统性漏接。

**做什么（本次＝Phase1 价格层）**：把价格从「判生死」降级为「管亮暗」。close 判据**不改（仍 5pp 绝对）**，但 close 后**不剔除**→转 **dormant（暗态）** 仍在库内跟；dormant 后价格重走强（连续超额为正 ≥Y′ 日 且 自暗态低点累计回升 ≥Z pp）→**点亮**回 closing（二段）；允许多轮循环。日报暗态线不渲染、仅留「暗态 N 条」计数入口。Y′/Z 由回测定值并经 Doctor 确认后落库。

**对齐结论摘要**（2026-06-24 多轮对齐确认）：
- 核心需求：价格只管亮暗、信号才管去留；closed 不剔除转暗态、价格再强则点亮二段。
- 形态：close 仍 5pp；点亮门槛＝连续超额为正≥Y′日 且 自暗态低点回升≥Z pp（值由回测定）；暗态不渲染+留计数入口；dormant↔closing 可无限多轮、每轮独立记录。
- 去留信源：渊图驱动（Phase2）。
- 分期：价格层先行（本次）、信号层二期；回测作 Phase1 交付前置。

**Doctor 原始指令**（逐字引用）：
> "2、还按5%绝对值回撤来close，但是不剔除，只变暗（不显示），然后达到一个超额指标（绝对值加连续性）后，再点亮。"
> "信号驱动用这样的逻辑：'产业逻辑：买入'信号依然在，这条线就跟着（就是亮与暗的区别）；'产业逻辑'一旦改变，这条线就提示风险，这时候再变暗就不跟了。"
> （分期/回测/信源/呈现/门槛形态经 2026-06-24 AskUserQuestion 多轮对齐选定。）

**任务规模估算**：
- 预计涉及文件：3–4（`tools/closure_engine.py`、`tools/gen_daily_report.py`、recap schema 迁移、回测脚本/产物）
- 预计耗时：跨小时（含回测）
- 涉及项目：烛照九阴（+ recap.db）

---

## §二 · 交付标准（Acceptance Criteria）

> 填写规则：`[ ]` 默认 / `[?]`+证据 / `[!]`+原因 / `[~]`+需 Doctor 判断点；`[✓]` 只 Doctor 打。无证据=回退 `[ ]`。

### A. 文件层面

- [?] `tools/closure_engine.py` 已含 dormant/relight 状态机分支（非删改、原 5pp close 逻辑保留）
  - 证据：closure_engine.py run_machine 新增 dormant/relight（复核确认 :181-194）；首腿等价旧引擎（leg_peak 首腿==全程峰值，X_PEAK/DD_ABS=0.05 未变）。独立子 agent 已核「首腿等价成立」。
- [?] recap schema 已加承载暗态/点亮的列（ensure_cols，不破坏既有列）
  - 证据：APPLY_COLS 增 dormant_since/relit_date/relit_count；/tmp/t2 PRAGMA 实测三列在 industry_signals 与 yuantu_buy_signals 双表均在；.bak 行数 1299/1299、102/102 未丢、integrity ok。
- [?] `tools/gen_daily_report.py` 已加「暗态 N 条」计数入口、且 dormant 线不渲染
  - 证据：ytdays 滤 dormant（:337-338）+ 台账 SQL open/closing 排除 dormant；D["dormant_n"] + .dormant-note；测试日报实渲「🌙 暗态 3 条」；主栏无 t-dormant 卡（仅 CSS 定义）。
- [?] 回测对比产物已落于 docs/（新旧规则对比报告）
  - 证据：`docs/兑现回测_案二点亮扫参_20260624.md`（16 行扫参网格·去重/全量双视图·误杀率/二段捕获量化·Y′/Z 推荐·诚实局限）+ 脚本 `tools/bt_case2_relight.py`。
- [?] 无文件被删除
  - 证据：本次仅新增（bt_case2_relight.py、决策记录.md、PRD、报告）+ 改 closure_engine/gen_daily_report/STATUS；无删除。

### B. 一致性层面

- [?] `gap_status` 取值集合统一纳入 `dormant`
  - 证据：引擎产出 {open,closing,dormant}（run_machine :147,195）；报表 STATUS_ZH 加 dormant（:59）、fulfill_of 加 dormant 分支（兜底不报错）；台账 SQL 含 dormant 语义（open/closing 排除）。独立子 agent 核「无 dormant→KeyError 风险」。
- [?] closure 写库仍走 `config.RECAP_DB`、新增代码无硬编码真盘绝对路径
  - 证据：UPDATE/connect 用 config.RECAP_DB（:291,297 复核）；Grep 新增代码无 `Database/烛照九阴/recap.db` 硬编码。
- [?] `STATUS.md` 已同步暗态/点亮口径
  - 证据：STATUS.md 加「兑现口径（案2 Phase1）」段（dormant/点亮 Y′=4/Z=5pp/新列/Phase2 边界）。

### C. 功能层面

- [?] `closure_engine.py --apply` 在 /tmp 副本跑通、自带 .bak
  - 证据：/tmp/t2 --apply 回填 1401 条、生成 recap.db.bak_20260624_closure、integrity ok。
- [?] 真实样本验证 dormant + 二段点亮（relit 字段非空）
  - 证据：/tmp/t2 分布 closed 消失→dormant 365 / closing 556（含二段）；relit_count 分布 0:269/1:432/2:206/3:33；样本「商业航天 峰值+62.9%→现+33% dormant_since 20260113 relit 20260508 count2」。（注：用真实历史样本而非构造样本，覆盖更实；独立子 agent 已核数据自洽）
- [?] dormant 不进主栏/台账、暗态计数 +1
  - 证据：测试日报 dormant 未渲染卡片、「🌙 暗态 3 条」入口在位（D["dormant_n"]=3 与 yuantu dormant 计数一致）。
- [?] `gen_daily_report.py` 跑通无报错、暗态入口可见
  - 证据：--output /tmp/ztest --no-archive 生成 2.37MB 无 Traceback；四步脊 scrollIntoView×4 保留。渲染观感留 Doctor 目测。

### D. 自审层面

- [?] self-audit 段已写
  - self-audit：① dormant 是否被误当默认态扩大范围？——否，dormant 仅由真实 close 触发（/tmp/t2 closing 556 仍是多数，open/closing 未被污染），独立子 agent 已专门核此项。② 回测样本是否挑选迎合？——否，回测同时给去重(主)+全量(副)双视图、并诚实标注「捕获率畸高根因＝ETF 长窗超额普遍大 + 27% 右侧截断 + 误杀嫌疑系代理判据」，未藏短。③ 最大诚实保留：价格层点亮"来者不拒"、伪点亮占比 ~31%，**价格层单独筛不出噪音、真分辨靠 Phase2 信号层**——已写进回测报告与决策记录，不夸 Phase1 效果。④ Z 取 5pp（保守档）非回测评分首选 8pp，因 Z 对真二段几乎无影响、低 Z 多压噪，且 Doctor 已拍板 5pp。
- [?] checkpoint/progress 已落
  - 证据：本 PRD 即 checkpoint；决策记录 3 条 + STATUS 口径段 + 烛阴 logs 已落。

### E. 沟通层面

- [?] /save 已触发 + 落 per-agent 烛阴档
  - 证据：`agents/烛阴/logs/2026-06-24-四步脊锚跳与兑现口径PRD.md`（本次开工前会话）；本 Phase1 执行将在收尾再 /save 补一笔。
- [?] git commit 命令已贴给 Doctor（不在沙箱跑 git；≥2 仓合并）
  - 证据：收尾回报含两仓（烛照九阴 + brain）合并 code block 分段命令。
- [?] 关键决策已落档 brain
  - 证据：`烛照九阴/architecture/决策记录.md` 3 条（案2/5pp过敏/closed盲区）；Y′=4/Z=5pp 落 STATUS + closure 常量 + 回测报告。

### F. 任务专属

- [?] **状态机**：close 不 break、记 dormant_since 续扫、点亮回 closing 记 relit_date、多轮
  - 证据：run_machine :181-194；Grep dormant/relit 命中；/tmp/t2 relit_count 多轮（最高 3）；独立子 agent 核「多轮无上限、首腿等价」。
- [?] **扫描窗口口径**：沿用 `WINDOW=120`
  - 证据：dormant/relight 在发现日后 120 交易日窗内观察（run_machine ds=[...][:WINDOW]，未改）。**局限（如实）**：信号发现>120 交易日后的二段不被捕获；若需更长窗属后续口径调整，本次沿用。
- [?] **recap 列**：PRAGMA 显示新增列、既有不丢
  - 证据：/tmp/t2 PRAGMA 双表均含 dormant_since/relit_date/relit_count；.bak 行数 1299/102 未减；integrity ok。
- [?] **回测（交付前置）**：对比报告含误杀率/二段捕获 + Y′/Z 推荐
  - 证据：`docs/兑现回测_案二点亮扫参_20260624.md` 存在（16 行网格 + 双视图 + 量化 + 推荐 + 局限）。
- [?] **Y′/Z 落库经 Doctor 确认**：写入 closure 常量且＝确认值
  - 证据：Doctor 2026-06-24 AskUserQuestion 拍板 **Y′=4/Z=5pp（稳）**；closure_engine RELIGHT_STREAK=4、RELIGHT_REBOUND=0.05（:38-39）。
- [?] **防空壳 + 往返**（/tmp 验证；生产落库待下一班）
  - 证据：/tmp/t2 --apply 整套验证（只增不减、integrity ok、.bak）。**生产 recap 本次未动**——案2 落库 + 暗态日报由下一日 07:00 调度班 closure --apply（/tmp 往返放回）自然带出；如需即时上线可 Doctor 指令触发一次受控 --apply+放回。

---

## §三 · 非交付项（范围排除）

- 不包含：**信号层**（渊图 thesis-change → 风险提示 → 真正剔除）= **Phase2**，依赖渊图 KG 能产出 thesis 证伪/降级/移除信号（**前置依赖待核实渊图端能力**）。
- 不包含：**close 判据改动**——仍 5pp 绝对回撤；案1 的「35% 相对回撤 + 5pp」混合判据**本次不纳入**（Doctor 选案2）。
- 不包含：渊图 KG 端 thesis-invalidation 信号的产出本身（渊图的活）。
- 不包含：Artifact 渲染管线改造（沿用现有 echarts 内联/籁琴转 img 转换；暗态计数入口若随日报自然带出则不另算工程）。
- 不包含：主栏「最近 K 条」取数口径（已于 2026-06-24 议定保持现状）。

---

## §四 · 状态

- [ ] 进行中（立 PRD 2026-06-24 10:30；执行完毕转下行）
- [x] 待 Doctor 验收（CC 已对 23 条全填三态[?]+证据 · 独立子 agent 复核通过「可进待验收」· 时间：2026-06-24 18:40）
- [ ] 已交付（Doctor 已把所有 `[?]` 转为 `[✓]` · 时间：____）
- [ ] 已取消（Doctor 显式取消 · 时间：____ · 原因：____）

**关闭路径**：全 ✓ 关闭 / Doctor 取消关闭；CC 不自动关闭。

---

## §五 · 变更记录

- 2026-06-24 10:30 CC（烛阴）：立 PRD · 含 23 条交付标准 · 经 3 轮 AskUserQuestion 对齐（信源/点亮门槛/暗态呈现/分期/回测位置）· 起草稿待 Doctor 审批。
- 2026-06-24 CC（烛阴）：Doctor 批准·开工 Phase1·回测先行 → doctor_decision: 已审；开始执行，回测先动。
- 2026-06-24 CC（烛阴）：回测扫参（子 agent）→ Doctor 拍板 Y′=4/Z=5pp；实现 closure dormant/relight + recap 列 + 日报暗态入口；/tmp 全套验证通过；独立子 agent 对照 §二 复核「全部客观项通过·可进待验收」。§二 23 条全填 [?]+证据 → §四 待 Doctor 验收。待 Doctor 打 ✓。

---

## v 历史
- v1.0（2026-06-24）：初版 · Phase1 价格层 · 案2 暗态点亮 · 信号层列 Phase2。
