---
title: 逐文件实施清单 · 受益公司库领域分库
tags: [checklist, 白泽大宗, 龙鱼五力, 烛照九阴, 实施]
created: 2026-06-29
updated: 2026-06-29
status: draft          # 待 Doctor 二次确认 → 才动手
type: checklist
related: 2026-06-29_统一受益公司库_合并口径设计稿
---

# 逐文件实施清单 · 领域分库

> 配套设计稿 v2（已审定）。本清单待 **Doctor 二次确认** 才动手。
> 铁律：CC 不在沙箱跑 git 写命令；弃用一律 archived/指针、不物理删除；跨 4 git 仓 commit 合一个 code block 分段连发给 Doctor 终端。

## 仓边界

4 个 git 仓 + 1 个无版本 db：① `Financial/白泽大宗`、② `Financial/烛照九阴`、③ `Database/行业研究`（龙鱼五力引擎）、④ `Database/龙鱼-标的分析库`；+ `Database/宏观-大宗商品/business_breakdown.db`（无 git）。

---

## A · business_breakdown.db（无 git · 原地）
- **A1** 表 `fundamentals` → 改名 `benefit_relations`（`ALTER TABLE RENAME`）。改前 `.bak` 备份。
- **A2** `stock_scores` 表（226 行评分镜像）**弃用**：停止更新；**数据保留不 drop**（可逆），文档标「已弃用·评分真源在龙鱼库」。

## B · 白泽大宗（git）
- **B1** `scripts/database/build_warehouse.py`：① 构建表清单去掉 `stock_scores`（L59/67/75）——不再 build 镜像；② `fundamentals`→`benefit_relations`（L59/68/77）。
- **B2** `scripts/data_collection/sync_longyu_universe.py`：① (A) ingest 分支弃用（不再写 `longyu_scores.json`）；② (B) 扩池保留，下游由「写镜像」改为「触发沙箱跑分」（接 D1）；③ 写 db 的表名 → `benefit_relations`。
- **B3** `scripts/reports/build_weekly_report.py`：`_load_longyu_scores()`（L72）重指——经 `lib_public_read.lookup_company(ts_code)` **实时读龙鱼库 records** 取 `six_total/rating`，弃 `longyu_scores.json` 依赖（L37/147/162…）；读不到则该列留空标「待跑分」，不阻塞出稿。
- **B4** 新增 `scripts/lib_public_read.py`：`lookup_company`→龙鱼库 / `lookup_benefit`→白泽 `benefit_relations` / `lookup_commodity`→白泽行情。三口只读。
- **B5** `data/weekly/longyu_scores.json` → archived + 指针。
- **B6** `docs/WORKFLOW_周报自动化.md` + `README.md` + `INDEX.md`：更新职责边界 + owner 只读契约。

> 经核：白泽脚本里 `data_source.py`/`refresh_commodity_prices.py`/`supplement_db_v4_1.py`/`compute_elasticity_tier1.py` 读的是 **`stocks_fundamentals.json`（JSON）**，**不是** db 的 `fundamentals` 表——**默认不动**（除非选「全量改名」，见末尾岔口）。

## C · 烛照九阴（git）
- **C1** `tools/fundamentals_lookup.py`：① `FROM fundamentals`→`benefit_relations`（L41）；② 读 `stock_scores` 取六维那段（L55）改为经 `lib_public_read.lookup_company` 读龙鱼库；③ 可选整体切 `lib_public_read`。

## D · 龙鱼五力引擎（Database/行业研究 · git）
- **D1** 沙箱自动化跑分（Doctor 定调②）：复用现链 `batch_score.py`→`score_with_llm.py`→`record_writer`，定「白泽新受益 ts_code → 沙箱分片跑分（timeout 43/只）→ 落龙鱼库 records」入口（扩 `baize-weekly-report` SKILL 或新脚本）。**跑分+写 json 沙箱可做；git commit 仍 Doctor 终端。**

## E · 龙鱼-标的分析库（git）
- **E1** `README.md` 顶部加契约：本库＝公司级公共真源，白泽/烛照九阴/CC 只读调用。其余**不动**（保持评分真源）。

## F · brain / SKILL
- **F1** `Claude/Scheduled/baize-weekly-report/SKILL.md`：跑分落龙鱼库 + 周报读龙鱼库的流程更新。
- **F2** brain：`CLAUDE.md`（数灵金融线布局）+ 龙鱼五力/白泽大宗 `architecture/决策记录.md`：登记本次领域分库决策。

---

## 执行与可逆
- 弃用项全部 archived/`_DEPRECATED` + 指针，无物理删除；db 改名前 `.bak`，`stock_scores` 不 drop。
- 跨 4 git 仓：沙箱只改文件 + 跑分，**所有 git commit 合一个 code block 分段连发**给 Doctor 终端跑。
- 验收按设计稿 §八。

## 待 Doctor 二次确认（两个岔口）
1. **改名范围**：仅 db 表（`benefit_relations`，连带小）／连 `stocks_fundamentals.json` 文件+内部 key 全量改（再波及白泽 5 脚本）。
2. **执行方式**：一次全做／分期（一期：砍镜像 A2/B1/B2A + 周报接龙鱼读 B3/B4；二期：改名 A1/B1/C1 + 公共模块 + 沙箱自动化 D1 + 文档）。
