---
title: PRD · 剑酒行情日更端到端失败告警（被动·health+看板发光）
tags: [prd, acceptance, 剑酒青丘, 海螺姑娘]
created: 2026-06-23
updated: 2026-06-23
status: 待验收
doctor_decision: 待
type: prd
project: 剑酒青丘
template_version: v1.0
---

# PRD · 剑酒行情日更端到端失败告警（P1-B）

## §一 · 任务目标

给行情日更（Cowork SKILL `market-data-daily-update`）补「端到端失败告警」。现状：日更失败只进 `cron-行情.log` / 沙箱日志（被动、无人盯，0622 代理崩溃无人知）。MVP 走**被动告警**：日更末尾跑一个只读自检脚本，把各表新鲜度 + 整体成败写成 `Database/Market-Data/_health.json`；conch survey 读它 → 行情库节点在资产看板上按健康度**发光报警**（fail→断链红、落后→陈旧黄）。纯沙箱、零凭证、只读 db，并与既有资产看板/survey 闭环。

**Doctor 原始指令**（逐字引用）：
> "现在"（同意现在排 P1-B 端到端失败告警，先出小 PRD/设计）

**对齐结论摘要**（2026-06-23）：
- 告警形态 = **被动**（_health.json + 看板发光）；主动推送（邮件/IM）**不做**，另议。
- 范围 = **仅剑酒行情日更**（market-data-daily-update）；白泽周报 / 烛照日报沿同范式另推。
- 新鲜度判定走「**表级一致性 + 周末/节假日近似**」，**不联网**校准 tushare trade_cal（避免网络/凭证依赖）。

**任务规模估算**：~4 文件（新 market_health.py；conch_engine survey 扩读 health；asset_manifest 加 health_file/摘 issue；SKILL 加一步）。跨小时内。项目：剑酒青丘（写）+ 海螺姑娘（survey/manifest）+ Scheduled SKILL（仓外）。

---

## §二 · 交付标准

> `[ ]` 未开始 / `[?]` 已达成+证据 / `[!]` 未达成+原因 / `[~]` 不确定+需 Doctor 判断；`[✓]` **只 Doctor 打**。

### A. 文件层面
- [?] 新建 `infrastructure/取数工具/market_health.py`，退出码 0
  - 证据栏：py_compile OK；运行输出 `health: stale latest=2026-06-23 fails=[]` 退出 0。
- [?] 写出 `Database/Market-Data/_health.json`，`json.load` 通过，含字段
  - 证据栏：文件已写；`json.load` OK；含 `generated/target_date/days_since_latest/tables/overall/fails/warns/note`。

### B. 功能层面
- [?] `_health.json.tables` 覆盖 5 监控表，每表有 `max_date`+`lag_days`+`ok`（+`sev`）
  - 证据栏：5 表齐；实测 stock_daily/north_flow=20260623(ok)、daily_market/limit_list_daily/theme_etf_daily=20260622(lag1·warn)。
- [?] `overall` 判级 + `fails`/`warns`
  - 证据栏：**执行中细化**——落后≥2天/表不一致=`fail`(红)、落后1天=`stale·warn`(黄·避免聚合当日步未跑的误报)、整库过期>4天=stale；实测 overall=`stale`、warns=3表、fails=[]。（较 PRD「不一致即 fail」更分级，请 Doctor 认可此细化。）
- [?] `market_health.py` 只读 db、不写 .db、只写 `_health.json`
  - 证据栏：`sqlite3.connect(file:...?mode=ro&immutable=1)`；全脚本无 db 写；仅 `json.dump` 到 _health.json。
- [?] conch `survey` 读 `_health.json` → 行情库节点 status 随之变
  - 证据栏：survey 后「公共行情库」status=`stale`、detail「自检 stale·落后:daily_market/limit_list_daily/theme_etf_daily」、`conch_health` 已写；剑酒/别的 project 状态未被动。
- [~] 资产看板重嵌后「公共行情库」按 health 着色发光
  - 证据栏：看板内嵌 md 节点已 stale（黄·陈旧柔光）；**视觉需 Doctor 在 Cowork 目视**。

### C. 一致性层面
- [?] `market_data.db` 节点新增 `health_file` → `Database/Market-Data/_health.json`
  - 证据栏：已加；survey 据此读取生效。
- [?] 剑酒青丘 issue「缺端到端失败告警」摘除
  - 证据栏：剑酒 issues=[]、status=healthy、meta 注「端到端失败告警已上线」；needs_repair 现仅剩白泽/烛照。
- [?] SKILL 末尾新增跑 market_health.py 一步
  - 证据栏：`Scheduled/market-data-daily-update/SKILL.md` 加「第 9 步·端到端健康自检·必跑」。

### F. 任务专属红线
- [?] 不主动推送（邮件/IM）
  - 证据栏：脚本/survey 无任何 SMTP/webhook/通知代码；只写 _health.json + 看板读取。
- [?] survey 只回写 node 健康、不臆造 edges；market_health 不联网
  - 证据栏：survey 仅改 conch + 带 health_file 的 db 节点 status；edges 数未变；market_health 无 import 网络库、仅 sqlite ro。
- [?] 涉 git → commit 命令贴 Doctor 终端
  - 证据栏：回报含剑酒仓（market_health.py + 脚本注释 + PRD）+ 海螺姑娘仓（conch_engine survey 扩展 + manifest）commit 命令；SKILL（Scheduled 目录）另注。未在沙箱跑 git。

---

## §三 · 非交付项
1. **不做主动推送**（邮件 / IM / 系统通知）——MVP 只做"失败能被看见"（health + 看板）。
2. **仅剑酒行情日更**；白泽周报 / 烛照日报的告警另推（沿同范式）。
3. **不联网校准交易日历**（不调 tushare trade_cal）——用表级一致性 + 周末/节假日近似判新鲜度。
4. **不改看板渲染逻辑**——复用现有五态发光（仅数据驱动）。
5. 不做历史告警留存 / 告警分级策略 / 静默期。

---

## §四 · 验收与状态
- 验收人：Doctor。方式：A/B/C 命令可验；看板着色需 Doctor 目视。
- 关闭：§二 全 `[✓]`（Doctor 打）或显式取消。CC 不打 ✓、不自动关闭。
- 执行后交验收前：派独立子 agent 对照 §二 复核。
- **独立复核结论（2026-06-24·干净子 agent）**：无不通过项，所有 `[?]` 独立核验成立（market_health 退出0/字段齐/只读零网络、survey 映射 health→行情库 status 成立、edges 未变、剑酒 healthy、SKILL 第9步在）。**需 Doctor 裁定**：B② 判级口径由 PRD「表间不一致即 fail」**细化为「lag≥2天=fail/1天=warn(stale)/整库>4天=stale」**（CC 已如实标注，请认可）。**需目视**：看板「公共行情库」黄色陈旧柔光。**状态 → 待 Doctor 终审；CC 未打 ✓。**

## §五 · 备注（设计）
- `_health.json` 拟结构：`{"generated":ts,"target_date":"YYYYMMDD(近似最近收盘日)","tables":{"stock_daily":{"max_date","lag_days","ok"},...},"overall":"ok|fail","fails":[...]}`。
- 新鲜度阈值：`lag_days > 1` 标 stale；表间 max_date 不一致 或 overall fail → broken。阈值可配。
- survey 扩展：database 节点若有 `health_file` 字段 → 读之、按 overall/lag 覆盖该节点 status（这是 survey 唯一对 database 节点的 status 写入，仍不碰 edges / 不臆造）。
