---
title: 会话日志 2026-07-06 — 龙鱼看板行业分类接入与 Artifact 渲染真凶排查
tags: [log, 龙鱼五力, O MY HTML, 卡片页, artifact]
created: 2026-07-06
updated: 2026-07-06
status: active
type: log
project: 龙鱼五力 / O MY HTML
---

# 会话日志 — 2026-07-06（龙鱼看板行业分类 + 渲染真凶）

**项目**：龙鱼五力 × O MY HTML（个股库看板 artifact `longyu-stock-library`）
**主题**：看板版式微调 → 数据/版式解耦生成器 → 接周日双scorer → 板块｜环节两级行业分类接入 → AI硬件落「其他」真凶排查

> 承接同日 `2026-07-06-龙鱼个股库看板星河暗artifact.md`（v1–v4 星河暗建卡）。

---

## 完成的工作

**A. 看板版式微调（直接改 artifact index.html）**
- 字体改思源宋体（`--sans`），英文与数字统一 Times New Roman（`--en`）；中文思源宋体、英文/数字 Times（Latin 前置于 `--sans` 让混排 Latin 走 Times）。
- 金问号（财务≤2.5/5）/红叹号（回避）确认在卡片左上角（`top:-10;left:-10`）。
- 卡片渲染从「星河星云卡」换成 **GAI 项目卡·深色版**（参照 `global-asset-inventory`）：18px 圆角 + 顶部白光 sheen `::after` + 深色玻璃三段渐变 + hover 抬升微辉光；保留星云/星点/悬浮标记。

**B. 数据/版式解耦生成器 `_index/build_dark_board.py`**
- 只刷数据不碰版式：从 `records/*.json` 重算 PAYLOAD，就地替换 artifact 里 `const PAYLOAD = …;` 单行；latest 腿取 `analysis_date==r.latest`、同日双 scorer 取 deepseek 腿。
- 双环境适配：Mac（够到 artifact 文件）就地刷 + bootstrap 模板 `board_template.html` + 落 standalone；定时沙箱（够不到 `~/Claude's workspace`）用模板 + 新数据生成 standalone 供 `update_artifact`。
- 沙箱验证 95 标的 / 常更 20 / 持仓 10、注入/模板/兜底三模式全过。

**C. 接周日双scorer 定时任务**
- `longyu-weekly-dualscorer`（周日00:01）新增第6步「刷看板」：跑生成器 → `update_artifact` 推送；铁律加「只刷数据不碰版式 + 模板由 Mac 就地运行 bootstrap」。

**D. 板块｜环节 两级行业分类**
- 真源 `_index/细分环节.json`（ts→{板块,环节,label,name} + `_meta.板块序/板块色`），全 95 只、8 板块（AI硬件28/半导体11/商业航天31/机器人4/资源14/油气化工5/新能源0/其他2）。Doctor 迭代：先进封装+算力芯片归 AI硬件；乾照/钧达/天银移入商业航天（宇航电源/星上配套）；排雷标的按主业。
- 生成器读它 → payload 带 `sector`(板块·色/组/图例)/`seg`(环节·卡片小标签)/`seglabel`(板块｜环节·侧栏)。
- 看板 JS 接线：SECTORS 10大类→8板块色、SEC_EN、卡片 indtag→环节、侧栏→板块｜环节、图例/分组/副标题「行业」措辞（Doctor 要「行业」不要「板块」）。

**E. AI硬件落「其他」真凶排查（关键）**
- 表征：无论刷不刷新、update_artifact 推没推，按行业分组 AI硬件 标的落「其他」。我先猜缓存/重开，绕了多轮（Doctor 批评「不找真问题一顿猜」）。
- 真因：看板 JS 加载时有一行 `DATA.forEach(d=>{ d.sector=sectorOf(d.industry); d._q=… })` **无条件重算**，把 payload 的 8 板块分类整个覆盖——空 industry 落「其他」、对不上新表的掉出分组。改为 `if(!d.sector)…/if(!d._q)…` 条件化，payload 优先；同步修 `board_template.html`；重建 standalone + `update_artifact` 推送。
- Doctor 追问「缓存问题 gotchas 里没记录么」→ grep brain 确有先例（烛阴 2026-06-25「星空刷新必须走 update_artifact」、白泽 2026-06-12「Artifact 静态快照不自更」），但散在 agent logs、未沉到 O MY HTML 项目 GOTCHAS（空）→ 补写两条。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 看板走「数据层单一真源 + 生成器只换 PAYLOAD 行」 | 版式手工调过，周更只应换数据、不覆盖 CSS/JS | build_dark_board.py + board_template.html + update_artifact 推送范式 |
| 行业分类做 `板块｜环节` 两级、真源 `细分环节.json`、色按板块 | Doctor 要参渊图口径的中粒度；records 不改 | 生成器读表出 sector/seg/seglabel；改分类=改 json 重跑 |
| 先进封装+算力芯片归 AI硬件；排雷标的按主业；乾照/钧达/天银入商业航天 | Doctor 逐条裁定 | 8 板块，商业航天31/AI硬件28 |
| 客户端 sectorOf 重算改条件化 | 分类真源上移数据层后，无条件重算会冲掉 payload | AI硬件正常独立成组 |
| UI 显示不对先读渲染链路、开工先 grep GOTCHAS/brain | 本次因「数据对≠渲染对」猜缓存耗多轮 | 写入方法论铁律（GOTCHAS ERR-002 预防措施）|

## 遗留问题 / 待办

- [ ] Mac 上跑一次 `build_dark_board.py` 正式 bootstrap 模板并刷 artifact；git add `_index/{build_dark_board.py,细分环节.json,细分环节_审阅.md,board_template.html,龙鱼个股库看板_暗.html}`。
- [ ] 分类边界待 Doctor 定：航天机电（暂宇航电源）、中兴（暂商业航天｜地面终端，可另立其他｜通信设备）、昊华（暂油气化工，可挪半导体｜特气材料）；卓胜微/唯捷/三安/蓝思是否也入航天。
- [ ] 定时任务 `update_artifact/request_cowork_directory` 点一次「Run now」预授权。
- [ ] 新能源板块现 0 成员（钧达移出）——保留词表待将来。

## 相关笔记

- [[龙鱼五力]] · [[O MY HTML]] · [[星空]]
- GOTCHAS 新增：`O MY HTML/GOTCHAS.md` ERR-20260706-001（Artifact 静态快照·必 update_artifact）/ ERR-20260706-002（客户端 sectorOf 覆盖 payload 分类 + 渲染链路铁律）
- 先例：烛阴 `logs/2026-06-25-信号方向场与星空D>S.md` · 白泽 `logs/2026-06-12-周报日报转Artifact.md`
- 产物：`Database/龙鱼-标的分析库/_index/{build_dark_board.py, 细分环节.json, board_template.html, 龙鱼个股库看板_暗.html}`
- artifact：`longyu-stock-library`
