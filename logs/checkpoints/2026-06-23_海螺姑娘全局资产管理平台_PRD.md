---
title: PRD · 海螺姑娘全局资产管理平台
tags: [prd, acceptance, 海螺姑娘]
created: 2026-06-23
updated: 2026-06-23
status: 进行中
doctor_decision: 待
type: prd
project: 海螺姑娘
template_version: v1.0
---

# PRD · 海螺姑娘全局资产管理平台（升格大活）

## §一 · 任务目标

把「全局资产管理」**内化为海螺姑娘的能力**：现有 `conch_engine.py`（dry-run 默认 + 多维 `is_obsolete` + 审计/rollback + 项目类型自适应）从「单项目维护」**升格**出一个**全局盘点模式**，遍历本地所有项目级资产（不含 VV）产出一份资产 **manifest**；新建一个**看板 artifact** 作为海螺姑娘的 view 层——把资产按 **Resource / Database / Project（+ Infrastructure 子组）** 铺成「上下游关系星河」，平时以**五态发光**显健康、点击弹详情卡；对**待修 / 断链**资产提供「审查并修复」按钮，触发 conch 对该资产做**专项 dry-run pass** 出分级方案，**Doctor 逐项批准才 `--apply`**（conch 自带 rollback）。目的是全局资产可视化 + 后续对出问题的资产专项整理/修复。

**Doctor 原始指令**（逐字引用）：
> "做一个AI powered Artifact；目的是盘点我们本地（不含VV）所有项目级别资产；分成"Resource"，"Database"，"Project"；按"上下游"顺序排列并展示互相调用/消费的关系线；点击实现弹出详情卡；不点击的时候，以发光的不同颜色，或类似加某种符号的形式直观展现状态；对需要修复的资产有"审查并修复"按钮；海螺姑娘这一类的project单成一组，命名为"Infrastructure"。"
> "能不能把这个看板内化成海螺姑娘的一部分——目的是全局资产管理，后续可以对出问题的资产专项整理/修复"
> "升格大活"

**对齐结论摘要**（2026-06-23 三轮对齐确认）：
- 数据来源 = **策展 manifest（单一真源）+ 定时探活回写**；上下游关系线**手工策展**（conch 不臆造 edges）。
- 「审查并修复」= **审查→分级方案，不自动改**；引擎复用 conch（dry-run 默认、`--apply` 必经 Doctor、可 rollback）。
- 载体 = **新建独立 artifact**，复用现有 `brain-vault-dashboard` 的星河/详情卡/新鲜度渲染代码；**不改**现有金融看板。
- 三类定义：Resource=外部信源/原料；Database=本地数据库/存储；Project=代码项目。**Infrastructure = Project 子组**，成员 = {海螺姑娘, 司南}。
- 状态 = **五态**：健康(绿)/陈旧(黄·按表级新鲜度非纯 mtime)/断链(红)/待修(橙)/孤儿(灰)；审修按钮**仅在 待修/断链 亮**。
- 海螺姑娘**升格**为全局资产管理平台（mission 重定义 + 真新代码）。
- 数灵转移在 manifest 中标 **archived**（已退役为文档记录）。
- 实时性现实形态：conch 全局盘点（定时任务/手动）回写 manifest → 看板读快照 + Reload；**非页面级秒级实时**（artifact 沙箱不能直接读本地 .db）。

**任务规模估算**：
- 预计涉及文件：~6–9（`conch_engine.py` 加全局盘点命令；新 `data/asset_manifest.json` schema；新看板 artifact HTML；海螺姑娘 `系统概览.md`/`决策记录.md`；可能 `.conchconfig.yml` / 探活定时任务 SKILL）
- 预计耗时：跨小时（多组件）
- 涉及项目：海螺姑娘（主·写）+ 被盘点的 14 项目（只读）

---

## §二 · 交付标准（Acceptance Criteria）

> CC 填写规则：`[ ]` 未开始 / `[?]` 已达成+证据 / `[!]` 未达成+原因 / `[~]` 不确定+需 Doctor 判断；`[✓]` **只能 Doctor 打**。没证据=没填。

### A. 文件层面

- [ ] `conch_engine.py` 新增全局盘点子命令（如 `survey`）：`python3 ~/Documents/Claude/Projects/海螺姑娘/conch_engine.py survey` 退出码 0
  - 证据栏：
- [ ] 资产 manifest 落于 `~/Documents/Claude/Projects/海螺姑娘/data/asset_manifest.json`，且 `python3 -c "import json;json.load(open(...))"` 解析通过
  - 证据栏：
- [ ] 看板 artifact HTML 建于 `~/Documents/Claude/Artifacts/{新id}/index.html`，且 `list_artifacts` 能查到该 id
  - 证据栏：
- [ ] 海螺姑娘 `architecture/系统概览.md` 的「项目使命」段已填为「全局资产管理平台」（非空占位）
  - 证据栏：

### B. 一致性层面

- [ ] `Grep -i "VV" asset_manifest.json = 0 matches`（不含 VV 任何资产）
  - 证据栏：
- [ ] manifest `nodes` 覆盖项目总览全部 14 项目（逐一项目名可在 nodes 查到）
  - 证据栏：
- [ ] manifest 中 `group == "Infrastructure"` 的 project 节点**恰为** {海螺姑娘, 司南}
  - 证据栏：
- [ ] 数灵转移节点 `status`/`group` 标记为 `archived`
  - 证据栏：
- [ ] manifest `nodes[].kind` 取值 ⊆ {`resource`,`database`,`project`}（无其它值）
  - 证据栏：

### C. 功能层面

- [ ] 看板打开后渲染出三类节点 + Infrastructure 子组，且**上下游关系线可见**
  - 证据栏（CC 截图自检 / 否则 `[~]` 标 Doctor 测）：
- [ ] 五态着色可辨（legend 含 健康/陈旧/断链/待修/孤儿 五项）
  - 证据栏：
- [ ] 点击任一节点弹出详情卡（含 概要/状态/上下游/最近活跃/已知问题）
  - 证据栏（`[~]` 可能需 Doctor 测）：
- [ ] 「审查并修复」按钮**仅在 待修/断链 节点出现**（代码可查渲染条件）
  - 证据栏：
- [ ] 点击「审查并修复」→ 触发 `conch ... scan`（dry-run）并返回分级方案卡；按钮链路**不含 `archive --apply`、不改任何资产**（代码可查 + conch 默认 dry-run）
  - 证据栏：

### D. 自审层面

- [ ] 若分段自主执行：self-audit 段 + checkpoint 文件已落（路径列出）
  - 证据栏：

### E. 沟通层面

- [ ] 涉 git（海螺姑娘项目仓 / brain 仓）→ commit 命令**贴给 Doctor 终端**，未在沙箱跑 git 写
  - 证据栏：
- [ ] 看板 create_artifact 注册后，已 present / 告知 Doctor 打开方式
  - 证据栏：

### F. 任务专属（海螺姑娘红线）

- [ ] 「审查并修复」按钮链路**永不自动 `--apply`**；任何 archive/repair 的 `--apply` 仅 Doctor 显式批准后单独跑
  - 证据栏：
- [ ] 状态/新鲜度判定**表级/字段级**（非仅文件 mtime · G-X24）；manifest `status` 字段带可追溯判定依据
  - 证据栏：
- [ ] conch `survey` **只回写 node 健康/新鲜度，不臆造 `edges`**（edges 为手工策展真源）
  - 证据栏：
- [ ] 未改动现有 `brain-vault-dashboard` artifact（新建独立 id；`git diff` 该文件 = 空 / 看板 list 中两个独立 id）
  - 证据栏：

---

## §三 · 非交付项（本任务不做）

1. **不做一键自动修复**——conch 只 dry-run 出分级方案，`--apply` 一律待 Doctor 批准。
2. **不做全自动上下游关系检测**——`edges`（调用/消费/反哺）手工策展进 manifest；conch 不产关系。
3. **不保证页面级秒级实时**——走 conch 全局盘点（定时/手动）回写 manifest + 看板读快照 + Reload。真·页面级实时待有可读本地资产的 MCP 另议。
4. **不含 VV 任何资产**。
5. **不改现有 `brain-vault-dashboard`**（新建独立 artifact）。
6. MVP 不含：历史趋势/多期对比、跨设备同步、访问权限、关系线自动校验。

---

## §四 · 验收与状态

- 验收人：Doctor。验收方式：§二 逐条核（A/B/F 命令可验；C 部分需 Doctor 在 Cowork 打开看板目视/点击测）。
- 关闭条件：§二 全 `[✓]`（Doctor 打）或 Doctor 显式取消。**CC 不自动关闭、不打 ✓。**
- 执行后、交验收前：派**独立审查子 agent**（句芒 project-review / 干净 Task）对照 §二 复核（只读产出物），再交 Doctor 终审。

---

## §五 · 备注

- conch 现有 CLI：`scan / archive(--apply) / detect / explain / init / rollback`，`--scope` 指定目录。全局盘点拟新增 `survey`（遍历项目总览各 `code_path` 跑 detect/健康聚合 → 写 manifest）。
- 渲染复用 `brain-vault-dashboard`：spokes/edges/feedback、`node_meta`、`colorKind`、源对象+freshness 环、详情卡均可移植；五态需在现有 broken/stale/orphan 基础上补「待修(橙)」+ 健康/陈旧拆分。
- 关系线 manifest 的策展负债（会漂）→ 靠 conch survey 定期回写**节点健康**降噪，但 edges 仍需人工维护（记入海螺姑娘 GOTCHAS）。
