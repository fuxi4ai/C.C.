---
title: PRD · 驾驶舱接入回调级别读数（全局横幅·纯展示）
tags: [prd, acceptance, 龙鱼五力, 剑酒青丘, 烛照九阴]
created: 2026-07-19
updated: 2026-07-19
status: 进行中
doctor_decision: 待
type: prd
project: 龙鱼五力(落地) × 剑酒青丘(读数来源) × 烛照九阴(数据源)
template_version: v1.0
---

# PRD · 驾驶舱接入回调级别读数

## §一 · 任务目标

**动机**：回调级别判别器（nowcast L1/L2/L3）已于 2026-07-18 接入烛照九阴日报并验收（9/9，2026-07-19 Doctor 批）。当时明确列为**非交付项**的「接驾驶舱」现由 Doctor 拍板启动。影子驾驶舱看的是**标的级**三时钟，缺一条**市场级**的环境读数——同样是「限制新仓」，发生在 L1 平静期和 L3 急跌期，含义完全不同。

**范围**：在 `longyu-holdings-board` artifact 第二标签页「影子驾驶舱」**顶部**加一条全局状态横幅，显示当前回调级别 + 确认态 + 最近邻 + 同级历史中位。数据经 `get_cockpit_payload` 新增顶层键 `market_grade` 下发。

**Doctor 原始指令**（逐字引用）：
> 「3、起草」（对「级别读数是否接驾驶舱」）
> 「全局横幅·纯展示」（对「级别读数在里面扮什么角色」）

**关键边界（本 PRD 的核心约束）**：级别读数是**市场级**，驾驶舱是**标的级**，粒度不同。故本版**只做展示，不介入任何标的级判断**——不进 `compile_action`、不改三时钟、不做第四只时钟。依据：纪要 §一 将级别判别器定性为**弱信号 risk-overlay、非 alpha**，`auto_trade_allowed: false`；且 L3 事件级样本 n=13、本轮 episode 尚未完结，不具备门控真实行动的成色。

**任务规模估算**：
- 预计涉及文件：修改 3（`cockpit_data.py` 加 `market_grade` 组装、`longyu_holdings_mcp.py` 透传、artifact index.html 加横幅），新建 0，新建 DB 0，新建定时任务 0
- 预计耗时：1 个工作窗口 + Doctor 重启 Desktop 一次 + 手测
- 涉及项目：龙鱼五力（落地）/ 剑酒青丘（`adjustment_grade.py --json` 来源）/ 烛照九阴（`market_data.db` 只读）

---

## §二 · 交付标准（Acceptance Criteria）

> ⚠️ CC 填写规则：`[?]` 已达成+证据 / `[!]` 未达成+原因 / `[~]` 不确定+需 Doctor 判断的具体点。
> **`[✓]` 只能 Doctor 改**。没证据 = 等于没填。

### A. 数据层（payload）

- [ ] A1 `build_cockpit_payload()` 返回值新增顶层键 `market_grade`，与 `clocks`/`cases` **平级**（体现市场级 vs 标的级的分层）
  - 证据栏（CC 填）：
- [ ] A2 `market_grade` 字段齐备：`level` / `verdict` / `confirm` / `nn`（最近邻）/ `hist`（同级已完结 episode 中位）/ `asof` / `source`
  - 证据栏（CC 填）：
- [ ] A3 读数经 `adjustment_grade.py --json` 取得，**不重新实现判定逻辑**；Grep `cockpit_data.py` 中 `S2|S3|slow|verdict =` 的赋值 = 0 matches（除透传外无本地判定）
  - 证据栏（CC 填）：
- [ ] A4 **降级安全**：`adjustment_grade.py` 不可用 / 超时 / 返回非法 JSON 时，`market_grade` 返回 `{"level": None, "reason": "<原因>"}`，`build_cockpit_payload()` **其余键照常返回**、不抛异常
  - 证据栏（CC 填 · 须实测三种失败态，不能只写"已做 try"）：

### B. 隔离层（本 PRD 的硬约束）

- [ ] B1 `compile_action()` 函数签名与函数体**零改动**（`git diff` 该函数 = 0 行）
  - 证据栏（CC 填）：
- [ ] B2 Grep `market_grade` 在 `compile_action` / `info_clock` / `price_clock` / `vol_clock` / `build_rows` 五个函数体内 = **0 matches**
  - 证据栏（CC 填）：
- [ ] B3 同一 `asof` 下，接入前后 `clocks` 数组逐行全等（`action` / 三时钟 / `changed` 均不变）——用接入前的 payload 存基线再比对
  - 证据栏（CC 填 · 须贴 diff 结果，不能只写"没变"）：
- [ ] B4 `cockpit.db` 三表（case / clock_state / oos_ledger）schema 与行数不变
  - 证据栏（CC 填）：

### C. 展示层（artifact）

- [ ] C1 驾驶舱标签页顶部渲染横幅，含级别徽标 + 确认态 + 最近邻 + 同级历史中位；`level` 为 None 时显示「级别读数不可用」且**不破版**
  - 证据栏（CC 填 · 两态都要截图或 DOM 断言）：
- [ ] C2 横幅文案含**市场级**限定语（如「市场环境」），使读者不会误读为某标的的状态
  - 证据栏（CC 填）：
- [ ] C3 横幅**不含**任何买卖指令性措辞；禁词表 `买入|卖出|加仓|减仓|止损|目标价` 在横幅 DOM 内 = 0 matches
  - 证据栏（CC 填）：
- [ ] C4 第一标签页（持仓看板）渲染零改动；`build_dark_board.py` 未被本次改动触碰
  - 证据栏（CC 填）：

### D. 一致性 / 自审

- [ ] D1 `py_compile` 通过（`cockpit_data.py` + `longyu_holdings_mcp.py`）
  - 证据栏（CC 填）：
- [ ] D2 G-X1 self-audit 段已写（含：有无跨级升格、有无枚举外状态、有无动非交付资产）
  - 证据栏（CC 填）：
- [ ] D3 改动前已备份 artifact 现版（`index.bak.<timestamp>`），与 0718 部署惯例一致
  - 证据栏（CC 填）：

### E. 沟通 / 部署

- [ ] E1 MCP 工具改动后需 Doctor **重启一次 Desktop**——已在交付回报中明确告知
  - 证据栏（CC 填）：
- [ ] E2 `update_artifact` 推送成功，Doctor 实机可见横幅
  - 证据栏（CC 填 `[~]` · CC 无法代替 Doctor 观察，须 Doctor 确认）：
- [ ] E3 git commit 命令已贴给 Doctor（龙鱼库 + brain 两仓；遵守 G-X2，CC 不在 sandbox 跑 git 写命令；**先按 v2.8.1 探测半开状态**）
  - 证据栏（CC 填）：
- [ ] E4 /save 已触发 + 落 brain/logs/
  - 证据栏（CC 填）：

---

## §三 · 非交付项（范围排除）

- **不参与行动权限门控**：`market_grade` 不进 `compile_action`，L3 不会把任何 Case 推向「限制新仓/检查减仓」。理由见 §一 关键边界——Doctor 2026-07-19 明确选「纯展示」。
- **不做第四只时钟**：三时钟为标的级、级别读数为市场级，混入会破坏坐标系；且三时钟口径已冻结，动它要重开已实证规则。
- **不写入 cockpit.db**：市场级读数不属于 case 台账，P0 三表 append-only 语义不变。（未来若要做「宣 L3 时各 Case 的前瞻表现」回溯，另立 PRD 设计独立表——本版不预写，G-X10。）
- **不改 `adjustment_grade.py` 的判定逻辑**：本版只消费其 `--json`，一行不动。
- **不改第一标签页**（持仓看板）。
- **不接日报**：日报已于 0718 接入并验收，本 PRD 与其独立。

---

## §四 · 状态

- [x] 进行中（2026-07-19 立 PRD · Doctor「起草」+「全局横幅·纯展示」）
- [ ] 待 Doctor 验收（CC 已对所有交付标准填三态 · 时间：____）
- [ ] 已交付（Doctor 已把所有 `[?]` 转为 `[✓]` · 时间：____）
- [ ] 已取消（时间：____ · 原因：____）

**关闭路径**：全 ✓ 或 Doctor 显式取消。**CC 不自动关闭、不自打 ✓**。

---

## §五 · 变更记录

- 2026-07-19 CC：立 PRD · 含 17 条交付标准（A4 / B4 / C4 / D3 / E4）· **起草≠开工，等 Doctor 批准 §二 后才动第一行代码**

---

## §六 · 待 Doctor 在开工前定的两点

> 这两点不定也能开工，但定了返工少。CC 给推荐+理由，不选边。

1. ~~**横幅的视觉范式**——驾驶舱是暗色看板，日报温度卡是暖色卡片页，两套色系。~~
   **2026-07-19 CC 自我更正：前提写错了。** `longyu-holdings-board`（含影子驾驶舱）根变量是 `--bg:#f5f4ed`，与烛照九阴日报**逐字相同**，是同一套暖色卡片页设计系统。CC 把它与 `longyu-stock-library`（`build_dark_board.py` 产的**暗色标的库看板**）搞混了——那才是暗色的。
   → **本项作废，无需 Doctor 裁定**：既然同色系同 token，**直接复用日报温度卡的视觉范式**（徽标/字号/间距全套），不是"跨色系搬样式"而是同系统内复用。
2. **横幅在 L1（平静）时是否隐藏**。
   *推荐*：**不隐藏，常驻**。理由：横幅的价值一半在「现在是 L3」，另一半在「现在**不是** L3」——只在坏消息时出现的指示器，会让人把它的出现本身当成信号，且平静期看不到它就无法确认它还活着（同 G-X8「已落盘假象」的反面：常驻＝可证其在工作）。
   → **2026-07-19 Doctor 定：同意常驻。**
