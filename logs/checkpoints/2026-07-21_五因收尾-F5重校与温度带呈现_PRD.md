---
title: PRD 2026-07-21 — 五因风险温度·收尾（F5 重校 + 温度带呈现 + 清过期标签）
tags: [PRD, 烛照九阴, 剑酒青丘, 五因, S2, 收尾]
created: 2026-07-21
updated: 2026-07-21
status: 开工完成·待Doctor验收
type: checkpoint
project: 烛照九阴 × 剑酒青丘（S2 计温线）
---

# PRD · 五因风险温度收尾

**承接**：Doctor 0719 裁定「仅 F4 有意义…需挖因子、改 function」。经本场盘点确认：**该裁定的两条线已被 S2 工作部分推进**——function 已重构为「触发层(F4/F5)×环境层(F1/A6/B6)→三态」（`resolve_temp` 单一真源），挖因子已产出 A6/B6（lift 2.06/2.20）。故本轮**重心＝收尾**（Doctor 本场拍板「收尾派」），不再改 function 结构、不新挖因子。

## 目标

把 S2 计温线从「函数已重构、但校准与呈现有历史残留」收敛到**口径自洽、呈现诚实、可对外**的状态。

## 范围

**In（本轮做）**
1. **F5 两腿重校**：债腿降信息层后，重跑校准取「油价腿 + F5 整体（两腿）」新 lift，清作废的三腿口径（1.46/76 触发）。
2. **温度带呈现＝维持三态 + 标 k/n 证据**（Doctor 本场拍板）：三态旁证据一律 S2 grid 的 k/n 形式，禁精确百分比（PRD-S2 A4），标「样本薄·预注册中」。
3. **清过期三腿标签**：`3.2%/4.4%/9.7%` 三态↔冰点率映射标签（三腿口径，已失效）从 config + 生产文案清除/替换为 k/n。

**Out（本轮不做，另立）**
- 不改 `resolve_temp` 三态结构（认 S2 为终态）。
- 不新挖触发层第 3 事件因子（机理起点法另立项）。
- 不动 F2 容量腿重建、F3、驾驶舱接级别读数（各有独立 TODO/PRD）。

## 交付标准（可验证 · CC 不打 ✓，全留待 Doctor 逐条验收）

**D1 · F5 重校**
- [ ] 跑 `calibrate_risk_factors.py`（**债腿 `bond_scoring=false` 生效口径**），产出：油价腿单独 lift（复核 5% 档）+ F5 整体（两腿 OR）新 lift + 新触发事件数 n。
- [ ] `config/risk_factors.json` f5.calibration 更新为两腿新口径；**删除**「n=610/76触发/lift 1.46」作废行（或明确标注「已作废·见新值」）。
- [ ] `_meta.calibration_note`（line 15）里「F5 外部(lift 1.46…)」同步为新值或标注作废。
- [ ] 新 lift 的样本量 n 与「可信门槛」如实标注（若两腿 n<20 需标薄样本，不得渲染为确证）。

**D2 · 温度带 k/n 呈现**
- [ ] 生产日报温度带三态旁证据全部 k/n 形式（触发×环境 grid：如 ≥1盏=3/21、2盏=3/10），**无任何精确百分比**渲染到用户可见层（PRD-S2 A4）。
- [ ] 「样本薄·预注册中」注记在场。
- [ ] 仅改 S2 活跃路径；**v1 回滚分支不动**（gen_daily_report.py:2093 等 v1 注释保留）。

**D3 · 清过期标签**
- [ ] `config` f1.desc/f1.calibration/_temp_note 里的 `3.2%/4.4%/9.7%` 三态冰点率标签：替换为 k/n 或标注作废口径。
- [ ] **甄别保留**：F1 自身调节效应证据（n=31/lift 2.68）属因子层、非温度带标签——保留但同转 k/n 或标样本量，不与三态标签混。
- [ ] 生产文案（gen_daily_report.py 活跃路径）无残留过期百分比标签。

## 验收方式

1. **口径自洽**：grep 全 config + gen_daily_report 活跃路径，无 `1.46`/`76触发`/`3.2%/4.4%/9.7%` 作为现行结论出现（作废标注除外）。
2. **端到端**：重生成一次日报，温度带证据全 k/n、无精确百分比、样本注记在场。
3. **过七问**（剑酒青丘回测设计七问）：F5 两腿新 lift 的报告让读者不查代码即可答第 1/2/3/6 问（可评vs未触发、基准同源、观测单位、控行情增量）。
4. **可回滚**：所有生产改动 .bak 就地；v1 分支未触；config 改动可单行还原。

## 硬约束 / 流程

- **PRD 批＝批方向**；逐文件 diff 仍需二次确认才动手（propose-then-confirm）。
- F5 重校若需跑 calibrate：**构造命令交 Doctor 终端**（或只读 device_bash），CC 不在沙箱触网/跑写命令。
- 金融生产禁占位数据（G-X77）：重校若某腿数据不足，标「样本薄/数据缺失」，不硬凑 lift。

## 未决 / 风险

- F5 两腿重校后**可能 lift 掉到弱信号甚至无区分力**（债腿本在稀释、但油价腿单独 1.74 尚可）——若 F5 整体转无效，触发层将实质只剩 F4，届时需 Doctor 再裁 F5 是否降信息层（**这是本轮可能触发的下一个裁定点**，不在本 PRD 内解决，如实上报）。
- 样本普遍薄（触发×环境 grid 3/21、2盏 3/10），一切结论 k/n 呈现、预注册中，不作确证。

## 相关

- [[2026-07-19_S2计温function重构_PRD]]（本 PRD 的上游·14 条验收待排）
- [[通用教训]] G-X76（回测七问·多想一步）· G-X77（禁占位数据）
- [[剑酒青丘/frameworks/回测设计七问]] · config/risk_factors.json · tools/{risk_function,calibrate_risk_factors,gen_daily_report}.py

## 落地记录（2026-07-21 · CC 开工完成 · 交付框待 Doctor 验收，CC 不打 ✓）

- **D1**：`config/risk_factors.json` f5.calibration 换两腿新口径（lift 2.22/9事件/样本不足门槛·方向真·油价腿5%单调·真支撑长样本）；_meta.calibration_note 追加两腿更新 + 0717 注 inline 作废指针。作废的 1.46/76触发 现仅存作废语境。就地 .bak_20260721-f5recal。
- **D2**：温度带渲染经查**本已达标**（k/n·样本薄·预注册中·无渲染精确%）；仅刷新 s2.evidence_resonance 3/21→3/22、evidence_note 换 2026-07-21 grid。
- **D3**：报文经查**本已干净**（grep 命中全假阳性）；过期 3.2/4.4/9.7% 仅在 _temp_note 内部注，已追加作废标记；f1.desc/calibration 的 9.7/4.4% 甄别为 F1 调节效应证据(n=31)、保留。
- **未触发降层裁定点**：F5 两腿 9 事件<门槛但油价腿单调+长样本支撑，CC 判 **F5 维持计温、不降层**；已如实上报待 Doctor 认可（见"未决/风险"）。
- **待 Doctor 验收**：① config 口径自洽（1.46/76触发 仅作废语境 ✓ 已核）② 端到端 regen 看 F5 新值+温度带 k/n ③ F5 两腿报告过七问 ④ 可回滚（.bak 在）。
