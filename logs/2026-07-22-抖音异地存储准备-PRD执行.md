---
title: 会话日志 2026-07-22 — 抖音视频异地存储准备（查重优化+补跑+介质无关外移准备层）PRD 执行
tags: [log, DVA, 抖音, 异地存储, 查重, 补跑, PRD, resolver, manifest]
created: 2026-07-22
updated: 2026-07-22
status: active
type: log
project: DVA
---

# 会话日志 — 2026-07-22（抖音异地存储准备 · PRD 执行）

**项目**：DVA（抖音视频链路）+ Database/Douyin（数据层，只读消费 + 产物落 DVA-ops）
**主题**：Doctor「对 douyin 视频异地存储做准备、优化查重、补跑逻辑」→ 立 PRD → 执行三块工具 → 独立复审过 → 待 Doctor 验收

---

## 完成的工作

- **对齐 + 立 PRD**（brain-prd）：四问确认「立 PRD / 介质无关准备层 / 工具齐活零外移零计费 / 重跑定性+今后自描述」。PRD 落 `logs/checkpoints/2026-07-22_抖音视频异地存储准备_PRD.md`。
- **摸清现状（实测）**：视频 1556 个 / 66.6GB；转写 1455；**0 字节转写 14 条**（11 SansanYe/2 老石谈芯/1 AI个体指南）；`dy_downloader.db` 1470 行 `file_path` 全为 Mac 绝对目录路径（外移即失效）。
- **A 查重内容感知**（改 `Projects/DVA/dyd/dva_asr.py`）：新增 `_transcript_complete()`——0 字节且无 `asr_status:empty` 标记 = 失败残留、判未完成须重跑（治 E1 假完成）；`_write_transcript_outputs` 空结果也写 json（`asr_status:"empty"`），使 0 字节自带定性。
- **B 三方对账 + 补跑清单**（新 `DVA-ops/reconcile_douyin.py` + 公共库 `dva_ops_common.py`）：aweme_id 为轴 join 视频/字幕/DB；实测 needs_rerun 14 / missing_transcript 119 / backfill **133=14+119**，`--force` 仅 14 条，sec_uid 从 `_data.json` 全补齐。**只出命令不实跑**。
- **C 介质无关准备层**：`build_manifest.py`（1556 行、rel_path 相对无绝对）+ `resolver.py`（aweme_id→hot/offsite/missing，取代 DB 绝对路径，`--self-test` PASS）+ `offsite_migrate_dryrun.py`（**结构无执行分支**、未转写标红拒绝、`_FORBIDDEN` 拒危险 flag、磁盘零变更）+ `README_异地存储准备.md`（外移前置铁律 F4 + 可逆顺序 F5）。
- **验证**：6 个 .py py_compile 全过；5 新增文件 grep `/Users/lunarabbit` = 0；F1 单测 4 态全对；resolver 自测三态 PASS；dry-run 前后 `Downloaded` 字节一致。
- **独立复审（Step 7.5）**：干净子 agent 逐条复跑，全部真成立，无完成幻觉/漏项；1556/14/133 经独立 `find -size 0` 旁证；未打 ✓。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| MVP = 工具齐活·零外移·零计费 | 67G 搬动 + ASR 计费不可逆，先让工具验稳 | 14 空壳重跑 / 补跑 / 外移全出命令，Doctor Mac 侧跑 |
| 幂等锚真相源 + 内容感知（非文件存在性） | 0 字节 txt 真静音 vs 失败不分是 E1 根因 | 14 条被正确判为 needs_rerun，进补跑清单 |
| resolver 取代 DB 绝对 file_path | 绝对路径外移即失效 | 位置按 aweme_id+rel_path+根变量解析，介质无关 |
| 外移前置铁律：未转写不可外移 | 外移后 ASR 拿不到 mp4 就补不了 | dry-run 对 133 未转写标红拒绝，仅 1423 可外移 |

## 遗留问题 / 待办

- [ ] **工具落点待拍板**：`DVA-ops/` 非 git 仓 + FUSE 写（durability 风险），建议迁 `Projects/DVA/tools/`（git 版控、Mac commit 落 durable），数据/产物仍留 `DVA-ops/state/`。待 Doctor 定后再出最终 git 命令。
- [ ] **权威补跑清单/manifest 须 Doctor Mac 跑**：sandbox 产物含 sandbox 绝对路径，仅供验证；Mac 侧 `reconcile_douyin.py`/`build_manifest.py` 重跑才得正确路径。
- [ ] 14 空壳 `--force` 重跑 + 119 缺转写补跑（会计费）→ Doctor Mac 跑 `backfill_cmds_*.sh`。
- [ ] 实际外移 = 另立 PRD + Mac 脚本落地（本准备阶段不含）。
- [ ] PRD `[✓]` 仅 Doctor 落笔；两处 `[~]` 待 Doctor 判定。

## 相关笔记

- [[2026-07-22_抖音视频异地存储准备_PRD]]（checkpoints）
- [[DVA/GOTCHAS]]（E1 空转写假完成 · BUG-20260610-002/INFRA-20260521-003 截断丢 id 家族）
- [[全局偏好-Settings镜像]]（propose-then-confirm + AskUserQuestion 拍板 + 不在沙箱跑 git/ASR + 每轮开声照做）

---

## 增补（2026-07-23 · 转写策略层 + 终态）

- Doctor 验收「批过」→ PRD `[?]`→`[✓]` 代录（§五归属块）。
- Doctor 指出常更表有「无需转字幕」的号 → reconcile 原按文件缺失虚报 119。增强：`dva_ops_common` 加 watchlist(mode)+analyze-ignore 策略层；reconcile/manifest/offsite 全接入。
- **终态**：Doctor 已 Mac 侧跑完 14 条 needs_rerun 重跑、**全 true_silent**（F1/F2 真实 ASR 验证通过）；策略生效后 needs_rerun=0/missing=0/excluded 119（麦橘94 download-only + 独夫14 ignore + 范围外11）。
- **外移解锁**：dry-run 由「拒133」→「1556 全可外移·66.6GB·0 拒绝」。
- 待 Doctor：DVA 仓 commit tools 增强；工具 mv 已由 Doctor 完成（tools/ 在 git）；实际外移另立 PRD。
