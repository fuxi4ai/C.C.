---
title: 会话日志 2026-09-26
tags: [log, 渊图, 全局偏好]
created: 2026-09-26
updated: 2026-09-26
status: active
type: log
project: 跨项目（brain 全局偏好 + 渊图）
---

# 会话日志 — 2026-09-26

**项目**：跨项目（brain 全局偏好 + 渊图）
**时长**：约 4 小时（05:00–10:00 PT）
**主题**：接手「todo处理」场 · 分轨措辞改写 · 问答板改名 QA

## 完成的工作

**A · 接手「todo处理」会话（local_2d73f575）**
- 该会话因上下文超 1M，每轮 `400` 中断而卡死；实为**渊图同名实体簇归并的收尾场**（非普通 TODO 分流）。
- 独立重放 `surgery_xiazhi_merge_20260926` / `surgery_xiandao_merge_20260926` 两件手术：变更面逐条一致、零夹带、墓碑可单独复原、不变量全 0、残余重复簇 = 1（Coherent/Finisar 有意共享）。
- 认定**三项实错**（均由这两件引入）：① `wiki/firstrarematerials.md` 活跃悬空卡未弃用；② `company_Xiazhi` 某 props 值含 markdown `**`（全库唯一）；③ `company_XiandaoGroup.description` 尾带空格（全库唯一）。
- 修毕 + 建**可重放复验脚本** `outputs/verify_xiazhi_xiandao_replay_20260926.py`（30 项硬检查，修前 24/26 exit 1 → 修后 30/30 exit 0）。
- 治理留痕落 `brain/渊图/GOTCHAS.md`（ERR-20260823-001 追记，含陈旧计数订正）。

**B · 全局 Settings「分轨」措辞改写（Doctor 令：「落签归Doctor」「不自签」→ 路由式）**
- **四轮合计 83 处 / 26 件**：注入层 14 处 · 治理活文档 18 行 · 补漏 38 行/19 件 · 外科式收尾 13 处/9 件。
- 反向「退」：按 Doctor 裁「历史记录不改」，回退**越界改写的记录 17 行 / 7 件**（批次终结/最后更新/实施留痕/归档件）。
- 每轮均派**未参与实施的 subagent 独立复验**；复验逮出并修掉：3 处坏句、2 处自相矛盾、1 处 PRD 域口径对撞、1 个基线 bug、4 处注记不实。

**C · skill 发布与改名**
- `brain-prd`：description 尾句去死路式、明标「PRD 域属签字分轨的例外」；三层同步发布。
- `问答板` skill：**机器名 `wenda-ban` → `QA`**，全链改（真源目录 / `.skill` 包及其内层目录名 / README / 已装清单 / 记忆索引与正文），旧包弃用改名、旧运行层目录移入隔离区。
- 运行层实证：`skills/` 只剩 `QA`，与 canonical **行级零差异**，canonical 与包内 SHA 全等 `ebe0935b`。

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 加「默认动作」而非重复强调规则 | 该规则原已装三处却仍失效——症结是**只有禁止、没有下一步动作**（「不自签」被读成「交回 Doctor」） | Settings 块一/块二共 14 处改写；后由 Doctor 追加「直接替换原句」 |
| 历史记录一律不改 | Doctor 裁：不进开局上下文、只作追溯记录的，改了就是篡改记录 | 回退 17 行；此后判据统一为「进不进注入层」 |
| PRD 域不插路由式，改标「例外」 | PRD 域是分轨的例外，插路由式＝扩权 | brain-prd description 改法定型 |
| 机器名/目录名/包内层目录名一律 ASCII `QA` | 实测确立：**清单显示名由【目录名】决定**，非 frontmatter 的 `name`；非 ASCII 目录名被 slug 成 `---` | 根治 `---` 残影与重复注册 |
| 三条一次性脚本不重跑 | 脚本按固定串替换，重跑会改写自己写的改动记录（非幂等） | 已标注为一次性 |

## 遗留问题 / 待办

- [ ] **下个新会话验证窗口**：① 技能清单应只剩 `anthropic-skills:QA`（`---` 与 `wenda-ban` 应消失）；② Settings 重贴的逐行 diff（本场注入定格，核不了）
- [ ] `.skills/` 现留 4 个备份/弃用件（`QA.skill.bak_*`、`_DEPRECATED_问答板.skill_*`、`问答板.skill.bak*`）——**有意未清，可逆优先**，Doctor 择时定
- [ ] 隔离区两条旧运行层目录（`~/Library/Application Support/Claude-3p/skill-quarantine-20260926/`）处置权在 Doctor
- [ ] `brain-permanent` 侧注记里早期对**行号/行数**的引述已过期（改多次），引用请以描述为准（注记内已标）

## 相关笔记

- [[全局偏好-Settings镜像]] · [[Doctor协作偏好]]
- [[渊图/GOTCHAS]]（ERR-20260823-001 追记）
- [[经验库]] · [[通用教训]]
