---
title: 会话日志 2026-05-14 — 凭证治理 · 清单建立
tags: [log, brain, credentials, security, governance]
created: 2026-05-14
updated: 2026-05-14
status: active
type: log
project: Brain Vault
---

# 会话日志 — 2026-05-14 · 凭证治理 · 清单建立

**项目**：Brain Vault 基础设施
**主题**：凭证管理审计 + 清单总账 + 元数据补全

---

## 审计结论

**防御 ✓ 治理 ✗** —— gitignore 排除完整、git 历史无完整 key 泄漏、工作树干净，但缺失：
- 统一凭证清单（不知道有几把钥匙）
- 凭证元数据（创建/轮换/暴露/责任人）
- 暴露事件登记机制
- 各项目"外部依赖"段（key 用在哪不明）

DeepSeek key 暴露事件（前一日）状态未闭环。

---

## 完成的工作

### 1. references/凭证清单.md（新建）
- 4 条现役凭证入档：DeepSeek / GitHub PAT / Tushare / DashScope
- 存放位置约定段：项目专用 → `.env.{用途}` / 系统级 → Keychain
- 暴露事件登记 E-001：DeepSeek key（Doctor 决策"接受风险，不轮换"，理由预付费余额低）
- 审计节奏表（周/月/季）

### 2. .tools/.env.graphify header 元数据
- service / scope / created / last_rotated / next_rotation / exposure / inventory 7 字段
- 轮换操作指引内嵌

### 3. 各项目"外部依赖"段补齐
- `DVA/系统概览.md`：DashScope ASR + 已废弃豆包
- `渊图/系统概览.md`：Tushare（独立查询不入图谱）
- `龙鱼五力/系统概览.md`：Tushare + 渊图调用
- 其他 5 项目（O MY HTML / Optical communication / 司南 / PEC / 海螺姑娘）确认无外部 API 依赖，骨架"上游/下游"段保留待填

---

## 做出的决策

| 决策 | 内容 |
|------|------|
| DeepSeek key 处置 | **不轮换**，接受风险（预付费 / 余额低 / 截断前缀无害） |
| 凭证清单形态 | 单文件总账 `references/凭证清单.md`，元数据 only，不存 key 本体 |
| 存放分层 | 项目专用 → `.env.{用途}` / 系统级 → macOS Keychain / 共享 → 1Password 可选 |
| log 里 sk-3fe87... 截断前缀 | **不清理 GitHub remote 历史**，8 位前缀危害极低 |

---

## 遗留问题 / 待办

待 Doctor 确认/补充：

- [ ] **Tushare token 存放位置**确认 → 更新清单 #3 的"存放位置"列（推测 `~/.zshrc`）
- [ ] **DashScope key 存放位置**确认 → 更新清单 #4 的"存放位置"列
- [ ] **GitHub PAT 创建日期 / 下次轮换**精确化 → 当前填的 2026-05-14 / 2026-11-14 是估算

可选后续工程化（未实施，需要时再启）：

- [ ] `.tools/audit-secrets.sh` 周期审计脚本（rgrep 高熵 + git history 扫描）
- [ ] `.tools/rotate-key.sh` 轮换流程模板
- [ ] 凭证健康加进 `brain-vault-dashboard` artifact

---

## 已知坑

- 沙箱无法删除 `.git/index.lock`（Operation not permitted）→ 本次 commit 需 Doctor 在 macOS 终端执行
- 沙箱 git 不认 macOS 全局 alias `git unlock`，需用本机 git

---

## 相关笔记

- [[凭证清单]] — 本次产出主件
- [[2026-05-14-graphify-接通-DVA-渊图]] — DeepSeek 暴露事件原始记录
- [[备份策略-20260514]] — 凭证文件不进 git，但要进备份策略
- [[graphify-集成-20260514]] — 现役 DeepSeek key 使用上下文

---

## 追加：E-002 DashScope 硬编码（同日下午发现）

为补 Tushare/DashScope 存放位置扫项目时，发现：

- **Tushare** ✓ 治理良好——通过 `~/.tushare/token` 约定路径读取，env 备选
- **DashScope** 🔴 严重——`DVA/dyd/dva_asr.py:54` 把完整 key 写成 `os.getenv("DASHSCOPE_KEY", "<完整key>")` 的 default fallback；`DVA/dyd/config.yml` 也明文存储

DVA 项目本身**不是 git 仓库**，未 push 到 remote，所以未公开泄漏。但：
- 硬编码 default 违反"凭证与代码分离"原则
- 任何 backup/同步操作会捎走 key
- 本次审计扫描时 key 字面值进入了 Cowork 工具结果（一次性历史）

清单 #4 标红，E-002 已登记，整改方案待 Doctor 三选一：
- A · 立即轮换 + 重构（删 fallback、config 用 `${DASHSCOPE_KEY}` 占位）
- B · 接受风险（同 E-001，前提是预付费小额）
- C · 仅重构不轮换（最低成本去硬编码）

**复盘新增**：`os.getenv("KEY", "明文")` 是反模式，应一律改为 `os.getenv("KEY") or sys.exit("缺 KEY")`。
