---
title: 会话日志 2026-09-22 — DVA自愈链诊断与三条教训
tags: [log, DVA, brain基建]
created: 2026-09-22
updated: 2026-09-22
status: active
type: log
project: 跨项目（DVA 自愈链 · brain 教训库 · TTS 桥）
---

# 会话日志 — 2026-09-22（承 09-19 凌晨 /resume 场，跨三日）

**项目**：跨项目（DVA 自愈链 · brain 教训库 · TTS 桥）
**主题**：DVA 自愈链全链诊断（消费者定位 → 三次误判更正 → 两个真根因）· 三条通用教训 · TTS 落点收口
**时长**：09-19 02:05 PT 起手 → 09-22 02:47 存档，中间断续推进

---

## 完成的工作

### 一、TTS 桥落点收口（09-19 凌晨场）

- **brain-resume Step 0.5 落点改指**：`.tts-scratch` → 「`output_directory` 传 `.`（落 BASE＝`~/Documents/Claude/临时文件/`）」，并写入两条判据（BASE 与播放白名单同一 env；**报错给出的允许目录就是真实 BASE**）。**四端同版 `b75b7f107c897c8bab17`（12,316 B）逐字节一致**（canonical=portable=包内=runtime），frontmatter 单块、YAML 可解析。
- **诊断出「配置已改 ≠ 已生效」**：`ELEVENLABS_MCP_BASE_PATH` 每进程启动读一次；09-19 凌晨 config 已由 Doctor 改到新路径，但**未 ⌘Q 重启** ⇒ 运行中的 server 仍持 09-12 旧 BASE（`4AI/临时文件`）。症状＝**生成成功、播放被拒**，极易误判为工具故障。
- **旧目录处置**：本场为开声临时重建的 `4AI/临时文件/` 已用 `rmdir`（只删空目录）清除，恢复 README 声称的「已不存在」态。
- 记忆两条更新：`project_tts_bridge_integration`（判据 + 判据来源）、`feedback_4ai_temp_folders`（改配置≠已生效）。
- ⚠ **生效验证仍未做**：需 ⌘Q 重启后新会话开声落新路径才算闭环。

### 二、浪浪Insight「零产出」→ 更正为「通道受阻、内容不缺」

- **首判错误**：我报「浪浪自 09-14 起零产出」——那是误读班次汇总表的「最近更新」列（它是「最后一次完整成功」口径，不是产出时间）。实际 09-18 17:13 刚落过一条。
- **真因**：DYD「双路独立分页」的 `visible_routes_incomplete`——两条 route（aid `1128`/`6383`）均撞 `PostRoutePayloadMissing` 中途跳出。`downloadFailedIds`/`metadataFailures`/`asrFailedIds` **三个失败列表全空**。
- **硬证据（内容不缺）**：`Transcripts/浪浪Insight` = **json 79 / txt 79 / other 0**，与 09-18 浏览器覆盖认证的 `declaredCount=79`（endReached=true）**精确吻合**。
- **隐患（已上报）**：partial 常态化 ⇒ **信号价值归零**——真漏一条新作品时症状完全相同，无人分得出。

### 三、自愈链消费者定位（跨系统三层）

- **消费者＝Mac 侧 Codex automation `dva-fuxi-mac`**：`~/.codex/automations/dva-fuxi-mac/automation.toml`，`kind = "heartbeat"`，`status = ACTIVE`，`name = "DVA 有界自愈闭环"`。
- **执行入口＝`run_dva_self_heal.py`**（Mac 侧 Codex 仓 `runtime/tools/fuxi/`）：`--inspect`（全时段只读诊断）/ `--poll`（只读轮询 + 本地进度记忆）/ `--execute`（SSH → fuxi `ops/bootstrap/dva_self_heal_bootstrap.py`）。**有 darwin + 硬编码路径双闸**，沙箱跑必报 `LAUNCHER_ENTRY_REFUSED`——这是设计，不是缺陷，也解释了 fuxi 侧为何对该组模块零引用。
- **rrule 原写法 `DTSTART;TZID=Asia/Shanghai:…\nRRULE:FREQ=DAILY;INTERVAL=1` 与在跑的四家不同形**（在跑者全是裸 `FREQ=…;BYHOUR=…;BYMINUTE=…`）→ VV 09-19 改型为 `FREQ=DAILY;BYHOUR=3;BYMINUTE=15`（PT 03:15 ＝ 北京 18:15）。
- ⚠ **冬令时隐患**（VV 提出、CC 复核成立）：固定 PT 03:15 在 PST 下＝北京 **19:15**；且这是在跑四家的**共性问题**（皆硬编码 PT），非 dva 独有。

### 四、三次误判与当场更正（本场主线）

| # | 我的判断 | 实际 | 更正触发 |
|---|---|---|---|
| ① | 用「fuxi 侧 18:15 后零写入」推 Mac 侧没跑 | `--poll` 从设计上就**不写 fuxi**（只读 + 只写 Mac 本地状态）——那个面恒为零 | 自查（写下过替代假设却没去查） |
| ② | 用「`memory.md` 缺失」推 heartbeat 从未运行 | **跑过**：`state.json` 的 `observedAt` / `lastBusinessProgressAt` 是直接证据 | VV 反证 |
| ③ | 把 09-20 healthcheck FAILED/exit 2/3 blockers 当班次实错，建议转 VV 查 | fuxi **出游离线**所致；且 healthcheck 验的是 09-19 班，离线期间读不到 | Doctor 告知 fuxi 离线 |
| ④ | 把 `coordinator.succeeded` 当 09-19 恢复成功的迹象 | 它带 `currentCycle: false` + `ordinal: 1`，**不是当前 cycle 的记录** | 自查（补读字段） |

### 五、Mac 侧核完的实质结论（fuxi 离线期间唯一能推进的一截）

- **rrule 首验成功**：09-20 PT 03:59（北京 18:59）落 plan，紧随 18:15 触发；`lastBusinessProgressAt` ＝ 北京 09-20 18:54。
- **两个真根因**（VV 均已诊断，两 plan 皆 `executionAuthorized=false`·`businessRuns=0`，**未执行**）：
  - ㈠ **policy 时间戳比较 bug**：inspect 在**自身 terminal journal 更新之前**捕获快照，policy 却拿 `observed_at` 比 inspect 的 `recordedAt`——**不可能满足的次序** ⇒ 递归的无副作用检查链，**一个合法 writer 却永不产生业务动作**。
  - ㈡ **enrollment 错配**：bootstrap 仍批准更旧的 release transaction，与已装 policy 不匹配 ⇒ 新 cycle 拿不到 coordinator enrollment。
- **阻断**：fuxi 出游离线至 09-26；**Mac 消费镜像停在 09-18 代次**（`dva-mirror-20260918T191145Z-047034d0`·published 09-18T19:16:11Z）未再前进。
- **对照组（好消息）**：09-20 的 refill 班是 **fuxi 本地自主**跑出 attempt 1 exit 0 **SUCCESS**（不依赖 Mac）——证明「对象活着、问题在通道」。

### 六、经验提炼（Doctor 令「CC 你提炼经验」）

- **新增 G-X190 / G-X191 / G-X192**（`permanent/通用教训.md`，编号先 grep 探 max=189 取 max+1，未心算；状态一律 🔄 待独立验收）：
  - **G-X190 判别力门**：落收敛结论前问「若假设相反，这个观测面会不同吗」——不会 ⇒ 零判别力，换面。含「必然性检验」（从「没痕迹」推「没发生」须先证「发生必然留痕」，且要**找反例**）。
  - **G-X191 字段名 ≠ 语义**：`failed_runs_only` 是**通知策略**不是执行筛选器；贴不出「读该字段的那一行」⇒ 只能算猜。
  - **G-X192 writer 零副作用 ⇒ 先疑自我循环**：门禁拒绝会留拒绝证据；零拒绝证据却一直在动 ⇒ 查前置条件的更新者是不是它自己。
- **G-X182 补双向指向**（那条「下游零留痕时先离线复现其准入门禁」与本条 G-X190 是**同一枚硬币两面**）。
- **经验索引重建**：829 → **842 条**。

---

## 做出的决策

| 决策 | 原因 | 影响 |
|------|------|------|
| TTS 落点改指 `Claude/临时文件/` 并发布四端 | 09-19 目录并轨 + Doctor 裁「改指 BASE」（AskUserQuestion 推荐项） | brain-resume 四端同版 + 记忆更新 |
| 「浪浪零产出」当场更正为「通道受阻、内容不缺」 | 79/79 硬证据推翻我的误读 | 存档纠错，避免下游误判断供 |
| 批准 VV 由原 coordinator 接管 09-19 票据跑全链 | 设计内唯一 retry owner 路径 + 09-18 有成功先例 + 当前状态静态干净（CC 独立核过：FAILED/75·无 writer·无锁） | Doctor 裁「批准全链」（AskUserQuestion） |
| 不即时落签，改为挂 dated 待办至 09-26 | fuxi 侧四项（票据兑现/coordinator 终态/镜像新代次/消费端）**一项都核不到** | brain/TODO.md DVA 条就地改写（同根追记，不新建） |
| 三条教训一分为三（不并入一条） | 判据动作各不相同：换面 / 贴行 / 查前置条件 | G-X190/191/192 |
| 验收性质定性为**事实性**（非功能验收） | 无 PRD（例外不适用）· 无新增功能 · 产物全是机器事实；按 09-19 签字分轨应归未参与实施的 subagent | 撤回我上一轮「落签归 Doctor」的错误表述 |
| Step 4 不补建 DVA 系统概览 stub | 文件不存在时禁自动补建（brain-save v3.2） | 见下方 ⚠ 空转报告 |

---

## 遗留问题 / 待办

- [ ] **DVA 自愈链终验顺延至 fuxi 回线（09-26）**——`brain/TODO.md` 已追记，含四项补核清单 + 验收性质提示（runtime 有代码变更 ⇒ 含功能性成分，该部分归 Doctor 或指定验收方）。
- [ ] **浪浪 partial 常态化的处置未裁**——两条互为排斥的路：①让 recovery 自动跑（partial 每天自动转 complete，噪音消掉、真缺口重新有信号）；②承认「路由不全但浏览器覆盖完整」为正常态、班不再为它报 partial。CC 倾向 ①，但需先定位 recovery 的消费者（＝已定位：`dva-fuxi-mac`）。
- [ ] **TTS 生效验证未做**：需 ⌘Q 重启本壳后新会话开声落 `Claude/临时文件/` 才算闭环。
- [ ] **DVA 两个真根因待授权执行**（policy 时间戳修法 + certification-only enrollment 修法）——均需 fuxi 在线，随终验同批。
- [ ] 冬令时后 Codex automation 全体漂移 1 小时（共性问题，非 dva 独有）——本轮不改，记录在案。

---

## 相关笔记

- [[2026-09-19-接手渊图会话与skill发布链修复]]（前场 · brain-save 发布链）
- [[2026-09-19-备份归口与skill真源裁定]]（前场 · 目录并轨与 TTS 迁移决议）
- [[2026-09-18-龙鱼截图流与DVA金融线实核]]（DVA 自愈观察的起点）
- [[通用教训]] **G-X190 / G-X191 / G-X192**（本场新增）· G-X182（补双向指向）
- `permanent/经验索引.md`（重建 842 条）· `TODO.md`（DVA 条追记）
- 外部证据面：`~/.codex/automations/dva-fuxi-mac/automation.toml` · `Codex/Project Mirror/DVA/ops/state/dva-codex-supervision/state.json` · `run_dva_self_heal.py`
