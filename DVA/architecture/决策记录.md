---
title: DVA · 决策记录
tags: [DVA, decision]
created: 2026-05-14
updated: 2026-06-17
status: active
type: decision
project: DVA
---

# DVA · 决策记录

> 项目内重大决策的时间线。一条决策一个 H2。

格式：

```
## YYYY-MM-DD · 决策标题

**背景**：为什么要做这个决策

**选项**：
- A：...
- B：...

**选择**：B

**理由**：

**影响**：
```

---

<!-- 在下方追加新决策 -->

## 2026-06-03 · 单链接 harvest-links 孤儿字幕统一走 seed+import 正式入库

**背景**：对投知君君跑 harvest 时，`import-transcripts` 递归扫整个 Transcripts 根，连带报出 6 条其他作者（全栈观察员、每日AI评论、独夫之心、矛盾财经史、闭眼Kan世界、黄大圣叨叨叨）的 `无法定位作者`。它们是历史用单链接 `harvest-links.js` 转出来的字幕——按设计单链接只下载+转写+汇总、不 seed DB，故 DB 无视频记录、反查命不中。6 条均有下载 mp4+`_data.json`。

**选项**：
- A：封档止警（`.transcript.*` → `.dupbak`），不入库。
- B：正式 seed+import 入库（本地复刻 `seedDVAFromDYDDir` seed 记录 → Mac 跑 import 注入+重建索引）。

**选择**：B

**理由**：6 条都有完整下载元数据，可正经入库；保留数据可用性优于丢弃。新建 5 个单视频作者可接受。

**影响**：
- DVA 库新增 5 个作者（黄大圣已有记录、并入），各 1 条视频。
- 建议治本：给 `harvest-links.js` 末尾补 seed 步骤，让单链接产物不再成为 import 孤儿。
- 经验固化见 [[经验库]] EXP-20260603-001-P（seed/import 环境分工）。

## 2026-06-05 · 常更作者表 watchlist + 一键批量更新 update-all

**背景**：Doctor 需要一张「需要经常性更新的作者」表，自己手动选作者入表，再用一条命令把全表的视频、字幕、入库、分析全部更新。现有能力已齐：`dva harvest` 做视频+字幕+入库，`dva analyze-level1` 做分析——缺的是「表」和「批量编排」。

**选项**：
- A：watchlist 用 CLI 子命令管理（`dva watch add/remove/list`），存 DB indexes/watchlist.json；批量命令在主进程内直接调 handler。
- B：同 A，但批量命令对每位作者 spawn 子进程跑 harvest/analyze。

**选择**：B

**理由**：`handleHarvestCommand` 内部多处 `process.exit(1)`（下载目录不存在、sec_uid 解析失败等），若在主进程内直接 await，一位作者失败会终止整批。子进程隔离后单个失败只挂掉该子进程，批次继续，并能逐作者汇总成功/失败。

**影响**：
- 新增 `基础模块/watchlist.js` + `dva watch`（add/remove/list/update）+ `dva update-all`（别名 watch update / update-watchlist）。
- 默认每作者抓 20 条新视频（Doctor 选定），`--limit` 可覆盖、作者可设专属 limit，`--no-analyze` 只采集不分析。
- 版本进 v6.6，详见项目 CHANGELOG。

## 2026-06-05 · 分析 LLM 默认切换为 DeepSeek V4 Pro

**背景**：Doctor 指定 DVA 分析步骤改用 DeepSeek V4 Pro。DeepSeek 提供 Anthropic 兼容端点（`https://api.deepseek.com/anthropic`），`llm-client.js` 已走 `@anthropic-ai/sdk` 且透传 BASE_URL / MODEL，故无需改调用代码。

**选项**：
- A：只在 `~/.dva_secrets`（cron）+ shell rc（交互）里设环境变量，代码默认不动。
- B：把代码默认值也改成 DeepSeek，环境变量仅覆盖、唯一必填项缩到 API key。

**选择**：B

**理由**：Doctor 要 DeepSeek 做「标准 API」，覆盖 cron 与手动两条路径。改默认后只剩 key 必填，少一处忘设环境变量的坑；回退原生 Anthropic/APIYI 仍可用三个环境变量覆盖。

**影响**：
- `基础模块/llm-client.js`：MODEL 默认 `deepseek-v4-pro`，BASE_URL 默认 `https://api.deepseek.com/anthropic`。
- `基础模块/dva_config.js`：llm.model 默认同步改，注释更新。
- `run-update-all.sh` 的 SETUP 注释：`~/.dva_secrets` 改为 DeepSeek 三件套。
- ⚠️ ANTHROPIC_API_KEY 现在必须是 **DeepSeek key**；旧 Anthropic/APIYI key 配新默认 base URL 会失败。
- 残留：`3-文本分析/mind-model/mind-extractor.js`。**[2026-06-13 审核更正]** 此前本条记成产物标签 `extractionMethod:'llm-claude-sonnet'`，实为笔误——实际标签是中性的 `'llm-extraction'`（`mind-extractor.js:208`），无需改。真正的 Claude 残留在该文件**头注释**（"改用 Claude claude-sonnet-4-6"）和运行前提里的 `ANTHROPIC_API_KEY`/APIYI 地址，已于审核中改为 DeepSeek/`DVA_LLM_*`。
- DeepSeek 型号经 web 查证（2026-06）：在售为 `deepseek-v4-flash` / `deepseek-v4-pro`；旧 `deepseek-chat`/`deepseek-reasoner` 将于 2026-07-24 弃用。

## 2026-06-14 · 运维产物统一落数据层 DVA-ops（弃用镜像 ops）

**背景**：Codex/V.V. 做 DVA 定期更新，需写运维日志/summary/失败报告/ASR 续跑状态。源项目只读；V.V. 起初把运维区建在 `Codex/Project Mirror/DVA/ops/`。

**选择**：统一落 `~/Documents/Database/Douyin/DVA-ops/`，弃用镜像 ops。

**理由**：Doctor 原则——被自动任务"修改/写入"的文件放**已放开路径**（`Database/Douyin/` 已授权可写、禁删）；与数据同盘、V.V. 复盘方便。

**影响**：
- 建 `DVA-ops/{logs/cron,summaries,failures,asr_pending,cache,tmp}`。
- PRD v1.2 / 改源提案 v2.0 全部 ops 路径迁此。
- V.V. 需同步改其 `Project Mirror/DVA/{README,INDEX,GOTCHAS}.md` 的 ops 路径。

## 2026-06-14 · DVA 对接分工：源码改动归 CC，Codex 自动任务绝不写源

**背景**：定期更新自动化的部分加固需改源码（如错误日志改向），与"源只读"边界冲突。

**选择**：源项目代码修改由 **CC 在 Doctor 授权下做**；Codex/V.V. 全程只读源、不改源，运维只在 ops 层包装。需改源处先写《改源提案》交 Doctor。

**理由**：守住源只读边界的同时仍能加固；改动权集中、可审计。

**影响**：本轮 CC 实施 P0（`dva.js:80` 错误日志改 env 网关）；P1 经核查源项目早已修复；P2（harvest ASR 分段 / import 收窄）待授权。

## 2026-06-16 · DVA 自动更新外包 Codex，沙箱自动化路线整条作废

**背景**：6/13–6/14 尝试在 Cowork 沙箱里跑 DVA 自动更新，连撞三堵墙——douyin CDN 白名单、单条 bash 45s 上限+后台进程不跨调用存活、DashScope 结果 OSS 区域错配。后 Doctor 定：自动更新外包给 Codex automation。

**选择**：DVA 定期补库由 **Codex 跑**（`Database/Douyin/DVA-ops/run_refill_watchlist.sh` 调 `node dva.js update-all`），不再走沙箱；Cowork 侧旧定时任务 `dva-update-all-reminder` 禁用+归档。

**理由**：Codex 环境无沙箱 45s/CDN/OSS 墙，6/14 实跑通（6/6 作者成功）；沙箱路线（CDN 白名单 / launchd 回退 / sandbox-cdn-retest）整条不再需要。

**影响**：
- 沙箱三条待办（CDN 白名单 / OSS 北京区 / launchd 回退）作废。
- 主线三项已完成：Codex 建定时任务、自动任务 export `DVA_ERROR_LOG_PATH`（不写源 GOTCHAS）、dva.js P0 env 网关（已提交 35a50e7）。
- 残留运维事项归 Codex：3 条 ASR TOS 上传失败（卷宇宙漫谈/黑盒调查局/独夫之心）的失败队列复跑；Codex 审计提的可观测性加固（partial-success 退出码）。
- 旧 Cowork 任务 dva-update-all-reminder 已 disable + SKILL.md 归档；登记表空壳 Doctor 在 app UI 手动删除。

## 2026-06-16 · DVA TOS 上传韧性：不动源·靠 ops requeue 兜底（纠正"缺 retry"误判）

**背景**：6/14 三条 ASR（卷宇宙漫谈/黑盒调查局/独夫之心）TOS 上传"连续 5 次失败"→ 06-16 经 DVA-ops 定向复跑全成。CC 一度提议"上传步骤加 retry/backoff"；经查实属**误判**——retry 已存在。

**实况（`dyd/dva_asr.py` · 零改动）**：`upload_to_tos()` **已有指数退避重试**（max_attempts=5·2/4/8/8s≈22s·socket_timeout=90s·connection_time=15s·复用同一 tos_key·失败清孤儿），docstring 明指 SSL UNEXPECTED_EOF/连接超时；DashScope 侧 `_urlopen_json` 亦有 4 次重试。6/14"连续 5 次失败"＝现有 5 次预算被瞬时窗口（>22s）打满，**非缺重试**。

**选项**：A 不动源·靠现有 DVA-ops requeue 兜底 / B ops 层固化自动定时 requeue / C in-line 加宽（jitter+attempts）/ D 两层都加。

**选择**：**A**（Doctor 2026-06-16 裁定）。

**理由**：源已做对；作者刻意 fast-fail（`socket_timeout=90`「失败快速返回而非卡满 5 分钟」）防单条 wedge 整批，加宽 in-line 与此设计对冲。正确分层＝in-line 吸收**秒级**抖动、**分钟级**外停交 ops requeue（6/14 残留正是被 DVA-ops 定向复跑接住）。

**影响**：
- DVA 源仓**零触碰**（代码+文档）；不加 in-line 预算、不改 socket_timeout。
- 「上传步骤加 retry」待办**关闭＝不做**（先有猎物再造枪）。
- 若未来同类失败频次上升，再议选项 B（ops 层自动 requeue 固化人肉复跑）。

## 2026-06-17 · watchlist 单一真相源化：退役孤儿空表 + DB root guard 写死绝对路径

**背景**：常更表存在两份 watchlist.json——正源 `~/Documents/Database/Douyin/DVA-Database/indexes/watchlist.json`（= `config.db.root`，Codex `update-all` 实读那张，6 位作者）与孤儿 `~/Documents/Claude/Documents/Database/...`（多一层 `/Claude/`、78 字节空表）。孤儿成因＝2026-06-13 沙箱 `export HOME=挂载根` 使 `os.homedir()` 拼出的 `config.db.root` 错位，`watchlist.js ensureFile()` 在错路径建空表（**非** artifact 功能所致）。

**选项**：A 退役孤儿+加护栏（孤儿空、无数据可合并）/ B 仅加护栏不动孤儿 / C 不动等观察。

**选择**：**A**（Doctor 2026-06-17 二次确认）。Codex 执行。

**理由**：孤儿空表无作者数据，"整合"实为退役+堵成因；正源 6 人原样不动。护栏写死绝对根 = 最硬兜底，宁可拦掉沙箱/换机也不再静默建孤儿。

**影响**：
- 孤儿退役为 `watchlist.json._DEPRECATED_20260617` + README（不删、可逆）；正源 6 人零改动。
- `assertDvaDbPath` 护栏入 `基础模块/watchlist.js` + `douyin_database.js`：断言 `config.db.root === 预期绝对路径` 且落盘目标在根内，否则中止。
- **副作用（有意）**：护栏永久拦截任何 `os.homedir()` 不同的环境（Cowork 沙箱/换机/换用户名）运行 DVA。
- 运行约定：DVA 采集只在 Mac 跑（Codex/launchd/手动），Cowork 侧 CC 只贴命令、不在沙箱执行（→ GOTCHAS RISK-20260617-001）。
- 对账 summary：`DVA-ops/summaries/summary-2026-06-17-watchlist-single-source-reconcile.md`。

## 2026-06-18 · 新增 watchlist `download-only` 模式（而非将就 transcribe-only）

**背景**：麦橘MERJIC（AI 绘画/教程类，非财经）要进常更表，Doctor 明确「只下载」。常更表原仅 `full`（下载+转写+入库+分析）/ `transcribe-only`（下载+转写）两档，最省也会转写、花 ASR 钱。

**选项**：A 将就用 transcribe-only（每次仍转写）/ B 新增 `download-only` 档（只下载）/ C 进表但 enabled=false。

**选择**：**B**（Doctor 2026-06-18 二次确认改动清单后）。

**理由**：麦橘非财经，full 的分析无意义；transcribe-only 每次白花 ASR 钱；download-only 精确匹配「只下载」本意。

**影响**：
- 仅改 `dva.js` 6 处纯增量：`handleHarvestCommand` 加 `--download-only`（下载后短路，跳过 seed/ASR/import）；`handleUpdateAllCommand` 加 `download-only` 分支（push flag + analyze 标 skip）；help/用法串文档。
- `watchlist.js` **零改动**——`watch add --mode` 自由字符串透传存储，无需校验。（可复用：DVA 加新 mode 只动 update-all/harvest 两处分发，不碰 watchlist 存储层。）
- SansanYe 仍走 transcribe-only；麦橘走 download-only。
- 文档 `使用流程.md`/`README.md` 本次按约定未同步，待 Doctor 发话。

## 2026-06-18 · 常更表 category 分类机制 + 概念级改名 DVA-金融分析

**背景**：Doctor 要给常更表加「分类」打勾，并把"DVA 后续分析模块/库"改名为"DVA-金融分析/库"。

**选项 & 选择**（brain-prd 4 轮 AskUserQuestion 锁定）：
- 改名范围：**概念/标签级** ✅（vs 物理目录级 / 不改名）——物理改 `DVA-Database`/`3-文本分析` 会连带 `assertDvaDbPath` 硬编码 + launchd + Codex 多点爆破，概念级拿 95% 好处近零风险。
- 打勾载体：**Cowork 交互 widget** ✅——但 artifact 碰不了本地文件，落地为 widget→回传→CC→Mac `set-category` 命令（见经验库 EXP-20260618-001-P）。
- **category × mode 正交** ✅——category 管去向/消费者，mode 管深浅。金融⇒full、PEC语料⇒transcribe-only 固定；设计⇒{download-only,transcribe-only} 二选。
- MVP：**只做分类+路由+打勾** ✅，投递管线（VV/PEC/金融库）留下一期。

**理由**：核心是"概念改名避开物理多点爆破"+"category 与 mode 解耦以容纳设计类的双 mode"。

**影响**：
- `watchlist.js` 加 `category` 字段 + `setCategory` + category↔mode 约束（非法组合抛错）；`dva.js` 加 `watch set-category` + `add --category` + `list` 显示分类。
- 概念改名仅落 help/`使用流程.md`/`README.md` 面向用户处；**物理路径/目录/guard 零改动**（独立子 agent 复核确认）。
- 消费者路由（仅标记）：金融→DVA-金融分析库、设计→VV Design Reference、PEC语料→PEC。
- PRD：`logs/checkpoints/2026-06-18_常更表分类与概念改名_PRD.md`（待 Doctor 验收）。
- Doctor 打勾结果：老毛/投知君君=金融；卷宇宙/黑盒/独夫/浪浪=PEC语料；SansanYe=设计·转写；麦橘=设计·只下载。

## 2026-06-22 · 看板「常更作者日报」完成判据：mode 编码目标管线 + 容差

**背景**：Brain Vault Dashboard 的 DVA 二级页从手填静态升为全数据驱动，需要一套**纯数据算出**作者「完成/进行中/待启动」的判据，不能再手配目标。

**决策**：完成判据直接读 watchlist 的 `mode` 字段当**目标管线**，状态 = 实时进度 vs 目标 + 容差 3：
- `download-only`（麦橘）→ 下载 >0 即**完成**。
- `transcribe-only`（PEC语料 4 家 + SansanYe）→ ⚠️**此条已被下方「2026-06-22 修正」纠正**（原写「入库≈下载 且 subtitle_status=已识别≈入库」是测错地方）。
- `full`（金融：老毛/投知君君）→ 入库≈下载且 level1≈入库 → 完成（注：full 的 `subtitle_status` 多为「未检查」，完成看 **level1** 不看字幕勾）。
- 容差 3：落后 ≤3 视作追平，防一拉新视频就从完成闪成进行中。

**理由**：`mode` 是常更表里现成的、稳定的编辑决策（哪条作者走哪条管线），用它派生完成态＝名实相符、零手配、随采集自动刷新。

**影响**：
- snapshot.py `dva_authors()` 每作者算 downloaded(递归 mp4)/ingested(global-index videoCount)/transcribed·pending·unchecked(per-video subtitle_status)/level1/state。
- 「待入库」扣除 `analyze-ignore.json` 排除项（snapshot 复刻 dva.js `isIgnored`：id 精确 + 标题正则 `re.IGNORECASE`），单列「排除 N」——排除项按规则永不入库、不算缺口（独夫之心：待入库 50·排除 11）。
- 二级页 `renderDvaReport()` 数据驱动，每作者另显 watchlist.lastUpdatedAt（最后更新）。
- 来源 → logs/2026-06-22-看板DVA日报数据驱动与星图反哺修复.md · [[星空]]

### 2026-06-22 修正 · transcribe-only 完成判据测错了地方（续会话纠正）

**纠正**：transcribe-only 模式（PEC语料 + SansanYe）**故意跳过 seed入库 + import-transcripts**（dva.js `handleHarvestCommand`：transcribe-only 只跑 下载+ASR、不入 DVA-DB，产物给 PEC 消费）。所以它们的 DVA-DB `ingested`/`subtitle_status` 本就为 0/未检查，拿来判完成是**测错位置**。

**正确口径**：转写产物落 `~/Documents/Database/Douyin/Transcripts/<昵称>/`（`<标题>__<aweme_id>.transcript.txt`，按 aweme_id 去重）。完成 = **`transcripts ≥ 下载 − 排除 − 容差`**（transcripts = Transcripts/ 里去重的 .transcript.txt 数）。snapshot `dva_authors` 加 `transcripts` 字段读 Transcripts/；二级页文案改「下载 N · 转写 M」、去掉对它们不适用的入库/待入库。

**影响**：5 个 transcribe-only 作者由「进行中·待入库一大堆」转正为完成 → 总览 8/0（真实现状）。`full`/`download-only` 判据不变。来源 → logs/2026-06-22-DVA转写口径与semantic-parser排障-星图源卡片.md。

## 2026-06-22 · 星图「源」节点上游 = 自动化任务（source_tasks）

**决策**：星图「源」节点点击卡的「上游」显示**产出/拉取该源的定时任务**名 + 最近运行时间（换行·到日·今天/昨天/N天前）。

**关键边界（Doctor 两轮校正）**：「上游」只列**真有产出/拉取该源的定时任务**的源——
- 有：渊图（金融领域常更作者语料提炼 touzhijunjun）/ Tushare（market-data-daily-update）/ 四维度复盘课件（recap-kejian-daily-ingest）/ 管线 JSON（baize 周报**产出**的本周真源）/ 抖音（DVA update-all·本机 crontab·无 lastRunAt 显「本机 cron」）。
- **无**（被 baize 消费的外部数据商/外部行情/他项目输出、非任务产出 → 上游显「—」）：**Gangtise edb / SMM/web补价 / 龙鱼五力**。

**实现要点**：① lastRunAt 是 **app 内部态**（`Scheduled/<task>/` 只有 SKILL.md、磁盘无运行记录）→ snapshot 出 `source_tasks` 映射（task_id + last_run 占位），**看板刷新时**由 agent 调 `mcp__scheduled-tasks__list_scheduled_tasks` 按 task_id 填 lastRunAt（refresh 任务 SKILL.md 已加此步）。② 卡片读 `DATA.finance_chain.source_tasks`。来源 → logs/2026-06-22-DVA转写口径与semantic-parser排障-星图源卡片.md · [[星空]]

## 2026-06-22 · 关系提取救孤岛：候选改 per-batch「本地概念∪枢纽」（透传来源视频 id）

**背景**：semantic-parser 抽出 887 概念但只 ~31 个有连边、856 个孤岛。

**诊断**：`MAX_CONCEPTS_FOR_RELATION=30` + 关系提取对**所有视频批次都用同一张「全局 top30 概念表」**当候选 → 关系只在 top30 内连、低频概念从没当过候选 → 必然零边。**不是抽不出，是没让上场**。

**决策（设计 D）**：每批候选 = **本地概念（srcVids 与本批 2 视频 aweme_id 有交集）∪ 全局 top30 枢纽**，封顶 80。来源映射走「**透传 Step 1 来源 id**」（非子串匹配——子串会漏被抽象/改写的概念）。

**实现（semantic-parser 4 处）**：discoverConcepts 给概念打 `srcVids`(来源视频 id Set；concept/merge 的 prompt 不变 → Step1/1b 缓存照命中)；mergeConceptBatch + mergeConcepts 外层 union srcVids；extractRelations 候选改 per-batch 本地∪枢纽；generateConceptsJSON 不动（srcVids 只在内存、不写 concepts.json）。

**影响**：关系 571→**1462**、连通概念 31→**806**（887 仅剩 81 真孤立）。token ~2×、批次不变。来源 → logs/2026-06-22-DVA概念图谱救孤岛与可星空化星云.md。

## 2026-06-22 · semantic-parser 单独跑也写 Reports/{昵称}/（根治全局↔作者目录错位）

**背景**：单独跑 `node semantic-parser.js <sec_uid>` 产物落**全局** `Reports/`，而 dva-to-graphify / dva-to-starfield 等读 `Reports/{昵称}/` → 错位、要手搬。

**决策**：仿 `analyze-author.js` 的范式（跑前把 `config.analysis.outputDir` 重定向到 `Reports/{safeNickname}/`），但**只在 semantic-parser 的 `require.main===module` 块**做——经 analyze-author 调 `main()` 时本块不跑、由它已重定向的目录接管，**不双层嵌套**。昵称解析用 `douyin_database.getGlobalIndex().authors[sec].nickname`。

**影响**：以后单独跑直接落作者目录、不用手搬。来源同上。

## 2026-06-22 · 龙鱼儿退役 → CC 接管图谱查询职 + 金融分析落点澄清

**事实一（落点）**：「DVA-金融分析库」**是概念/标签名**（2026-06-18 概念级改名·物理零改动），`Database/Douyin/` 下无此目录。金融作者语料分析物理落 **`Reports/{作者}/`**（concepts/relations/decisions/mind-model/wisdom/author-profile；投知君君另有 institutional-tracker/ticker-timeline）；视频底料落 `DVA-Database/`。

**决策（职责）**：**龙鱼儿退役**（graphify MCP 的结构化查询消费方）→ `dva-to-graphify` 那条线作废、不再提；**CC 直接读 `Reports/{作者}/concepts.json + relations.json` 做结构化查询**（枢纽/汇点/源点/取舍簇等），接管该职。来源同上。

## 2026-06-23 · DVA 主仓是本地 git 仓（无 remote，commit 不 push）

**事实**：`Projects/DVA/` 自带 `.git`，但**未配置任何 remote**。`git push` 会报「没有配置推送目标」。Doctor 2026-06-23 确认：DVA 一直当**本地仓**用，commit 留本地即可，不推远端。

**对照**：`brain/` 仓有 remote（`fuxi4ai/C.C.` main），照常 commit+push。两仓收尾命令不要混——给 DVA 贴 push 会失败。

**影响**：以后 /save 或改 DVA 代码后，只给 DVA 贴 `git add + commit`（**不贴 push**）；brain 仓才贴 push。来源 → logs/2026-06-23-DVA-5条GOTCHAS梳理治本.md

## 2026-06-23 · DVA 在金融线/星空的归属：仅「产业逻辑语料」子类反哺渊图

**背景**：星空金融线把 DVA-视频 整库当孤儿节点 + 画 DVA→渊图 反哺，造成「金融线=DVA」口径误导（CC 一度按此误开工）。

**裁定**：DVA 库整体是**交易哲学/经验**（多分类·金融/设计…·按道/法/术组织），**不进金融工作流**、移出金融线。真正反哺渊图的是 DVA 里**一个很细分的子类——「对产业逻辑有贡献的语料」**（当前主要来自投知君君的市场机构视角/产业逻辑分析）。单位是**语料类型**，不是作者、不是「金融」大类、不是整库。

**落地（星空 artifact `brain-vault-dashboard`）**：删 DVA-视频 库节点 → 改画「DVA·产业逻辑语料」绿信源反哺渊图；DVA-视频 作其上游源、带新鲜度（专为观察库本身）。

**影响**：星空只留细信源线；DVA 整库不在金融线。来源 → logs/2026-06-23-金融线梳理整改与遗留清理.md

## 2026-07-11 · DVA 历史全补标准流程（装 chromium → 小样 → 9999 绕 falsy → 按 mode 分发）

**背景**：常更表历史全补首跑（`DVA_REFILL_LIMIT=9999`）逐作者下载 0 条、卡 Playwright Chromium 缺失。踩坑后固化标准姿势，下次全量补库/换机照此，免再踩。

**标准流程**：
1. **装浏览器用 venv playwright**（版本才匹配 chromium-1228）：`~/Documents/Database/Douyin/DVA-ops/runtime/venv/bin/playwright install chromium`；**勿用 npx**（Node 版版本可能对不上）。
2. **全补 limit 用足够大有限数 `9999`，不用 `0`**——update-all 的 `0 || null` falsy 陷阱会把 0 静默吞成默认 20（见 GOTCHAS BUG-20260711-002）。
3. **先小样 `DVA_REFILL_LIMIT=5` 验证** browser 错误归零 + 有视频入库，再上 9999 全量（省得又白跑半天 ASR）。
4. **转写按各作者 mode 自动分发**：full（下载+转写+入库+分析）/ transcribe-only（仅转字幕落 `Transcripts/` 供 PEC）/ download-only（仅下载）。category 绑定 mode，故等价「转写按分类规则走」。
5. **进度三查**：`state/runs/*.json` 结构化状态 · `tail -f` 最新 log · `watchlist.json` 逐作者时间戳。
6. **refill 脚本硬编码 `--no-analyze`**：金融作者 level1 分析需事后单跑。

**理由**：增量走 API 即可，历史深翻页才回落浏览器路径 → 浏览器缺失只在全补时暴露；且 refill preflight 只验 playwright python 包、不验浏览器 build（盲点）。

**影响**：2026-07-11 全量 8/8 通过、browser 错误全程 0、新增字幕 独夫+18/黑盒+3/老毛+1。来源 → logs/2026-07-11-DVA常更表全量补库与Playwright坑修复.md · GOTCHAS ERR-20260711-001 / BUG-20260711-002


## 2026-07-22 · 抖音视频异地存储准备（查重内容感知 + 补跑 + 介质无关外移准备层）

**背景**：抖音冷层 `Downloaded/` 1556 视频 / 66.6GB 占满本地盘，需外移腾空间；但现链路两坑——① 0 字节转写（真静音 vs 失败）被幂等当"已完成"跳过（E1，14 条）；② `dy_downloader.db` 1470 行 `file_path` 写死绝对目录，外移即失效。

**决策**（PRD `logs/checkpoints/2026-07-22_抖音视频异地存储准备_PRD.md`，Doctor 四问定向：立 PRD / 介质无关准备层 / 工具齐活零外移零计费 / 重跑定性+今后自描述）：
- **查重锚真相源 + 内容感知**：`dva_asr._transcript_complete` = DB `subtitleSource` > txt `size>0` > 0字节查 json `asr_status`（`empty`=真静音跳过 / 无标记=失败残留须重跑）；空结果也写 `asr_status:"empty"` json 令 0 字节自带定性。取代"文件存在即完成"。
- **resolver 取代 DB 绝对 file_path**：位置按 `aweme_id + rel_path + 根变量(DOUYIN_ROOT)` 解析（hot/offsite/missing），介质无关；DB 绝对路径外移即失效的根问题由此绕过。
- **外移前置铁律（F4）**：一个视频"已有真转写"才可外移——否则外移后 ASR 拿不到 mp4 就补不了；dry-run 对未转写标红拒绝（实测 133 拒 / 1423 可外移）。
- **可逆（F5）**：sha256→写 pointer→拷异地→异地校验→**校验全过才删本地原件**；准备阶段只 dry-run（脚本结构无执行分支）。
- **MVP 零外移零计费**：14 空壳重跑 / 119 缺转写补跑 / 实际外移 / 全量 manifest 一律 Mac 侧出命令（沙箱禁下载·ASR + FUSE 不 durable）。

**落地**：改 `Projects/DVA/dyd/dva_asr.py`；新增工具 `dva_ops_common/reconcile_douyin/build_manifest/resolver/offsite_migrate_dryrun.py` + README（落点 `DVA-ops/` vs 迁 `Projects/DVA/tools/` 待 Doctor 拍板——DVA-ops 非 git 仓）。独立子 agent 复审通过。py_compile×6、grep 绝对路径 0、F1 单测、resolver 自测、dry-run 零变更均过。**来源** → logs/2026-07-22-抖音异地存储准备-PRD执行.md


## 2026-07-23 · 补跑/外移必读转写策略（watchlist mode + analyze-ignore）· 修虚报

**背景**：Doctor 指出常更作者表里部分作者/内容明确无需转字幕（麦橘MERJIC=设计·download-only；独夫之心广告/连麦/问答；卢雅熙coco/摄影师等视觉一次性号）。原 reconcile 未读策略 → 把「故意不转」当「缺转写」，119 虚报。

**决策**（Doctor 2026-07-23 两问定向，均取推荐）：
- **补跑/外移判定必读策略源**：`watchlist.json`（category×mode，download-only=只下载不转）+ `analyze-ignore.json`（per-video ids/patterns）。`dva_ops_common.transcribe_policy` 统一给 transcribe/no_transcribe/ignore/out_of_scope。
- **非常更号=范围外·默认不转**：reconcile 只对常更转写号做补跑判定；一次性 harvest 号默认不进转写范围（无需逐个登记）。
- **needs_rerun（0字节残留）仍重跑**（除 download-only/ignore）——承 Doctor「跑 14」；missing 仅常更转写号才算真缺。
- **外移判据随之修正**：只拦「待转写未转」；download-only/ignore/范围外=永不转写→可直接外移（否则 119 条被永久错拦）。

**实测**：策略生效后 needs_rerun=0/missing=0（Doctor 已 Mac 跑完 14 条重跑、全 true_silent），excluded 119（94+14+11）。外移 dry-run 由「拒133」→「1556 全可外移·66.6GB·0 拒绝」。**落地**：`tools/{dva_ops_common,reconcile_douyin,build_manifest,offsite_migrate_dryrun}.py`。**来源** → logs/2026-07-22-抖音异地存储准备-PRD执行.md（增补段）


## 2026-07-23 · 外移 fuxi-station 的两阶段提交 + committed-manifest 为 session 真相源

**背景**：批 01（20GB/438 文件）真传前，经 VV（对端 AI）六轮审查，把「推 + 校验 + 写 pointer/DB」的顺序与真相源彻底重构。

**决策**：
- **两阶段提交**：远端逐条 `Commit`（双端 SHA-256 + 原子 Move）→ session 级 `FinalizeSession`+`StatusSession` 全量 scrub 定案 → **才**批量写本地 pointer + `dy_downloader.db.offsite`。**定案前绝不动本地真相**——否则 session 未全 committed 时会留半批本地状态，且中断不可恢复。
- **committed manifest 为 session 真相源**（非 `_plan.json`）：`FinalizeSession` 发布确定性 `manifest.json`，其 SHA-256 绑进 `SESSION.COMMITTED`；`StatusSession` 读 committed manifest 并先验 SHA 绑定，**不读可变的 `_plan.json`**。理由：上传态 plan 可被替换，不能当状态真相。
- **进 Phase 3 前双门**：manifest_sha 硬门（Finalize/Status 两返回须均 64hex 且相等）+ pointer 只读预检（任一冲突则 pointer/DB 一律不写）；**发布成功回执前** `final_scrub` 逐条回验 pointer+DB 与 committed plan 一致。
- **永不删源**：删源独立步、逐批 Doctor 批准、须先过抽样恢复演练。

**落地**：`Projects/DVA/tools/offsite_push_fuxi.py`（Mac 编排器）+ `tools/fuxi/dva_remote_commit.ps1`（远端·纯 ASCII·Prepare/InstallPlan/Commit/Status/FinalizeSession/StatusSession）。可复用护栏清单见 [[经验库]] EXP-20260723-002-P。**来源** → logs/2026-07-23-DVA外移-VV六轮安全硬化.md


## 2026-07-23 · 删源收官：只删 mp4 留元数据 · 恢复演练+三闸 · 状态记 receipt 不改 schema

**背景**：四批外移 1556 条闭环后删本地源腾空间。删源不可逆，走「恢复演练 + 逐批批准」纪律。

**决策**（Doctor 逐项拍板，均取推荐）：
- **恢复演练前置**：外移回执 verified 只证上传写对，删源前补独立恢复演练——ssh 进 fuxi 就地重算 sha256 抽样（30 条分层全绿）+ 逐批全量核验（全 PASS），证「远端取得回」而非仅「记录在」。
- **删源粒度＝只删 mp4、留元数据**：空间 99.8% 在 mp4（71.47GB vs 元数据 0.16GB）；保留 `_data.json`/`.webp` 唯一原件（原始抖音元数据+封面），可逆友好、几乎不占地。删整文件夹只多腾 0.16GB 却丢全部原件，不划算。
- **三道独立闸**：远端全量核验（有）+ 删前本地 hash==pointer 记录（同一份、防删错）+ 默认 dry-run（`--commit` 才真删）。
- **删源状态记 receipt.source_deleted（体系内建字段）**，不给 DB `aweme` 加列（DB 无 source_deleted 列，不为记状态擅改 schema）。
- **逐批 + 每批真删单独确认**，不把不可逆步骤和演练混连发。

**实测**：b04 208 / b03 612 / b02 298 / b01 438 全量核验 FAIL 0、删源跳过 0；四批 receipt `source_deleted=true`；Downloaded mp4 1556→0、腾 ~71.5GB、元数据（1572 json + 93 webp）完好。**工具**：`DVA-ops/state/recovery_drill/`（`verify_recovery_ssh.py`/`delete_source.py`/`full_b0x.json`，可复用于将来批次）。可复用范式见 [[经验库]] EXP-20260723-003。**来源** → logs/2026-07-23-DVA抖音删源收官.md
