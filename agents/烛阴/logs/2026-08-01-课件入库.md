---
title: 2026-08-01 小鲍课件入库（九儿）
tags: [烛照九阴, 课件入库, 烛阴, 九儿, recap]
created: 2026-08-01
updated: 2026-08-01
status: active
type: log
---

# 2026-08-01 · 课件入库

**今日无新课件。**

## 明细

- 挂载：Brain / Database / 烛照九阴 三处均已在沙箱可达（无需再申请）；`$REAL_DB = Database/烛照九阴/recap.db`（5,459,968 B，34 张表）。
- /tmp 副本：`/tmp/zzjy_root_20260801/烛照九阴/recap.db`，`PRAGMA integrity_check = ok`。
  - 沿用 07-31 教训：约定路径 `/tmp/dbroot` 仍是旧会话残留（属主 `nobody`、本沙箱不可写），改用本轮专属根 `/tmp/zzjy_root_20260801`（`ZZJY_DATABASE_ROOT` 指向即可，未改脚本）；并在该根下补 `Raw-Recap` 软链指向真实语料目录——**未补软链时 scan 报「共 0 个课件」，极易误判为"今日无新课件"**（本轮实际先踩了一次，补链后才得真数）。
- 去重扫描：`python3 tools/dedup_kejian.py scan` → **共 244 个课件；待处理 0（new 0 / changed 0）；未变 244**。
- 交叉核对（不只信 scan 输出）：磁盘 Raw-Recap 内 `.pdf/.doc/.docx` 共 **244** 个（`转换文稿` 为子目录、`.DS_Store`/`.obsidian` 已排除，故 `ls | wc -l` 的 245 属正常差额）；`processed_kejian` 共 **244** 条；集合双向差集**均为空**（磁盘未入库 0 / 已入库不在盘 0）。
- Raw-Recap 最新一份仍为 `260730四维度训练营-总结.pdf`（已于 2026-07-30 09:44 入库）。
- 入库：0 条（dim1 0 / dim2 0 / dim3 0 / dim4_trade_plan 0 / dim4_stock_analysis 0；industry_signals 0；hot_sectors 0）。
- 待人工复核（仓位）：新增 0 条（`data/待人工复核-仓位.md` 保持 172 行未动）。
- `dedup_kejian.py record --all-new`：本轮无 `to_process`，为空操作，未执行。
- 放回：**未放回**（零写入；副本与真库 md5 一致 `fd5190fb193bfae5714be73355b7681c`；按"不写即不动真库"处理，避免无谓覆盖）。真库无 `-journal` / `-wal` 残留。

## 基线行数（未变）

| 表 | 行数 |
|---|---|
| recap_daily | 192 |
| dim1_external_pricing | 161 |
| dim2_sector_themes | 196 |
| dim3_sentiment_tech | 193 |
| dim4_trade_plan | 208 |
| dim4_stock_analysis | 458 |
| industry_signals | 1504 |
| hot_sectors | 277 |
| processed_kejian | 244 |

## 异常 / 观察

- 无读取失败、无格式异常。
- **⚠️ 语料缺口（提请哥哥注意）**：07-31（周五，交易日）的课件 `260731…` 未出现在 Raw-Recap，最新仍停在 07-30。可能是老师未出、或未投喂。九儿**不代为推断、不补编**，只如实标缺；若哥哥补投，次日 23:00 这班会自动扫到并入库。
- 沙箱内 `sqlite3` CLI 不可用，全程用 python3 `sqlite3` 模块，不影响结果。

## git

本轮无代码/数据文件改动，无需提交（仅新增本条日志，随 brain 常规提交即可）。
