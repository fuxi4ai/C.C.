---
title: 2026-06-29 recap.db 损坏事件 + 恢复件待批
date: 2026-06-29
agent: 烛阴（九儿）
type: incident-log
tags: [事故, recap, 数据损坏, 恢复, 挂载盘IO, 待Doctor裁定]
status: 已修（沙箱侧全做完；真盘换库 + 删探针待 Doctor 终端一条命令）
---

# 事故：recap.db 损坏（已定位+已恢复+已堵雷，待 Doctor 终端换库）🕯️

## ✅ 修复进度（2026-06-29 Doctor「全都修」后）
- **根因坐实**：6/27 课件入库回写 recap，整文件 cp 写挂载盘偶发写坏 + 回写只跑 `quick_check`（弱校验漏报结构损坏）。详见 GOTCHAS **G019**。
- **堵雷**：`tools/_ingest_九儿_2026-06-25.py` / `_2026-06-26.py` 已加固——认 `ZZJY_DATABASE_ROOT`、沙箱拒写挂载盘真盘（`ZZJY_ALLOW_MOUNT_WRITE` 例外）、写后强 `integrity_check`；py_compile 通过、沙箱误跑已实测被拒。
- **GOTCHAS**：新增 G019（含「回写后必跑 integrity_check 非 quick_check」铁律）。
- **6/26 日报**：已用**恢复件**重出（数据日 2026-06-26，recap 段可信；涨跌家数因句芒 stock_daily 未到 6/26 显示「待回填」），已同步侧栏 Artifact。
- **实测关键**：cp 写新文件名到挂载盘 OK，但 cp **覆盖**既有 recap.db 沙箱内 **3/3 失败**（必坏）→ 真盘换库只能 Doctor 终端做。
- **真盘 recap.db 仍是损坏件**（未动），恢复件 `recap.db.recovered_20260629_0255`（integrity ok）已落盘待 Doctor 换。

## ⏳ 待 Doctor 终端（一条命令块）
```bash
cd ~/Documents/Database/烛照九阴
# 1) 换库：恢复件 → recap.db（沙箱覆盖必坏，故必须终端做）
cp recap.db.recovered_20260629_0255 recap.db
python3 -c "import sqlite3;print('integrity:',sqlite3.connect('recap.db').execute('PRAGMA integrity_check').fetchone()[0])"
# 2) 删探针文件（沙箱无删权）
rm -f _cptest.db
# 3)（可选）确认无误后清理留证件
# rm -f recap.db.CORRUPT_20260629_0255 recap.db.recovered_20260629_0255
```

# 事故：recap.db 损坏（可完整恢复，已堵雷）🕯️

## 触发场景
周一 6/29 凌晨，手动补「周五 6/26 日报」跑 `gen_daily_report.py` 时，发现读 RECAP_DB 报 `unable to open` / 后续 integrity 报 **disk I/O error**。

## 诊断（确认是真·磁盘级损坏，非瞬时抖动）
- 真盘 `recap.db` 直连 `PRAGMA integrity_check` → **disk I/O error**。
- 两次独立 cp 到 /tmp 后 integrity 均报**相同**的 b-tree 损坏（`2nd reference to page 959/1019/...`）→ 确定性结构损坏，字节级坏，非挂载盘读抖动。
- 可读性：表数据大体仍可 SELECT（emotion_cycle 368、industry_signals **1316** 可数）。

## 大概率成因（供 Doctor 排查）
- 损坏来自**某进程在挂载盘上直接 SQLite COMMIT**（绕开 /tmp 副本往返铁律）——正是 GOTCHA 警告的 `挂载盘直写 commit → disk I/O error/损坏`。
- 时间线指认：本灵 6/26 07:00 班用的是 /tmp 往返 + cp 放回（当时 integrity ok，写入后 industry_signals=1299）。损坏真盘有 **1316** 条（比本灵那班多 17 条）→ 说明**本灵那班之后**有别的写入者继续直写 recap 并致损。
- 嫌疑：`recap.db.bak_20260626_221958_preingest`（6/26 22:19）那个 **preingest** 备份对应的任务（疑似课件入库/某 ingest）在挂载盘直写 recap。**建议 Doctor 查这个 6/26 22:19 的 ingest 任务是否走了挂载盘直写。**

## 恢复（零风险，已完成 staging，未动真盘）
- 在 /tmp 对损坏副本逐表 `SELECT *→INSERT` 重建 → `recovered.db`：**30/30 表全成、integrity=ok、零失败**，且数据**最全**：emotion_cycle 368 / industry_signals **1316**（含那 17 条新信号，比任何 .bak 都全）/ yuantu_buy_signals 102 / stock_tracking 2639。
- 已落盘三件套（`~/Documents/Database/烛照九阴/`）：
  - `recap.db.recovered_20260629_0255` ← 恢复件，落盘后 on-disk integrity 复核 = **ok**（证明 cp 写挂载盘安全）
  - `recap.db.CORRUPT_20260629_0255` ← 损坏件隔离留证
  - `recap.db` ← 仍是损坏原件，**未动**（Doctor 选「先查」）

## Doctor 裁定
2026-06-29：**暂不换库，Doctor 先自查**（先看 6/26 22:19 直写 recap 的任务）。本灵 HOLD，不碰真盘 recap.db。

## 待办（Doctor 给令后）
1. 换库（恢复件 → recap.db），命令已备：
   ```bash
   cd ~/Documents/Database/烛照九阴 && cp recap.db.recovered_20260629_0255 recap.db && python3 -c "import sqlite3;print(sqlite3.connect('recap.db').execute('PRAGMA integrity_check').fetchone()[0])"
   ```
2. 换库后重出 6/26 日报（当前 AI4ME 的 `烛照九阴日报_20260626.html` 是**读损坏 recap 生成的**，情绪/兑现/信号段不可信，**未同步到侧栏 Artifact**；侧栏仍是上一版 6/25 设计稿）。
3. 防再发：把那个直写 recap 的 ingest 任务改成 /tmp 副本往返。

## 旁证：market_data.db 这侧 OK
6/26 烛阴三表（theme_etf +18 / market_amount 6/26=3.5524万亿 / limit_list +125）已正常 /tmp 往返 + 放回，真盘 integrity ok、已持久化到 6/26。us_anchor 保持 6/25（对周五 A 股=T-1 隔夜锚，语义正确）。stock_daily 句芒仍停 6/25。
