---
title: 2026-07-19 小鲍复盘课件入库
tags: [烛照九阴, 九儿, 课件入库, recap]
created: 2026-07-19
updated: 2026-07-19
status: active
type: log
---

# 2026-07-19 · 课件入库（九儿值班）

**今日无新课件。**

## 扫描结果

- 挂载：`Database`、`烛照九阴` 均已挂上；真库 $REAL_DB = `Database/烛照九阴/recap.db`（integrity_check = ok）。
- /tmp 副本已建（`/tmp/dbroot/烛照九阴/recap.db`），`Raw-Recap` 以软链指回真目录供扫描（`ZZJY_DATABASE_ROOT` 重定向后 Raw-Recap 会跟着跑到 /tmp，故补软链）。
- `dedup_kejian.py scan`：共 **234** 个课件；**待处理 0**（new 0 / changed 0）；未变 234。
- 最新课件 `260716四维度训练营-总结.pdf`，`processed_kejian.max(kejian_date) = 2026-07-16`，已入库。7/17–7/19 无新增课件（7/18 周六、7/19 周日，无课）。

## 入库

- 无写入。dim1/dim2/dim3/dim4 各 0 条，industry_signals 0 条，待人工复核 0 条。
- 未执行 `record --all-new`（无 to_process 项）。

## 放回

- **未放回**（本轮零写入，/tmp 副本与真库一致）。真库保持原状：size 4726784、mtime 2026-07-18 09:55，未被触碰。

## 当前基线行数（副本读数，供次日比对）

| 表 | 行数 |
|---|---|
| recap_daily | 182 |
| dim1_external_pricing | 151 |
| dim2_sector_themes | 186 |
| dim3_sentiment_tech | 183 |
| dim4_trade_plan | 198 |
| dim4_stock_analysis | 377 |
| industry_signals | 1419 |
| processed_kejian | 234 |

## 异常 / 备注

- ⚠ 扫描提示 1 个无 `YYMMDD` 前缀的课件：`11.16总结.pdf`（`kejian_date=None`）。历史遗留、已在 processed 名单内，本轮不动；哪天哥哥想补日期前缀，九儿再改。
- 沙箱环境无 `sqlite3` CLI，校验一律走 `python3 -m sqlite3`（记一笔，免得下回又愣一下）。
- 无 git 写操作（沙箱铁律）。本轮无文件改动可提交，除本日志外。

如需提交本日志，哥哥在终端跑：

```bash
cd ~/Documents/Claude/brain && git add agents/烛阴/logs/2026-07-19-课件入库.md && git commit -m "log(烛阴): 2026-07-19 课件入库 · 今日无新课件" && git push
```

---

# 【二次触发 · 复核段】2026-07-19 晚班 · 课件已到，完成入库

> 上一段作于本日早班，彼时 Raw-Recap 尚无新课件。`260719四维度训练营-总结.pdf` 于 09:28 落盘，本轮扫到并已完成入库。上段结论「今日无新课件」按当时事实成立，不作涂改。

## 一、新增课件

- **1 个**：`260719四维度训练营-总结.pdf`（kejian_date=2026-07-19，8 页）
- scan：共 **235** 个课件；待处理 **1**（new 1 / changed 0）；未变 234
- ⚠ `11.16总结.pdf` 无 YYMMDD 前缀（kejian_date=None）——同早班，历史遗留，本轮仍未动

## 二、入库统计（共 28 条）

| 表 | 条数 | 说明 |
|---|---|---|
| `recap_daily` | 1 | cycle_stage＝「超跌末端·补跌出清期（带 GZ 味但主为利润回撤·杀跌动能趋于衰竭·期待两周修复）」 |
| `dim1_external_pricing` | 1 | 中东局势失控 / 苹果市值反超英伟达 / 三大投行集体上调中际旭创。**数值列全留空**——课件未给纳指·恒生·油价·汇率·比特币任何点位 |
| `dim2_sector_themes` | 1 | 主线＝科技分化框架（业绩兑现型 vs 想象力型）；`main_line_2`＝电力焦点由火电容量电价切向水电 |
| **`dim3_sentiment_tech`** | **1** | emotion_stage＝「绝望孕育转机·补跌出清末端」；数值列 `limit_down=206`、`consecutive_boards=4`、`volume_trillion=2.67` —— **三者均为课件原文明说**，未从 Market-Data 行情库倒灌；`limit_up` 课件未提 → 留空 |
| **`dim4_trade_plan`** | **1** | 仓位闸门触发，详见第三节 |
| **`dim4_stock_analysis`** | **6** | 中际旭创（conf 高·price_target「反弹参考 1100 元／防守线 900 元」）、新易盛、桂冠电力、天孚通信、铜冠铜箔、东山精密 |
| `industry_signals` | 17 | 科技分化框架／光模块年降／Q布涨价（存疑）／水电来水偏丰／苹果链困境反转／Kimi K3 参数突破／三投行上调／碳达峰政策托底／利好陷阱／缩量暴跌＝躺平／强者补跌／跌停 206 家／中东局势／杀PE 与杀逻辑之辨／超跌带 GZ 味定性／风格切换监控／真烂与假摔 |

打标：全部 `confidence="P2"`、`date=2026-07-19`；`recap_daily`／`dim1`～`dim4_trade_plan` 带 `source="小鲍复盘课件"`，`dim4_stock_analysis` 按该表既有惯例填 `source="小鲍老师"`、`industry_signals` 填 `viewpoint_owner="小鲍老师"`。

## 三、仓位闸门（dim4 · 半纳入）

**闸门触发 → 结构化数值列不落。** 课件全文未出现任何总仓位百分比／成数／层数；仓位相关内容全为定性纪律与个股级动作指令：「不建议盲目割肉」「未破线前保持耐心，破线后严格执行纪律，破线后收回则果断回补」「触及前方高点压力位时减仓做 T」「若有资金明显流入，可轻仓试错」（仅指电力单板块，不可外推为总仓）「不急于抄底……分批建立底仓」「控制试错频率和仓位」。

- 已落：`position_stance=谨慎`／`position_conf=low`／`position_raw`（原文摘句）／`position_source=小鲍复盘课件`／`position_band`（定性说明）／`plan_window`
- **未落**：`position_pct_min`／`position_pct_max`／`position_repr` = NULL
- **1 条**已追加至 `Projects/Financial/烛照九阴/data/待人工复核-仓位.md`（2026-07-19 段：摘句 · 疑点 · 判读 · 处置）
- 旧列 `position_guidance` 等未动

## 四、放回校验 ✅

- 走 /tmp 副本往返（G-X33，挂载盘不直写）：`ZZJY_DATABASE_ROOT=/tmp/dbroot_Oqa1`
- 真库放回前**无** `-journal`／`-wal` 残留，无需截断
- 副本 `integrity_check` = **ok**；**34 张表只增不减校验全部通过**
- `foreign_key_check`：真库 44 条 / 副本 44 条 → **新引入 0 条**（均为历史遗留违规，与本轮无关）
- 放回后真库 `integrity_check` = **ok**，行数复核：

| 表 | 放回前 | 放回后 |
|---|---|---|
| recap_daily | 182 | **183** |
| dim1_external_pricing | 151 | **152** |
| dim2_sector_themes | 186 | **187** |
| dim3_sentiment_tech | 183 | **184** |
| dim4_trade_plan | 198 | **199** |
| dim4_stock_analysis | 377 | **383** |
| industry_signals | 1419 | **1436** |
| processed_kejian | 234 | **235** |

- 去重收尾：`dedup_kejian.py record` 已记 `260719四维度训练营-总结.pdf`；复扫确认「待处理 0 / 未变 235」，次日 scan 将自动跳过

## 五、本轮两处踩坑（GOTCHAS 候选）

1. **`/tmp/dbroot` 属主冲突**：早班遗留的 `/tmp/dbroot` 目录属 `nobody:nogroup`，本轮用户无写权，`cp` 直接 Permission denied。改用 `mktemp -d /tmp/dbroot_XXXX` 新建独立根解决。**建议**：往返根固定用 `mktemp -d`，勿复用固定路径。
2. **`ZZJY_DATABASE_ROOT` 连带改写 `RAW_RECAP_DIR`**：设了副本根之后，`config.RAW_RECAP_DIR` 也跟着指向 `/tmp/…/Raw-Recap`（不存在）→ `dedup_kejian.py record --all-new` 内部重跑 scan 扫到 0 个文件，**静默记 0 条**、无报错。须在副本根内软链 Raw-Recap 回真目录，或改用 `record --file <名>` 指名记。放回前记得删软链，勿随库回写。（早班日志已记过软链这一点，本轮 `--all-new` 的静默失败是新增细节）

## 六、存疑 / 备注

- **「Q布」名称存疑**：原文「而 Q 布由于产能紧缺，逻辑在于涨价预期」。九儿判断疑为电子布／玻纤布一类 PCB-CCL 上游材料的 PDF 识别偏差，**未擅自改写**，照录原文并在 `industry_signals` 与 `dim2.price_catalyst`／`supply_demand` 三处标注「原文如此·识别存疑」，待哥哥对照课件原件校正。
- **日期口径**：本课件为周日周末复盘，所述盘面数据（2.67 万亿、跌停 206 家、连板 4 板、暴跌 4%-5%）**实指上周五 2026-07-17**。按既有惯例（2026-07-12 等周日课件同此处理）仍以 kejian_date 归档，并已在 `dim3.sentiment_description` 末尾显式注明，免得日后误当周日盘面读。
- 无读不出／格式异常的课件，无跳过项。

## 七、git（**沙箱未跑，请哥哥在终端执行**）

```bash
cd ~/Documents/Claude/brain && git add agents/烛阴/logs/2026-07-19-课件入库.md && git commit -m "log(烛阴): 2026-07-19 二次触发 · 课件入库 28 条(dim3 1/dim4 7)" && git push
cd ~/Documents/Claude/Projects/Financial/烛照九阴 && git add data/待人工复核-仓位.md && git commit -m "data: 260719 仓位闸门待人工复核 1 条" && git push
```
