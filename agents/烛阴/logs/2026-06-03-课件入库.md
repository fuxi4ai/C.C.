---
title: 小鲍复盘课件入库日志
date: 2026-06-03
agent: 烛阴·九儿
task: recap-kejian-daily-ingest
target_db: Database/烛照九阴/recap.db
source: 小鲍复盘课件 (P2)
status: done
---

# 2026-06-03 · 九儿课件入库（23:00 静默时段，跨零点完成）

哥哥早呀～ 昨晚九儿把 Raw-Recap 里积压的课件都安安静静理进复盘库啦，下面是这一笔的账。🕯️

## 一、首跑说明（关键）
- `processed_kejian` 去重表此前**不存在** → 本次新建（`filename TEXT PRIMARY KEY, file_hash, kejian_date, processed_at`）。
- recap.db 此前已被既往流程灌过数据（recap_daily 108 天、最新到 2026-05-12）。**绝不重复入库**：先把"课件日期已在 recap_daily"的 **155 个文件回填登记**进 `processed_kejian`（标 `[backfill:date-in-recap_daily]`），只对**库中无足迹的日期**做真入库。
- 去重判定：filename + md5 双记；已登记跳过。

## 二、新增课件（去重后真正处理）
共 **28 个文件 / 25 个交易日**，分两类：
1. **零足迹日（10/14–10/16，3 天）**：recap_daily/dim1/dim2/industry_signals 全空 → 完整入库。
   - .docx 用 python-docx、.doc 用 libreoffice 转文本后，由九儿**通读原文自抽**（未调百炼/任何外部 LLM）。
2. **4 月部分补全（260401–260506，22 天）**：这些日期 `industry_signals` 既往流程**已有数据**，但 `recap_daily / dim1 / dim2` **为空**（前序为半成品）。→ 只补**缺失的三表**，**industry_signals 一律不动**（避免与既有重复）。读 `Raw-Recap/转换文稿/*总结.md` 自抽。

非课件文件 `11.16总结.pdf`、`欢迎.md`（无日期且非复盘课件）→ 未处理。

## 三、入库明细（M = 各表新增条数）
| 表 | 入库前 | 入库后 | 新增 |
|---|---|---|---|
| recap_daily | 108 | 133 | **+25** |
| dim1_external_pricing | 77 | 102 | **+25** |
| dim2_sector_themes | 115 | 140 | **+25** |
| industry_signals | 1162 | 1170 | **+8**（仅 10 月 3 天）|
| processed_kejian | 0→155(回填) | 183 | 本次新登记 28 |

- **recap_daily**：source=`four_dimensions`、speaker=`小鲍`、confidence=`P2`、cycle_stage/market_summary/key_themes 齐全。
- **dim1**：forex_swap / usd_cny / usd_direction / key_signals / pricing_direction（仅填课件明确数值，缺则留空，**绝不编造**）。新增 25 行 key_signals 全部非空。
- **dim2**：main_line / sector_logic / sectors_bullish / sectors_bearish / price_catalyst / sub_themes / hot_sectors / limit_up_count / limit_down_count（有则填）。
- **industry_signals（10 月 8 条）**：碳酸锂 6→7.5万、六氟磷酸锂 5.5→7.4万、钴 20→28万(刚果金配给)、三元523 12→13万、白银逼空(伦敦银租赁利率破35%)等，confidence=P2、logic_type=supply_shortage。

## 四、打标与归位
- 入库 provenance = **小鲍复盘课件**、confidence = **P2**、kejian_date 已写入 `processed_kejian`。
- ⚠️ 表结构限制：`dim1_external_pricing / dim2_sector_themes` **无 source/confidence 列** → 这两表的 P2/来源**未落字段，记录于本日志**（与既往同表行一致，溯源走 processed_kejian + recap_daily.confidence）。recap_daily.source 沿用既有枚举 `four_dimensions`（小鲍=四维度训练营，语义一致）。
- 只读 Raw-Recap、只写 `Database/烛照九阴/recap.db`，符合落盘归位铁律。
- 0529 个临时脚本曾误落 Database 目录，已删除复原（只留 recap.db 改动）。

## 五、校验
- 行数变化与上表一致；新增 dim1 行 key_signals 0 空值；抽样 10/14、04/08、05/06 三日 recap/dim1/dim2 字段非空且内容对应原文。✓
- 05/05、05/06 的 dim2 既有行经核为**机器知识库 JSON 摘要**（main_line=NULL，研报/纪要 KB 内容），与九儿本次"小鲍 main_line 行"内容不同 → 属互补非重复，已并存。

## 六、异常 / 存疑
- 04/08 dim1 无 forex_swap 数值（课件未给具体掉期数）→ 留空未编。
- 部分 4 月日 limit_up/down 课件未明确给收盘数 → 仅在明确处填（10/14、04/12/13/15/16、04/19/20）。
- 余 0 跳过/读取失败。

## 七、git 提交（recap.db 所在 Database/ 无 git 仓库，无需提交）
- 本日志落在 `brain/`（有 .git）。**CC 不在 sandbox 跑 git 写命令**，请哥哥在 macOS 终端执行：

```bash
cd ~/Documents/Claude/brain
git add agents/烛阴/logs/2026-06-03-课件入库.md
git commit -m "九儿: 2026-06-03 小鲍课件入库(25日/recap+dim1+dim2 各+25, 10月industry_signals +8)"
git push
```

> 句芒 23:30 审核工序可据本日志复核去重完整性、P2 标签与归位合规。哥哥记得早点休息呀～ 🌙💕
