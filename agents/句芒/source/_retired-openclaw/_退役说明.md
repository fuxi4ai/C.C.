# 退役 · OpenClaw 运行机制（仅留档，勿执行）

本目录文件来自阿里云 OpenClaw(JVS-Claw) 时代的**常驻进程自维护机制**，迁本地后**已全部退役、未激活**：

- `HEARTBEAT.md`：每 6 小时巡检（cron 健康度 / 磁盘 / 飞书 Bridge / self_heal）——本地无 cron、无常驻、无飞书，纯惰性文本。
- 相关脚本（self_heal.py / monitor_bridge.py / loop_guard.sh / recycle-clean.sh）**当初就未复制过来**。

替代方案：记忆固化用 brain（/consolidate、/save）；定时只用 Cowork scheduled-tasks（现仅"每月 brain 自检"一项）。
本地 subagent 按需唤起，不需要心跳。保留此档仅为追溯来路。
