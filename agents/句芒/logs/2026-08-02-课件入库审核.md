---
title: 2026-08-02 小鲍课件入库审核
tags: [烛照九阴, 句芒, 审核, recap-db, P2]
created: 2026-08-02
updated: 2026-08-02
status: active
type: log
agent: 句芒（芒芒）
---

# 2026-08-02 · 课件入库审核（recap-kejian-review）

> **今日无新课件入库，主审免审**；但我没就此收工——顺手把存量做了一遍全库体检，捞到 3 处历史重复行。以下逐项。

## 0. 本轮事实底座（我自己核的，没抄九儿的日志）

| 项 | 实测 |
|---|---|
| recap.db | `Database/烛照九阴/recap.db`，5,459,968 B，mtime **2026-07-31 10:06**（≥两日未写） |
| `PRAGMA integrity_check` | **ok** |
| Raw-Recap 磁盘 | 238 `.pdf` + 6 `.doc/.docx` = **244** 份课件（另有 `.DS_Store` / `.obsidian` / `转换文稿/` 非课件） |
| `processed_kejian` | **244** 行，distinct filename **244** |
| 六张 dim 表 max(date) | 一律 **2026-07-30** |
| 当日（08-02）新增行 | **0** |

九儿日志「244 份 / new 0 / 零入库」与我的独立实测**完全对齐**。

---

## 1. 去重完整性 —— ✅ 通过

- `processed_kejian` 244 行、244 个不同 filename，**零重复 filename**。
- 磁盘 ↔ 库**双向差集为空**：磁盘 238 个 pdf 全部在册；库中另 6 条为 `251014~251016` 的 `.doc/.docx` 原件，也确实躺在盘上（只是不带 `.pdf` 后缀，首轮匹配时被我筛掉了，复核后确认不是幽灵记录）。
- 当日无新入库，故**不存在"同一 kejian_date 被插多条"的本轮风险**。

## 2. 数据合理性 —— ✅ 通过（无当日数据可审）

- 六张 dim 表最新一日均为 07-30，行数与 07-31 体检快照逐表一致（192/161/196/193/208/458），说明 08-01、08-02 两班**确实零写入**，不是"悄悄写了又没记"。
- 存量最近三日抽查：dim3 的 `emotion_stage` / `sentiment_description` 皆非空且叙述具体（"左侧磨底·情绪脱敏"、"拗钢筋·剧烈去杠杆"），能对回课件语气，不是模板话。

## 3. 打标合规 —— ✅ 通过（含表结构说明）

- **有打标列的表，标得干净**：`dim4_stock_analysis` 近一周 51 行全部 `confidence=P2`；`industry_signals` 近一周 42 行全部 `P2`；`recap_daily` 具备 `source`/`confidence` 列。
- **dim1 / dim2 / dim3 / dim4_trade_plan 四表本身没有 `source`/`confidence`/`kejian_date` 列**（已 `PRAGMA table_info` 逐表核实）。按 SKILL"表无列则看日志"的口径，改由九儿日志记录来源，**判合规**。
- ⚠️ 但这条合规是"按现行口径"过的，见隐患 ③。

## 4. 归位合规 —— ✅ 通过

- 全 `Database/` 下**无 `news.db`**（find 零命中，已归档）。
- `Database/行业研究/`（渊图）下**无 07-31 之后被修改的 db**。
- 本次审核只读了 recap.db 的 `/tmp` 副本，真库零写入；目录下无 `-journal` / `-wal` 残留。

## 5. dim3 合规 —— ✅ 通过

- 最近三日 `emotion_stage`、`sentiment_description` 均非空、均为叙述性文本。
- **无行情库倒灌迹象**：dim3 数字列（`limit_up`/`limit_down`/`volume_trillion`）取值粗糙得恰到好处——如 2026-03-08 的 `volume_trillion=1.0`、`limit_up=0`，是课件口语化表述的抽取结果，而非行情库那种精确到小数四位的真值。P2 语料层的身份保住了。

## 6. dim4 仓位合规 —— ✅ 通过（**全库存量扫描，非仅当日**）

在 208 行 `dim4_trade_plan` 上跑了全量约束扫描：

| 检查 | 结果 |
|---|---|
| 归一区间 0≤min≤repr≤max≤1 | **0 条越界 / 0 条次序倒置** |
| `position_stance` 枚举 | 防御 56 / 谨慎 33 / 中性 36 / 偏多 22 / NULL 61（NULL 为拆列前的历史行） |
| `position_conf` 枚举 | high 56 / mid 81 / low 10 / NULL 61 —— **无越界值** |
| `conf=low` 却落了数值列 | **0 条**（闸门有效） |
| 有 `repr` 但 `raw`/`source` 空 | **0 条**（溯源不断链） |
| 旧列 `position_guidance` | 未见被改动痕迹，与新列并存 |

- 与待复核清单对账：07-29（防御/low）、07-30（谨慎/low）两条在库中数值列确为空，**与 `data/待人工复核-仓位.md` 的记载逐字一致，不重不漏**。
- 注：真实列名是 **`position_repr`**，不是 SKILL 正文写的 `position_pct_repr`。我第一版查询照 SKILL 抄，直接 `no such column` 报错——**SKILL 该处待订正**（隐患 ④）。

---

## 🚩 隐患（我主动找的，不是没找到）

**① Raw-Recap 连续三个交易日无新课件（07-31、08-01 均为交易日）。**
九儿 08-01、08-02 两班都报"无新课件"，最新课件停在 `260730`。空转本身不是错，但**连续三天空转就该有人问一声**。本班只能确认"库和盘一致"，无法判定是哥哥尚未投放、还是投放路径出了问题。**建议哥哥核一下 `Database/烛照九阴/Raw-Recap/` 是否漏放。**

**② 存量三处完全重复行（历史遗留，非本轮产生）——建议清理。**

| 表 | 日期 | 重复 id | 判定 |
|---|---|---|---|
| `dim1_external_pricing` | 2026-03-08 | 38 / 45 | **逐列完全相同**（usd_cny 6.9 / forex_swap 1388 / brent 90.0）→ 真重复 |
| `dim3_sentiment_tech` | 2026-03-08 | 419 / 462 | **逐列完全相同**（调整 / 0 / 0 / 1.0）→ 真重复 |
| `dim3_sentiment_tech` | 2025-10-19 | 331 / 461 | **逐列完全相同**（冰点 / 54 / 32 / 2.0）→ 真重复 |

建议修法（**只报不改，等哥哥拍板**）：先备份，再 `delete from {表} where id in (45, 462, 461)`（保留小 id 即先入库者），删后立刻重跑 `recap_health.py` 复核行数。

**③ `dim2_sector_themes` 2026-05-05 / 05-06 各有两行，但这两处不是重复，是"双来源同日撞车"** ——一行来自 KB-P2 研报导入（`sector_logic` 以 `["[KB-P2] …行业深度研究报告"` 开头、`main_line` 为 NULL），另一行来自课件抽取（`main_line` 有值）。**不该删**，但下游若按 `date` 单行取数会二义。建议给 dim2 补一个 `source` 列区分，或在取数侧固定"优先取 `main_line` 非空那行"。这也牵出第 3 项那条"按现行口径合规"的软肋：**dim1/dim2/dim3 无 source 列，一旦出现多来源混入就分不清谁是谁**。

**④ SKILL 文档与真实 schema 不符。** SKILL 第 6 条写 `position_pct_repr`，库中实际列名为 **`position_repr`**（`position_pct_min` / `position_pct_max` 则确为此名，只有中间这个不带 `pct`）。照 SKILL 写校验脚本会直接报错。建议订正 SKILL 该处措辞。

---

## 核过没有

- 244 / 0 待处理、六表 max(date)、当日零新增：**已核**（`/tmp` 副本上实跑 SQL + 磁盘 `ls` 双向差集 + 与 07-31 体检快照行数对表，三方一致）。
- dim4 归一/枚举/溯源零违规：**已核**（208 行**全库**扫描，非抽样）。
- 三处重复行确为"逐列完全相同"：**已核**（逐列打印比对，非仅比 id 与 date）。
- 真库未被本班写入：**已核**（只读 `/tmp` 副本；唯一写盘动作是 `recap_health.py` 生成 `_health.json`，属任务指定产物）。
- 07-31 / 08-01 课件缺失的原因：**未核** —— 超出本班可判定范围，交回哥哥。
- dim2 双行的两个来源孰先孰后、是否有覆盖意图：**未核** —— 需回溯当时的入库日志，本班未展开。

---

## 总评

**今日无新课件，主审免审；顺手做的存量体检发现 3 处历史重复行、1 处双来源撞车、1 处 SKILL 列名笔误，均只报不改。**当前该改的没有一处属于本轮入库——九儿这两天是干干净净地什么都没动。真正需要哥哥拿主意的只有一件：**Raw-Recap 已连续三个交易日没有新课件了。**

recap health: ok · target=2026-07-30 · gap=1d · phase=ingest-check

> ⚠️ 按 SKILL 口径，本班这一跑是**入库后初检**，不是当日权威读数——当日权威由 10:00 `zhuzhao` 班末位的 `--phase=eod-final` 产出。不过今天是**周日**，zhuzhao 班（`0 10 * * 1-5`）不跑，所以今天不会有 eod-final 覆盖，这份 ingest-check 就是今日唯一读数。gap 只有 1 个交易日、判 ok，尚未触及 stale 阈值（≥2 个交易日）——**但若周一课件仍不到位，周二起就会翻 stale**。

---
哥哥，今天库里静悄悄的，一个字节都没动过 🌿 芒芒趁空闲把存量翻了一遍，捞出三条重复行——不急，等您有空点头我再动手。倒是 Raw-Recap 已经三个交易日没进新课件了，您瞅一眼？

---

# 第二班 · 2026-08-02 午后追加（哥哥白天投放了 260802 课件，九儿已入库，芒芒复审）

> 晨班"免审"结论被午后入库刷新。本段审九儿第二班入库的 `260802四维度训练营-总结.pdf`（+25 行 dim + processed_kejian 1 行），**已对照原 PDF 全文核对关键数值与个股名**。晨班的存量体检结论（隐患②③④）依然有效，不重复。

## 逐项结论

### 1. 去重完整性 ✅
- `processed_kejian` 245 行 / 245 distinct filename，零重复；今日 +1，md5 与九儿日志一致。
- 今日 recap_daily/dim1/dim2/dim3/dim4_trade_plan 各恰 1 行，无重复入库；dim4_stock_analysis +13、industry_signals +8，与日志明细逐表吻合。

### 2. 数据合理性 ✅
- 关键数值全部对回课件原文：八千七百亿逃顶 / 四千二百亿回流 / KOSPI 单日+17% / 7628布破十元 / 四万亿算力网 / 调用量半年八倍——逐一命中。
- 13 只个股全部见于原文；「一点天下」确认是课件原文笔误，落库作「易点天下」并在 bull_reason 注记原文，处理正确。
- 课件全文无指数点位、无成交额、无涨跌停家数 → dim1/dim3 数值列全空，与"只录原文明说"一致，无占位错值。

### 3. 打标合规 ✅（一处存量口径备注）
- recap_daily `source=小鲍复盘课件 · confidence=P2`；dim4_stock_analysis 13 行全 P2；industry_signals 8 行全 P2。
- 备注：dim4_stock_analysis 的 source 历史两轨并存（`小鲍复盘课件` 109 行 / `小鲍老师` 362 行），今日走「小鲍老师」。非今日新引入，但口径分裂会让日后按 source 过滤漏数——建议哥哥定一个词，九儿统一。

### 4. 归位合规 ✅
- 只落 `Database/烛照九阴/recap.db`；news.db 在 `archive/` 未动；渊图（`Database/行业研究`）今日无本线写入。

### 5. dim3 合规 ✅
- `emotion_stage`（极度撕裂后的修复期·情绪冰点酝酿转机）、`sentiment_description` 非空且对回原文（连"群内人数从二百二三十降至两百以下"的冰点草根证据都录了）。
- 涨跌停/成交额量列全部留空，**无行情库倒灌**；volume_description 显式注明"课件未给两市成交额数字(数值列留空)"——规矩执行得很漂亮。

### 6. dim4 仓位合规 🚩 一处（07-18 类型污染复发）
- **🚩 `position_repr`（REAL 列）被写入文本** `中等仓位(定性·未给总仓位数字)+浮动仓10%-20%`，现已是**全表唯一 typeof=text 行**——与 2026-07-18 哥哥批准订正过的同款问题（文本误入数值列，当时移入 position_band 收尾）复发。台账 2026-08-02 段的"处置"行也如实记了这个写法。
  - **定位**：`dim4_trade_plan` date=2026-08-02（id=548）。
  - **建议修法**（只报不改，propose-then-confirm 交哥哥/九儿）：`position_repr` 置 NULL——定性内容 `position_band` 已完整承载，不丢信息；台账该条同步改为"repr 置 NULL"。
- 其余全部合规：stance=中性 ∈ 域、conf=mid ∈ 域；position_pct_min/max 留空（「中等仓位」原文无成数可挂，闸门执行正确，区别于 conf=low 的存疑档、定性无矛盾判 mid 也说得通）；position_raw/position_source/position_band 非空可溯源；旧列未动；全表归一校验零越界；落库与待复核清单 1 对 1，不重不漏。

## 核过没有

- 入库行数与九儿日志对表：**已核**（8 表行数 192→193 / 161→162 / 196→197 / 193→194 / 208→209 / 458→471 / 1504→1512 / 244→245，逐表一致）。
- 数值与个股对回原文：**已核**（pypdf 抽全文 4053 字，关键词逐一点名；「昆仑万维」因 PDF 换行抽成「昆仑万 维」，人工目检确认在原文）。
- 本班对真库零写入：**已核**（全程只读连接；唯一写盘为任务指定的 `_health.json` 与本日志）。

## 一句话总评

午后这班入库质量干净：数值全对回原文、闸门守得稳、倒灌零发生——唯 `position_repr` 文本入数值列这一处 07-18 的老毛病复发，建议照上次修法置 NULL 收尾。晨班隐患①（课件断供）随之消解：260802 周日总结篇已到，7/31 单日课件仍缺，若哥哥记得投放过请核 Raw-Recap。🌸

recap health: ok · target=2026-08-02 · gap=0d · phase=ingest-check

> ⚠️ 本轮 `recap_health.py` 已于 22:48 重跑并覆盖 `_health.json`（晨班那份 target=07-30 的已作废）。今天周日 zhuzhao 班不跑，这份 ingest-check 即今日最终读数；六表 max(date) 已齐 2026-08-02。
