---
title: 2026-06-16 行情更新（盘前增量）
tags: [句芒, market-data, 盘前, 增量更新]
date: 2026-06-16
agent: 句芒
type: log
---

# 2026-06-16 盘前行情更新 · 芒芒

> 运行时点：上海 06:20（UTC 2026-06-15 22:20）盘前。目标交易日 = 前一交易日 = **20260615（周一）**。

## 结论
✅ 已更新到 20260615。daily_market 派生表补齐 1 个交易日（含指数/北向），其余主表本已最新。

## 各表状态（更新后 · 活库校验 integrity=ok）
| 表 | 更新前 max | 更新后 max | 行数变化 |
|---|---|---|---|
| stock_daily | 20260615 | 20260615 | 1268591（已最新，无新增） |
| daily_market | 20260612 | **20260615** | 1560 → **1561**（+1） |
| sector_daily | 20260602 | 20260602 | 9312（无本地成分映射，留缺，未臆造） |
| north_flow | 20260615 | 20260615 | 806（已最新，无新增） |

校验：各表行数**只增不减**、非空；integrity_check = ok。

## 取数（步骤 1–5）
- stock_daily / north_flow 在库已含目标日 20260615 → **无缺失交易日，无需网络回补**。
- TUSHARE_TOKEN：从 `~/Documents/Database/.env` 读取成功（len=56）。

## 派生表聚合（步骤 6 · aggregate_derived.py）
- 沙箱 `Path.home()` 默认路径不可用 → 显式传 `--db` 指向库。
- 口径闸生效：跳过 97 个池内时代日（<5000 只，20260105…20260602 旧 ~840 只池，不混写）。
- 补 20260615：daily_market 1560→1561。

## 指数/北向补缺（步骤 7 · fill_index_north.py）
- 经 `MARKET_DB` 环境变量覆盖库路径（该脚本无 --db）。
- 北向：无缺口（聚合已从 north_flow 回填）。
- 指数：Tushare HTTP index_daily 拉到 20260615 的 sh/sz/cyb，仅填 NULL 列，填 3 格，剩余 NULL=0。

## 20260615 daily_market 成品行
- 上证 sh_pct_chg=+1.61%（close 4096.47）
- 深成 sz_pct_chg=+3.79%（close 15531.11）
- 创业 cyb_pct_chg=+5.30%（close 4033.53）
- 成交 volume_trillion=3.051 万亿
- 涨停 146 / 跌停 4 / 最高连板 3
- 北向 north_money=409723.04
- 家数 total_stocks=5508

## ⚠️ GOTCHA 命中与处理（重要）
- **disk I/O error on commit**：230MB 库在挂载盘直接 `con.commit()` 报 disk I/O error（已知 211MB+ 大库挂载盘写入问题）；commit 失败后留下 **12824B 热 journal**，挂载盘连只读 open 都 I/O error。
- 按脚本头注释「拷 /tmp 写好整库放回」路径处理：
  1. 连 journal 一起拷到 `/tmp/sjm_market/` → 在可写盘 open，SQLite 自动回滚（回到 commit 前状态，无污染）、integrity=ok、journal 消失。
  2. 在 /tmp 副本上重跑 aggregate（--db）+ fill_index_north（MARKET_DB）→ 1561 行、整行填满、integrity=ok。
  3. cp 回挂载盘为 `market_data.db.new` → 校验 ok → `mv -f` 原子替换原库。
  4. 残留旧 `-journal` **删不掉**（Operation not permitted）→ 按 GOTCHA 截断为 **0 字节**（无害）。
- 全程**未在 sandbox 跑 git**；**未臆造任何行情**（拉不到即留缺）。

## 步骤 8 · 周快照
- 今天周二 → **跳过**（仅周五跑）。

## 待办 / 提醒
- sector_daily 仍停在 20260602（缺本地成分映射，长期留缺，需哥哥侧补映射或 Mac 侧取数）。
- 大库挂载盘 commit I/O error 反复出现，cron 直跑聚合脚本会失败——本次靠 /tmp 中转兜住；建议把「/tmp 中转」固化进取数脚本或 cron 包装，免得每次手动救。
