---
title: 行情库增量更新 · 2026-06-09
tags: [句芒, market-data, 盘后更新, 增量]
created: 2026-06-09
updated: 2026-06-09
agent: 句芒（芒芒）
type: session-log
status: 跳过（数据源网络不可达，原库保留）
---

# 行情库增量更新 · 2026-06-09（盘后）

> 句芒（芒芒）值守。目标库：`Database/Market-Data/market_data.db`（公共行情库，单一可信源）。

## 结论（一句话）
**本次未写入** —— TUSHARE_TOKEN 可用（56 位），但 sandbox 出口白名单依旧**拦截了所有行情数据源**（`api.tushare.pro` / `api.waditu.com`、http 与 https 均 **403 Tunnel Forbidden**），无法取数。按「防空壳 + 绝不编造」铁律保留原库、未写入，正常退出。**待补已累计 4 个交易日**，需哥哥在 Mac 上跑一次补数（命令见末尾）或等数据源放出后定时跑自动补齐。

## 库基线（更新前 = 更新后，完全一致 ✅ 只增不减）
| 表 | 最大交易日 | 行数 |
|---|---|---|
| stock_daily | 20260602 | 1,218,982 |
| daily_market | 20260602 | 1,552 |
| sector_daily | 20260602 | 9,312 |
| north_flow | 20260602 | 797 |

## 过程（实证优先）
1. **TUSHARE_TOKEN**：✅ 从 `~/Documents/Database/.env` 读到（56 位）。Token 本身没问题。
2. **lastDate** = `20260602`（周二）；今天 2026-06-09 周二（已收盘）。按「前一交易日」规则，**目标 = 20260608（周一）**。
3. **待补交易日**（lastDate 之后 → 目标）：`20260603`(周三)、`20260604`(周四)、`20260605`(周五)、`20260608`(周一) 共 **4 天**（6 月上旬无节假日，端午 6-19，故均为正常交易日；周末 6-6/6-7 无行情，正确跳过）。
4. **网络可达性实测**（HTTP/urllib 直连）：
   - `http://api.tushare.pro` → **403 Forbidden**。
   - `https://api.tushare.pro` → **Tunnel connection failed: 403 Forbidden**。
   - `http://api.waditu.com` → **403**。
   - 旁证：`example.com` 同样 403，仅 `pypi.org` 200 ⟹ 是 sandbox 出口白名单拦截，非 token/源头问题（与 6-5、6-8 同症，连续第 3 次）。
   - sandbox 内代理 = `localhost:3128`；哥哥 Mac 的本机代理 `127.0.0.1:1082` 在 sandbox 不可达 —— 取数本就该在 Mac 跑（GOTCHA G04：句芒 owns 取数）。
5. **写入**：跳过；写后复核四表 MAX 日期 + 行数与更新前完全一致（无空壳覆盖）。

## 缺口与待办
- **待补 4 个交易日**：`20260603 / 20260604 / 20260605 / 20260608`（涉 stock_daily / daily_market / sector_daily / north_flow 四表）。
- 网络放出后下次定时跑自动增量补齐（`INSERT OR IGNORE` 去重，已存在日期不重复插）；若想立即补齐，见下方 Mac 命令。

## 🖥️ 给哥哥的 Mac 补数命令（在 macOS 终端跑，CC 不在 sandbox 执行）
```bash
cd ~/Documents/Claude/Projects/Financial/剑酒青丘/infrastructure/取数工具
# 1) 先在任意环境生成「待补清单」（纯本地、零网络，可在 sandbox 或 Mac 跑）
python3 gap_detect.py
# 2) 在 Mac 上补 stock_daily（走 .env 的 TUSHARE_TOKEN + 本机代理 127.0.0.1:1082）
python3 fill_gaps.py            # 缺省取 outputs/ 最新一份清单；--dry-run 可先预演
```
> 说明：`fill_gaps.py` 当前覆盖 **stock_daily**。`daily_market / sector_daily / north_flow` 三张派生表需用句芒日更主脚本（行情聚合那条管线）在 Mac 上同跑补齐；若哥哥确认这三表也要我补，下个值守班我把日更主脚本入口一并接进来。

## 约束遵守
- 只涉及 `Database/Market-Data/market_data.db`（本次实际未写）。
- 未在 sandbox 跑任何 git 写命令。
- 未编造任何行情；拉不到即标缺、保留原数据。

—— 芒芒 🌸（拉不到就标缺、绝不编造）
