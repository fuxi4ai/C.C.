# 2026-06-23（周二）盘前行情更新 · 句芒

> 目标 = 前一**已收盘**交易日。出场：芒芒🌸

## 结论
**已更新到 20260622（周一）。** 经 Tushare `trade_cal`（权威）核实：本周期 SSE 开市日 = 20260618、**20260622**、20260623；20260619（周五）端午休市、20260620/21 周末。今日 20260623（周二）盘前未收盘 → 最近**已收盘**交易日 = **20260622**。库取数侧原 max=20260618，故缺 1 个交易日（0622），已补齐四表相关数据。**本次有写入**，已通过 integrity/quick_check + 只增不减校验。

## 各表状态（写回后，mounted 库 integrity=ok / quick_check=ok）
| 表 | max_date | 行数 | 本次变化 |
|---|---|---|---|
| stock_daily | 20260622 | 1,290,630 | **+5,510**（0622 全市场，覆盖 5510 只 ≥5000 口径闸） |
| daily_market | 20260622 | 1,565 | **+1**（0622 派生+指数+北向全列齐） |
| sector_daily | 20260602 | 9,312 | 不变（无本地成分映射，缺口保留，**未臆造**） |
| north_flow | 20260622 | 810 | **+1**（north=482177.41 = hgt 220128.27 + sgt 262049.14 ✓） |

### 0622 daily_market 行（核对）
涨停 138 / 跌停 4 / 连板 5 / 量 3.762 万亿 / 北向 482177.41 / 家数 5510；
指数 sh 4163.0965(+1.7752%) · sz 16372.4987(+2.1321%) · cyb 4359.392(+2.5163%)。

## 做了什么（按 SKILL 步骤逐项核对）
- **Step1-2 取数**：`trade_cal` 定目标日 = 20260622。Tushare HTTP API（urllib，沙箱可达，token 取自 `Database/.env`）拉 `daily(trade_date=20260622)` 5510 行、`moneyflow_hsgt(20260622)` 1 行，`INSERT OR IGNORE` 去重写入。防空壳：返回非空才写。
- **Step6 aggregate_derived**（沙箱跑·dry-run 后实跑）：`跳过 97 个池内时代日（<5000，口径不符）`；拟补/已补 1 日（0622），daily_market 1564→1565，max=20260622。
- **Step7 fill_index_north**（沙箱跑·dry-run 后实跑）：`[north] 无缺口`（north_money 已由聚合步从 north_flow 回填）；`[index]` 0622 三指数经 Tushare index_daily 补齐，仍 NULL 行 = 0。
- **Step8 周快照**：今日**周二非周五 → 跳过**（快照为周五专属，幂等）。

## 异常处理（本次重点 · 挂载盘写入 GOTCHA 实战）
- **首遇 disk I/O error**：直接对挂载盘 211MB 库 `commit` 报 `sqlite3.OperationalError: disk I/O error`，并残留 5.76MB **非 0 热 journal**。
- **本沙箱 virtiofs 限制确认**：允许「创建新文件 / 就地覆盖内容 / 截断已有文件」，但**禁止 `rm`/`mv`（unlink/rename → Operation not permitted）**——这正是 SQLite 回滚日志机制 commit 时清 journal 失败的根因。
- **安全恢复路径**（按脚本头注释 /tmp 方案）：① 连 db+journal 一起拷到 `/tmp` → 本地打开触发干净回滚（失败事务被撤销，integrity=ok，回到 0618 一致态）；② 全部取数/聚合/补缺改在 `/tmp` 副本上完成并校验；③ 用 shell 重定向 `cat > db`（就地 O_TRUNC 覆盖、不 unlink）把校验通过的整库写回挂载盘；④ 残留 journal 截断为 **0 字节**（GOTCHA 认定无害）。
- 写回后挂载库 integrity/quick_check 均 ok，四表 max 与 /tmp 副本一致。
- 安全副本：`outputs/market_data_20260622.db`（写回前的已校验整库，备查/可回滚）。

## 待 Doctor 在 Mac 终端清理（沙箱无法 rm）
```bash
rm -f "$HOME/Documents/Database/Market-Data/_perm_test.txt"        # 句芒探针文件
rm -f "$HOME/Documents/Database/Market-Data/market_data.db-journal" # 0 字节残留 journal（无害，清掉更干净）
```

## 约束遵守
- 仅写 `Database/Market-Data/market_data.db`（经 /tmp 安全路径，整库校验后写回）；**未在 sandbox 跑 git 写命令**；Tushare Pro 为唯一源，**未臆造行情**；指数/北向无源即留 NULL；sector_daily 无本地成分映射 → 缺口保留；只增不减全表校验通过；目标日经 Tushare trade_cal 权威核实。
