---
title: 行情库增量更新 · 2026-06-04
tags: [句芒, market-data, 盘后更新, 增量]
created: 2026-06-04
updated: 2026-06-04
agent: 句芒（芒芒）
type: session-log
status: 跳过（数据源无新数据，原库保留）
---

# 行情库增量更新 · 2026-06-04（盘后）

> 句芒（芒芒）值守。目标库：`Database/Market-Data/market_data.db`（公共行情库，单一可信源）。

## 结论（一句话）
**本次未写入** —— TUSHARE_TOKEN 可用、网络已可达 `api.tushare.pro`，但数据源对目标交易日**返回 0 行**（连库里已有的 6-2 也拉不到），即源头数据前沿 ≤ 20260602，无可取新数据。按「防空壳 + 绝不编造」铁律保留原库、未写入，正常退出。

## 库基线（更新前 = 更新后，完全一致 ✅ 只增不减）
| 表 | 最大交易日 | 行数 |
|---|---|---|
| stock_daily | 20260602 | 1,218,982 |
| daily_market | 20260602 | 1,552 |
| sector_daily | 20260602 | 9,312 |
| north_flow | 20260602 | 797 |

## 过程（实证优先）
1. **TUSHARE_TOKEN**：✅ 从 `~/Documents/Database/.env` 读到（56 位），`ts.pro_api()` 连接正常（本次网络已通，非上次的 403）。
2. **lastDate** = `20260602`；今天 2026-06-04 周四（CST 16:37，已收盘），目标本应为前一交易日 **20260603**。
3. **Tushare 拉取（主）**：
   - `pro.daily(trade_date=20260603)` → **0 行**；`20260604` → **0 行**。
   - 反证：库里已有的 `20260602 / 20260601 / 20260529` 用 `pro.daily` 重拉也都 **0 行**；`trade_cal` 返回空表。
   - ⟹ 该 token 下数据源**可取前沿 ≤ 20260602**，目标窗口源头无数据（非数据丢失），库已对齐前沿。
4. **mootdx 兜底**：环境未安装 `mootdx`；且 mootdx 取的是**真实当前交易日**行情，与目标日 20260603 不对应，硬塞会构成"编造行情"，**不采用**。
5. **写入**：跳过；写后复核四表 MAX 日期 + 行数与更新前完全一致（无空壳覆盖）。

## 缺口与跳过
- **20260603（6-3 周三）待补**：源头暂无数据，数据源放出后下次定时跑自动增量补齐（`INSERT OR IGNORE` 去重，不会重复）。
- mootdx：模块缺失 + 场景不适用，兜底未启用。

## 约束遵守
- 只涉及 `Database/Market-Data/market_data.db`（本次实际未写）。
- 未在 sandbox 跑任何 git 写命令。
- 未编造任何行情；拉不到即标缺、保留原数据。

---
### 附：本日早些时候的首跑记录（已被本条覆盖）
- 07:01 首跑：实测 `api.tushare.pro` / `waditu.com` / TDX 端口均 **403 Tunnel Forbidden**，判定网络白名单拦截、跳过。
- 16:37 本次复跑：网络已恢复可达，Tushare 连得上但返回空数据 → 改判为「数据源无新数据」，结论仍为跳过、保留原库。

—— 芒芒 🌸（拉不到就标缺、绝不编造）
