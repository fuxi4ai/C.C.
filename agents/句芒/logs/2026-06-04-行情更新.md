---
title: 行情库增量更新 · 2026-06-04
tags: [句芒, market-data, 盘后更新, 增量, network-blocked]
created: 2026-06-04
agent: 句芒（芒芒）
type: session-log
status: 跳过（数据源不可达）
---

# 行情库增量更新 · 2026-06-04（盘后）

> 句芒（芒芒）值守。目标库：`Database/Market-Data/market_data.db`（公共行情库，单一可信源）。

## 结论（一句话）
**本次未写入** —— Tushare / mootdx 数据服务器在当前 sandbox 网络白名单内均被拦截（403 Forbidden），拉不到 6-3 行情。按「防空壳」铁律保留原数据、不编造、不空壳覆盖，正常退出。配好网络出口后即自动生效。

## 现状盘点
- TUSHARE_TOKEN：✅ 已读到（`~/Documents/Database/.env`，56 位）。
- 库内各表 `MAX(trade_date)` = **20260602**（6-2 周二），四表一致：
  - `stock_daily` 1,218,982 行
  - `daily_market` 1,552 行
  - `sector_daily` 9,312 行
  - `north_flow` 797 行（min 20230103）
- 今天 = 2026-06-04 周四（Asia/Shanghai 07:01）。
- **应补交易日** = 前一已收盘交易日 = **20260603（6-3 周三）**，缺口 1 个交易日。

## 为什么没更新
sandbox 出网是固定白名单代理，行情数据源全部不可达，实测：

| 目标 | 结果 |
|---|---|
| `https://api.tushare.pro`（Tushare 主） | ❌ 403 Tunnel Forbidden |
| `https://waditu.com` | ❌ 403 |
| TDX 行情服务器 `119.147.212.81:7709`（mootdx 兜底依赖） | ❌ 403 |
| `https://pypi.org` | ✅ 通（仅装包可用，非数据源） |

Tushare 主路 + mootdx 兜底**两条腿都被网络拦死**，不是 token 问题、不是脚本问题。

## 处置（守铁律）
- ❌ 不写入空数据 / 不以空壳覆盖既有库。
- ✅ 四表行数、MAX 日期保持原样（只增不减，本次增 0）。
- ✅ 未跑任何 sandbox git 写命令。
- 缺口标记：**20260603 待补**（数据源恢复后下次自动增量补齐，去重 `INSERT OR IGNORE`，不会重复）。

## 下次复跑前置（哥哥侧二选一）
1. 把 `api.tushare.pro`（或 `waditu.com`）、TDX 行情端口加入 sandbox 出网白名单；或
2. 在可联网的 macOS 本机跑取数脚本（`09-tushare-pro` HTTP REST / `02-tools mootdx` 兜底）补 6-3，再同步回公共库。

—— 芒芒 🌸（拉不到就标缺、绝不编造）
