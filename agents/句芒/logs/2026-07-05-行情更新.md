---
title: 2026-07-05 行情更新（Cowork 定时任务）
tags: [句芒, market-data, 日更, 行情]
created: 2026-07-05
agent: 句芒
type: log
---

# 2026-07-05 行情更新 · Cowork 定时任务

> 出场：句芒（芒芒）🌸 — 公共行情库 `Market-Data/market_data.db` 唯一写入方。

## 结论：**已最新，无需更新**（库 max = 20260703，上一收盘交易日）

- 触发时刻：沙箱钟 上海 2026-07-06 周一 02:3x（系统 currentDate 2026-07-05 周日）。A股上一**已收盘**交易日 = 周五 **20260703**（周六/日无行情；周一 07-06 尚未开盘）。
- 库起始/终止 `stock_daily.trade_date` 最大日 = **20260703**，已覆盖目标日 → **无缺口，未取数、未写库**。宁可缺数不可假数——此处无缺、原样保留。

## 各表现状（max / 行数，全部 20260703）

| 表 | max | 行数 | 写入方 |
|---|---|---|---|
| stock_daily | 20260703 | 1,340,241 | 句芒 |
| daily_market | 20260703 | 1,574 | 句芒 |
| north_flow | 20260703 | 818 | 句芒 |
| limit_list_daily | 20260703 | 40,157 | 烛阴 |
| theme_etf_daily | 20260703 | 6,343 | 烛阴 |

## 管线步骤（成败与否都跑派生/自检）

1. **前置**：五盘（Brain/Database/outputs/uploads/剑酒青丘）沙箱已平铺挂载，未触发 request_cowork_directory（G-X51 防悬挂：先探测、只补缺）。TUSHARE_TOKEN 从 `Database/.env` 读到；`curl api.tushare.pro` → HTTP **200**（出网正常）。
2. **增量取数**：库 max 已 = 目标日 20260703 → 跳过，无增量。
3. **aggregate_derived**（`--db` 显式指向真实挂载库）：`dry-run` → 「无全市场覆盖日需补」；口径闸照例跳过 97 个池内时代旧日（覆盖<5000 只）。**无派生列缺口，未写。**
4. **fill_index_north**（`MARKET_DB` 显式覆盖）：`dry-run` → north 无缺口、指数 sh/sz/cyb 无缺口。**未写。**
5. **周快照**：今日非周五 → 跳过（幂等，非快照日）。
6. **健康自检** `market_health.py`（带 `MARKET_DATA_DIR`）：`overall=**ok**`，latest=2026-07-03，days_since=2（周末容差内），五表全 **ok·lag0**，`fails=[]`、`warns=[]`、`overdue_tables=[]`。已写 `Market-Data/_health.json`（供海螺看板读取）。

## 备注
- 本次为**周日/周一凌晨盘前**触发，自 07-03（周五）以来无新交易日，故"已最新"属预期，非漏跑。
- 脚本默认 DB 路径 `~/Documents/Database/...` 在平铺挂载沙箱下解析失败 → 已用真实挂载路径 `/sessions/.../mnt/Database/Market-Data/market_data.db` 经 `--db`（aggregate）/ `MARKET_DB` env（fill_index_north）/ `MARKET_DATA_DIR` env（health）显式覆盖，均正常。（沿用既有做法，非新坑。）
