---
title: 2026-07-07 行情更新
tags: [句芒, Market-Data, 行情更新, log]
created: 2026-07-07
updated: 2026-07-07
status: active
type: log
---

# 2026-07-07（周二）行情增量更新 · 句芒班

> 目标交易日 = 前一交易日 = **20260706（周一）**。周二更新周一。

## 结论：已更新 1 个交易日 20260706 ✅

三主表从 20260703 增量推进到 **20260706**，全市场覆盖 5517 只，派生列 + 三指数 + 北向全部补齐。健康自检我这三表全绿（烛阴两表按时序暂黄，非故障）。

## 前置探测（G-X51 防悬挂 · 只探不重复 request）
- `ls -d /sessions/*/mnt/*/` → Brain / Database / outputs / uploads / 剑酒青丘 **均已挂载**，未调 `request_cowork_directory`（省去悬挂面）。
- 出网实测 `curl api.tushare.pro` → **200**，直连可达。
- TUSHARE_TOKEN：环境无 → 从 `Database/.env` 读到，正常。

## 各表状态（更新后）

| 表 | max(trade_date) | 新增行数 | 说明 |
|---|---|---|---|
| stock_daily | 20260706 | **+5517** | 全市场覆盖；total 1340241→1345758 |
| daily_market | 20260706 | **+1** | 行完整：sh 4041.24/-0.06%、sz 15416.80/-1.16%、cyb 3948.86/-1.77%、量3.112万亿、涨停73/跌停52/连板4、北向396336.82、家数5517 |
| north_flow | 20260706 | **+1** | north_money 396336.82（hgt 174931.33 + sgt 221405.49） |
| limit_list_daily | 20260703 | 0 | 烛阴班（07:00）写，本班 05:xx 跑在其前，暂落后1日属正常时序 |
| theme_etf_daily | 20260703 | 0 | 同上，烛阴班收尾转绿 |

## 步骤执行
1. **增量取数**：lastDate=20260703 → 目标20260706；周六日无行情，仅缺周一20260706。Tushare `daily(trade_date=20260706)` 返 5517 行、`moneyflow_hsgt` 返北向一行。`INSERT OR IGNORE`（stock_daily UNIQUE(ts_code,trade_date)、north_flow UNIQUE(trade_date)）去重；只增不减校验通过。空壳保护：返回≥5000 才写。
2. **派生表聚合** `aggregate_derived.py`：拟补1日、实补20260706（涨停73/跌停52/连板4/量3.112万亿/家数5517）；跳过97个池内时代日（<5000只口径不符）。daily_market 1574→1575。
3. **指数/北向补缺** `fill_index_north.py`：北向无缺口；指数补 20260706 三格（sh/sz/cyb），仅填 NULL 列。
4. **周快照** `snapshot_market_db.py`：**跳过**（仅周五跑，今日周二）。
5. **健康自检** `market_health.py`：**overall = stale（黄）**，我这三表 stock_daily/daily_market/north_flow 全 `ok`、lag 0；warns=[limit_list_daily, theme_etf_daily]（烛阴两表 lag 1、overdue=false、班前正常时序）；**fails=[]、overdue_tables=[]**（无真故障）。`_health.json` 已写。烛阴 07:00 班收尾会再跑一次转绿。

## 遗留/异常
- **⚠️ 挂载盘 disk I/O error（老坑复现，已妥处）**：直写挂载盘 `market_data.db` 的 sqlite `commit()` 抛 `disk I/O error`——但页已落盘（`/tmp` 字节副本 integrity=ok 且 20260706 完整），仅 journal 清理步失败、残留 7.7MB 活 journal（若下次开库会回滚掉好数据）。
- **处置**：① 派生/指数两步改在 `/tmp/market_data.db` 上跑（避开 commit I/O），跑完 `cp → 源/.new`、校验、`mv .new → market_data.db`（mv 在本挂载**可用**、原子）；② 残留 journal 用 `truncate -s 0` 归零（**本挂载 `rm` 报 `Operation not permitted`、`mv`/`truncate`/`cp` 均可**，故删不掉就截零——0 字节 journal 无害）。最终源库 integrity=ok、三主表 20260706、journal 0 字节。
- 首次 stock_daily 直写那次亦走此法：源库页已含 20260706 → 只截零 journal 即保住数据。

## 脚本路径 env 注记（沙箱平铺挂载）
- `aggregate_derived.py`：认 `--db`，传绝对路径（本班传 `/tmp/market_data.db`）。
- `fill_index_north.py`：不认 `--db`，认 env `MARKET_DB` + `DOTENV`；沙箱下 `parents[6]` 定位失效，必须显式设 env。
- `market_health.py`：认 env `MARKET_DATA_DIR`。
- **本班新账**：挂载盘写库遇 disk I/O → 走 `/tmp` 跑+回 `mv` 换盘、journal 截零（`rm` 不可用）。leftover `.new`/测试文件删不掉，截零留痕即可。

---

## 复核段（14:52 二次触发 · 不覆盖上记录）

同日再次被调度触发，复核确认早班（05:xx）成果稳固、无需再动库：

- **五表一致到 20260706**：stock_daily 1,345,758 行 / daily_market 1,575 行 / north_flow 819 行 / limit_list_daily 40,308 行 / theme_etf_daily 6,361 行。烛阴两表已从早班的 20260703 追平到 **20260706**（07:00 班已收尾）。
- **aggregate_derived --dry-run**：全市场覆盖日无缺补（仍跳 97 个池内旧日）。
- **fill_index_north --dry-run**：north 无缺口、index 无缺口。
- **market_health**：`overall = ok`、latest=2026-07-06、days_since=1、**fails=[]、overdue_tables=[]**——早班的 stale（黄）已转 **ok（绿）**。`_health.json` 重写（generated 2026-07-07T14:52）。
- 本次**零写库**（全库已达目标日，遵只增不减/防空壳，未触任何数据）。挂载盘本次读路径未撞 disk I/O。
