---
title: 句芒行情日志 2026-06-29 — 补 06-26 核心表 + 行情库回绿
date: 2026-06-29
agent: 句芒
type: log
tags: [句芒, Market-Data, 行情更新, 沙箱直连tushare]
---

# 2026-06-29 · 补周五 06-26 核心表

> 触发：海螺看板「公共行情库」节点显示 stale，哥哥要芒芒自己把缺口补上。以句芒身份执行（公共数据层 owner）。

## 背景：真实缺口
- 周一 06-29 应到的最新交易日 = 上周五 **06-26**（trade_cal is_open=1；06-27/28 周末休市）。
- 重扫后 `_health.json` 实况：三张核心表 **stock_daily / daily_market / north_flow** 卡在 06-25，缺 06-26（各落后 1 个交易日）；limit_list_daily / theme_etf_daily 已到 06-26。
- 早先 `_health.json`（06-25 22:32）已过期，标的落后表与现实反转，先重跑 market_health 校正、再定位真缺口。

## 取数（沙箱直连 Tushare · HTTP REST）
**重要纠偏**：「沙箱拉不到 Tushare」是过时论述。约 06-11 白名单开放后，沙箱经 `localhost:3128` 代理 + `Database/.env` 的 TUSHARE_TOKEN 直连 `api.tushare.pro` 可取数。本次全程在沙箱内完成取数，未走 Mac 终端。

- `daily?trade_date=20260626` → stock_daily 5513 行（口径 ≥5000 ✓）。
- `moneyflow_hsgt?trade_date=20260626` → 北向 413140.63（hgt 188.. + sgt ..，量纲与近 5 日同级 ✓）。
- `aggregate_derived.py` → daily_market 1568→1569，06-26：涨停 60 / 跌停 31 / 连板 6 / 量 3.575 万亿 / 家数 5513。
- `fill_index_north.py` → 指数 sh/sz/cyb 06-26 填 3 格（仅 NULL 列），北向无缺口（聚合已填）。

## 大库写入 GOTCHA（命中并按规处置）
- 首次直接在挂载盘 commit 5513 行触发 `disk I/O error`——但数据**已落盘**、仅 journal 收尾失败，挂载库残留 6.3MB 热 journal。
- 处置：按脚本头注释「拷 /tmp 写好整库放回」——在 `/tmp` 上补完整库（5 表齐 06-26，quick_check ok）→ 截断挂载库残留 journal 为 0 字节（挂载盘不允许 rm，截断即无害）→ `cp -f` 就地覆盖放回 → 复核 quick_check ok。
- 写前已备份 `market_data.db.bak_补0626_20260629`（06-25 干净态）。防空壳 + 只增不减校验均过。

## 结果
- 5 表全部 max=20260626；`_health.json` overall=**ok**（target 06-26，warns/fails 空）。
- 海螺看板「公共行情库」节点 stale → **healthy**（survey+build+push 已刷）。

## 遗留
- 挂载库旁残留 0 字节 `market_data.db-journal`（无害，但删不掉需哥哥终端 `rm`）。
- `剑酒青丘/backtest/outputs/` 留了一份探针用的 `gap_report_20260628_*.json`（gap_detect 副产物，可清）。
- 待办（另开）：全局更正「沙箱拉不到 Tushare」过时提法——已检索出清单，待哥哥确认 diff 后改。
