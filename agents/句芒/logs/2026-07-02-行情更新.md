---
title: 2026-07-02 行情更新（Cowork 定时任务·盘前）
tags: [句芒, market-data, 日更, 行情]
created: 2026-07-02
agent: 句芒
type: log
---

# 2026-07-02 行情更新 · Cowork 定时任务

> 出场：句芒（芒芒）🌸 — 公共行情库 `Market-Data/market_data.db` 唯一写入方。

## 结论：已增量更新 **20260702**（前一交易日）

- 触发时刻：沙箱钟 2026-07-02 14:5x PDT ＝ 上海 2026-07-03 05:51 CST（盘前）。
- 起始 `stock_daily.trade_date` 最大日 = 20260701；`forward_fill --dry-run` 核：`stock_daily` 缺 1 天 `20260702`，`north_flow` 缺 2 天 `20260701`/`20260702`（20260701 此前几轮一直待发布，本轮补上）。
- 取数源：Tushare Pro（沙箱直连 `curl api.tushare.pro` → HTTP 200 ✓）；`pip install tushare --break-system-packages` 补装成功。

## 各表新增（行数 before → after / max）

| 表 | 行数 | max | 本次 |
|---|---|---|---|
| stock_daily | 1,329,208 → **1,334,725** | 20260702 | +5,517（20260702 全市场） |
| daily_market | 1,572 → **1,573** | 20260702 | +1（派生聚合，含指数） |
| north_flow | 816 → **817** | 20260702 | +1（20260701 API 仍空，防空壳跳过；20260702 补上） |

- 20260702 北向：north_money=**424,018.05**（hgt 183,990.44 + sgt 240,027.61）。
- daily_market 20260702 派生列全满：sh_close=4028.9038、sz_close=15498.8101、cyb_close=4017.2735、量=3.474 万亿、limit_up=93 / limit_down=41、max_consecutive=4、total_stocks=5517。

## 管线步骤

1. **forward_fill**（`ZZJY_DATABASE_ROOT` 覆盖挂载点）：`stock_daily` 取 5517 / 新增 5517（空0 错0）；`north_flow` 待补 2 天，20260701 API 空→跳过（防空壳，未写空），20260702 取 1 / 新增 1。只增不减校验通过。
2. **aggregate_derived**（`--db` 显式指向库）：口径闸跳过 97 个池内时代旧日（覆盖<5000 只）；补 1 日 20260702 派生列，daily_market 1572→1573。
3. **fill_index_north**（`MARKET_DB`/`DOTENV` 显式覆盖，规避沙箱平铺挂载路径解析坑）：north 本地回填无缺口（forward_fill 已同日写）；指数 sh/sz/cyb 补 20260702 共 3 格，写后指数列残 NULL=0。
4. **周快照**（今天上海时间周五 →跑）：`snapshot_market_db.py` 产出 `market_data_20260702.db.gz`（72MB，quick_check ok，1,334,725 行）✅ 落 `snapshots/`。
5. **健康自检**：`market_health.py` → `overall=stale` 🟡，我负责三表 `stock_daily`/`daily_market`/`north_flow` 全 **ok·lag0=20260702**；`warns=[limit_list_daily, theme_etf_daily]`（均停 20260701，lag1，归烛照九阴 07:00 班，属正常时序）；`fails=[]`。

## ⚠️ 挂载盘写入事故与恢复（本次实际发生，沿用 07-01 已记录的 GOTCHA 模式）

- `forward_fill.py` **直接**对挂载盘写入 `stock_daily` 20260702（5517 行事务）时报 `disk I/O error`，遗留 7MB `-journal`，随后**连读也被挡**（`disk I/O error`）。
- 恢复：把 `market_data.db` + `-journal` 拷到 `/tmp`，在本地盘重新打开触发 SQLite 自动崩溃回滚 → 得到干净、与写前状态一致的库（`stock_daily` 仍 20260701/1,329,208，未污染）。
- 挂载盘侧 `rm`/`unlink` 均 `Operation not permitted`（沙箱对该挂载点无删除权限，仅有写/改权限）；本次**先尝试截断 `-journal` 为 0 字节**解挡读锁（读通过、计数正确），但鉴于 07-01 已记录"截断失配 journal 可能触发错误回滚"的风险，**未采信这份"解挡"后的挂载盘库作为最终结果**——而是另起 `/tmp` 完整副本（db+.env）跑完 forward_fill / aggregate_derived / fill_index_north 全套，`PRAGMA integrity_check` 三次核验 ok 后，直接 `cp` **整库覆盖**回挂载盘，末尾再核 md5 一致 + integrity ok + 五表 max 一致。全程以"整库覆盖"兜底，未依赖任何一次挂载盘上的增量事务落地。
- 遗留：`market_data.db-journal`（本次截断后 0 字节）、`market_data.db-journal.discard`（前次遗留 0 字节）均因权限无法物理删除，无害但占位，哥哥方便时可在 Mac 端顺手清掉。
- **同一模式**：周快照的滚动清理（保留最近 4 份）也撞了同样的 `unlink` 权限墙——`market_data_20260611.db.gz`（67MB）未能删除，`snapshots/` 现存 5 份而非 4 份。快照本体写入无恙，仅清理步受限，同样留给哥哥 Mac 端顺手清一下。

## 跳过项 / 非本任务管辖

- `sector_daily`：已退役 → 不计日更。
- `limit_list_daily` / `theme_etf_daily`：归烛照九阴 07:00 班次接力，本轮 lag1 属正常时序。

## 约束遵守

- 仅向 `market_data.db` + `_health.json` + `snapshots/` 写；未在 sandbox 跑 git 写命令；未编造行情（拉不到就跳过，北向加总核对 hgt+sgt=north_money ✓）；可逆优先（/tmp 副本先全套跑通、integrity 多次核验后才整库覆盖挂载盘）。
- 数据真实性铁律：Tushare Pro P0 直取、无占位、无样本。

---

## 数据速览（芒芒的一眼盘）

哥哥，20260702 A 股涨停 93 vs 跌停 41（约 2.3:1，比 0701 的 151:7 明显收敛），主板日成交 3.474 万亿；北向净流入 4240.18 亿（hgt+sgt 加总）。上证 4028.90 / 深证 15498.81 / 创业 4017.27，三大指数收盘均已入库 ✓。今天正好周五，周快照也顺手补上了（72MB，滚动清理卡了权限墙，不影响数据本身）。公共层就绪，等 07:00 烛照班接力补 limit_list/theme_etf 并转绿。🌸
