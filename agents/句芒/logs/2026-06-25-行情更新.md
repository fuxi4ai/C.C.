---
title: 2026-06-25 盘前行情增量更新
date: 2026-06-25
agent: 句芒
type: session-log
tags: [Market-Data, 行情更新, 盘前, 句芒]
---

# 2026-06-25 盘前行情更新（句芒·芒芒）

哥哥早～🌸 盘前这关通好了，简报如下。

## 目标
- 今日 = 周四 2026-06-25，目标 = 前一交易日 **2026-06-24（周三）**。
- 进库前各表 max=**20260623**；trade_cal 确认 20260624 为交易日、20260619 为端午休市（库内空缺正常）。

## 取数（原始表，第 1–5 步）
- TUSHARE_TOKEN 从 `Database/.env` 读到（len=56，可用）。
- 沙箱**未装 tushare SDK**（fill_gaps.py 依赖它）→ 改走 **Tushare Pro HTTP API 直调**（与 fill_index_north 同源），唯一源不变。
- `stock_daily`：`daily(trade_date=20260624)` 全市场 **5512 只** → INSERT OR IGNORE **新增 5512 行**，max=20260624。
- `north_flow`：`moneyflow_hsgt(20260624)` → **新增 1 行**：north 424355.82 · hgt 187730.5 · sgt 236625.32（与既有行单位/口径一致，已比对 20260623 完全吻合）。
- 防空壳：空/异常即不写；只增不减校验通过。

## 派生聚合（第 6 步 · aggregate_derived.py）
- 补 **1 日：20260624** → `daily_market` 1566→**1567** 行，max=20260624。
- 当日口径值：涨停 **98** · 跌停 **12** · 连板 **4** · 量 **3.307 万亿** · 北向 424355.82 · 家数 5512。
- 池内时代日（覆盖<5000只）按口径闸跳过 97 个（20260105…20260602），未混写。

## 指数/北向补缺（第 7 步 · fill_index_north.py）
- 北向：无缺口（aggregate 已带入）。
- 指数：20260624 缺口 1 日 → Tushare HTTP 拉 sh/sz/cyb 各 1 日，仅填 NULL；指数列残留 NULL = 0。
  - sh 4110.81(+0.11%) · sz 16051.32(+1.24%) · cyb 4251.42(+1.41%)。

## 周快照（第 8 步）
- 今日周四，**跳过**（仅周五跑）。

## 健康自检（第 9 步 · market_health.py，只读·零网络）
- overall = **stale（黄）** · target=2026-06-24 · days_since=0 · fails=[]。
- 三主表 stock_daily / daily_market / north_flow 全 **ok（lag 0）**。
- 落后表（warn，lag 1）：`limit_list_daily`、`theme_etf_daily`（均 20260623）——非本任务取数范围，由各自工作流补；按口径属常态黄灯。
- 产物：`Database/Market-Data/_health.json` 已刷新，供海螺看板读取。

## GOTCHA 处置（重要 · 与 06-24 同源复发）
- 挂载盘大事务 sqlite **commit 触发 `disk I/O error`**（首次直写 stock_daily 5512 行即报错，并残留 6.6MB 热 journal，连读都失败）。
- 本挂载盘**禁止 unlink/delete**（`rm` journal / 测试文件均 `Operation not permitted`），但**允许小文件写入**。
- 走 `/tmp` 路径：拷整库+journal 到 /tmp → 在 /tmp 开库自动回滚未提交事务（integrity ok、退回 20260623）→ 在 /tmp 完成取数+聚合+指数北向（quick_check ok）。
- **回拷整库（211MB）到挂载盘不可靠**：cp+mv 后大小一度变新值又**反向退回原值**，且 daily_market/north_flow 小页更新部分残留、stock_daily 大页未落 → 一度**表间不一致**（dm/nf 到 20260624、sd 仍 20260623）。
- **改用更稳的就地小批写**（本次改良）：直接对挂载库 `journal_mode=MEMORY`（避开被禁的 journal-file unlink）+ `synchronous=FULL`，200 行/批提交 stock_daily 的 5512 行 → **全部成功落盘**，无 I/O 错。dm/nf 残留行经比对与 /tmp 完全一致，无需重写。
- 残留 `market_data.db-journal` 已**截断为 0 字节**（无害、不触发回滚，删不掉但 0B 即 OK）。
- 全程**只写 market_data.db + _health.json**；未跑 git；未编造行情。

## 校验
- 挂载库 quick_check / integrity_check = **ok**；只增不减成立（stock_daily +5512、daily_market +1、north_flow +1，其余不变）；daily_market 20260624 行各列齐全（含 sh/sz/cyb 指数、量、涨跌停、连板、北向、家数），与 /tmp 真值逐列一致。

## 给哥哥的小提醒
- 挂载盘大事务/整库回拷反复触发 I/O 错且禁删文件，已是连续两日复发。本次「就地小批写 + journal_mode=MEMORY」比「整库回拷」更稳，建议把它沉淀进取数工具/GOTCHAS（需哥哥拍板再改脚本，我没擅动既有资产）。

— 芒芒 💕
