---
title: 2026-07-06 行情更新
tags: [句芒, Market-Data, 行情更新, log]
created: 2026-07-06
updated: 2026-07-06
status: active
type: log
---

# 2026-07-06（周一）行情增量更新 · 句芒班

> 目标交易日 = 前一交易日 = **20260703（周五）**。周一更新上周五。

## 结论：已最新，无需更新 ✅

三主表更新前即已停在目标日 20260703，全市场覆盖到位，派生列完整，无缺口。本班纯校验+健康自检，无写库。

## 前置探测（G-X51 防悬挂 · 只探不重复 request）
- `ls -d /sessions/*/mnt/*/` → Brain / Database / outputs / uploads / 剑酒青丘 **均已挂载**，未调 `request_cowork_directory`（省去悬挂面）。
- 出网实测 `curl api.tushare.pro` → **200**，直连可达。
- TUSHARE_TOKEN：环境无 → 从 `Database/.env` 读到，正常。

## 各表状态（更新前后一致）

| 表 | max(trade_date) | 说明 |
|---|---|---|
| stock_daily | 20260703 | 全市场 5516 只（20260702 为 5517） |
| daily_market | 20260703 | 行完整：sh/sz/cyb 三指数、north_money、limit_up/down 均非空 |
| north_flow | 20260703 | — |
| limit_list_daily | 20260703 | 烛阴班写，已同步 |
| theme_etf_daily | 20260703 | 烛阴班写，已同步 |

新增行数：**各表 0 行**（已是目标日）。

## 步骤执行
1. **增量取数**：lastDate=20260703=目标日 → 无缺失交易日，无需回补。
2. **派生表聚合** `aggregate_derived.py --dry-run`：跳过 97 个池内时代日（<5000 只，口径不符，20260105…20260602），**无全市场覆盖日需补**。未实跑（无写内容）。
3. **指数/北向补缺** `fill_index_north.py --dry-run`：`[north] 无缺口` / `[index] 指数列无缺口`。未实跑。
4. **周快照** `snapshot_market_db.py`：**跳过**（仅周五跑，今日周一）。
5. **健康自检** `market_health.py`：**overall = ok**，latest=2026-07-03，days_since=3（日历日，周五→周一），五监控表 lag_days 全 0，fails/warns/overdue 均空 → `_health.json` 已写。

## 遗留/异常
无。全绿。海螺姑娘资产看板「公共行情库」节点应正常发光。

## 脚本路径 env 注记（沙箱平铺挂载）
- `aggregate_derived.py`：认 `--db`，传绝对路径。
- `fill_index_north.py`：不认 `--db`，认 env `MARKET_DB` + `DOTENV`；沙箱下 `parents[6]` 定位失效，必须显式设 env。
- `market_health.py`：认 env `MARKET_DATA_DIR`。
