---
title: 补0708行情（market-data-backfill-0708 定时任务）
agent: 句芒
date: 2026-07-08
type: log
tags: [Market-Data, 行情更新, 补数, 定时任务]
---

# 2026-07-08 晚 · 补 07-08 公共行情（scheduled: market-data-backfill-0708）

> 背景：今早 05:50 的 `market-data-daily-update` 未把 07-08 补上，三表停在 20260707。本次为专项补数 run，口径完全按日更 SKILL。

## 结果：三表 07-08 全部补齐，health 全绿 ✅

| 表 | 更新前 max | 更新后 max | 新增行数 |
|---|---|---|---|
| stock_daily | 20260707 | **20260708** | **+5518**（口径闸 ≥5000 ✅） |
| daily_market | 20260707 | **20260708** | **+1** |
| north_flow | 20260707 | **20260708** | **+1** |

- 更新交易日：仅 **20260708**（缺口就这一天）。
- 聚合派生（aggregate_derived，dry-run 核对后实跑）：补 20260708 一日 —— 涨停57 / 跌停43 / 连板6 / 量2.583万亿 / 北向338404.12（百万元）/ 家数5518。
- fill_index_north（dry-run 后实跑）：north 无缺口；指数 sh/sz/cyb 填 3 格（仅 NULL 列），填后指数 NULL 行=0。
- market_health：**overall=ok**，五表（含烛阴的 limit_list_daily / theme_etf_daily）全部 max=20260708、lag=0，`_health.json` 已更新。

## 过程与异常（GOTCHA 实录）

1. **挂载探测（G-X51 防悬挂）**：`Documents` 已挂载，Database/Projects 都在其下，**零 request**，无悬挂面。
2. **tushare 包沙箱未装** → `pip install tushare --break-system-packages` 后正常（v1.4.29）。`api.tushare.pro` 直连 200。token 走 `Database/.env`。
3. **挂载盘 disk I/O error（已知 GOTCHA，如约撞上）**：forward_fill 直写挂载库时 sqlite 写事务报 disk I/O error，留下 7.7MB 热 journal。按预案走 **/tmp 副本往返**：
   - db+journal 拷 /tmp → sqlite 打开自动回滚热 journal → quick_check ok、数据回到 20260707/1351275 行原状（**无损**）；
   - /tmp 上依次跑 forward_fill（`ZZJY_DATABASE_ROOT`）→ aggregate_derived（`--db`）→ fill_index_north（`MARKET_DB`/`DOTENV` env）；
   - 整库放回：cp 回挂载点 + cmp 校验字节一致 → 热 journal **截断为 0 字节**（rm 被沙箱权限挡，按 SKILL「残留 journal 截断为 0 即无害」处理）→ mv 就位 → 只读 quick_check ok。
4. **今早 05:50 失败原因推断**：本次直写挂载库立刻复现 disk I/O error → 早班大概率同样死在这一步（而非取数失败）；建议日更 SKILL 把「/tmp 副本往返」从兜底预案提为**默认路径**，可与哥哥商量后改（propose-then-confirm，本次不动 SKILL）。

## 约束遵守
- 只写 `market_data.db`（及其旁的 `_health.json`）；未跑任何 git 写命令；无编造行情——全部数据来自 Tushare Pro 实拉。
- 遗留：挂载库旁热 journal 已截断为 0 字节（无害残留，哥哥方便时可在 Mac 上手动删掉）。
