---
title: 行情库增量更新 · 2026-06-05
tags: [句芒, market-data, 盘后更新, 增量]
created: 2026-06-05
updated: 2026-06-05
agent: 句芒（芒芒）
type: session-log
status: 跳过（数据源网络不可达，原库保留）
---

# 行情库增量更新 · 2026-06-05（盘后）

> 句芒（芒芒）值守。目标库：`Database/Market-Data/market_data.db`（公共行情库，单一可信源）。

## 结论（一句话）
**本次未写入** —— TUSHARE_TOKEN 可用，但 sandbox 网络白名单**拦截了所有行情数据源**（`api.tushare.pro` / `api.waditu.com` 均 403 Tunnel Forbidden），无法取数。按「防空壳 + 绝不编造」铁律保留原库、未写入，正常退出；数据源放出后下次定时跑自动增量补齐。

## 库基线（更新前 = 更新后，完全一致 ✅ 只增不减）
| 表 | 最大交易日 | 行数 |
|---|---|---|
| stock_daily | 20260602 | 1,218,982 |
| daily_market | 20260602 | 1,552 |
| sector_daily | 20260602 | 9,312 |
| north_flow | 20260602 | 797 |

## 过程（实证优先）
1. **TUSHARE_TOKEN**：✅ 从 `~/Documents/Database/.env` 读到（56 位）。Token 本身没问题。
2. **lastDate** = `20260602`（周二）；今天 2026-06-05 周五（已收盘），按「前一交易日」规则，**目标 = 20260604（周四）**，待补交易日 = 6-3（周三）、6-4（周四）两天（6 月上旬无节假日，端午为 6-19，故两天均为正常交易日）。
3. **网络可达性实测**：
   - `POST http://api.tushare.pro` → **Connection blocked by network allowlist**。
   - `POST http://api.waditu.com` / `https://api.tushare.pro` → **403（Received HTTP code 403 from proxy after CONNECT）**。
   - 旁证：`google.com` / `query1.finance.yahoo.com` 同样 403，仅 `pypi.org` 可达 ⟹ 是 sandbox 出口白名单拦截，非 token/源头问题。
4. **缓存兜底排查**：`tushare-cache/tushare.db`（财务/业务构成，前沿 end_date 20251231）、`valuation/valuation.db`（估值，前沿 2026-05-07）均**不含日线 OHLC**，无法回补四表缺口。
5. **mootdx 兜底**：环境未安装 `mootdx`，且其取的是「真实当前交易日」行情，与目标日 6-3/6-4 不对应，硬塞构成"编造行情"，**不采用**。
6. **写入**：跳过；四表 MAX 日期 + 行数与更新前完全一致（无空壳覆盖）。

## 缺口与跳过
- **20260603（6-3 周三）、20260604（6-4 周四）待补**：网络放出后下次定时跑自动增量补齐（`INSERT OR IGNORE` 去重，不会重复插）。
- mootdx：模块缺失 + 场景不适用，兜底未启用。

## 约束遵守
- 只涉及 `Database/Market-Data/market_data.db`（本次实际未写）。
- 未在 sandbox 跑任何 git 写命令。
- 未编造任何行情；拉不到即标缺、保留原数据。

—— 芒芒 🌸（拉不到就标缺、绝不编造）
