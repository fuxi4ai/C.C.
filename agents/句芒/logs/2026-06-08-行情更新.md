---
title: 行情库增量更新 · 2026-06-08
tags: [句芒, market-data, 盘后更新, 增量]
created: 2026-06-08
updated: 2026-06-08
agent: 句芒（芒芒）
type: session-log
status: 跳过（数据源网络不可达，原库保留）
---

# 行情库增量更新 · 2026-06-08（盘后）

> 句芒（芒芒）值守。目标库：`Database/Market-Data/market_data.db`（公共行情库，单一可信源）。

## 结论（一句话）
**本次未写入** —— TUSHARE_TOKEN 可用（56 位），但 sandbox 出口白名单**拦截了所有行情数据源**（`api.tushare.pro` / `api.waditu.com`、http 与 https 均 **403 Tunnel Forbidden**），无法取数。按「防空壳 + 绝不编造」铁律保留原库、未写入，正常退出；数据源放出后下次定时跑自动增量补齐。

## 库基线（更新前 = 更新后，完全一致 ✅ 只增不减）
| 表 | 最大交易日 | 行数 |
|---|---|---|
| stock_daily | 20260602 | 1,218,982 |
| daily_market | 20260602 | 1,552 |
| sector_daily | 20260602 | 9,312 |
| north_flow | 20260602 | 797 |

## 过程（实证优先）
1. **TUSHARE_TOKEN**：✅ 从 `~/Documents/Database/.env` 读到（56 位）。Token 本身没问题。
2. **lastDate** = `20260602`（周二）；今天 2026-06-08 周一（已收盘）。按「周一更新上周五 = 前一交易日」规则，**目标 = 20260605（上周五）**，待补交易日 = 6-3（周三）、6-4（周四）、6-5（周五）三天（6 月上旬无节假日，端午 6-19，故三天均为正常交易日）。
3. **网络可达性实测**（HTTP/urllib 直连，对照旧 query_tushare_pro 调用法）：
   - `http://api.tushare.pro` → **403 Forbidden**。
   - `https://api.tushare.pro` → **Tunnel connection failed: 403 Forbidden**。
   - `http://api.waditu.com` / `https://api.waditu.com` → **403**。
   - 旁证：`query1.finance.yahoo.com` 同样 403，仅 `pypi.org` 200 可达 ⟹ 是 sandbox 出口白名单拦截，非 token/源头问题（与 6-5 同症）。
4. **缓存兜底排查**：`tushare-cache/`（财务/业务构成）、`valuation/`（估值）均**不含日线 OHLC**，无法回补四表缺口。
5. **mootdx 兜底**：已弃用删除（环境未装、只能取当前交易日不能回补），不采用——硬塞构成「编造行情」。
6. **写入**：跳过；写后复核四表 MAX 日期 + 行数与更新前完全一致（无空壳覆盖）。

## 缺口与跳过
- **20260603（周三）、20260604（周四）、20260605（周五）待补**：网络放出后下次定时跑自动增量补齐（`INSERT OR IGNORE` 去重，已存在日期不重复插）。
- 累计待补已达三个交易日（6-3 / 6-4 / 6-5），均因数据源出口被白名单拦截。

## 约束遵守
- 只涉及 `Database/Market-Data/market_data.db`（本次实际未写）。
- 未在 sandbox 跑任何 git 写命令。
- 未编造任何行情；拉不到即标缺、保留原数据。

—— 芒芒 🌸（拉不到就标缺、绝不编造）
