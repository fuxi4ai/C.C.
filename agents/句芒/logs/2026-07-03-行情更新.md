---
title: 2026-07-03 行情更新（Cowork 定时任务）
tags: [句芒, market-data, 日更, 行情]
created: 2026-07-03
agent: 句芒
type: log
---

# 2026-07-03 行情更新 · Cowork 定时任务

> 出场：句芒（芒芒）🌸 — 公共行情库 `Market-Data/market_data.db` 唯一写入方。

## 结论：已增量更新 **20260703**（最近一个已收盘交易日）

- 触发时刻：沙箱钟 2026-07-03 14:5x PDT ＝ 上海 2026-07-04 05:5x CST。周五 20260703 已收盘、Tushare 已发布，故更新至 20260703（非"盘前更新前一日"情形）。
- 起始 `stock_daily.trade_date` 最大日 = 20260702；`trade_cal` 核：20260703 为交易日、20260704/05 周末、下一交易日 20260706（周一）。缺口 = 单日 20260703。
- 取数源：Tushare Pro（沙箱直连 `curl api.tushare.pro` → HTTP 200 ✓）。沙箱未装 tushare 库 → 改用 HTTP API 直调（urllib），字段/列序对齐 `config._COLS`。

## 各表新增（行数 before → after / max）

| 表 | 行数 | max | 本次 |
|---|---|---|---|
| stock_daily | 1,334,725 → **1,340,241** | 20260703 | +5,516（20260703 全市场） |
| daily_market | 1,573 → **1,574** | 20260703 | +1（派生聚合，含指数） |
| north_flow | 817 → **818** | 20260703 | +1（20260703） |

- 20260703 北向：north_money=**391,515.25**（hgt 171,764.45 + sgt 219,750.8，加总核对 ✓）＝ 净流入约 **3915.15 亿**。
- daily_market 20260703 派生列全满：sh_close=4043.6432（+0.37%）、sz_close=15597.5116（+0.64%）、cyb_close=4019.9341（+0.07%）、量=3.205 万亿、limit_up=108 / limit_down=22、max_consecutive=5、total_stocks=5516。

## 管线步骤

1. **增量取数**（HTTP API 直调，写 `/tmp` 副本）：`daily(trade_date=20260703)` 取 5516 行 / 新增 5516；`moneyflow_hsgt(trade_date=20260703)` 取 1 行 / 新增 1。`INSERT OR IGNORE` 去重，只增不减校验通过。防空壳：先验非空才写。
2. **aggregate_derived**（`--db` 显式指向 /tmp 库）：口径闸跳过 97 个池内时代旧日（覆盖<5000 只）；补 1 日 20260703 派生列，daily_market 1573→1574。
3. **fill_index_north**（`MARKET_DB`/`DOTENV` 显式覆盖）：north 本地回填无缺口；指数 sh/sz/cyb 补 20260703 共 3 格，写后指数列残 NULL=0。
4. **周快照**（今天周五 →跑）：`snapshot_market_db.py` 产出 `market_data_20260703.db.gz`（73MB，quick_check ok，1,340,241 行）✅ 落 `snapshots/`。
5. **健康自检**：`market_health.py` → `overall=stale` 🟡，我负责三表 `stock_daily`/`daily_market`/`north_flow` 全 **ok·lag0=20260703**；`warns=[limit_list_daily, theme_etf_daily]`（均停 20260702，lag1，归烛照九阴 07:00 班，属正常时序）；`fails=[]`。

## ⚠️ 挂载盘写入事故与恢复（本次实际发生，沿用已记录的 GOTCHA 模式）

- 首次**直接**对挂载盘写 `stock_daily` 20260703（5516 行事务）时报 `disk I/O error`，遗留 7MB `-journal`，随后连 `integrity_check` 读也被挡（`disk I/O error`）。
- 恢复：把 `market_data.db` + `-journal` 拷到 `/tmp`，本地盘重新打开触发 SQLite 自动崩溃回滚 → 得干净、与写前一致的库（`stock_daily` 仍 20260702/1,334,725，未污染，integrity ok）。
- 在 `/tmp` 完整副本跑完取数 / aggregate_derived / fill_index_north 全套，`PRAGMA integrity_check` ok 后，`cp` **整库覆盖**回挂载盘。挂载盘侧 `rm -f -journal` → `Operation not permitted`（该挂载点无删除权限）；改用 `truncate -s 0` 把遗留 `-journal` 截为 0 字节（无害），再 `cp+sync` 覆盖，重开核验：integrity ok、五表 max 一致、journal 0 字节。
- **同一权限墙**：周快照滚动清理（保留 4 份）撞 `unlink` 权限墙——`market_data_20260612.db.gz`（67MB）未能删除，`snapshots/` 现存 5 份而非 4 份。快照本体写入 + 双校验无恙，仅清理步受限，留给哥哥 Mac 端顺手清一下。

## 跳过项 / 非本任务管辖

- `sector_daily`：已退役 → 不计日更。
- `limit_list_daily` / `theme_etf_daily`：归烛照九阴 07:00 班次接力，本轮 lag1 属正常时序，烛照班收尾会再跑 health 转绿。

## 约束遵守

- 仅向 `market_data.db` + `_health.json` + `snapshots/` 写；未在 sandbox 跑 git 写命令；未编造行情（北向加总核对 hgt+sgt=north_money ✓）；可逆优先（/tmp 副本全套跑通、integrity 核验后才整库覆盖挂载盘）。
- 数据真实性铁律：Tushare Pro P0 直取、无占位、无样本。

---

## 数据速览（芒芒的一眼盘）

哥哥，20260703 A 股反弹了 🌸——涨停 108 vs 跌停 22（约 4.9:1，比 0702 的 93:41 明显走强），三大指数收红：上证 4043.64（+0.37%）、深证 15597.51（+0.64%）、创业 4019.93（+0.07%），一扫前一日大跌的阴霾。主板日成交 3.205 万亿（较 0702 的 3.474 万亿略缩）；北向净流入约 3915.15 亿。今天周五，周快照顺手补上（73MB，滚动清理照旧卡权限墙，不碍数据）。公共层就绪，等 07:00 烛照班接力补 limit_list/theme_etf 并转绿。💕
