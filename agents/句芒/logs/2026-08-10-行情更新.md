---
title: 2026-08-10 行情更新（句芒傍晚班）
tags: [句芒, 行情更新, Market-Data, 日志]
created: 2026-08-10
updated: 2026-08-10
status: active
type: log
---

# 2026-08-10（周一·北京 16:38 班）行情更新

**结论：overall ok 🟢。** stock_daily / daily_market 推进到 20260810；north_flow 补回 20260807（0810 当日北向 Tushare 未发，结构性 T+1，防空壳跳过，下一班自动补）。今日周一，非快照日。

## 更新明细

| 表 | 之前 max | 之后 max | 行数变化 | 说明 |
|---|---|---|---|---|
| stock_daily | 20260807 | **20260810** | 1,498,551 → 1,504,089（+5,538） | Tushare pro.daily 全市场，INSERT OR IGNORE |
| daily_market | 20260807 | **20260810** | 1,599 → 1,600（+1） | 聚合管线派生 |
| north_flow | 20260806 | **20260807** | 842 → 843（+1） | 补回周五缺口；0810 空→跳过 |

**0810 盘面**（daily_market 新行，皆真实源）：上证 3966.59（+0.67%）/ 深成 14316.96（+0.04%）/ 创业板 3537.21（-0.73%）；成交 2.539 万亿；涨停 103 / 跌停 5 / 最高连板 5；覆盖 5,538 只（≥5000 口径闸通过）；north_money NULL（北向晚发，留空不编）。**0807 北向已回填**：north_money=345,150.10（hgt 157,452.06 + sgt 187,698.04，万元）。

## 执行路径（全程 /tmp 往返，零事故）

1. 挂载已齐（Database/剑酒青丘/brain 均在），零 request、零悬挂面；token 在 `Database/.env`（拷副本进 /tmp 供 config.load_env 读，不打印值），沙箱直连 Tushare 实测 200；`PYTHONPATH=Database/pylib-linux` 直用 tushare 1.4.29（pylib 仍生效，无需 pip 回退）。
2. 抢锁 `.market-data-writer.lock`（owner=mangmang-market-daily-20260810-163818）成功。
3. **预防性 /tmp 往返**（沿用前两班经验，不赌挂载盘直连）：指纹冻结 live（sha256 + 无非零 WAL/journal 确认）→ backup API 一致性快照到 `/tmp/mdback` → 逻辑等价核验（integrity ok、三表行数一致）。
4. 取数/聚合/补缺全落 /tmp 副本（各脚本 db 路径认法不同，分别显式指向 /tmp 副本）：`forward_fill`（认 `ZZJY_DATABASE_ROOT`）补 0810 个股 + 0807 北向；`aggregate_derived`（认 `--db`）补 0810 daily_market；`fill_index_north`（认 `MARKET_DB`/`DOTENV`）本地回填 0807 north_money + Tushare 拉 0810 sh/sz/cyb 三格（仅 NULL 列）。
5. 换库前校验全过：tmp integrity_check + quick_check ok、三表只增不减、0810 新行抽查完整（含指数三格）、0807 北向回填到位；live 指纹复核**未被他人动过**。
6. 换库 rename 三连：staging 落源目录 sha256 回读一致 → live 挪 `_archive/market_data.db.preswap-20260810-mangmang`（留痕）→ staging 上位 → 新 live 只读复检通过。
7. 周一非快照日（仅周五），跳过 snapshot。
8. `market_health.py`：**overall ok**，fails=[]、real_warns=[]、overdue=[]；benign warns 三条——north_flow lag1（结构性 T+1）、limit_list/theme_etf lag1（烛照班班前正常时序，非句芒表）。
9. owner 校验后放锁（rename 为 `.RELEASED-20260810-mangmang`）。

## 留痕与尾巴

- `_archive/market_data.db.preswap-20260810-mangmang`（268MB，换库前主库副本）——沙箱删不了，连同前几份 preswap/precrash 残档一起留哥哥定夺；确认无碍后可删。
- 下一班（0811）注意：0810 北向按 T+1 结构性缺口自动补回（forward_fill 会带）。
- 本班零事故，/tmp 往返 + rename 三连打法继续稳定。
